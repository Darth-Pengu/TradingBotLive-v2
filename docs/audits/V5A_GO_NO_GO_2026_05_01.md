# V5A-GO-NO-GO-CHECKLIST-001 — Audit (2026-05-01 ~13:00 UTC)

> Session 6 of 6 in 2026-05-01 chained-prompt sequence. Final precondition audit
> before Jay flips TEST_MODE=false on bot_core. Read-only — does NOT flip the bot.

## §1 Verdict

**🔴 NO_GO**

Aggregate: **3 PASS / 4 CONDITIONAL / 3 FAIL**

Per session §3:
- GO: all PASS or ≤2 CONDITIONAL with mitigations and zero FAIL.
- CONDITIONAL_GO: ≤4 CONDITIONAL with mitigations and zero FAIL.
- **NO_GO: any FAIL OR ≥5 CONDITIONAL.**

3 FAILs trigger NO_GO unambiguously.

## §2 Per-precondition evidence

| # | Precondition | Verdict | Evidence |
|---|---|---|---|
| 1 | Wallet capacity | 🔴 **FAIL** | Helius `getBalance(4h4pstXd5...)` = **0.064 SOL** at 13:00 UTC. Floor 0.5 SOL; PASS 2.5 SOL. Below floor → cannot meet `MIN_POSITION_SOL=0.05 × MAX_SD_POSITIONS=20` worst-case (1.0 SOL). Top-up to ~3 SOL is a Jay action (V5a precondition pending since 2026-04-21 1.5 SOL outflow). |
| 2 | Path B parity-of-truth | ✅ **PASS** | id 6580 `corrected_pnl_sol = -0.094244978` matches on-chain truth `-0.094245` within 0.000000 SOL (well under 0.005 PASS threshold). `correction_method='live_actual_v1'` written 2026-05-01 12:52 UTC via Path B backfill. |
| 3 | TIME_PRIME contradiction closed | 🟡 **CONDITIONAL** | Session 1 commit `13d4324` deployed; `TIME_PRIME_MULTIPLIER=1.0` env confirmed set on bot_core via Railway MCP; `TIME_PRIME_HOURS_AEST=""` (empty default); branch never fires by code logic. Post-fix behavioral verification limited: only 1 SD-paper trade closed since Session 1 deploy at 12:14 UTC (HIBERNATE windows + small post-fix duration). Forward-looking PASS pending a NORMAL-mode window with sample. |
| 4 | Post-grad loss investigation outcome | ✅ **PASS** | Session 3 investigation complete (`docs/audits/POST_GRAD_LOSS_INVESTIGATION_2026_05_01.md`). Session 4 surfaced major correction: all 280 graduation_* exits in 14d are from `analyst` (not speed_demon); analyst disabled since 2026-04-28 13:02 UTC via ANALYST-DISABLE-002. Current attributable SOL ≈ 0/week (bleed already stopped). POST-GRAD-ENTRY-GATE-001 re-scoped to insurance-only Tier 2. |
| 5 | 48h paper observation post all behavioral changes | 🔴 **FAIL** | Sessions 1-5 deployed today within ~45 min: TIME-PRIME 12:14 UTC, STATE-RECONCILE 12:25 UTC, POST-GRAD 12:35 UTC, ML-RETUNE 12:45 UTC, LIVE-FEE-CAPTURE-002 12:55 UTC. <1h since most-recent behavioral deploy. 48h window opens ~2026-05-03 12:55 UTC. Current sample: 1 SD-paper close since Session 1 deploy. |
| 6 | ML retune verification | 🟡 **CONDITIONAL** | Session 4 STOPPED per §8 (AGGRESSIVE_PAPER+TEST_MODE override at `signal_aggregator.py:158` sets effective threshold to 30 regardless of env value; bot_core has no second gate). NO env change deployed → trivially no rollback. Per session prompt nuance: "acceptable if reverted cleanly, but reduces confidence in the model's edge." Threshold sweep documented optimum=55 for future implementation. |
| 7 | Service health | 🔴 **FAIL** | bot:status RUNNING ✓; bot:emergency_stop ABSENT ✓; market:sol_price=84.3 (recent) ✓; **market:mode:current = HIBERNATE** at 12:58:54 UTC. Per §2 PC7: "Fail if `market:mode:current` is HIBERNATE (cannot test live in HIBERNATE)." MARKET-MODE-001 fix had cycled to NORMAL during Session 1; current mode reflects normal cycling per recalibrated thresholds (MARKET-MODE-001-RE-CALIBRATE 24h observation queued). |
| 8 | Drift-free env vars | 🟡 **CONDITIONAL** | ML threshold drift documented across services: SA=65, bot_core=40, web=45 (per ENV_AUDIT_2026_04_29 and Session 4 verification). Divergence is documented but not corrected — `BOT-CORE-ML-GATE-001` and `AGGRESSIVE-PAPER-DISABLE-001` queued. AGGRESSIVE_PAPER_TRADING segregation correct (only on signal_aggregator). MIN_POSITION_SOL=0.05 ✓. MAX_POSITION_SOL_FRACTION not in Railway env (relies on code default 0.10 — flagged in AGENT_CONTEXT.md §2). |
| 9 | Recent bot_core P&L sanity | 🟡 **CONDITIONAL** | 7d SD-paper aggregate: -0.98 SOL on 682 trades (mean -0.0014/trade). Negative aggregate but ≥-2 SOL threshold = CONDITIONAL per session prompt. 14d aggregate +9.0 SOL (positive); recent 4d -1.18 SOL. Direction-of-trend is degradation; no single-day drawdown >1 SOL observed. Watch rolling 14d to confirm regression vs noise. |
| 10 | Kill switch readiness | ✅ **PASS** | Railway env `TEST_MODE=true` flip available (per CLAUDE.md "Live trading mode — session-gated"); `bot:emergency_stop` Redis key path present in bot_core code; both mechanisms tested in 2026-04-25 EMERGENCY_STOP incident and verified functional. |

## §3 Recommendations

### Live mode parameters when verdict becomes GO or CONDITIONAL_GO

```
TEST_MODE: false (set on bot_core ONLY; signal_aggregator stays true)
MAX_POSITION_SOL: 0.10 (reduced from 0.25 for first 24h; limits blast radius)
MAX_SD_POSITIONS: 5 (reduced from 20 for first 24h)
Suggested live trial duration: 24h before scale-up evaluation
Re-run V5A-GO-NO-GO after 24h
```

**Rationale for reduced caps:** Recent 7d SD-paper aggregate is -0.98 SOL (mildly negative). Paper-to-live edge ratio is empirically untested at scale (single live trade id 6580 showed 96× divergence between paper +0.002 and live -0.094 SOL). Conservative initial sizing limits blast radius until live data confirms paper conclusions.

### Scale-up plan (post-24h positive trial)

If first-24h positive: increase `MAX_POSITION_SOL` to 0.15, `MAX_SD_POSITIONS` to 10. Run another 24h. If still positive, full caps (0.25 / 20).

If first-24h negative: revert TEST_MODE=true, investigate root cause (likely candidate: `no_momentum_90s` leak amplification at sizing factor — `NO-MOMENTUM-90S-AUDIT-001` becomes critical-priority).

### Kill-switch refresher (Jay)

Primary: Railway UI → bot_core → Variables → set `TEST_MODE=true`. Effective on next bot_core cycle (typically <60s).

Backup: Redis MCP `set bot:emergency_stop "1"`. Effective immediately on bot_core consumer.

Both mechanisms have been tested historically; documented in CLAUDE.md "Emergency Stop Reset" + "Operating Principles".

## §4 Outstanding risks even at GO

1. **Paper-to-live edge ratio is untested at scale.** Single live data point (id 6580) showed 96× divergence (paper +0.002 → live -0.094 SOL on a 0.365 SOL pre-grad pump.fun position). FEE-MODEL-001, FEE-LATENCY-REALISM, and Path B address fee/slippage accuracy on the LIVE side. Paper-side edge degradation under live execution costs is the open question.
2. **`no_momentum_90s` + `stop_loss_20%` pre-grad bleed unaddressed.** Combined ~-15 SOL/14d on SD-paper. Live mode amplifies linearly with sizing factor. NO-MOMENTUM-90S-AUDIT-001 queued.
3. **HIBERNATE mode cycling reduces live throughput.** Recalibrated MARKET-MODE-001 thresholds may swing the bot to HIBERNATE during quiet periods, blocking entries during trial windows. Re-run audit during NORMAL mode windows.
4. **Reduced confidence in ML edge.** Effective paper threshold is 30 (AGGRESSIVE_PAPER+TEST_MODE override); empirical optimum is 55 per Session 4 sweep. `BOT-CORE-ML-GATE-001` queued. Live mode would use signal_aggregator's env value 65 (since AGGRESSIVE_PAPER+TEST_MODE override doesn't apply when TEST_MODE=false). The shift from 30→65 at flip is a regime change for paper→live signal admission criteria.

## §5 Plan for first 24h of live operation (when GO)

### Hourly monitoring queries

```sql
-- 1. Live trade count + cumulative P&L
SELECT COUNT(*) AS n,
       ROUND(SUM(corrected_pnl_sol)::numeric, 4) AS sum_corrected,
       ROUND(SUM(realised_pnl_sol)::numeric, 4) AS sum_realised
FROM paper_trades WHERE trade_mode='live' AND entry_time > <FLIP_EPOCH>;

-- 2. Path B success rate (should be ≥95% live_actual_v1)
SELECT correction_method, COUNT(*)
FROM paper_trades WHERE trade_mode='live' AND entry_time > <FLIP_EPOCH>
GROUP BY 1;

-- 3. Drawdown vs DAILY_LOSS_LIMIT_SOL=4.0
SELECT total_balance_sol, daily_pnl_sol
FROM portfolio_snapshots ORDER BY snapshot_at DESC LIMIT 1;

-- 4. Wallet vs portfolio reconciliation
-- Helius getBalance(TRADING_WALLET) compared to portfolio_snapshots.total_balance_sol
-- Divergence > 0.01 SOL is a red flag (sell-storm or unaccounted outflow)
```

### Live-trial flip-back triggers

Revert to TEST_MODE=true if any of:
- Cumulative drawdown > 5% on 24h window
- Sell-storm circuit breaker fires on 3+ distinct mints
- Helius parse failure rate > 10% over 100+ live trades
- RuntimeError / AttributeError / ImportError in bot_core logs
- DAILY_LOSS_LIMIT_SOL=4.0 trip
- HIBERNATE rejection of all signals for >2h consecutive

### What to log at flip + flip-back

- Sentry release tag at flip moment
- bot:portfolio:balance Redis snapshot
- portfolio_snapshots.total_balance_sol at flip
- Trade-mode distribution post-flip in 24h sample

## §6 STOP conditions hit

None. All preconditions assessed:
- DB reachable: ✓
- Helius RPC reachable (getBalance + parseTransactions both worked this session): ✓
- Railway MCP reachable: ✓
- Redis MCP reachable: ✓

## §7 Decision Log entry (mirrored to ZMN_ROADMAP.md)

```
2026-05-01 V5A-GO-NO-GO-CHECKLIST-001 ✅ AUDIT COMPLETE — Verdict: NO_GO.
3/10 PASS (PC2 Path B parity, PC4 post-grad outcome, PC10 kill switch),
4/10 CONDITIONAL (PC3 TIME_PRIME small post-fix sample, PC6 ML retune
stopped, PC8 env drift, PC9 7d -0.98 SOL), 3/10 FAIL (PC1 wallet 0.064
SOL << 0.5 floor, PC5 <1h since latest deploy, PC7 market_mode HIBERNATE).
Blocking: wallet top-up (Jay action), 48h paper observation window opens
~2026-05-03 12:55 UTC, market_mode HIBERNATE→NORMAL cycling. Live flip NOT
recommended. Re-run V5A-GO-NO-GO after blockers resolved. Audit:
docs/audits/V5A_GO_NO_GO_2026_05_01.md.
```
