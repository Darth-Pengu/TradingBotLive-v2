# V5A-FLIP-002-V3R — ⛔ NO-FLIP / HALTED at Phase 1 on STOP-M (HIBERNATE + dead signal pipeline)

**Date:** 2026-06-02, ~22:5x Sydney AEST (UTC ~12:5x). Tuesday.
**Session prompt:** referenced this audit as `docs/audits/V5A_FLIP_002_V3R_2026_06_01.md` (prompt-authoring date 2026-06-01); filed under the **actual session date 2026-06-02** per house audit-dating convention. Same document.
**Verdict:** ⛔ **NO FLIP.** Preflight gate failed at Phase 1.7/1.8 — `market:mode:current=HIBERNATE` with a near-dead signal pipeline (14 new tokens/hr). Per D-S4 (binding) and §1.7/§8 STOP-M, HIBERNATE aborts. **Zero state writes. Bot remains TEST_MODE=true paper, sizing 4.0/0.25/20, no emergency_stop. Wallet 5.064095633 SOL on-chain, unchanged (read-only session).** PC4 stays `[ ]` outstanding. **No rollback needed — nothing flipped.** Not STOP-Rollback (market-timing halt, not a failed flip) — re-attemptable in the next non-HIBERNATE window with the SAME authorization framework.

---

## §1 Headline

This was an authorized live-flip session (Jay-pasted = PC4 authorization per CLAUDE.md "Live trading mode — session-gated"; D-S5 timing waived per operator amendment). It executed Phase 1 preflight fully (read-only) and **halted at the market-regime gate before any state was written**. Every readiness check passed **except market regime**, which failed two independent STOP-M triggers:

1. **`market:mode:current = HIBERNATE`** (§1.7 / D-S4 — the explicit abort condition).
2. **Signal pipeline effectively dead** (§1.8) — `market:new_token_count_1h = 14` (was 10,257 on 2026-05-20, a ~700× collapse); `service:health.pumpportal = "no signals"`.

A flip into this state would be inert at best (almost nothing to trade), wasting the D-S7 observer window and the live-data-gathering purpose; at worst it trades thin illiquid conditions where the cost-fidelity gap is widest. The runbook and the binding V5A decisions are unanimous: do not flip in HIBERNATE.

**Distinction from V5A-FLIP-001-V2 (2026-05-20):** that session FLIPPED and ROLLED BACK after an in-container reconcile failure (14 phantom rows). **This session never flipped** — it stopped at the preflight gate. There is no contamination, no synthetic loss, no rollback. The wallet was only read, never transacted.

---

## §2 Wallet safety

| Time | Source | Value |
|---|---|---|
| Phase 1.6 (session start) | Helius `getBalance(4h4pst…)` | 5.064095633 SOL (5,064,095,633 lamports) |
| (cross-check) | Redis `bot:onchain:balance` | 5.064095633 |
| Session end | — (no on-chain activity; read-only session) | 5.064095633 SOL (unchanged) |

**Delta: 0 lamports.** No transaction was ever signed or submitted. `execution.py` was never invoked (TEST_MODE never left `true`).

---

## §3 What the preflight verified (everything-else-GO)

| # | Check | Result | STOP |
|---|---|---|---|
| 1.1 | Railway auth | ✅ logged in; 10 services | A — no |
| 1.2 | MCP no-op battery | ✅ railway / redis / helius / DB-shim / git all callable | — |
| 1.3 | Commits at HEAD | ✅ `f3591eb`, `3c50520`, `7458f2d` all ancestors of HEAD `7d33994`; local==origin (0/0) | H — no |
| 1.4 | Source presence of fixes | ✅ reconcile filter, entry-sig wiring, PC3 live MC gate, per-decision estop, sell-storm=8, ORIGIN_MISMATCH all present | H — no |
| 1.5 | Deploy-SHA hard verify | ⚠ **UNCONFIRMABLE** — Railway CLI v4.6.0 lacks `deployment list` (needs ≥4.10.0). Forced-redeploy fail-safe NOT triggered (flip halted on STOP-M; a redeploy is a flip-enablement write). | Deploy — n/a |
| 1.6 | Wallet | ✅ 5.064095633 SOL ≥ 5.0 | Wallet — no |
| **1.7** | **Market mode (D-S4)** | ❌ **HIBERNATE** (cfgi 34.0, sentiment 22.5, SOL $78.84; ts fresh 12:54:34Z); `market:mode:override` not set | **🔴 STOP-M** |
| **1.8** | **Signal liveness** | ❌ **14 new tokens/hr**; pumpportal "no signals" | **🔴 STOP-M** |
| 1.9 | Path B re-validation (id 6580) | ✅ on-chain native delta **−374,251,786 lamports** (exact); DB row `live_actual_v1`, both sigs populated | PathB — no |
| 1.10 | Orphan baseline (both tables) | ✅ **`trades` live-open = 0** (load-bearing); paper-open = 0; any-mode open = 6 (paper orphans Bug-1 skips); MAX(pt.id)=10926 | Orphan — no |
| 1.11 | Sell-storm breaker | ✅ default 8 | — |
| 1.12 | Concurrent session | ✅ git clean (0/0) | L — no |

Full evidence: `.tmp_v5a_flip_v3r/01_preflight.md`.

The structural fixes that made V3 safe vs V2 are confirmed present in source AND reachable from HEAD, and the May-20 failure vector is clean (live-mode reconciler query returns 0 open rows). The blocker is purely market regime, not code or safety.

---

## §4 The STOP-M condition in detail

`market:health` blob (Redis, ts 2026-06-02T12:54:34Z — fresh, written by market_health which is alive):

```
mode=HIBERNATE  sentiment_score=22.5  cfgi=34.0  cfgi_btc=23.0
sol_price=78.84  sol_1h=-0.18%  sol_24h=-1.94%  dex_vol_24h=$1.755B
```

Signal-flow evidence:
- `market:new_token_count_1h = 14` (vs 10,257 on 2026-05-20 per API-MCP-PREFLIGHT-001).
- `market:migration_count_1h` absent.
- `service:health.pumpportal = warn / "no signals"`.
- `bot:filter:fill_mc_ceiling:rejects:2026-06-02 = 2` (bot processed only ~2 gate-eligible signals today — consistent with the lull; vs 1,982 on 2026-05-28).

**Market context shift since the runbook was authored / since the last preflight (2026-05-20):** SOL ~$85 → $78.84; market mode DEFENSIVE → HIBERNATE; new-token flow 10,257/hr → 14/hr. This reads as a broad memecoin-market lull, not a one-service outage (market_health, redis, gecko, jupiter, jito, vybe, dexpaprika all `ok` in `service:health`).

**Why no override:** `market:mode:override` is not in the §2 scope fence (permitted writes are emergency_stop / consecutive_losses / bot:status DEL / position-key clears / the flip env vars only). Forcing an override to NORMAL would (a) be out-of-scope (STOP-Scope), and (b) defeat the exact safety purpose of D-S4. Both STOP-M triggers are real market state, not a stale/missing key.

---

## §5 Scope discipline — ZERO writes

- **No** `TEST_MODE` change (stayed `true`).
- **No** env var changed (sizing remains 4.0/0.25/20; the Phase 2 reconcile to 1.5/0.10/5 was never reached).
- **No** Redis writes (no `bot:emergency_stop`, no `bot:consecutive_losses`, no DELs). CLEAN-003 never ran.
- **No** Postgres writes (read-only SELECTs only).
- **No** forced redeploy / no `railway up` / no service restart.
- **No** other service touched. No code change. No schema migration.

The only side effects of this session are: read-mostly DB/Redis/Helius queries, and the documentation commit (this audit + canonical doc updates).

---

## §6 STOP register — every STOP evaluated

| STOP | Fired | Note |
|---|---|---|
| STOP-A | no | Railway logged in |
| STOP-H | no | commits + source present; newest STATUS = 2026-05-28 (expected predecessor) |
| STOP-Deploy | n/a | not reached (forced redeploy not attempted — flip halted) |
| STOP-Wallet | no | 5.064 SOL ≥ 5.0 |
| **STOP-M** | **YES (×2)** | HIBERNATE + dead pipeline |
| STOP-PathB | no | id 6580 exact on-chain match |
| STOP-Orphan | no | trades live-open = 0 |
| STOP-Reconcile | n/a | post-flip only — never flipped |
| STOP-Contamination | n/a | post-flip only — never flipped |
| STOP-DailyHalt | n/a | post-flip only — never flipped |
| STOP-Scope | no | zero state writes |
| STOP-Loop | no | no verify retried |
| STOP-L | no | git clean |
| STOP-Claude | no | no limit hit |
| STOP-Rollback | n/a | nothing flipped → nothing to roll back; this is a re-attemptable market-timing halt, NOT a post-rollback terminal state |

---

## §7 Carry-forward (next attempt)

The flip is **re-attemptable** under the same authorization framework once the market recovers — this is NOT STOP-Rollback (no failed flip, no diagnosis-and-fix gate). The next session can re-paste V5A-FLIP-002-V3R (or a thin V3R re-run) and proceed when:

1. **Market regime recovered:** `market:mode:current` ∈ {NORMAL, DEFENSIVE, AGGRESSIVE} (HIBERNATE clears) **AND** `market:new_token_count_1h` back into the thousands (pumpportal flowing). D-S5 timing remains waived; D-S7 4–6h active-observer commitment still applies, so re-attempt when Jay can watch.
2. **Deploy-SHA tooling fixed:** Railway CLI is **v4.6.0** on this machine — upgrade to **≥4.10.0** so Phase 1.5 `list-deployments` can positively confirm the running container includes `7458f2d`. Until then the only path to confirm the running code is the Phase 1.5 forced-redeploy fail-safe (idempotent; ~15–25 min). **Deploy-SHA for `7458f2d` remains UNCONFIRMED since 2026-05-28** — this is the one genuine open item besides market regime.
3. Everything else is pre-verified GO (re-verify at flip time, but no changes expected): wallet 5.064 SOL, all 3 fixes at HEAD + in source, Path B engine intact, orphan baseline clean.

No follow-ups need to be *fixed* before the next attempt (unlike the V2 rollback, which required 3 code fixes). The only prerequisites are environmental (market) and tooling (CLI version).

---

## §8 Outputs

- **NEW** this audit `docs/audits/V5A_FLIP_002_V3R_2026_06_02.md`.
- **UPDATE** `AGENT_CONTEXT.md` (header refresh; PC4 stays `[ ]`; §2 env unchanged; wallet line refreshed with 5.064095633 SOL Helius reading; HIBERNATE no-flip note).
- **APPEND** `ZMN_ROADMAP.md` Decision Log (one row — NO-FLIP / STOP-M).
- **PREPEND** `STATUS.md` + `MONITORING_LOG.md` (full session entry).
- **Single push** of audit + canonical docs.
- **Scratch (gitignored)** `.tmp_v5a_flip_v3r/`: `PROGRESS.md`, `01_preflight.md`, `preflight_db.py`.

---

End of session audit. Bot in paper mode, untouched. Re-attempt in the next non-HIBERNATE window.
