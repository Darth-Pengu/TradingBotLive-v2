# ZMN Roadmap

> **Single source of truth for all ZMN work items.** Last updated: 2026-04-22.
>
> All prior audit docs remain in `docs/audits/` as evidence and context; their
> actionable items are consolidated here. Update this roadmap at the end of every
> session that completes or changes an item's status. Append a changelog entry.
>
> Audit docs are linked in the **Source** column of each item below — open the
> audit for evidence; trust the roadmap for current status and priority.

---

## Current state snapshot — 2026-04-23

| Field | Value |
|---|---|
| Mode | `TEST_MODE=true` (paper). Live mode is session-gated per CLAUDE.md "Live trading mode — session-gated". **HOLDER-DATA-PIPELINE-001 ✅ FIXED 2026-04-22 (fc87b03 + 4c5508b)** — GeckoTerminal schema dict fix + age-bypass for GATES-V5 HOLDER/BSR gates on fresh tokens. Graduation path confirmed working (GRAD_ACCEPT `61VJc9L1Xekp` 198 holders). V5-staged approach (v5a/b/c/d) remains the live-enable shape; paper cadence should resume post-deploy. |
| Trading wallet (on-chain) | **0.064 SOL** (`4h4pst…ii8xJ`) — awaiting 5 SOL top-up planned for v5 staged attempts |
| Holding wallet (on-chain) | 0.098 SOL (`2gfHQ…ttWJ9`) |
| Paper portfolio balance | **20.0 SOL** (Redis `bot:portfolio:balance` — reset to `STARTING_CAPITAL_SOL` by DASH-RESET `1176e12`) |
| 7-day Speed Demon perf | **0 trades closed pre-fix** (post-DASH-RESET wipe + HOLDER-gate silence since 2026-04-21 — archive table `paper_trades_archive_20260421` preserves the 2,923-trade 7d history). Fresh sample accumulation resumed post HOLDER-DATA-PIPELINE-001 fix (2026-04-22) — first 24h cadence data available 2026-04-23. |
| Active personalities | **Speed-Demon-only** post-Analyst-disable 2026-04-23 (see ANALYST-DISABLE-001, commit `cc8e5c9` — governance Redis override addresses graduation-sniper bypass that the env flag alone did not cover). Speed Demon gated by ML≥65 live / 30 paper + 4 GATES-V5 entry gates. Whale Tracker dormant. ANALYST-POST-GRAD-001 queued (multi-session build; see Tier 2 entry for Phase-1 distilled design summary). |
| Open positions | 0 paper, 0 live |
| Latest commits | `2b4cb73` BITFOOT-2026-BASELINE (verdict BASELINE-A directionally, survivorship-inflated), `e053448` BITFOOT-DIP-SAMPLE-001 retry (verdict DIP-D), `cc8e5c9` ANALYST-DISABLE Redis governance override, `2243d1f` STATUS.md convention seed, `e7dd24a` CLEAN-004 docs, `322e105` live_flip_prep CLEAN-004 script, `512663b` HOLDER-DATA-PIPELINE + BUG-021 partial + CLEAN-004 reset, `c012475` Nansen Session A', `c7f73ee` Nansen v2 ship, `1dec17b` GATES-V5 |
| Sentry integration | ✅ live across 8 services; no new-class issues post-GATES-V5. Only known-noise `ZMN-SIGNAL-AGGREGATOR-1` (SocialData credits — Jay topped up to $10 on 2026-04-22; noise should attenuate). |
| Dashboard live-mode coverage | Recent Trades + aggregates + Treasury + Open Positions all populate via Session 2 v2 + 2b. Post-DASH-RESET tables are empty; fresh paper accumulation blocked on HOLDER-DATA-PIPELINE-001. |
| Blocking issues | ✅ HOLDER-DATA-PIPELINE-001 CLEARED (`fc87b03` + `4c5508b`, 24h verification window ends ~2026-04-23 23:00 UTC). ✅ ANALYST-DISABLE-001 CLEARED 2026-04-23 (`cc8e5c9` governance Redis override — stops 11.9% WR / -2.48 SOL / 17h leak on pre-existing `analyst` personality). **🟡 BUG-022 INVESTIGATED 2026-04-28 (`ca4812d`)** — verdict 2a: writer never existed as committed code (one-shot uncommitted backfill ran 2026-04-13; nothing recurring since). All 853 current closed paper_trades rows are NULL on `corrected_pnl_sol`. Recommended fix Option A (one-time `UPDATE … SET corrected_pnl_sol = realised_pnl_sol, correction_method='pass_through' WHERE corrected_pnl_sol IS NULL` + ~10-line inline write at paper close). For post-`e078b4c` rows (all current rows), `realised_pnl_sol` already incorporates FEE-MODEL-001 corrections — the missing column is symbolic, not data-corrupting. Fix execution scope deferred to follow-up session. Audit: `docs/audits/CORRECTED_PNL_INVESTIGATION_2026_04_28.md`. **🔥 NEW 2026-04-29 ENV-AUDIT findings:** **WALLET-DRIFT-2026-04-29** (V5a blocker — trading wallet on-chain = 0.064 SOL vs ~1.6 SOL baseline; 1.5 SOL drain unaccounted-for; needs Helius forensics) + **TREASURY-TEST-MODE-002** (treasury alone has TEST_MODE=false; latent on-chain risk if wallet ever crosses 30 SOL trigger) + **ML-THRESHOLD-DRIFT-2026-04-29** (SA=65 / bot_core=40 / web=45; 44 of last 100 entries in (0,40] band confirms gate not enforced for paper). Audit: `docs/audits/ENV_AUDIT_2026_04_29.md`. **Outstanding MEDIUM (pre-existing):** SEC-001 (secret rotation — includes Nansen API key per MCP-RECON; waiver exists for one supervised window); stale signal_aggregator env values (SPEED_DEMON_BASE_SIZE_SOL=0.45 / MAX_SIZE_SOL=0.75 — see TUNE-004); `sig_type=None` population on paper opens (see OBS-013); Nansen MCP HTTP 401 (see SEC-001 update); CLEAN-004 consecutive_losses reset ✅ (script follow-up still pending); SocialData auto-top-up alert missing (see SOCIALDATA-AUTO-TOPUP-001 — today's $0 outage recurrence risk); `PRE_FILTER_SCORE_MIN=1.15` may be too restrictive (typical FILTER:PASS mults are 0.84-1.20; monitor post-fix cadence + tune if needed). |
| Redis workaround keys (daily-renewal) | `market:mode:override=NORMAL` (renewed 2026-04-22 ~20:30 AEST, 24h TTL), `nansen:disabled=true` (renewed 2026-04-22, 24h TTL) — still needs daily renewal until proper `AGGRESSIVE_LIVE` env var work |
| Research artifacts (new 2026-04-22 + 2026-04-23) | **Bitfoot dataset loaded at `session_outputs/bitfoot_analysis/`** — 32,851 labeled post-grad Solana ping outcomes + 22,288 peak multipliers over 10.3 months (Era-1, 2025-01→2025-12). Research source-of-record for ANALYST-POST-GRAD-001 + BITFOOT-ML-FEATURE-INTEGRATION-001. Not yet integrated into ZMN ML or personality code. **Added 2026-04-23:** `session_outputs/bitfoot_analysis/BITFOOT_DIP_SAMPLE_DONE.md` (verdict DIP-D — Bitfoot labels not reliable ground-truth; 20.5% of unflagged retrievable pings hit 2x+); `docs/audits/BITFOOT_2026_BASELINE_2026_04_23.md` (verdict BASELINE-A directionally — 2-filter still concentrates winners in 2026 trending data, absolute rates survivorship-inflated; Raydium post-grad vanished; Meteora up from 10%→44% of winners). |

## Decision Log (added 2026-04-30 by Session E)

> Per-lever decisions with date / status / one-line reasoning. Newest at top, append-only.
> The roadmap tier tables are catalogues; the Decision Log is the *judgement trail* —
> use it to understand why a given lever has its current shape.

| Date | Lever | Status | Reasoning (1-line) |
|---|---|---|---|
| 2026-06-03 | §B Phase-2 #11+#12 live startup-state (daily-loss persistence + on-chain balance seed) | ✅ CODE DEPLOYED `e739e07`; flip-confirmed; paper unchanged | **D04-F4 + D04-F2, bundled (both `_load_state`, both live-only).** #11: hard-zeroing `daily_pnl_sol` on every startup let any restart launder the daily-loss accumulator → DAILY_LOSS_LIMIT_SOL could never accumulate. Fix (live-only): reload today's (UTC) realized PnL from `trades` (`pnl_sol`, closed_at≥midnight); paper keeps hard-zero. #12: `_load_state` read the unfiltered latest snapshot → post-flip loaded the ~50 SOL paper balance, inflating exposure/drawdown denominators ~10× vs the real ~5 SOL wallet. Fix (live-only): seed `total_balance_sol` from on-chain getBalance (new `_fetch_onchain_balance_sol`), fallback to snapshot if RPC down. **Both `not TEST_MODE` → paper byte-for-byte unchanged.** py_compile + 6/6 structural. NOT paper-observable; flip-confirmed. Rollback: `git revert e739e07`. Next: #13 sizing caps (final Phase-2). |
| 2026-06-03 | §B Phase-2 #10 FIX-GOVERNANCE-FAIL-OPEN (cfgi read) | ✅ CODE DEPLOYED `b273b54`; cfgi fixed, regime-veto via #9 | **D04-F1/D09-F2/BUG-010.** Fixed governance reading the non-existent `market:cfgi` (→ always neutral 50) → now reads `market:health` (prefers cfgi_sol). VERIFIED already-in-place: bot_core applies gov.size_multiplier (CONSERVATIVE→0.8 works); the missing live regime VETO is now provided by #9 (more reliable than the dead governance gate). **Deferred+flagged GOVERNANCE-STALENESS-POLICY-001** (stale-governance→PAUSE/cap = halt-risk while governance dead; needs a decision). **🚩 Current-state finding:** `governance:latest_decision = CONSERVATIVE/0.8` (LLM dead: Anthropic 400/credits) → all paper trades currently sized 0.8× base; BUG-010 is a real go-live prerequisite for governance to function. py_compile + 4/4 structural. governance is non-trading (cfgi fix can't break paper). NOT paper-observable (cfgi used only when LLM revived). Rollback: `git revert b273b54`. Next: #11+#12 live startup state. |
| 2026-06-03 | §B Phase-2 #9 FIX-HIBERNATE-LIVE-VETO | ✅ CODE DEPLOYED `71272cd`; flip-confirmed | **bot_core now has an effective live regime veto.** D04-F3/D07-F2/D08-F1/D08-F3: only HIBERNATE skip was the SA bypass gated on AGGRESSIVE_PAPER (not TEST_MODE), so a bot_core-only flip with AGGRESSIVE_PAPER=true would trade live in HIBERNATE; bot_core's other gate (governance) is fail-open. Fix: (A) SA bypass gated on `AGGRESSIVE_PAPER and TEST_MODE` → live hard-skips HIBERNATE; (B) bot_core independent live veto reads `market:mode:current` fresh + returns on HIBERNATE when `not TEST_MODE`. **Paper unchanged** (bypass preserved; veto live-only). py_compile + 5/5 structural. NOT paper-observable (live veto); flip-confirmed. One lever (the regime veto). Rollback: `git revert 71272cd`. Next: #10 governance-fail-open. |
| 2026-06-03 | §B PHASE 1 DEPLOY-CONFIRMED + end-to-end Phase-0 validation | ✅ Phases 0+1 complete (code+deploy); paused at Phase-1→Phase-2 boundary | **All 4 Phase-1 deploys came up clean** (bot_core "ready" + trading after each; no import/Type/Syntax errors): #4+#8 `2a85508`, #6 `29fca1b`, #5+D02-F8 `09f71c1`, #7 `94457ef` (behaviour flip-confirmed-only; deployment confirmed). **END-TO-END VALIDATION:** `market:health` now `mode: DEFENSIVE` (was warm-up HIBERNATE), `data_degraded: false`, dex $1.65B; `market:migration_count_1h` climbed 2→6→10 — the bot correctly left HIBERNATE as the counter passed the DEFENSIVE threshold, proving the Phase-0 chain (pubsub→flow→migrations; redis-hardening→increments land; market-mode→classify on real data). NORMAL pending true capture-rate over a clean hour (MARKET-MODE-THRESHOLD-RECALIBRATE-003). Bot: paper, 6 services Online, DEFENSIVE, trading; Redis timeouts largely cleared; wallet 5.064 SOL. **Remaining before flip: Phase 2 (safety rails #9-#13) + Phase 3 (accounting).** Phase 2 = live-money safety gates → surfaced for sequencing, not auto-rolled. |
| 2026-06-03 | §B Phase-1 #7 FIX-EXEC-001-002-ROUTING — **PHASE 1 CODE-COMPLETE** | ✅ CODE DEPLOYED `3e4b502`; flip-confirmed | **Final Phase-1 fix; live-execution-correctness phase done (code).** `bot_core.py`. Removed the `if pos.bonding_curve_progress > 0:` gate so `_check_pool_state_fresh` runs for EVERY live sell → fixes D02-F4 (whale/Raydium bc=0 → were 400'ing on the pump.fun path) + D03-F3 (reconciler-restored positions lose bc_progress → stale pre-grad routing of graduated tokens). Helper returns 1.0 (BC closed→non-local/Jupiter) / 0.0 (BC live→pre-grad) so refresh always routes correctly; avoids a schema migration; fail-closed-to-stale on RPC error. EXEC-002 already resolved; D03-F8 force-abandon deferred (EXEC-FORCE-ABANDON-001). py_compile PASS; 4/4 structural. NOT paper-observable; flip-confirmed. **§B Phase 1 (live-execution correctness) = #4+#8, #5+D02-F8, #6, #7 all ✅ in code — flip-confirmed-only (live branch not paper-observable).** Deferred reliability follow-ups filed: SELL-STORM-PARK-PERSISTENCE-001 (D04-F10), EXEC-FORCE-ABANDON-001 (D03-F8), JITO-REIMPLEMENT-001. Next: §B Phase 2 (safety rails: #9 HIBERNATE-live-veto, #10 governance-fail-open, #11 daily-loss-persistence, #12 live-balance-seed, #13 sizing-caps), then Phase 3 (accounting). Rollback: `git revert 3e4b502`. |
| 2026-06-03 | §B Phase-1 #5 FIX-PARTIAL-SELL-SIZING (+D02-F8) | ✅ CODE DEPLOYED `3b6d0b1`; flip-confirmed | **Staged-TP live sells no longer dump the whole bag.** (Caught my own omission — #5 was dropped from the "next" list; it's a 🔴.) `execution.py` + `bot_core.py`. **D02-F5:** pre-grad `_execute_pumpportal_local` SELL hardcoded `"amount":"100%"` → every partial/staged-TP live sell dumped the entire position (phantom remainder). **D02-F8 (done here, was #7 scope):** Jupiter post-grad sell sold the FULL wallet balance. Fix: threaded `sell_fraction: float = 1.0` through `execute_trade` → both routes; `_close_position` passes `sell_fraction=sell_pct` (fraction of CURRENT balance, matching `remaining_pct *= (1-sell_pct)`); pre-grad sends `"X%"`, Jupiter sends `int(balance*frac)`. Full closes (1.0) unchanged. py_compile PASS; arithmetic 10/10 + 4/4 structural. NOT paper-observable; flip-confirmed (validate vs multi-staged-TP close). One lever. Rollback: `git revert 3b6d0b1`. Next: #7 EXEC-001/002 routing (final Phase-1, now smaller). |
| 2026-06-03 | §B Phase-1 #6 FIX-BUY-IDEMPOTENCY (double-submit guard + Jito-off + D02-F14) | ✅ CODE DEPLOYED `2f00b19`; flip-confirmed | **Stops live double-spend.** `execution.py`. **D02-F3:** the retry loop re-broadcast the same tx on a confirmation miss (double-spend on a buy that landed). New `_get_signature_status()` (getSignatureStatuses, 3-tier RPC); on confirm-miss execute_trade decides from on-chain status: landed→success (no resubmit), failed→resubmit, BUY-unknown→record-pending-with-sig (never double-buy), SELL-unknown→failure (caller #4 parks+retries, no maybe-unlanded close booked). **D02-F2/F7:** forced `use_jito=False` — the Jito bundle path returns the bundle UUID (not a tx sig → broke confirmation+Path B+fed the double-submit) and adds no tip (never lands); now uses real-sig `_send_transaction` (`JITO-REIMPLEMENT-001` follow-up filed). **D02-F14:** `_confirm_trade_helius` unset-parse-URL → confirmed=False (verify via getSignatureStatuses) not blind-True (env-checked: URL is set, so latent). py_compile PASS; `.tmp_phase1/verify_idempotency.py` 6/6 (helper unit-tested via mocked RPC) + 9/9 structural + review. **NOT paper-observable (live branch) — runtime-confirmed at the flip.** One lever (execution.py only). Rollback: `git revert 2f00b19`. Next: #7 EXEC-001/002 (+D02-F8 Jupiter sizing). |
| 2026-06-03 | §B Phase-1 #4+#8 MERGED (live-sell result-check + emergency-stop robustness) | ✅ CODE DEPLOYED `2a85508`; flip-confirmed (live branch not paper-observable) | **First live-execution-correctness fix.** Merged because Phase-0 analysis showed #4 changes the raise-behaviour #8 depends on (if #4 raised on a failed sell, emergency_stop's unguarded loop would abort on the first un-sellable mint). **#4 (D02-F1):** `execute_trade` returns `success=False` (never raises) → old `except ExecutionError` was dead code → a failed live sell was booked as a successful close (SOL stranded, fabricated oracle PnL, position popped + never retried). New `if not result.success:` + shared `_handle_failed_live_sell()` (park-and-continue, never raises) leaves the position OPEN for `_check_exits` retry, no booking/pop. Protects partial/staged-TP sells too. **#8 (D03-F1/D04-F8):** emergency_stop guards each close (one failure can't abort the stop), detects left-open via `key in self.positions`, sets durable Redis `bot:emergency_stop`, always runs alert+publish. py_compile PASS; `.tmp_phase1/verify_phase1_4_8.py` 10/10 PASS. **NOT paper-observable (live `else:` branch) — runtime-confirmed at the supervised flip.** One lever (bot_core only; paper branch untouched). D04-F10 (park persistence) deferred (less acute post-crash-loop-fix). Next: #6 FIX-BUY-IDEMPOTENCY, then #7 EXEC-001/002 (+D02-F8 Jupiter sizing). Rollback: `git revert 2a85508`. |
| 2026-06-03 | DEPLOY-OBSERVABILITY (§B Phase-0 #3) — **PHASE 0 COMPLETE** | ✅ CODE DEPLOYED `51ed450`; REDIS-HARDENING runtime-confirmed | **Phase-0 done: bot recovered + hardened + observable.** REDIS-CLIENT-HARDENING-001 runtime-confirmed (`2337565`): bot_core supervise-restarts = 0 (safety listeners stable; was #8/#9 every 60s), `market:migration_count_1h` climbing 2→6 (increments landing), pipeline flowing — HIBERNATE persists as warm-up (counter still filling; MARKET-MODE-THRESHOLD-RECALIBRATE-003 tracks steady-state). #3 makes a crashed internal service VISIBLE + ALERTED (the D12-F2/F3/F4 gap that made 05-28 silent): folded liveness checks into the existing `web` `_service_health_checker` (NO new billable worker) — internal-service rows in `service:health` (bot_core/signal_aggregator via heartbeat keys, signal_listener/market_health via freshness proxies; TTL → absence==down) + rate-limited Discord down-alert; dashboard gains a "ZMN Services" section. py_compile PASS; liveness keys verified present; HTML/JS aligned. Stronger-option follow-up: `continuous_audit.py` as its own Railway service (survives a `web` outage). **§B Phase-0 = #1 pubsub-isolation+leak-hotfix ✅, #2 market-mode ✅, #2.5 redis-hardening ✅, #3 observability ✅.** Next: §B Phase 1 (live-execution correctness: D02-F1/F3/F5, D03-F1) before any live flip. Rollback: `git revert`. |
| 2026-06-03 | REDIS-CLIENT-HARDENING-001 (Phase-0 reliability) | ✅ CODE DEPLOYED `b72fac2`; runtime-observe pending | **Surfaced by #1/#2 prod observation.** `market:health` showed dex=$1.65B healthy + `market:migration_count_1h=2` → HIBERNATE: the market-mode fix correctly enforced a PRESENT-but-low counter (`data_degraded=false`), and the 2/hr is a warm-up + Redis-timeout-dropped-increment artifact. The persistent `Timeout reading from redis.railway.internal` (~6s) keeps dropping counter increments/reads AND churns the safety listeners (60s backoff). Hardened ALL 15 `aioredis.from_url` sites across 11 files with `socket_keepalive=True, health_check_interval=30, retry_on_timeout=True` (health_check reconnects Railway-proxy-dropped conns; retry_on_timeout retries transient read-timeouts; keepalive holds idle conns). **Verified construct-safe on redis-py 7.4.0 BEFORE editing** (all-services change → a rejected kwarg would crash every service at startup; from_url accepted all 3). py_compile 11/11 PASS. **NEW `MARKET-MODE-THRESHOLD-RECALIBRATE-003` (🟡)** filed: once the counter accumulates a clean hour, verify capture-rate vs thresholds + consider warm-up bypass / Postgres-backed count. One lever (no trading-logic change). Rollback: `git revert`. Next: observe, then §B Phase-0 #3 DEPLOY-OBSERVABILITY. |
| 2026-06-03 | FIX-PUBSUB-ISOLATION RUNTIME-CONFIRMED + leak hotfix + FIX-MARKET-MODE-MISCLASSIFICATION | ✅ Phase-0 #1 DONE (bot RECOVERED); #2 deployed; NEW REDIS-CLIENT-HARDENING-001 filed | **The bot is back.** Prod observation of `98c8007`: all 6 services ● Online, crash-loop RESOLVED, pipeline flowing, paper trades entering — `supervise` confirmed catching the exact redis `TimeoutError` that caused the 05-28 outage. **Leak hotfix `9fa45b0`:** the supervise-restart exposed a connection leak (3 listeners — signal_listener._token_subscribe_listener, governance._trigger_listener, dashboard._redis_broadcaster — lacked `finally: aclose()` → each restart leaked a pool conn → `MaxConnectionsError`); added cleanup-on-exit + concise supervise logging; MaxConnectionsError gone post-deploy. **#2 FIX-MARKET-MODE-MISCLASSIFICATION `5a3e5aa`** (`market_health.py`): missing-data != dead-market — absent migration counter → `None` abstains (not veto; vs genuine 0), DefiLlama None+last-good (not 0.0), fabricated pumpfun leg dropped from binding decision, total-data-loss → DEFENSIVE not HIBERNATE, `data_degraded` flag. 10/10 classifier tests PASS incl. the $1.75B-dex+absent-counter→NORMAL outage case. **NEW `REDIS-CLIENT-HARDENING-001` (🟡, Phase-0, prod-observed):** persistent `Timeout reading from redis.railway.internal` (~6s cadence) is environmental Railway-Redis slowness the resilience fix tolerates but doesn't cure; safety listeners back off to 60s under it — harden `aioredis.from_url` (keepalive/health_check/retry_on_timeout) before live. Rollbacks: revert per-commit. Next: observe #2, then REDIS-CLIENT-HARDENING-001, then §B Phase-0 #3 DEPLOY-OBSERVABILITY. |
| 2026-06-03 | FIX-PUBSUB-ISOLATION (§B Phase-0 #1) | ✅ CODE DEPLOYED `98c8007`; runtime-observe pending; restores the crash-looped bot | **The #1 flip-unblocker — the bot has been DOWN since ~05-28 on a dual-service pubsub crash-loop.** Root fix: NEW `services/async_utils.py` `supervise(coro_factory, name)` (restart-on-crash w/ capped exp backoff; STOP on clean return; PROPAGATE CancelledError) wraps every member of all 7 service top-level `asyncio.gather`s (signal_listener/bot_core/ml_engine×2/signal_aggregator/market_health/governance) + dashboard bg-tasks; `main.py` single-service path routes through the existing `run_service()` supervisor (was a bare `await mod.main()` — the 2nd amplifier, D12-F5). A transient redis pubsub TimeoutError in an unguarded `async for pubsub.listen()` now restarts just that task (and re-subscribes) instead of tearing down the container → Railway crash-loop. **Design choice:** supervise-at-the-gather (zero edits to 7 different listener bodies) over per-listener reconnect rewrites — minimal, uniform, reversible, and supervised-restart IS the self-healing the audit required. Resolves PIPELINE-PUBSUB-ISOLATION-001 + BOT-CORE-EMERGENCY-LISTENER-PUBSUB-ISOLATION-001 in code; fixes D01-F1/F2/F3/F4/F5/F6 + D12-F5. **One lever:** no trading-logic touch; D10-F1 Redis-password log left for FIX-SECRET-LOGGING. py_compile 9/9 PASS; `.tmp_pubsub_fix/verify_pubsub_isolation.py` 25/25 PASS (behavioral A1-A4 + structural). Single `git push` redeploys ALL services (fix touches all; bot already down → simultaneous restore intended). **NOT runtime-verified against live Railway yet** — observe bot_core + signal_listener reach RUNNING. Rollback: `git revert 98c8007` + push. Next: §B Phase-0 #2 FIX-MARKET-MODE-MISCLASSIFICATION. |
| 2026-06-03 | FULL-CODE-AUDIT-001 | 🔎 READ-ONLY comprehensive pre-flip audit; ~90 findings (🔴×14); DO-NOT-FLIP; §B = canonical remediation sequence | **Systematic 12-dimension audit of the whole codebase (Opus 4.8 + multi-agent: recon→12 dims→adversarial verify; 77 agents).** Hunted every defect that can lose money / hang a position / crash a process / make accounting lie before the real-capital flip. **Verified NON-findings:** (a) TEST_MODE money-path gating is correct & defense-in-depth — no real on-chain send can fire in paper; (b) wallet private key NOT leaked in code (empirical). **NEW 🔴 execution blockers** (masked because validated trade id 6580 was a single full round-trip): **D02-F1** failed live sells booked as closed (`execute_trade` returns success=False/never raises → `except ExecutionError` at bot_core:1366 is dead, `result.success` never checked on the sell path → SOL stranded, accounting lies, position popped); **D02-F5** pre-grad `_execute_pumpportal_local` SELL hardcodes `"amount":"100%"` → every partial/staged-TP live sell dumps the whole position; **D02-F3** buy double-submit on confirm timeout (no idempotency); **D03-F1** emergency_stop has no per-position guard → unreliable in a mass dump. **CONFIRMS+EXTENDS the outage** (PIPELINE-PUBSUB-ISOLATION-001): pubsub-crash in **5 services not 2** (ml_engine/governance/dashboard); all 6 gathers miss `return_exceptions`; **single-service `main.py` has no supervised restart** (resilient `run_service()` wired only to dead legacy mode) = 2nd amplifier. **Outage INVISIBLE:** heartbeats unread, no internal-service dashboard rows, `continuous_audit.py` (only liveness alerter) undeployed — why ~05-28 went silent. **Fail-opens:** governance (BUG-010), AGGRESSIVE_PAPER HIBERNATE bypass, daily-loss reset-on-restart, phantom `MAX_SD_POSITIONS` (cap hardcoded 3 → V5A 5/7 ladder unenforceable), dead `market:loss_override` (3 writers/0 readers), stale-balance inflates exposure/drawdown ~10×. **Accounting:** live close books only final-partial PnL on staged TPs; Path B pairs full-entry with final-partial exit → corrupts `corrected_pnl_sol` on multi-sell trades. **Adversarial verify** downgraded 6/13 NEW 🔴→🟠 + refuted 1 (D09-F3). Confirmed RESOLVED: EXEC-002 (Jupiter NameError); solders signing API. ZERO state writes. Audit `docs/audits/FULL_CODE_AUDIT_001_2026_06_02.md` (§B = ordered pre-flip fix-sessions: Phase 0 restore [FIX-PUBSUB-ISOLATION + FIX-MARKET-MODE + DEPLOY-OBSERVABILITY] → Phase 1 execution → Phase 2 safety → Phase 3 accounting, all GREEN before `TEST_MODE=false`). Tiered follow-ups filed below. |
| 2026-06-02 | MARKET-REGIME-DIAGNOSTIC-001 | 🔎 READ-ONLY; HIBERNATE = pipeline OUTAGE misclassified (NOT a market lull); DO-NOT-FLIP; PC4 stays `[ ]` | **Resolves the V3R HIBERNATE verdict — the predecessor's "broad memecoin lull" interpretation is REFUTED.** All 6 sub-verdicts survived adversarial verification (conf 0.88-0.95). **(Q1) HIBERNATE-MISCLASSIFIED:** `_determine_market_mode` (`market_health.py:281`) ANDs 3 legs; live `dex_vol=$1.753B` clears NORMAL + `pumpfun_vol=$263M` (=`dex×0.15` placeholder, L390) clears AGGRESSIVE, but `grad_rate=0` (`market:migration_count_1h` ABSENT) single-leg-vetoes to HIBERNATE. **(Q2) LIVE-TRADES-IN-HIBERNATE:** HIBERNATE skip is `signal_aggregator.py:1741`, gated on `AGGRESSIVE_PAPER` (NOT TEST_MODE); bot_core has no independent HIBERNATE skip; a bot_core-only flip → aggregator keeps feeding → live trades in HIBERNATE (refutes "flip inert"); the flip's redeploy also revives the CRASHED bot_core into live mode. **(Q3)** bypass configured-active now (id 10926 06-02 12:47Z, labeled DEFENSIVE) but starved ~1 trade/24h. **(Q4) VALIDATION-WAS-TRADEABLE-REGIME:** +8.91 SOL/day / 91.9% WR / n=1066 (05-20..28) ran genuine NORMAL(830)/DEFENSIVE(236)/HIBERNATE(0); snapshots NORMAL 1747 / DEF 691 / HIB 0 → **PC2 NOT re-opened** (cost-fidelity gap applies separately; full Path-B cost-correction still leaves +32.5 SOL / 76.7% WR). **(Q5) FLOW-DEGRADED:** signal_listener + bot_core BOTH CRASHED, crash-looping ~6.7s on `redis.TimeoutError` in pubsub `.listen()` via unguarded `asyncio.gather` (`signal_listener.py:335`/`1395`; bot_core ~`L2410`); 3 external sources: pump.fun ~1,500-2,000 launches/hr + ~350 graduations/day → feed captures ~3%; bot DOWN since ~2026-05-28T13:00Z (zero trades 05-29..06-01; snapshots stop 05-28T12:57, the unconditional 5-min heartbeat → ~1,150 missing = process down). **VERDICT: PATH C + PATH D, same root cause; the system is DOWN, not hibernating — DO NOT flip into a broken pipeline.** ZERO state writes (read-only). NEW finding `docs/findings/MARKET_REGIME_GAP.md`; audit `docs/audits/MARKET_REGIME_DIAGNOSTIC_001_2026_06_02.md`. **NEW follow-up `PIPELINE-PUBSUB-ISOLATION-001` (Tier 1 🔴, V5A flip-blocker)** — promote+extend `BOT-CORE-EMERGENCY-LISTENER-PUBSUB-ISOLATION-001` to isolate the pubsub `.listen()` loops from the top-level `asyncio.gather` in BOTH `signal_listener` (L335/L1395) and `bot_core` (~L2410); acceptance gate before any flip = both services RUNNING (not CRASHED), counters recovered into the hundreds-thousands/hr, mode non-HIBERNATE because all legs genuinely clear (no manual override). **Secondary `MARKET-MODE-001-RE-CALIBRATE-002` (Tier 2 🟡):** the single-leg `grad_rate` veto conflates missing-data with dead-market; replace the `pumpfun_vol = dex_vol*0.15` placeholder (`market_health.py:390`) with a real stats call + recalibrate thresholds. |
| 2026-06-02 | RAILWAY-CLI-UPGRADE (V3R follow-on) | ✅ CLI 4.6.0→4.66.0; deploy-SHA RESOLVED; +1 Tier-2 follow-up | **Jay-requested CLI update; closed the deploy-SHA carry-forward + surfaced a startup fragility.** Railway CLI upgraded 4.6.0→4.66.0 (npm global) → `list-deployments` works → **bot_core active deployment = `39b44e7` (descendant of `7458f2d`)**, confirming all 3 V5A fixes (`f3591eb`/`3c50520`/`7458f2d`) RUNNING in production (was unconfirmed since 2026-05-28). bot_core healthy (`bot:status=RUNNING test_mode=true HIBERNATE`, heartbeat alive). **Observed transient crash + self-recovery:** the V3R docs pushes each redeploy bot_core (watchPatterns empty); deployment `298833d` CRASHED on `redis.TimeoutError` in `_emergency_listener` (`bot_core.py:2067`) via unguarded `asyncio.gather` (`bot_core.py:2410`); follow-up `39b44e7` came up SUCCESS (same code). **NEW Tier-2 🟡 `BOT-CORE-EMERGENCY-LISTENER-PUBSUB-ISOLATION-001`** (not V5A-blocking): isolate the listener's pubsub timeout so a Redis blip can't crash the container during a flip restart (Phase 2.3/4.3/5.1 depend on a clean restart). Wallet unchanged 5.064 SOL. Audit §9 addendum in `docs/audits/V5A_FLIP_002_V3R_2026_06_02.md`. |
| 2026-06-02 | V5A-FLIP-002-V3R | ⛔ NO-FLIP — halted Phase 1 on STOP-M (docs-only) | **Authorized V5A live flip halted at preflight on market regime; zero state writes; wallet unchanged.** `market:mode:current=HIBERNATE` (cfgi 34.0, sentiment 22.5, SOL $78.84) AND signal pipeline near-dead (`market:new_token_count_1h=14` vs 10,257 on 2026-05-20; pumpportal "no signals") — two independent STOP-M triggers. Per D-S4 (binding), HIBERNATE aborts; NORMAL/DEFENSIVE/AGGRESSIVE would have proceeded. `market:mode:override` not set (computed HIBERNATE governs); forcing one is out of §2 scope AND defeats the gate — not done. **NOT STOP-Rollback** — no failed flip, no contamination; re-attemptable in the next non-HIBERNATE window under the same authorization. **Everything-else-GO:** Railway authed; commits `f3591eb`/`3c50520`/`7458f2d` all ancestors of HEAD `7d33994`; all 3 fixes present in source; Path B engine intact (id 6580 on-chain native delta −374,251,786 lamports, exact); orphan baseline clean (`trades` live-open=0 — the May-20 vector); wallet 5.064095633 SOL; sell-storm default 8. **Open tooling item:** Railway CLI v4.6.0 can't `list-deployments` (needs ≥4.10.0) → running-container SHA for `7458f2d` UNCONFIRMED since 2026-05-28; Phase 1.5 forced-redeploy fail-safe not triggered (flip halted). Next attempt: upgrade CLI ≥4.10.0 OR use forced-redeploy; wait for non-HIBERNATE + recovered signal flow + Jay D-S7 watch. PC4 stays `[ ]`. Audit: `docs/audits/V5A_FLIP_002_V3R_2026_06_02.md`. |
| 2026-05-28 | LIVE-FEE-CAPTURE-ENTRY-SIG-WIRING-001 | ✅ 1 fix deployed; V5A-FIXES-001 §11 follow-up RESOLVED `7458f2d` | **Path B entry-sig wiring resolved before V5A-FLIP-002-V3.** Single-commit single-file fix (`services/bot_core.py` +9 LOC, 0 deletions) adds `entry_signature: str \| None = None` to the Position dataclass and wires `entry_signature=getattr(result, "signature", None)` at the live-entry Position construction site (`bot_core.py:1005`), mirroring the existing exit_signature mechanism (`_pt_exit_sig = getattr(result, "signature", None)` at L1434). Adds observability log `[ENTRY_SIG] captured entry_signature=<first 8>... for <personality> position (mint=<first 12>)` when populated. Resolves filed Tier 3 `LIVE-FEE-CAPTURE-002-PATH-B-ENTRY-SIG-NOT-WIRED-001`. **Impact:** close-path Path B parser at L1450 (`helius_parse_signature(pos.entry_signature)`) now receives a real signature on live closes, producing `correction_method='live_actual_v1'` instead of `'live_estimated_v1'`. Tonight's V5A-FLIP-002-V3 live trades will count toward `PAPER-FEE-MODEL-CALIBRATION-001` ≥10-row Path B prereq. **Fail-safe by design:** default None covers paper entries (paper_buy has no real sig), reconciler-restored Positions (trades table has no entry_signature column), and any buy result lacking a signature attr — all paths the close-path already handles as "skip entry parse, fall back to Path A". Paper mode behavior byte-for-byte unchanged. **Q1-Q5 investigation:** (Q1) exit_signature template works as expected; (Q2) `execute_trade` returns ExecutionResult.signature synchronously across all 3 routes (PumpPortal Local, PumpPortal, Jupiter) — not STOP-Async; (Q3) Position constructed at L1005 with `result.signature` already in scope (used at L1061 for DB INSERT $8, L1090 for ENTERED log); (Q4) close path reads in-memory `pos.entry_signature` at L1450 (not DB column — DB column was already populated at INSERT time L1061; the fix is purely in-memory wiring); (Q5) live reconciler reads `trades` table at L309 which has no entry_signature column → mid-position restart loses sig → close falls back to live_estimated_v1. Acceptable per session prompt §3 Q5 (SD short-hold, restarts rare). Schema migration would be STOP-Scope. Future enhancement (out of scope): reconciler could lookup `paper_trades.entry_signature` by mint+open match. **Verify 17/17 PASS** (`.tmp_entrysig/verify_entrysig.py`): source assertions (entry_signature field present in dataclass, live-entry kwarg wired, exit_signature template untouched, Path B reference unchanged, paper-entry regression-safe, reconciler unchanged, [ENTRY_SIG] log present); dataclass unit tests (`Position(entry_signature='testSig123')` sets field, `Position()` default is None); mock ExecutionResult fail-safe (getattr extracts present sig, defaults None on absent attr — no exception); Path B dry-run on id 6580 (entry_signature retrievable from DB; helius_parse_signature returns dict; `success=True` with `native_delta_lamports=-374251786` — confirms downstream consumer works end-to-end when given a real sig). **STOPs evaluated, none critical fired:** A (Railway MCP callable), D (no concurrent session — last commit `0cb9923` from 2026-05-22), H (precedence files readable), Z (TEST_MODE=true verified via Railway MCP), Async (sig synchronous), Investigate (exit_signature template OK), Scope (9 LOC ≪ 50, 1 file, no migration), Verify (17/17), Loop (no retries), L (clean fetch, single push), Claude (no limit). STOP-J inconclusive at session end (deploy verification incomplete — `railway logs -s bot_core` returned `No deployments found`; `bot:filter:fill_mc_ceiling:rejects:2026-05-28=1982` confirms bot active today; commit confirmed on `origin/main`; flagged in STATUS active-blockers for next-session resolution). **Scope discipline:** NO TEST_MODE change; NO env change; NO exit_signature mechanism modification (mirrored as template); NO schema migration; NO other service touched; NO fix of other Tier 3 follow-ups (PORTFOLIO-SNAPSHOT-MODE-FILTER-001 / HEARTBEAT-EMERGENCY-STOP-REFLECTION-001 / PAPER-CLOSE-TRADES-CLOSED_AT-HARDENING-001 untouched). **Behavioral verification deferred:** paper mode doesn't exercise the live entry branch; verification at V5A-FLIP-002-V3 Phase 10.5 (first live close should produce `live_actual_v1` — if `live_estimated_v1`, do NOT auto-rollback on this alone, profitability unaffected; flag for post-trial investigation). **Wallet UNCHANGED 5.064 SOL on-chain** (not re-verified — no on-chain activity expected this session, none occurred). Audit: `docs/audits/LIVE_FEE_CAPTURE_ENTRY_SIG_WIRING_001_2026_05_27.md`. |
| 2026-05-21 | V5A-FIXES-001 | ✅ 3 fixes deployed; 1 closed as non-bug; cleanup done — V5A-FLIP-002 UNBLOCKED | Autonomous overnight session executing 4 follow-ups from V5A-FLIP-001-V2: (1) **Bug 1 / V5A-FLIP-RECONCILE-FILTER-001** ✅ DEPLOYED commit `f3591eb` 2026-05-21 ~14:42Z — `mode_clause` in `_reconcile_positions` + `_load_state` now applies trade_mode filter to BOTH `paper_trades` AND `trades` (was only `paper_trades`). Root cause: V5A-FLIP-001-V2's 14 phantoms came from open paper rows in the `trades` corpus (where `pos.trades_ml_id` was falsy → `_close_position` skipped `trades.closed_at` UPDATE → rows stayed "open"), and the live reconciler reads `trades` without trade_mode filter. Both reconcile paths now filter; +2 `[RECONCILE]` log markers added. (2) **Bug 2 / V5A-FLIP-CLOSE-TRADE-MODE-001** ✅ DEPLOYED commit `3c50520` 2026-05-21 ~15:05Z — defensive-INSERT trade_mode now derived from `trades.trade_mode` lookup (was hardcoded `'live'`). Plan's Option A (entry_signature discriminator) infeasible — Position dataclass has no entry_signature field; adopted Option C (DB lookup). +`[ORIGIN_MISMATCH]` warning log for observability. (3) **Bug 3 / BOT-CORE-EMERGENCY-STOP-LIVENESS-001** ✅ CLOSED-AS-NON-BUG — per-decision check ALREADY EXISTS at `services/bot_core.py:604-609` inside `process_signal`. V5A-FLIP-001-V2 audit's "Phase 5 finding" conflated heartbeat.emergency (in-memory) with the per-decision check; 14 phantoms came from pre-existing trades-table orphans, not new positions opened during drain. (4) **V5A-FLIP-CONTAMINATION-CLEANUP-001** ✅ DONE — 14 phantom paper_trades rows ids 9940-9953 tagged `correction_method='paper_orphan_at_flip_v5a_001'`; id 6580 (real on-chain live trade) untouched. 4 new Tier 3 follow-ups filed (all non-blocking). V5A-FLIP-002-V3 prep notes written. **Wallet UNCHANGED 5.064 SOL on-chain throughout.** Bot remained TEST_MODE=true. Two clean container restarts. PC4 still `[ ]` (V5A flip itself remains Jay-authorization-gated for next D-S5 window). Audit: `docs/audits/V5A_FIXES_001_2026_05_21.md`. |
| 2026-05-20 | V5A-FLIP-001-V2 | ❌ FLIP ATTEMPTED → ROLLED BACK; PC1 SATISFIED but PC4 stays `[ ]`; 3 NEW Tier 1/2 follow-ups filed | TEST_MODE=false set at 10:00:05Z on bot_core; Phase 8 failed at `Startup reconciliation: 14 open positions in DB` (paper_trades reconcile lacks trade_mode filter — pre-existing orphan paper rows leaked into live container); risk_manager EMERGENCY_STOP at -1.86 SOL daily loss from in-memory closes; **wallet UNCHANGED 5.064 SOL on-chain (zero live transactions)**; rollback per §7 clean (TEST_MODE=true + sizing reverted + bot:emergency_stop DEL'd); 14 contaminated `paper_trades` rows (ids 9940-9953) committed with `trade_mode='live'` + NULL signatures need cleanup. Audit: `docs/audits/V5A_FLIP_001_2026_05_20.md`. STOP-Rollback applies — V5A-FLIP-002 cannot fire until V5A-FLIP-RECONCILE-FILTER-001 + V5A-FLIP-CONTAMINATION-CLEANUP-001 + BOT-CORE-EMERGENCY-STOP-LIVENESS-001 land + new D-S5 window + new authorization. |
| 2026-05-20 | API-MCP-PREFLIGHT-001 | ⚠ CONDITIONAL READY (read-only verification; NO code/env/Redis/DB/deploy change) `dc84f38` | **PC1 SATISFIED. Wallet 5.064 SOL on-chain.** Read-only preflight audit ~10 min before tonight's V5A flip (Wed 2026-05-20 19:00 AEST). **Material finding:** Helius `getBalance(4h4pst...)` returned **5.064095633 SOL** — exact +5.000000000 SOL top-up vs. 0.064 carry-forward from PC1-WALLET-TARGET-RECONCILE-001 + CLAUDE-MD-MCP-INDEX-001 earlier today. Matches D-S3 trial budget exactly. **Outstanding V5A blockers drop 2 → 1** (PC4 flip itself remains). **STOP-A FIRED** — `mcp__railway__list-services` returned "Not logged in to Railway CLI" (unchanged from yesterday). Per §4 interpretation: Phase 2 (depends on Railway env reads for API keys) ABORTED; Phases 3 (Redis+DB) and Phase 4 (code grep) continued via non-Railway tools — they produce useful evidence without env access. **Phase 1 (MCP no-op battery, 12 calls):** 10 OK (helius getBalance 5.064 SOL, redis 14 bot:* keys, github [project] repo, vybe 47 v4 paths, dexpaprika 35 networks, coingecko 11 hits, playwright 1 tab, shadcn @shadcn, context7 5 React matches, Google Drive 1 file). 2 failed: Railway (STOP-A), Socket (known-broken non-blocking). **Phase 2 (External API, CONSTRAINED — no-auth only):** Binance SOLUSDT 200 $85.02 (0.14% delta vs Redis $84.9, 699ms) ✅; Jito getTipAccounts 200 (8 accounts, 693ms) ✅; GeckoTerminal Raydium SOL/USDC pool 200 ($85.08 base, 351ms — first pool from prompt `5quBtoi…` was 404, retried with canonical `58oQChx4…` v4) ✅; Rugcheck SOL report 200 (score=1, risks=[], 986ms) ✅. Jupiter prompt URL `quote-api.jup.ag/v6/quote` DNS-unreachable — but CLAUDE.md says current host is `api.jup.ag/swap/v2/*`; `service:health.jupiter=ok HTTP 200` confirms current endpoint works. Authenticated APIs proxy-validated via Redis `service:health`: helius (rpc/gatekeeper/parse) all ok, vybe ok, dexpaprika ok, rugcheck ok, gecko ok, defillama ok, jupiter ok 200/67ms, anthropic key-configured-but-credit-exhausted (BUG-010 known), nansen warn 401 (expected per NANSEN_DRY_RUN=TRUE), **pumpportal WARN "no signals" — but this is a `last_signal` Redis key-write quirk in `dashboard_api.py:2041`, NOT a signal-pipeline outage** (`market:new_token_count_1h=10257`, `market:migration_count_1h=67` — 10K+ signals/hour flowing). `PUMPPORTAL_API_KEY` not referenced in `services/*` code — local API is unauthenticated by design. **Phase 3 (Redis state snapshot):** Bot RUNNING paper (`bot:status.status=RUNNING, test_mode=true, open_positions=0, market_mode=DEFENSIVE, consecutive_losses=0, daily_pnl=2.88`). `service:bot_core:heartbeat` alive, 2h 5min uptime, 0 positions, no emergency. **STOP-E does NOT fire** (0 paper + 0 live open positions across `paper:positions:*`, `bot:open_positions:*`, `bot:status.positions={}`). **STOP-I does NOT fire** (`bot:consecutive_losses=0`). `bot:emergency_stop` absent, `bot:loss_pause_until` absent. **`market:mode:current=DEFENSIVE`** — D-S4 manual judgment required at flip time. `market:mode:override` absent. `market:session.session=TRANSITION, sydney_hour=19` — D-S5 flip window OPEN. `governance:latest_decision.reasoning` confirms Anthropic credit exhaustion (BUG-010), falls back to CONSERVATIVE. **DB queries BLOCKED** (`DATABASE_URL` in local `.env` is `sqlite:///toxibot.db` local-dev; no DATABASE_PUBLIC_URL without Railway). STOP-F (portfolio snapshot delta) cannot be evaluated — deferred to flip session. **Phase 4 (9/9 code checks PASS):** (1) C1 paper gate `paper_trader.py:253-255`, (2) C1 live gate / PC3 `bot_core.py:953-965` (LIVE-MODE-FILTER-PARITY-001-V2 intact), (3) Path B parser `services/helius_parser.py` (file + id-6580 docstring), (4) Path B integration `bot_core.py:1436-1442`, (5) ML gate `bot_core.py:60, 130-144, 674` (BOT-CORE-ML-GATE-001), (6) Sell-storm circuit breaker `bot_core.py:221-222, 1342-1361` (default 8/300s, armed), (7) CLEAN-003 script `scripts/live_flip_prep.sh` (executable), (8) TIME_PRIME env-driven `bot_core.py:755-756` (default `""`/`1.0`), (9) Analyst suppression `signal_aggregator.py:153, 1820, 2515`. **Prompt-side correction:** prompt expected `ANALYST_DISABLED` in both SA and bot_core; actual design has it at SA only (correct architectural layer — bot_core uses `ML_THRESHOLD_BOT_CORE_ANALYST=0` reserved-not-active). Not a regression. **STOP summary:** A FIRED, B did-not-fire (PC1 SATISFIED!), C did-not-fire (id-6580 deferred), D did-not-fire (9/9 pass), E did-not-fire (0 positions), F cannot-evaluate (deferred), G N/A, H did-not-fire, I did-not-fire (0 losses), J did-not-fire (no concurrent), K did-not-fire. **Final verdict: ⚠ CONDITIONAL READY** — all critical items GO except Railway MCP itself. **Two paths for the flip session:** (A) Jay runs `! railway login` to restore MCP for CC-automated flip; (B) Jay flips manually via Railway dashboard (technical preconditions all GO). Both unblocked. **Scope discipline:** NO services/* code change, NO env change, NO Redis writes, NO DB writes, NO deploy, NO TEST_MODE flip, NO use of local `.env` keys as substitutes for Railway env (audit principle: verify Railway's current config, not Apr-21 local snapshot which may be post-rotation stale), NO scope creep into non-goals (TabPFN JWT, dashboard rebuild, ML training, BUG-010 fix, VYBE code drift). **Tier 3 follow-up filed:** `DASHBOARD-PUMPPORTAL-HEALTH-PROBE-001` (cosmetic observability defect in dashboard_api.py:2041 false-WARN). **Outputs:** NEW `docs/audits/API_MCP_PREFLIGHT_001_2026_05_20.md` (11 sections, ~5000 words), `AGENT_CONTEXT.md` (header refresh — no §6 PC1 status flip here; happens at flip session), this `ZMN_ROADMAP.md` row + Tier 3 row, `MONITORING_LOG.md` (entry), `STATUS.md` (prepend). Scratch (untracked): `.tmp_api_preflight/{PROGRESS.md, 01_mcp_battery.md, 02_external_probes.md, 03_state_snapshot.md, 04_code_checks.md}`. |
| 2026-05-20 | STOP-LOSS-20-NO-MOMENTUM-90S-COMBINED-EVAL-001 | ✅ VALIDATED — supports V5A relaunch (read-only; NO code/env/Redis/deploy change) `35a5b18` | **PC2 SATISFIED.** Combined eval of `STOP-LOSS-20-RUG-FILTER-EVAL-001` (F1+C1 fill-time MC gate) + `NO-MOMENTUM-90S-EVAL-001` (nm90 structural-bleed exit), pulled forward from ≥2026-05-27 anchor (PC2's true gate is "verdict supports continuing"). **Sample:** n=511 closed SD-paper trades, 7.13d wall-clock span, 8 distinct days, 7 days with ≥30 trades, span 2026-05-13 03:32 UTC → 2026-05-20 06:32 UTC. **F1/C1 angle (PASS):** max market_cap_at_entry $998 (≤$1K ceiling); 0 trades above $1K; 0 false-positive winners; `stop_loss_20%` exit rate 2.5% (target ≤5%); median MC $661; avg MC $653. MC bands: $0-$500 (n=71, +17.41 SOL, 100% WR), $500-$750 (n=330, +16.23 SOL, 100% WR), $750-$1000 (n=110, **-0.54 SOL, 53.6% WR** — marginal tier; future tightening could consider $750). Cumulative post-C1 Redis rejects: **12,443** vs 511 kept → ~96% reject rate (~24× the F1 reject rate, consistent with $1K vs $3K ceiling structure). **no_momentum angle (PASS):** **`no_momentum_90s` exit rate = 0%** (target ≤30%); was 76.5% W4 pre-C1. `TRAILING_STOP` rate **95.5%** (was ~14% pre-C1). Overall WR **90.0%** (target ≥60%) with every one of 8 days above **83.6%** (range 83.6-96.2%). Total **+33.10 SOL / +4.64 SOL/day**, no negative day, daily range +0.87 to +7.36 SOL. Strip-the-tail: total +33.10 → strip top 1 +30.27 → strip top 5 +27.01 → **strip top 10 +24.18 / +3.39 SOL/day** (target ≥+1.0; exceeds by 3.4×) → strip top 10% (51 trades) +16.23 / +2.28 SOL/day. NOT lottery-driven. Top single trade (+2.84 SOL — one of two `staged_tp_+1000%` moonshots) accounts for only 8.6% of total. **Cross-validation:** C1 STOP-A counterfactual baseline (523 trades / +32.62 SOL / 91.4% WR / 8.12d) vs actual post-deploy (511 / +33.10 / 90.0% / 7.13d) matches within 2.3% N, 1.5% PnL, 1.4 pp WR. Per-day actual (+4.64) is 15.4% above counterfactual prediction (+4.02) — consistent with mild favorable variance. The two angles are mechanistically linked: the gate blocks entries into $1k-$3k MC dead zone where nm90 fires, so nm90 exit channel has no work to do. **Cost-fidelity translation (Phase 5):** paper rate +4.64 SOL/day is an UPPER BOUND. First-order cost-only adjustment (17.6× under-count on fees per `COST_FIDELITY_GAP.md`): -14.3 SOL total → +18.8 SOL / +2.64 SOL/day live-equivalent. Add latency (1-15s vs 0) + MEV/sandwich (unmodelled): further haircut. V5A staged sizing (D-S6: 0.10/5 vs current 0.25/20) reduces gross + worsens % cost ratio. Estimated V5A-equivalent: **+0.5 to +1.5 SOL/day** with material uncertainty (could be break-even or modestly negative on bad days). Paper rate is NOT a live forecast. V5A's job is partially to generate the data to calibrate the gap (per `PAPER-FEE-MODEL-CALIBRATION-001` ≥10-Path-B-row prereq). Staged ladder (D-S6) + active observation (D-S7) are the right structural answer to the gap. **STOPs evaluated, none triggered:** STOP-A (no concurrent session — last commit `3ea0290` from this same chat session), STOP-B (n=511 > 250), STOP-C (8 days, 7.13d span), STOP-D (gate firing through 2026-05-20 per Redis), STOP-E (two angles mechanistically consistent), STOP-F/G (N/A). **Closes:** `STOP-LOSS-20-RUG-FILTER-EVAL-001` and `NO-MOMENTUM-90S-EVAL-001` as bundled. **§6 PC2 flipped** `[ ]` → `[x]` SATISFIED. Outstanding V5A blockers **3 → 2** (PC1 wallet ~5 SOL per yesterday's reconcile, PC4 flip itself). **Side-effect finding (NOT fixed per scope):** `BIGGEST_WINS_CLEAN_DATA_FLOOR_TS=1747104561.0` constant in DASHBOARD-DESIGN-REALIGNMENT-001 amendment is wrong-year (1747104561 = 2025-05-13, correct = `1778642961` for 2026-05-13). Caught by this session because the prompt's §3.1 SQL template used the same wrong value; first query pass returned 2,966 trades / 28d instead of expected ~7d / 511 trades. Filed Tier 3 `DASH-CLEAN-DATA-FLOOR-FIX-001` follow-up. **Recommended next:** V5A flip session (Jay-authorization-gated per CLAUDE.md "Live trading mode — session-gated"). **Outputs:** NEW `docs/audits/STOP_LOSS_20_NO_MOMENTUM_90S_COMBINED_EVAL_001_2026_05_20.md` (11 sections, ~3000 words), `AGENT_CONTEXT.md` (header + §6 PC2 flip + blocker count 3→2), `ZMN_ROADMAP.md` (this row + DASH-CLEAN-DATA-FLOOR-FIX-001 Tier 3 row), `MONITORING_LOG.md` (entry), `STATUS.md` (prepend). Scratch (untracked): `.tmp_combined_eval/queries.py` + `results.json` (raw evidence — gitignored). **NO services/* code change, NO env change, NO Redis writes, NO deploy.** |
| 2026-05-20 | CLAUDE-MD-MCP-INDEX-001 | ✅ INDEX ADDED (docs-only; NO code/env/Redis/deploy change) `4f70cba` | **MCP discoverability gap closed in `CLAUDE.md`.** Mirrored the `CLAUDE-MD-FINDINGS-INDEX-001` pattern (2026-05-19) by adding a new H2 section **"MCP servers available"** between `## Standing findings — read before related work` and `## Resolved Bugs`. Three sub-tables capture runtime reality verified this session via no-op calls: **Connected (10)** — redis, helius, github [project], vybe, dexpaprika, coingecko, playwright, shadcn, context7 [plugin], Google Drive; **Connected per UI but BROKEN in session (3)** — `mcp__railway__*` ("Not logged in to Railway CLI. Please run 'railway login' first"), `mcp__plugin_github_github__*` ("invalid session" — use project-scoped `mcp__github__*` instead, verified callable), `mcp__socket__*` ("No valid session. Send initialize first."); **Configured-but-unavailable (6)** — sentry △ (auth needed), defillama △ (auth needed), birdeye ✘ (no tools registered), nansen ✘ (expected per `NANSEN_DRY_RUN=TRUE` per AGENT_CONTEXT §6.7), Gmail △, Calendar △. **Notably absent:** Postgres MCP — DB queries use `DATABASE_PUBLIC_URL` injected into a Python script (per `Scripts/export_paper_trades.py` pattern). Self-amending instruction added so future sessions that change an MCP's state update the table; tables must reflect runtime reality verified by no-op calls, not aspirational state. **STOP-C did NOT fire** (10/13 succeeded; "most fail" threshold not met). **Phase 1 no-op verification battery:** railway list-services → error (not logged in); redis list pattern → succeeded; helius getBalance(4h4pst...) → 0.064095633 SOL; github [project] search_repositories → returned repo metadata; github [plugin] get_me → invalid session; vybe list-endpoints → 47 v4 paths returned; dexpaprika getNetworks → 35 networks; coingecko search_docs → 10 method matches; playwright browser_tabs list → 1 tab (about:blank); shadcn get_project_registries → `@shadcn`; socket depscore(left-pad) → no valid session; context7 resolve-library-id(React) → 5 React matches; Google Drive list_recent_files → 1 file. **Phase 3 grep** found "Postgres MCP" references in `CLAUDE.md:32` (already qualified "once installed" — fine), `docs/SETUP_NEW_MACHINE.md:194` (security-warning context — fine), `docs/CLAUDE_TOOLING_INVENTORY.md:5,183` (line 183 explicitly says "deliberately NOT installed" — fine), and several historical audit docs (`ZMN_RE_DIAGNOSIS_2026_04_19.md`, `ZMN_OPTIMIZATION_PLAN_2026_04_19.md`, `ZMN_CC_HANDOVER_2026_04_19.md`, `CC_TOOL_SURFACE_2026_04_19.md`) — all accurate-in-historical-context (the asyncpg shim *is* the Postgres-MCP-equivalent). New Tier 3 🟢 follow-up filed: `MCP-REFERENCE-CORRECTION-001` — sweep historical audit docs to add "(via asyncpg shim — not an actual MCP)" qualifier where "Postgres MCP" is used as shorthand. **STOPs:** A (no concurrent session — last commit `0ad2282` PC1 reconcile from earlier this same chat session), B (CLAUDE.md Standing findings section present — pattern intact), C (10/13 not "most"), D (no scope creep — single section added; didn't reorganize CLAUDE.md), E (no Claude limit), F (no git conflict pre-push). **Outputs:** `CLAUDE.md` (+1 new H2 section "MCP servers available" with 3 tables + notably-absent + self-amending instruction), `AGENT_CONTEXT.md` (header refresh), `ZMN_ROADMAP.md` (this Decision Log row + Tier 3 row for `MCP-REFERENCE-CORRECTION-001`), `MONITORING_LOG.md` (entry), `STATUS.md` (prepend). Scratch (untracked): `.tmp_claude_md_mcp_index/` empty (verification calls happened inline in this session, no separate doc needed). **NO services/* code change, NO env change, NO Redis writes, NO deploy.** |
| 2026-05-20 | PC1-WALLET-TARGET-RECONCILE-001 | ✅ RECONCILED (docs-only; NO code/env/Redis/deploy change) `47f5829` | **Single-scope reconciliation of an internal inconsistency.** Updated `AGENT_CONTEXT.md` §6 PC1 wallet top-up target from prior `≥1.5-2.5 SOL` (operational-minimum framing for swap-router viability) to `~5 SOL` per **D-S3 of `docs/findings/V5A_GO_LIVE_DECISIONS.md`** — the newer, more specific, explicitly-trial-governing authority that supersedes PC1's operational-minimum-only framing. D-S3 specifies daily realized-loss halt 1.5 SOL = 30% of budget; cumulative halt 3.0 SOL = 60% of budget; therefore trial budget = 5 SOL with ~2 SOL operational floor at cumulative halt. Operational-minimum reasoning (`MIN_POSITION_SOL=0.05 × MAX_POSITION_SOL_FRACTION=0.10` (code default) = effective ≥0.05 SOL per position) preserved inline as a lower-bound note so the rationale chain remains intact — still true as a lower bound; the trial budget is the binding target. **Related PC4 inconsistency flagged in-cell (NOT fixed here):** `DAILY_LOSS_LIMIT_SOL=4.0` currently on bot_core vs D-S3's 1.5 SOL daily halt — env-change + verification deferred to V5A flip session, which will set `DAILY_LOSS_LIMIT_SOL=1.5` on Railway and update PC4 accordingly. Helius `getBalance` re-verified this session: **0.064095633 SOL** (unchanged since 2026-04-21; no surprise top-up, so PC1 remains `[ ]` outstanding — target update only, not status flip). **STOPs evaluated, none triggered:** STOP-A (no concurrent session — last commit `753b5ce` 2026-05-20 ~00:00 UTC), STOP-B (PC1 NOT already reconciled — verified line 146 still read `≥1.5-2.5 SOL` at session start), STOP-C (D-S3 NOT amended — `V5A_GO_LIVE_DECISIONS.md` §5 Amendments empty), STOP-D (no scope creep — PC1 only; §6.6 historical snapshot left alone; §2 env table left alone; D-S3 not amended), STOP-E (no Claude limit hit), STOP-F (no concurrent-session git conflict). **Scope:** PC1 substance + PC4-related in-cell flag only. Did NOT touch PC2/PC3/PC4 substance, §6.6 V5A readiness snapshot (2026-05-01 audit), §2 env table, or any code/env/Redis/deploy. **No `V5A_GO_LIVE_DECISIONS.md` amendment** (the doc is the authority being cited; amendments require explicit Jay acknowledgement per §5 override path). **No new follow-up items filed** — PC4 reconciliation is the V5A flip session's responsibility; this session only surfaces it. **Outputs:** `AGENT_CONTEXT.md` (PC1 wording rewrite + header refresh), `ZMN_ROADMAP.md` (this Decision Log row), `MONITORING_LOG.md` (entry), `STATUS.md` (prepend). Scratch (untracked): `.tmp_pc1_reconcile/` empty (single-edit session, no derivation needed). **NO services/* code change, NO env change, NO Redis writes, NO deploy.** |
| 2026-05-20 | V5A-GO-LIVE-DECISIONS-RECORD-001 | ✅ DECISIONS RECORDED (docs-only; NO code/env/Redis/deploy change) `753b5ce` | Captured seven chat-side V5A go-live decisions from 2026-05-19/20 as a survivable record so chat-history-only ephemerality cannot lose them. **Outputs:** (1) NEW `docs/findings/V5A_GO_LIVE_DECISIONS.md` (≤1500 words, follows `COST_FIDELITY_GAP.md` pattern — survivable summary with pointers, override-path appended-amendments). Contains all 7 decisions (D-S3 daily 1.5 SOL realized halt + cumulative 3.0 SOL hard revert; D-S4 manual market-mode check at flip-time, NOT autonomous agent yet; D-S5 Wed/Thu AEST evening 18:00-21:00 Sydney window ramping US trading hours, avoid weekends + Sunday-Monday open + Friday afternoon; D-S6 conservative `MAX_POSITION_SOL=0.10` + `MAX_SD_POSITIONS=5` + NO auto-scale on WR — overruled because 80% WR in 24-80 trades is not statistically robust evidence; D-S7 Jay watches actively 4-6h post-flip, hourly first 12h) + §2 sizing graduation ladder (Hours 0-24 → Day 2-4 hold → Day 5-7 → Week 2; scale on cumulative PnL trajectory + closed-trade count + Path B sample, not short-window WR) + §3 strategic follow-up `GOVERNANCE-AGENT-MARKET-MODE-001` filed + §4 cross-refs + §5 override path. (2) Tier 1 roadmap: NEW `V5A-SIZING-GRADUATION-LADDER-001` (📋 ACTIVE RULE 🟡 — governs trial sizing, NOT a session to run; consult before any manual sizing change). (3) Tier 2 roadmap: NEW `GOVERNANCE-AGENT-MARKET-MODE-001` (📋 QUEUED Tier 2 🟡; autonomous classification of `market:mode` + halt authority but NOT start authority; gated on V5A live data across ≥2 distinct market regimes; broader strategic direction for autonomous handling of currently-manual operational gates). (4) `MARKET-MODE-001-RE-CALIBRATE-V2` Tier 1 row updated with "2026-05-20: absorbed by `GOVERNANCE-AGENT-MARKET-MODE-001`" supersession link — not deleted; recalibration concern (NORMAL bleeds, threshold structure may be wrong) becomes one input to the classifier scope. (5) `ML_THRESHOLD_RETUNE_002` dependency-gate (behind `PAPER-FEE-MODEL-CALIBRATION-001` per COST-FIDELITY-FINDINGS-DOCUMENTATION-001) confirmed unchanged — no drift. (6) `AGENT_CONTEXT.md` §6 gains NEW "Decisions (recorded)" subsection between "Known conditions at relaunch" and "Completed preconditions" — points at findings doc + roadmap items. STOP-B did not fire (V5A checklist §6 cleanly locatable at line 140). (7) `CLAUDE.md` "Standing findings — read before related work" table gains a NEW row for `V5A_GO_LIVE_DECISIONS.md` per yesterday's CLAUDE-MD-FINDINGS-INDEX-001 self-amending instruction — first session to fulfill that contract. **No STOP triggered.** STOP-A: no concurrent session detected; last commit `4210c4b` 2026-05-19 13:02 UTC ~10h gap. STOP-B: §6 locatable. STOP-C: no decision re-decided, all seven captured verbatim. STOP-D/E: N/A. **Verdict:** all seven decisions are now discoverable, survivable, with same lifespan and prominence as `COST_FIDELITY_GAP.md`. V5A checklist points to them. CLAUDE.md indexes them. Future sessions cannot miss them. Findings doc: `docs/findings/V5A_GO_LIVE_DECISIONS.md`. Scratch: `.tmp_v5a_decisions/` empty (no derivation needed — decisions are inputs not outputs). **NO services/* code change, NO env change, NO Redis writes, NO deploy.** |
| 2026-05-19 | CLAUDE-MD-FINDINGS-INDEX-001 | ✅ INDEX ADDED (docs-only; NO code/env/Redis/deploy change) `5adff0b` + `0639b3f` | Chat-side audit (2026-05-19) exposed a discoverability gap: `docs/findings/` was referenced only from `AGENT_CONTEXT.md` §6 + ZMN_ROADMAP Decision Log, so a new CC session that doesn't deep-read either could miss the standing findings layer entirely. `CLAUDE.md` is the single document every session reads first — this session adds a new H2 section **"Standing findings — read before related work"** between `## Roadmap` and `## Resolved Bugs` that indexes `docs/findings/COST_FIDELITY_GAP.md` with an about-column + read-before column. Self-amending instruction added: "When new findings are added to `docs/findings/`, append a row here as part of the session that creates them." Phase 1 catalog of `docs/findings/` returned only `COST_FIDELITY_GAP.md` — the prompt-anticipated `V5A_GO_LIVE_DECISIONS.md` does not exist and is not referenced anywhere in committed docs (`grep -ri V5A_GO_LIVE_DECISIONS` returned 0 matches), so per scope discipline only the existing file was indexed; the V5A decisions file will be indexed by its own creating session. Phase 3 cross-reference verification: all 5 existing `docs/findings/COST_FIDELITY_GAP.md` references (AGENT_CONTEXT.md lines 3 / 162 / 167; ZMN_ROADMAP.md lines 41 / 315) point at the existing file — no drift, no broken refs. **No STOP triggered.** Verdict: future CC sessions reading CLAUDE.md first will discover the findings layer without needing to deep-read AGENT_CONTEXT.md or ZMN_ROADMAP.md. Self-amending instruction is the durability mechanism. Outputs: `CLAUDE.md` (+15 lines new H2 section), `AGENT_CONTEXT.md` (header refresh only — no structural change), this Decision Log entry, `MONITORING_LOG.md` entry, `STATUS.md` prepend. Scratch (untracked): `.tmp_claude_md_findings/01_index_draft.md`, `02_cross_refs.md`. **NO code, NO env, NO Redis writes, NO deploy.** |
| 2026-05-19 | LIVE-MODE-FILTER-PARITY-001-V2 | ✅ DEPLOYED (code+deploy; `services/bot_core.py` live buy branch only; bot_core redeploy via single `git push`) | Implemented Option A from yesterday's LIVE-MODE-FILTER-PARITY-001 STOP-C scoping. Inserted a 20-line gate in `services/bot_core.py` `else:` (live) branch at `:953` — between `signal_type = ...` and `result = await execute_trade(...)` — mirroring `paper_trader.py:247-275` (C1) line-for-line. **One env var (`BOT_CORE_FILL_MC_CEILING_USD`) now governs paper AND live**; same `price * 1_000_000_000` MC formula; same strict-`>` threshold; reject-log `FILL_MC_CEILING reject (live): ...` (suffix `(live)` so grep separates paper/live; trailing `slippage=%.1f%%` dropped because live has no `_simulate_slippage` float); Redis counter `bot:filter:fill_mc_ceiling:rejects:live:<UTC-date>` with 14d TTL (distinct `:live:` segment so paper analytics aren't polluted). Two intentional divergences vs paper C1, both documented in audit §8 parity table: (a) MC term uses raw `fill_price` (no simulated slippage in live — paper's `_simulate_slippage` is a paper-sim artifact; both gate on the same fundamental USD MC at fill time); (b) failure mode is fail-open at the gate block (`_get_token_price → 0.0` → `fill_mc=0 < ceiling` → pass) — mirrors the C1 gate block's literal logic; paper's fail-closed happens earlier in `paper_buy` at `:238-240`, not inside the gate. **Gate is DORMANT under `TEST_MODE=true`** — bot remains in paper, first live-fire is at V5A relaunch. **STOP audit:** STOP-A (routing wrong), STOP-B (units mismatch), STOP-C (no clean insertion point), STOP-D (3-iter validation cap), STOP-E (deploy unconfirmed), STOP-F (Claude limit), STOP-G (concurrent-session conflict) — all evaluated, none triggered. Phase 1 reconfirmed `bot_core.py:82-83` paper_buy gated by TEST_MODE / `:836` paper→`paper_buy` / `:951` else→`execute_trade` at `:953-956` (line-numbers +3 vs original audit due to LIVE-TRADES-LOGGING-AUDIT-001 `b867daa` comment additions); `_get_token_price` at `:388-391` returns USD price-per-token in identical units to paper_trader's helper (the live branch already uses `price * 1_000_000_000` at `:996` for `_pe_market_cap` post-execution — direct empirical confirmation). Insertion point is clean and method-end `return` is semantically equivalent to paper's fall-through-on-failure path because the entry-method ends at `:1052` immediately after the if/else. **Verification standard: code-level + clean-startup + rolled-back proof** (per audit §6 + same standard as the predecessor LIVE-MODE-FILTER-PARITY-001 §6) — `python -m py_compile services/bot_core.py` clean; the 8-case verify harness `.tmp_live_filter_parity_v2/verify_live_mc_gate.py` extracts the gate block from the deployed source by marker and exercises it against mock inputs (above-ceiling reject + Redis incr; at-ceiling pass-strict; below pass-to-`execute_trade`; gate-disabled ceiling=0 pass; env-absent pass; price-fetch failure fail-open; two ceiling-tier sanity cases) — **ALL 8 PASS** in 2 dev-loop iterations (within the 3-cap; iter 1 had a Windows cp1252 codec crash on a `→`, iter 2 fixed it and switched the sentinel-return string-replace to a regex anchored on line shape after the indent-count was wrong at 20 vs 12 after `textwrap.dedent + indent`); raw stdout `.tmp_live_filter_parity_v2/verify_output.txt`; Railway container restart confirmed clean post-`git push`; paper traffic continues to land normally (the `if TEST_MODE:` branch is untouched, no re-indent of any surrounding code, no new imports — `os`/`datetime`/`timezone`/`logger` already imported at module top). **Gate cannot be observed firing in production this session** because live mode is off — explicit paper-mode caveat in audit §6. **PC3 in AGENT_CONTEXT §6 is now SATISFIED**, dropping outstanding V5A blockers from 4 → 3 (PC1 wallet top-up, PC2 observation through ≥2026-05-27 combined eval, PC4 flip-itself remain). The NO_MOMENTUM_90S_AUDIT_001 §10 "execution.py parity" open item — previously superseded by LIVE-MODE-FILTER-PARITY-001 into V2 — is now structurally resolved by Option A landing in `bot_core.py` rather than `execution.py` (per the predecessor audit's reasoning: `execution.py` has 3 execution routes + no fill-time price computation, so a clean port there is not achievable; gating in `bot_core.py` is the correct architectural placement). **Rollback:** `BOT_CORE_FILL_MC_CEILING_USD=0` (no redeploy; disables both paper and live gates simultaneously). Audit: `docs/audits/LIVE_MODE_FILTER_PARITY_001_V2_2026_05_19.md`. Scratch (gitignored): `.tmp_live_filter_parity_v2/{PROGRESS,01_investigation,02_design,verify_live_mc_gate.py,verify_output.txt}`. |
| 2026-05-14 | COST-FIDELITY-FINDINGS-DOCUMENTATION-001 | ✅ DOCS COMPLETE (docs-only; NO code/env/Redis/deploy change) | Made the ML-TRAINING-COST-FIDELITY-AUDIT-001 findings *survivable and actionable* in the docs future sessions actually read — not left in a dated audit doc that may not be discovered. **Outputs:** (1) NEW survivable findings doc `docs/findings/COST_FIDELITY_GAP.md` (≤1200 words, summary-with-pointers, cites the audit by section — not a re-derivation). Contains the plain-language finding, the sharper "label corruption in marginal band wider than median trade PnL" framing, the honest severity (NOT a current-profitability fire; matters for retune + Analyst), and the structural reality that the gap CANNOT be closed pre-V5A — calibration needs Path B data that only live trading produces. (2) AGENT_CONTEXT §6 V5A checklist gains a NEW "Known conditions at relaunch (acknowledged, NOT blocking)" subsection with a cost-fidelity entry linking to the findings doc. **Deliberately NOT a precondition checkbox** — closing the gap depends on relaunching; a blocking checkbox would be permanently un-tickable. Instead: an acknowledged-condition entry that sits in front of whoever runs V5A go/no-go. (3) `ML_THRESHOLD_RETUNE_002` **re-sequenced behind `PAPER-FEE-MODEL-CALIBRATION-001`** in AGENT_CONTEXT §6 Related milestones. Previously gated on "≥7d post-`BOT_CORE_ML_GATE` clean data (≥2026-05-19)"; now gated on `PAPER-FEE-MODEL-CALIBRATION-001` deploy + ≥7d post-recalibration data. Date-gate replaced with dependency-gate. Rationale: a threshold sweep is a marginal-trade optimization, and the marginal band is exactly where labels are corrupted by the ~17.6× cost-optimism — running on the current corpus optimizes on partially-fictional labels. Two independent reasons to wait: (a) C1's structural edge makes retune non-urgent; (b) audit confirms corrupted labels. The item is NOT deleted — re-sequenced. (4) `ANALYST-POST-GRAD-001` roadmap entry gains an explicit **cost-fidelity gate at top of cell:** Phase 0 sub-session (c) "paper-mode activation" must not ship until `PAPER-FEE-MODEL-CALIBRATION-001` is deployed AND ≥7d of post-calibration paper data has accumulated. Sub-sessions (a)(b)(d) NOT gated. Rationale: Analyst sizing 0.2-0.5 SOL vs SD 0.05-0.25 SOL — gap bites harder at higher leverage. (5) `ML-TRAINING-MODE-FILTER-001` confirmed as a SINGLE Tier 3 🟢 row (no duplicate filing) — verified single occurrence at line 329 of the Tier 3 table; both Decision Log references (LIVE-TRADES-LOGGING-AUDIT-001 + ML-TRAINING-COST-FIDELITY-AUDIT-001) point to the same canonical item. **No re-investigation:** the audit is the evidence base; every claim in this session's deliverables traces to the audit by section. **No new follow-up items filed** — the audit's 4 (`PAPER-FEE-MODEL-CALIBRATION-001`, `PAPER-LATENCY-MODEL-001`, `PAPER-MEV-SLIPPAGE-MODEL-001`, `ML-CONTAMINATION-FILTER-BIAS-001`) + 1 re-scoped (`ML-TRAINING-MODE-FILTER-001`) already filed by prior session; this session re-sequences `ML_THRESHOLD_RETUNE_002` and adds the Analyst Phase 0 gate. Audit source: `docs/audits/ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md`. Survivable doc: `docs/findings/COST_FIDELITY_GAP.md`. **NO code, NO env, NO Redis writes, NO deploy.** |
| 2026-05-14 | ML-TRAINING-COST-FIDELITY-AUDIT-001 | ✅ AUDIT COMPLETE — sim-to-real gap CONFIRMED (read-only; NO code/env/Redis/deploy change) | Answered chat-side question raised by LIVE-TRADES-LOGGING-AUDIT-001 §8/§9: does the ML train on realistic transaction costs and latency? **Verdict: sim-to-real gap CONFIRMED.** ML training target = `outcome` string (binary win/loss) derived from `pnl_sol > 0` at `paper_trader.py:415` + `bot_core.py:1149,1365`. `pnl_sol` is net of `_simulate_slippage` + `_simulate_fees` (`paper_trader.py:142-213`) — so the cost model DOES reach the training signal at the threshold zero. **Cost-model fidelity (DB query 2026-05-14, n=2874 closed paper_trades post-C1):** avg `fees_sol`=0.00170 SOL on avg `amount_sol`=0.116 SOL = **1.46% round-trip**. Path B truth (id 6580, sole `live_actual_v1` row): 0.094 SOL on 0.365 SOL = **25.8% round-trip**. **Per-trade under-count factor ≈ 17.6× at avg paper sizing** (prompt's ~12× anchor was on the single Path B row at higher amount; same %-gap, lower abs scaling). **Fidelity-tier distribution:** 2873/2874 = 99.97% `pass_through` (paper sim), 0 `live_estimated_v1`, 1 `live_actual_v1`. Of 8,680 ML-eligible rows (5812 `trades` + 2868 `paper_trades`, 30d, contamination-filter survivors), only 41 are `trade_mode='live'` (~0.47%); Path-B-fidelity share is 0.012%. Corpus is effectively 100% paper-sim. **Latency:** 4 latency columns exist on `paper_trades` (`signal_detected_at`, `scored_at`, `traded_at`, `total_latency_ms`) but **100% NULL across 2874 closed rows**. Paper fill uses real-time Jupiter/Gecko quote at `paper_buy` invocation time — wall-clock-current, but does NOT model the systematic in-flight pump that C1's fill-time MC gate exists to backstop. No latency feature in FEATURE_SCHEMA. **Severity in context (NOT a current-profitability fire):** (a) ML is already weakly predictive (AUC 0.536 per ML_SCORE_ATH_VALIDATION_001); (b) SD profitability is structural-filter-driven (C1 MC ceiling, independent of ML); (c) +1.49 SOL/day W3+W4 holds without addressing the gap. **DOES materially affect:** (1) **ML_THRESHOLD_RETUNE_002 (≥2026-05-19)** — re-derived optimum threshold optimizes against ~17×-optimistic labels; should ship with "calibrated against sim costs" caveat in verdict; (2) **Analyst Phase 0 (June)** — ML-driven on mature features at higher sizing; gap transfers into a new personality. **3 new roadmap items:** `PAPER-FEE-MODEL-CALIBRATION-001` (Tier 2 🟡, env-only knob re-tune gated on ≥10 Path B rows — currently 1; recommended Path-B accumulation via V5A relaunch); `PAPER-LATENCY-MODEL-001` (Tier 3 🟢, gated on LATENCY-OBSERVABILITY-001 backfill); `PAPER-MEV-SLIPPAGE-MODEL-001` (Tier 3 🟢, structural addition, defer until Path B sample supports calibration). **Re-scoped existing item:** `ML-TRAINING-MODE-FILTER-001` (from LIVE-TRADES-LOGGING-AUDIT-001 §9) → Tier 3 🟢 hygiene only; with 0.47% live share, mode-filtering is a rounding error on the training signal — fidelity problem lives in the paper sim, not the paper/live blend. **Also surfaced (unresolved, Tier 3):** `ML-CONTAMINATION-FILTER-BIAS-001` — the existing contamination filter (`exit_price BETWEEN entry_price * 0.97 AND 1.03`) may disproportionately remove rows where sim cost flipped marginal outcomes from win to loss near zero; not investigated this session. **Sequencing recommendation:** ML_THRESHOLD_RETUNE_002 ships with caveat, NOT blocked by this audit. PAPER-FEE-MODEL-CALIBRATION-001 should land before Analyst Phase 0 ships (Analyst gate added). DB queries: `.tmp_ml_cost_fidelity/query.py` + stdout. Audit: `docs/audits/ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md`. **NO code, NO env, NO Redis writes, NO deploy.** |
| 2026-05-14 | LIVE-TRADES-LOGGING-AUDIT-001 | ✅ FIXED + DEPLOYED (code+schema; bot_core redeploy) | Investigation of chat-side "paper trades leaking into a live_trades table". **Premise corrected:** there is **no `live_trades` table** (repo grep 0 hits; `to_regclass` NULL) — the chat-side export mislabelled the **`trades` table** (exact 9,521-row / date-range / personality-split match). `trades` is the **paper+live combined ML-training corpus by design** — `bot_core.py` writes to it from both the paper branch (`:881`) and live branch (`:973`); `ml_model_accelerator` trains from both `trades` + `paper_trades`. No misrouting bug. **Real defect:** `trades` had no `trade_mode` discriminator, so the 41 genuine live rows were *buried* among 9,480 paper rows (not *leaked* into a live-only table). `paper_trades` already has `trade_mode` and is correctly separated; `live_trade_log` is correctly live-only. **Classification (9,521 rows):** paper 9,480 (6,612 archive-matched + 2,868 current-matched) / live 41 / unclassifiable 0. The 41 live = 35 v3/v4 trial trades (no paper_trades mirror — predate DASH-ENTRY-001; **all 35 confirmed via `live_trade_log` TX_SUBMIT signatures**, sum −3.3609 SOL / 25.7% WR) + 1 genuine on-chain round-trip (`trades` id 6596 / paper 6580) + 5 reconcile-residual rows (`trade_mode='live'` in paper_trades, NULL sigs — not real money). **Isolated real-money result ≈ −3.36 SOL — cross-validates the ~3.4 SOL on-chain wallet drawdown in CLAUDE.md's `1b40df3` forensics.** **Fix:** added `trade_mode TEXT DEFAULT 'paper'` to `trades` (db.py CREATE + idempotent ALTER); tagged both `bot_core.py` INSERT sites (`'paper'`/`'live'` literals, branches already gate on `TEST_MODE` — no decision logic touched); one-time backfill `migrations/002_add_trade_mode_to_trades.sql` (mirror from paper_trades → archive → `'live'` for the 35 trial rows guarded by `live_trade_log` EXISTS), applied this session pre-push. **Verify (`verify_logging_fix.py`) ALL PASS iteration 1:** post-fix paper INSERT→`'paper'`, live INSERT→`'live'` (rolled-back txn, 0 production rows); post-migration split exactly paper 9,480 / live 41, 0 NULLs. Not STOP-A (logging ≠ decision logic), not STOP-B (live cleanly separable), not STOP-C (contained). **Purge recommendation: DO NOT PURGE** — the 9,480 paper rows are legitimate ML data; filter by `trade_mode`. **V5A: enabler, not blocker** — live rows now self-identify. New follow-up flagged: **ML-TRAINING-MODE-FILTER-001** (decide whether ml_model_accelerator includes/excludes/weights live rows — now possible, not decided here). PREREQ gate (LIVE-MODE-FILTER-PARITY-001 complete) passed. Audit: `docs/audits/LIVE_TRADES_LOGGING_AUDIT_001_2026_05_14.md`. |
| 2026-05-14 | DASHBOARD-DESIGN-REALIGNMENT-001 (amendment) | ✅ AMENDMENT LANDED (docs-only, NO code/env/Redis/deploy change) | Jay-resolved §9 open questions + 4 design additions folded in. **§9 resolutions:** (1) re-scope ACCEPTED; (2) ≥30d legacy coexistence YES; (3) SINGLE accent (no picker); (4) Sentry fold-in DEFER to v1.5; (5) active emergency-stop from phone DEFER (auth surface, separate session); (6) June parallel-track CONFIRMED. **Design additions:** (A) Card 2 expanded — today + all-time cumulative P&L on one card, both numbers using `COALESCE(corrected_pnl_sol, realised_pnl_sol)` on `paper_trades` only (no `trades`/`live_trades`/Redis-aggregate paths until LIVE-TRADES-LOGGING-AUDIT-001 reconciles contamination); (B) NEW Card 7 biggest wins — top-3 default, expand to top-10, each row symbol + `realised_pnl_sol` + `realised_pnl_pct` as Nx mult + mint tap-to-copy + DexScreener "↗" link; **CRITICAL scoping constraint hardcoded** = `trade_mode='paper' AND entry_time >= 1747104561.0` (C1 deploy floor 2026-05-13 03:29:21Z UTC); endpoint clamps `since` overrides to floor; module constant named `BIGGEST_WINS_CLEAN_DATA_FLOOR_TS` with comment ref to audit §3. (C) Celebration FX on ≥3x wins — client-side, fires on `realised_pnl_pct >= 200` (column verified `services/db.py:128-129`); confetti (vendored canvas-confetti ~7KB, respects `prefers-reduced-motion`) + `navigator.vibrate([60,30,60,30,120])` haptic + optional sound toggle (muted-by-default, persisted `localStorage`); `seenTradeIds` Set in `sessionStorage` prevents re-fire on reload; threshold = single JS constant `BIG_WIN_PCT_THRESHOLD=200` for trivial retune; in-app-only (no push subscription, no server keys, no event emitter). (D) Push-notification version EXPLICITLY DEFERRED to post-SD-validation (post-2026-05-27 combined eval + post-V5A flip stability) — backend emitter on `_close_position` must NOT be touched during SD validation; PWA service worker scaffolded with push handler stub (logged-to-console only) in BUILD-2. **Card count 6 → 7:** cap nudged from 6 to 7. Three merge alternatives evaluated and rejected (Card 6 merge produces confused dual-ordering; Card 2 fold-in causes cognitive density; footer-link demotes against Jay's "first build, not deferred" instruction). Still one route, zero tabs, zero sub-pages, single-column vertical scroll. 30 → 7 = ~77% smaller than Concept C (was 80%). Forward guardrail: any future amendment proposing >7 cards / tabs / sub-pages / second routes triggers fresh STOP-C audit. **STOP-B revised:** 3 of 7 cards need backend (Card 2 all-time half + Card 3 + Card 7) ≈ 43% — still under the 50% threshold. **Build breakdown re-stated:** BUILD-0 0.5h → 1.0h (three endpoints now: `/api/active-alerts`, `/api/top-wins`, `/api/lifetime-pnl`); BUILD-1 2.5h → 2.8h (Card 2 expanded + Card 7 lands here with Card 2 — both P&L-flavored, ship together motivationally); BUILD-2 2.5h → 2.8h (Cards 3/5/6 + celebration FX + PWA + push handler stub). **Total 7.5h → ~8.5h.** Sequencing UNCHANGED — June parallel-track, NOT May critical path. **3 new roadmap items:** `DASH-BIGGEST-WINS-SCOPING-001` (Tier 3, revisit floor when LIVE-TRADES-LOGGING-AUDIT-001 closes); `DASH-PUSH-NOTIFICATIONS-001` (Tier 3, gated on SD-validation completion); `DASH-CELEBRATION-FX-THRESHOLD-TUNE-001` (Tier 3, post-deploy fine-tune). DASH-001 row updated below. Amendment folded into design doc as §3 (cards re-stated), §3.1 (FX), §3.2 (push deferral), §5 (build re-stated), §6 (backend deps revised), §9 (resolved), §11 (cap rationale appended). **NO services/* code change, NO env change, NO Redis writes, NO deploy.** |
| 2026-05-14 | V5A-PRECONDITION-CHECKLIST-CLEANUP-001 | ✅ CHECKLIST REWRITTEN (docs-only, NO code/env/Redis-write/deploy change) | Rewrote `AGENT_CONTEXT.md` §6 V5a preconditions outstanding against verified 2026-05-14 live state. Trigger: chat-side state synthesis found §6 ~1-month stale ("Sessions A-D", "session-E snapshot" anchors meaningless after 5+ config changes; +1 new V5A blocker LIVE-MODE-FILTER-PARITY-001-V2 added 2026-05-14 not yet in §6). **Verification (all parallel, 2026-05-14 ~12:57 UTC):** Helius `getBalance(4h4pst…ii8xJ)` = 0.064095633 SOL (UNCHANGED since 2026-04-21); Railway MCP bot_core (`TEST_MODE=true`, `BOT_CORE_FILL_MC_CEILING_USD=1000` (C1), `SD_EARLY_CHECK_SECONDS=60`, `ML_THRESHOLD_BOT_CORE_SD=40`); Railway MCP signal_aggregator (`TEST_MODE=true`, `ANALYST_DISABLED=true`, `SD_MC_CEILING_USD=3000`, `NANSEN_DRY_RUN=TRUE`); Redis MCP (`market:mode:override` NOT FOUND, `nansen:disabled` NOT FOUND, `market:mode:current=NORMAL` automated, `bot:status` RUNNING / 39.73 SOL paper / 0 open / consecutive_losses=1, heartbeat 2026-05-14T12:56:38Z UTC). **Changes to §6:** (1) KEPT PC1 wallet top-up (re-anchored to today's verified balance); (2) REFRAMED PC2 observation window — was "Sessions A-D / 24-48h", now "post-C1 deploy 2026-05-13 03:38:37Z UTC → combined STOP-LOSS-20-RUG-FILTER-EVAL-001 + NO-MOMENTUM-90S-EVAL-001 ≥2026-05-27"; (3) ADDED PC3 LIVE-MODE-FILTER-PARITY-001-V2 (resolves audit §8.2 open question from yesterday); (4) KEPT PC4 V5A flip-itself, expanded with CLEAN-003 pre-flip + `market:mode:current=NORMAL` flip-time check + DAILY_LOSS_LIMIT + sell-storm breaker; (5) MOVED SD_MC_CEILING_002 + LIVE-FEE-CAPTURE-002 Path B to "Completed preconditions (historical)" subsection; (6) REMOVED `SD_EARLY_CHECK relax confirmation` (TUNE-009 deferred permanently); (7) REMOVED `nansen:disabled` Redis renewal (migrated to env `NANSEN_DRY_RUN=TRUE`); (8) FOLDED `market:mode:override` standing renewal into PC4 flip-time check. **STOP-C broader staleness flagged inline:** `<!-- STALE: ... -->` annotation on §6's LIVE-FEE-CAPTURE-002 entry pointing at §7 row "LIVE-FEE-CAPTURE-002 (Path B) 📋 V5a-blocking-but-degradable" which contradicts the 2026-05-01 Decision Log "✅ DEPLOYED" entry. NOT silently rewritten this session — flagged for a separate `AGENT_CONTEXT-SECTION-7-SYNC` hygiene session. **Honest V5A blocker count post-rewrite: 4 outstanding (PC1 wallet / PC2 observation / PC3 V2 / PC4 flip-itself), 2 completed (historical), 3 removed/folded.** STATUS UNKNOWN items: zero. Related milestones (NOT V5A preconditions): ML_THRESHOLD_RETUNE_002 (≥2026-05-19, paper-only, orthogonal); combined eval (≥2026-05-27, gates PC2). Audit: `docs/audits/V5A_PRECONDITION_CHECKLIST_CLEANUP_001_2026_05_14.md`. **NO services/* code change, NO env change, NO Redis writes (read-only on live state), NO deploy.** |
| 2026-05-14 | LIVE-MODE-FILTER-PARITY-001 | 🟡 STOP-C / SCOPING NEEDED (read-only investigation, NO code/env/Redis/deploy change) | Investigation of NO_MOMENTUM_90S_AUDIT_001 §10 open item: port the C1 fill-time MC ceiling to the LIVE execution path. **Routing CONFIRMED** — `services/execution.py` is the live path only (`bot_core.py:82-83` imports `paper_buy` only under `TEST_MODE`; `bot_core.py:836` paper branch → `paper_buy`, `:948` live branch → `execute_trade`); not STOP-B. **Gate ABSENT** — `execution.py` (816 lines) read end-to-end, no MC ceiling anywhere; not STOP-A. **STOP-C fired:** the C1 gate (`paper_trader.py:247-275`) gates on a **fill-time** `entry_price * 1e9` price it computes itself; `execution.py` has (a) 3 execution routes and (b) **no fill-time price computation** (it returns a signature from unsigned tx bytes; bot_core fetches price *after* at `:956`). The only MC value reachable inside `execute_trade` is `token.liquidity_usd` = signal-time `features["market_cap_usd"]` — gating on it would NOT mirror C1 (signal-time ≠ fill-time, fails prompt §4.3) and would merely duplicate SA's existing `SD_MC_CEILING_USD` signal-time gate. A clean single-gate port that achieves MC-computation parity is not possible inside `execution.py`. **Scoping doc produced** (`.tmp_live_filter_parity/02_design.md`): 3 options, recommend **Option A** — gate in `bot_core.py` live branch *before* `execute_trade` using existing `self._get_token_price(mint)`, mirroring `paper_buy`'s fill-time MC + env var + reject-log + Redis-counter exactly. **New roadmap item: LIVE-MODE-FILTER-PARITY-001-V2** (Tier 1 🟡, V5A relaunch blocker) — scoped to Option A, needs explicit authorization to edit the bot_core live branch. The NO_MOMENTUM_90S_AUDIT_001 §10 "execution.py parity" open item is **superseded by V2** (no longer "tracked in STOP-LOSS-20-RUG-FILTER-EVAL-001" — promoted to its own item). **V5A implication:** until V2 lands, a live relaunch reintroduces the $1k-$3k fill-time bleed C1 eliminated on the paper path; SA's signal-time `SD_MC_CEILING_USD` is NOT a substitute (it is the gate C1 was built to backstop). Audit: `docs/audits/LIVE_MODE_FILTER_PARITY_001_2026_05_14.md`. **NO services/* code change, NO env change, NO Redis writes, NO deploy.** |
| 2026-05-14 | DASHBOARD-DESIGN-REALIGNMENT-001 | ✅ DESIGN COMPLETE (read-only, NO code/env/Redis change) `fb1b80f` | Re-scope of DASH-001 ahead of any build session, triggered by DASHBOARD-AUDIT-002 promotion recommendation + Jay's clarified purpose statement: "dashboard will be used for monitoring once live and doesn't need the depth we do now for analysis... when it's profitable and working, dashboard I can check anywhere on the go is preferable. Especially on mobile — that would be awesome." Concept C "Unified Cockpit" (2026-04-19) re-scoped from a 3-route × 3-tab × 14-panel desktop dashboard (≈30 surfaces) to a **mobile-first glanceable monitor: single screen, 6 cards, PWA-installable, ~480px max-width column on every viewport**. **5-7× smaller** than Concept C by surface count. Card set (priority order): (1) Bot status, sticky-top, color-coded ALIVE/STOPPED/EMERGENCY/HIBERNATE pill + mode chip + last-heartbeat — `/api/status` EXISTS. (2) Today's P&L, large number, `COALESCE(corrected_pnl_sol, realised_pnl_sol)` per BUG-022 — `/api/session-stats` EXISTS. (3) Active alerts (conditional visibility — folds DASHBOARD-AUDIT-002 G-01 F1+C1 filter rejects + G-08 rollback triggers into one surface) — **PARTIAL backend, needs new `/api/active-alerts` endpoint (~30 lines, 0.5h)**. (4) Wallet, trading on-chain + paper portfolio + holding — `/api/status` + `/api/wallets` EXISTS. (5) Open positions, count + aggregate + tap-to-expand — `/api/positions` EXISTS. (6) Recent trades, last 5 + footer-link to legacy `dashboard.html` — `/api/trades` EXISTS. **Technical shape:** single-column vertical stack on every viewport (no multi-column desktop — keeps the design from drifting back to Concept C); Geist + Geist Mono typography; dark mode default + light toggle; single chartreuse accent (no picker in v1); polling-only (~6 req/min, ~1.5 MB/hr); PWA manifest + service worker (Add to Home Screen on iOS/Android, offline shell with stale badge); ≤80 KB total page weight; no JS framework; no analytical charting libraries (D3/ECharts/Plotly banned). **Auth:** reuse existing JWT-in-localStorage pattern; longer-lived/biometric token flagged for separate session as `DASH-AUTH-001` Tier 3 (NOT V5a-blocking). **Relationship to legacy dashboard.html:** **NEW MONITOR LIVES AT `/m`; LEGACY `dashboard.html` STAYS at `/`**. Coexist for ≥30 days; deprecate after if Jay never opens it. Materially different from Concept C's replace-everything posture. **Build breakdown:** **3 sessions × 2.5h = 7.5h** (vs Concept C's 4-6 × 3h = 12-18h, ~55% smaller). BUILD-0: backend `/api/active-alerts` endpoint, 0.5h, curl-testable, no UI. BUILD-1: UI scaffold + Cards 1/2/4 + light/dark toggle + JWT-auth reuse + `/m` route, 2.5h. BUILD-2: Cards 3/5/6 + PWA manifest + service worker + offline-shell test, 2.5h. **Sequencing:** **June parallel-track with Analyst Phase 0, NOT in the May trading-logic critical path** (C1 observation → combined eval ≥2026-05-27 → ML_THRESHOLD_RETUNE_002 → dip detection). BUILD-0 could ship as a standalone micro-session if F1+C1 observability is acutely needed before June. **Backend dependencies:** 1 new endpoint, ~30 lines, no schema changes, no env changes. **STOP-B check:** ≪50% threshold — UI is the bulk of the work. **STOP-C check:** card set sits at 6 within the ≤6 cap; scope did not creep back to Unified Cockpit. **Testability:** DASH-T-001 (Playwright regression suite) test list shrinks under re-scope (3-4h → ~2.5h); does NOT need its own realignment doc — a 30m test-list refresh once OBS-004 unblocks is sufficient. Reused tests: B-002/B-003/B-006/B-007/B-009; N/A under re-scope: B-001/B-004/B-005/B-008/B-014; new tests: alerts-collapse, pwa-manifest-valid, sw-caches-shell, sticky-status-card. Build NOT blocked on DASH-T-001. **DASH-001 row updated below.** **Open questions to Jay (audit §9):** (1) acceptance of the re-scope; (2) 30-day coexistence with legacy; (3) single accent vs accent picker; (4) Sentry-error fold-in for alerts card v1.5; (5) active emergency-stop controls from phone (deferred, auth-impact); (6) June timing window. **Outputs:** `docs/audits/DASHBOARD_DESIGN_REALIGNMENT_001_2026_05_14.md` (main deliverable, ≤2500 words, §1-10). `docs/audits/DASHBOARD_REDESIGN_2026_04_19.md` (header note prepended: SUPERSEDED for scope; original Concept A/B/C preserved). `AGENT_CONTEXT.md` (header refresh + dashboard pointer). `MONITORING_LOG.md` (entry). `STATUS.md` (this prepend). `.tmp_dashboard_realignment/` (untracked: 01_scope_diff.md, 02_card_specs.md, 03_technical_shape.md, 04_build_breakdown.md, 05_testability.md, PROGRESS.md). **NO services/* code change, NO env change, NO Redis writes, NO redeploy.** |
| 2026-05-13 | DASHBOARD-AUDIT-002 | ✅ AUDIT COMPLETE (read-only) `12434e2` | Re-evaluation of 2026-04-19 dashboard audit suite against current bot reality (F1 deploy 2026-05-11, C1 deploy 2026-05-13, ML weakly-predictive finding 2026-05-12, Analyst hard-disabled). HEAD `2c9e47b`; only 1 commit on dashboard files since 2026-04-19 (`bc622eb` BUG-021 trade_mode filter fix). STOP-B PASS. **Verdict: REAFFIRM REBUILD (DASH-001).** Decision-flow audit: **0 of 8 operator decisions fully SUPPORTED**, 4 PARTIAL (#1 bot health / #3 daily PnL / #5 regime shift / #8 wallet stable), 4 MISSING (#2 filter visibility / #4 rollback triggers / #6 pipeline ready / #7 deploy decision). Gap analysis surfaced 10 enhancement items; PATCH-NOW count = 1 definitive (G-01 F1+C1 filter visibility, M-effort ~2-3h) + 2 conditional (G-08 rollback surface, DASHBOARD-CORRECTED-PNL-WARN-001). Below §8 BUNDLE threshold (needs 3-5 small items). All 10 gaps survive DASH-001 rebuild and would be built there anyway. Bug status: 4 closed since 2026-04-19 (B-002 via BUG-022 pass-through, B-004 confirmed, B-011 + B-012 already closed); 9 still apply (defer to rebuild); 2 separate fix candidates (DASHBOARD-CORRECTED-PNL-WARN-001, DASHBOARD-HEALTH-CHECK-PROBE-DEPTH-001); 3 unverified (B-007/B-008/B-009 → DASH-T-001 Playwright suite, blocked on OBS-004). **STOP-A applies weakly:** prior audits cover everything except G-01 urgency. **Recommendation:** promote DASH-001 from QUEUED → Tier 1 scheduled. DASH-001 has been QUEUED ~4 weeks; F1+C1 deploys + ML retune queue + audit-pipeline-tracking need create accumulating observability gaps that the rebuild naturally closes. ⛔ DASH-PATCH stays deferred (rebuild-not-patch). Open question to Jay flagged in audit §1 / §7 / §9. Audit: `docs/audits/DASHBOARD_AUDIT_002_2026_05_13.md`. NO services/* code change, NO env change, NO Redis writes. |
| 2026-05-13 | NO-MOMENTUM-90S-FILTER-RETUNE-DEPLOY-001 (C1) | ✅ DEPLOYED `a3ee421` (env-only) | Single env-var retune of `BOT_CORE_FILL_MC_CEILING_USD` on bot_core: **3000 → 1000** at 2026-05-13 03:29:21Z UTC; container restart 03:38:37Z UTC clean (`Startup reconciliation: 0 open positions in DB`, `Bot Core ready`, `Listening for emergency alerts`, no RuntimeError/Traceback). First post-deploy log `FILL_MC_CEILING reject: 6X5V79NvN85P mc=$10753 > ceiling=$1000` at 03:41:38Z confirms env plumbed through to rejection logic. **Justification for skipping T1 (~2026-05-14):** see audit §12 (added this session). Audit's two timing paths: wait for T1 vs Jay-authorized fast-track. Jay took the second path with explicit authorization naming `BOT_CORE_FILL_MC_CEILING_USD=1000`. Cross-audit reinforcement from ML-SCORE-ATH-VALIDATION-001 (2/489 nm90 post-exit pumps at 56h median lag → timer NOT killing winners) confirms MC discriminator is the right lever, not the timer. **Pre-deploy STOP gates all PASS:** STOP-A retest on 8.12d sample (vs audit's 7.49d) — C1 marginal blocked 589 trades / sum_pnl -10.86 SOL saved / **+1.49 SOL/day W3+W4 rate, +3.02 SOL/day W4-only rate** (both exceed ≥+1.0 threshold); false-positive winners blocked = **0** (matches audit exactly). KEPT slice strengthened to 523 trades / +32.62 SOL / **91.4% WR** (vs audit's 433 / +30.71 / 94.2%). STOP-B PASS (last fill-path commit = `0f37e82` F1 itself, no behavioural drift). STOP-C PASS (no concurrent bot_core deploy in latest STATUS entries). STOP-D PASS (Jay authorization explicit + named env var). Single push, `git commit --amend` to backfill commit hash into STATUS.md per RAILWAY-REDEPLOY-DISCIPLINE-001. **Rollback procedure** (single env command, no code revert; documented in deploy doc): `BOT_CORE_FILL_MC_CEILING_USD=3000`. **Eval deferred** to combined STOP-LOSS-20-RUG-FILTER-EVAL-001 + NO-MOMENTUM-90S-EVAL-001 at ≥2026-05-27 (+14d from this deploy, supersedes original ≥2026-05-25 schedule). Audit: `docs/audits/NO_MOMENTUM_90S_AUDIT_001_2026_05_12.md` (§12 added). Deploy prompt: `docs/audits/NO_MOMENTUM_90S_DEPLOY_PROMPT_2026_05_12.md`. |
| 2026-05-12 | ML-SCORE-ATH-VALIDATION-001 | ✅ INVESTIGATION COMPLETE / EVIDENCE-PRODUCED (read-only) | Research-only audit answering "does the bot's ML score predict pump.fun token ATH?" **Verdict: ML is WEAKLY PREDICTIVE.** Sample 1,097 pre-gate SD-paper trades (2026-04-22 → 2026-05-05 14:16:48 UTC, pre BOT-CORE-ML-GATE). AUC=0.5361 on the binary "ATH ≥ 5×" classifier — barely above chance (0.50). Mean ath_multiplier monotonic by quintile (Q1=2.20 → Q5=2.72). Mean PnL/trade monotonic upward (Q1=+0.007 → Q5=+0.017 SOL). **But** %≥5× mega-winner rate is NON-monotonic and PARADOXICAL: the **30-40 band catches 9.0% mega-winners**, more than 80+ band at 4.8%. **Counterfactual gate sweep** on `ML_THRESHOLD_BOT_CORE_SD`: current live setting (**thr=40 is the WORST** in the 30-55 range) — would have cost **-1.26 SOL** over the 12d sample by removing the 30-40 band's +1.26 SOL aggregate PnL and its 17 of 76 ≥5× winners. Best historical = **thr=55** (+0.71 SOL vs no-gate, +1.97 SOL vs current live, **+0.16 SOL/day implied lift**). **No deploy recommended this session** — decision belongs to ML_THRESHOLD_RETUNE_002 (re-derive on post-gate window ≥2026-05-12, ≥7d clean data). **Killer chart for no_momentum_90s:** 0 of 489 nm90 exits had ath_mult ≥ 2×; 2 of 489 had GT-confirmed post-exit peak at >2-day lag (noisy). **Timer is NOT killing winners** at any meaningful rate — reinforces NO-MOMENTUM-90S-AUDIT-001's MC-discriminator finding. **Coverage strategy:** GeckoTerminal free-tier sustained throughput ~2-3 rows/min (sliding-window 429), full 1,097-mint fetch infeasible. Pivoted to paper_peak (45%) + GT direct (1.6%) + no_pump_exit_inferred (53%) = **99.9% effective ATH coverage**. New `mint_ath_lookups` PostgreSQL table created (research-only, NOT consumed by bot_core/SA). 29 GT rows fetched as bonus validation. New scripts: `scripts/ml_ath_validation_001.py` (idempotent fetch loop), `scripts/verify_ml_calibration.py` (standalone counterfactual tool). **NO services/* edit, NO env, NO Redis writes, NO deploy.** Audit: `docs/audits/ML_SCORE_ATH_VALIDATION_001_2026_05_12.md`. **New follow-up:** ML_THRESHOLD_RETUNE_002 (Tier-1) — gated on ≥7d post-gate data accumulation. |
| 2026-05-12 | NO-MOMENTUM-90S-AUDIT-001 (T0) | ✅ INVESTIGATION COMPLETE / DEPLOY-RECOMMENDED (read-only) | T0 run ~24h post STOP-LOSS-20-RUG-FILTER deploy. **Finding:** the post-filter `no_momentum_90s` rate surge (40.2% → 76.5%) is NOT an independent failure — it is the visible symptom of a structural pattern: SD-paper tokens entering above MC $1000 are net-negative regardless of exit reason; F1's $3K ceiling cleared the deepest tier, leaving the $1k–$3k tier as the residual bleed. **Phase 0 pre-claims verification:** mean PnL anchor ≈ -0.018 ✓; nm90 median hold ≈ 80-90s ✗ (actual median 51s — exit fires at window-open); $0-1k WR 72-100% ✓; $1k-3k WR 0% ✓. STOP-A passes on the load-bearing PnL anchor. **Feature discrimination (W3+W4 nm90 vs trail_win):** market_cap_at_entry is the SOLE discriminator. trail_win p25/p50/p75 = $550/$639/$720; nm90 p25/p50/p75 = $2615/$2778/$2909. **Max trail_win MC = $892** — zero overlap with proposed $1000 ceiling, zero false positives. **Candidate counterfactuals (W3+W4 1223 trades, 7.49d sample):** C1 ($1000 ceiling): incremental over F1 = 589 trades blocked / -10.86 SOL saved = **+1.45 SOL/day W3+W4 rate, +3.67 SOL/day W4-only rate**, 0 FP. C2 ($1500): +1.31/d, 0 FP. C3 ($750): +1.23/d but 59 FP at -2.09 SOL — too aggressive. **Verdict:** DEPLOY-RECOMMENDED — C1 = tighten `BOT_CORE_FILL_MC_CEILING_USD` from 3000 → 1000. **Scope:** retune of F1 (same env var). Audit prompt §9 prohibits touching F1 in THIS session; deploy is a follow-on prompt at `docs/audits/NO_MOMENTUM_90S_DEPLOY_PROMPT_2026_05_12.md`. Two timing paths: (a) wait for STOP-LOSS-20-RUG-FILTER-EVAL-001 (≥2026-05-25) and bundle; (b) bring eval forward with Jay-authorized early retune — recommended. **T1 re-run scheduled ≈2026-05-14** to test regime-vs-structural via STOP-F. **Signal age (Phase 2.4) INFEASIBLE** — `signal_detected_at`/`scored_at`/`traded_at` NULL on 100% of post-filter rows (LATENCY-OBSERVABILITY-001 still open). **Time-of-day** is confounded with W4 timing — not actionable standalone. **NO services/* edit, NO env, NO Redis writes, NO deploy.** Audit: `docs/audits/NO_MOMENTUM_90S_AUDIT_001_2026_05_12.md`. Baseline: `.tmp_no_momentum_90s/T0_BASELINE.json`. |
| 2026-05-11 | STOP-LOSS-20-RUG-FILTER-DEPLOY-001 | ✅ DEPLOYED `0f37e82` (env-active 12:24:12 UTC) | Filter F1 (fill-time MC ceiling, env=$3,000) deployed to `services/paper_trader.py:paper_buy` per the 2026-05-09 investigation. Single Edit hunk (+29 lines) after `entry_price` computation: env-controlled gate `BOT_CORE_FILL_MC_CEILING_USD` (default 0 = disabled in code); reads ceiling at fill time; if `entry_price * 1_000_000_000 > ceiling`, returns `{success: False, error: "fill_mc_ceiling_exceeded"}` and increments Redis observability counter `bot:filter:fill_mc_ceiling:rejects:<date>` (14d TTL). Code change is fail-safe — invisible until env set; instant rollback by env→0 without redeploy. Railway env `BOT_CORE_FILL_MC_CEILING_USD=3000` set on **bot_core ONLY**. STOP gates re-verified pre-deploy: STOP-A PASS (investigation 2d old); STOP-B PASS (no behavioural change to paper_trader/bot_core since 27f623b); STOP-C PASS (9.5d ROI at $3K = **+1.38 SOL/day**, up from +0.93/day at investigation — bleed accelerating); STOP-D PASS (no concurrent deploy). Two redeploys triggered (code push + env set). Post-deploy verification harness (`.tmp_stop_loss_20_rug_deploy/post_deploy_verify.py`) checks: (i) Redis counter accessibility; (ii) no new SD-paper trades with `market_cap_at_entry > 3000` since deploy; (iii) trade-rate sanity (>=50% of pre-deploy rate). Re-evaluation scheduled +14d post-deploy via `STOP-LOSS-20-RUG-FILTER-EVAL-001`. Rollback procedure documented; no live-wallet exposure (TEST_MODE=true unchanged at flip). Audit: predecessor `docs/audits/STOP_LOSS_20_RUG_INVESTIGATION_001_2026_05_09.md` + deploy prompt `docs/audits/STOP_LOSS_20_RUG_FILTER_DEPLOY_PROMPT_2026_05_09.md`. |
| 2026-05-09 | STOP-LOSS-20-RUG-INVESTIGATION-001 | 🟢 INVESTIGATION COMPLETE / DEPLOY-RECOMMENDED (read-only) | Diagnoses `stop_loss_20%` as the largest SD-paper bleed since 2026-05-02 (n=65, sum −6.03 SOL). The label is a misnomer — these are sub-second rugs (median hold 0.99s, median observed drop −74.0%) caught at the next 2s exit-check tick because the f-string label fires whenever multiple ≤ 0.80, regardless of how deep the drop went. Root cause: SA `SD_MC_CEILING_002` gate evaluates BC reserves *as carried in raw_data at PumpPortal-publish time* (fresh-mint vSol≈30, vTokens≈1.073e9 → MC≈$2,400, always under $3K, always passes). bot_core fills via Jupiter/Gecko *current* USD price, which has pumped during the 1-15s signal-to-fill window. The DB column `market_cap_at_entry = entry_price × 1B` reflects the fill-time MC. **Smoking gun: 100% of 65 stop_loss_20% rugs have `market_cap_at_entry > $3,000` (median $7,881, max $181,519); 0% of 208 TRAILING_STOP wins do (median $623, max $832). Zero overlap.** Filter F1 (fill-time MC ceiling, default disabled, env-controlled `BOT_CORE_FILL_MC_CEILING_USD`) at $3,000 has counterfactual ROI **+0.80-0.93 SOL/day** across 7d/14d/17d-POST-cliff windows with **0% winner false positives**. STOP-A through STOP-G all pass. STOP-E by 0%; STOP-F by 2.7-3.1×. Other features (bc_progress, token_age, ml_score, slippage, market_mode, cfgi, rugcheck) all show ratios in [0.82, 1.21] — none discriminate. The lever has to be at fill time. New roadmap items (4): **STOP-LOSS-20-RUG-FILTER-DEPLOY-001** (Tier 1 🟢, paste-ready prompt, single-lever, env=$3000, ~30-45min wall clock, paper-only at flip — no live risk); **STOP-LOSS-20-RUG-FILTER-EVAL-001** (Tier 2, +14d post-deploy re-eval); **PAPER-ENTRY-PRICE-DENOMINATION-001** (Tier 3 🟢, observability follow-up — TRAILING_STOP wins cluster at MC $321-832 well below BC fresh-mint baseline ~$2,400, likely Jupiter v3 sub-baseline pricing for fresh pump.fun pools, doesn't affect F1). Existing **OBS-INTRA-HOLD-DRAWDOWN-001** reinforced (peak_price NULL on 100% of rugs). NO services/* / env / Redis writes. Audit: `docs/audits/STOP_LOSS_20_RUG_INVESTIGATION_001_2026_05_09.md`. Deploy prompt: `docs/audits/STOP_LOSS_20_RUG_FILTER_DEPLOY_PROMPT_2026_05_09.md`. |
| 2026-05-08 | VYBE-URL-CODE-DRIFT-001-FIX-2026-05-08 | ⏸ STOP per Step 7 #2 (no code change) `<hash>` | Investigation per chat-side prompt to fix the 3 hardcoded `.com` Vybe URLs at `services/signal_aggregator.py:753, 850, 2568`. Prompt anticipated a `.com → .xyz` TLD swap based on the 2026-05-05 audit's `.xyz → 401` probe inference. Probe with valid `VYBE_API_KEY` (sourced from Railway signal_aggregator env, not written to disk) reveals the prior inference was wrong: both `.com/token/...` and `.xyz/token/...` return HTTP 404 ("The requested endpoint does not exist") — the 401 was an auth-middleware artifact when probing without a key. Vybe MCP `search-endpoints` + `get-endpoint` confirm canonical paths are now versioned: `/v4/tokens/{mint}/top-holders` and `/v4/tokens/{mint}` — both noted in OpenAPI as **"Replaces"** the older `/token/...` paths. Probes against `.xyz/v4/tokens/...` return HTTP 200 with valid data (BONK test mint). **Two breaking downstream issues for URL-only fix:** (1) v4 Token Details has no `creator` field — L850 `_fetch_creator_history` continues to return empty even after URL+path fix; (2) v4 `/top-holders` returns `ownerName` not `ownerLabel`/`label` — L2568 KOL detection needs paired field-name update (otherwise `kol_count` stays 0 → `whale_boost` stays 1.0). L753 `_fetch_holder_data_vybe` is the only site URL+path fix alone fully restores. Per Step 7 condition #2 ("more than a TLD swap"), STOP triggered; NO code change to `services/signal_aggregator.py`; NO Railway redeploy. **New roadmap items (2 Tier 2 🟢):** **VYBE-CREATOR-LOOKUP-DEPRECATED-001** (need alternative creator data source for L850 — Helius parseTransactions first-slot, pump.fun metadata, or other; bigger scope, separate session); **VYBE-KOL-FIELD-MAPPING-001** (trivial L2568 `ownerLabel`/`label` → `ownerName` field-name update; bundle with V2 fix). Recommended follow-up `VYBE-URL-CODE-DRIFT-001-FIX-V2` (Path A1 in audit §7) — URL+path migration at all 3 sites + L2568 ownerName field update. Cost S. Caller analysis: no caller breaks on empty returns (silent-failure pattern is pre-existing). Lift bound unchanged from CLIFF supplement: bounded above by ~+0.4-0.5 SOL/day at current sizing, current-edge-restoration only, NOT cliff-recovery. Audit: `docs/audits/VYBE_URL_FIX_2026_05_08.md`. NO services/* / env / Redis writes. |
| 2026-05-08 | STATE-SNAPSHOT-2026-05-08 | ✅ COMPLETE (read-only) | Refreshed [STALE]/[ASSUMED] tags from 2026-05-07 handover ahead of DEFENSIVE-OVERRIDE-PROBE-EVAL window. **Key finding: 🔴 PROBE EXPIRED.** `market:mode:override` absent at 2026-05-08 13:21 UTC; `market:mode:current=NORMAL`. Probe was set 2026-05-06 22:29 UTC with 24h TTL → expired 2026-05-07 22:29 UTC; daily renewal did NOT fire. Probe ran ~24h (n=54 SD-paper closed) before lapsing — under the 80-trade target. Probe-period sample (2026-05-07 onwards, n=263 SD-paper, +2.06 SOL net, 52.9% WR) is contaminated (mixed override-forced DEFENSIVE + threshold-determined NORMAL). Mode coverage gap: `mode_at_entry` absent in 263/263 sample rows — per-row mode reconstruction not possible from DB alone. **Recommendation:** eval session re-runs probe with renewal commitment vs accepting underpowered 24h sample. **Other findings:** trading wallet UNCHANGED at 0.064095633 SOL; holding wallet rose 0.0098 → 0.190842421 SOL (+0.181, treasury dormant — confirm with Jay); Anthropic 🔴 still firing per Redis `governance:latest_decision`; Vybe URL drift 🔴 unchanged (signal_aggregator.py:753, 850, 2568 still on `.com`); Helius 🟢; bot_core ML gate `ea0da2f` present in HEAD `15a334a`. **NO services/* edit, NO Redis writes, NO env / deploy.** New roadmap items: **MODE-AT-ENTRY-FEATURE-001** (Tier 2 🟢 — paper_trader / signal_aggregator should write `mode_at_entry` to features_json so future audits can do per-row mode-coverage analysis); **HOLDING-WALLET-DRIFT-2026-05-08** (Tier 3 🟢 — confirm +0.181 SOL increase with Jay). Audit: `docs/audits/STATE_SNAPSHOT_2026_05_08.md`. |
| 2026-05-07 | DEFENSIVE-OVERRIDE-PROBE-001 | ✅ STARTED `<hash>` | Set `market:mode:override=DEFENSIVE EX 86400` at **2026-05-06T22:29:10Z UTC** to A/B-probe the **DEFENSIVE-VS-NORMAL-PNL-INVERSION-001** finding from MARKET-MODE-001-RE-CALIBRATE Path C. Pre-probe baselines reinforce the inversion at multiple windows: (a) post-Session-2 5-day sample — NORMAL -1.09 SOL on 121 trades / 24.8% WR vs DEFENSIVE +0.25 SOL on 45 / 28.9% WR; (b) last 48h SD-paper (this session §2 baseline) — NORMAL -0.80 SOL on 103 / 33.0% WR vs DEFENSIVE +0.44 SOL on 17 / 64.7% WR. Probe purpose: generate ≥80 SD-paper trades under DEFENSIVE override over 48h to either confirm at scale or refute as small-sample noise. **§4 propagation verified:** `services/market_health.py:412-414` override-read path fired at 22:34:45Z with explicit `Market mode OVERRIDE active: DEFENSIVE` log line; `market:mode:current` flipped NORMAL→DEFENSIVE within 34s of SET; `bot:status.market_mode=DEFENSIVE` confirmed by 22:39:31Z heartbeat. First post-SET trade observed at 22:39:48Z (`speed_demon:C7Ad1dff…`, no_momentum_90s -0.0152 SOL) — throughput flowing under DEFENSIVE; bot_core ML gate at 40 still rejecting <40 ml_score signals (independent control intact). **Re-evaluation milestone: ≥2026-05-08T22:29Z UTC** (48h). New roadmap items: **DEFENSIVE-OVERRIDE-DAILY-RENEWAL-001** (Tier 2 OPERATIONAL — daily Redis renewal until probe ends; auto-expires safely if forgotten); **DEFENSIVE-OVERRIDE-PROBE-EVAL-001** (Tier 1 — paste-ready 48h evaluation prompt to follow; decides KEEP / REVERT / EXTEND). Rollback triggers: cumulative < -0.50 SOL on n≥30 / WR < 18% on n≥30 / throughput <5 SD-paper trades/day for 24h consecutive / HIBERNATE >2h consecutive. Rollback procedure: `DEL market:mode:override` via Redis MCP. **NO services/* edit, NO deploy, NO env change.** Sole runtime change: 1 Redis key SET with 24h TTL. Predecessor audit: `docs/audits/MARKET_MODE_001_RE_CALIBRATE_FINDINGS_2026_05_05.md`. |
| 2026-05-07 | CLIFF-VYBE-SOCIALDATA-SUPPLEMENT-001 | ✅ INVESTIGATION COMPLETE `<hash>` | Targeted time-correlation of Vybe URL drift + SocialData credit drain against 2026-04-21 cliff. **Both verdicts: NOT CLIFF.** Vybe: `git blame` shows all three `.com` URLs at `services/signal_aggregator.py:753/850/2568` introduced 2026-04-04 (672eb8ac, 94b3c564) and 2026-04-08 (751680e3) — 13-17 days BEFORE cliff; bot's +598 SOL pre-cliff era ran with broken Vybe URLs throughout. `git log -S 'vybenetwork.xyz' -- services/signal_aggregator.py` returned 0 hits — `.xyz` never appeared in this file. Sibling `services/nansen_wallet_fetcher.py:209` blame `c9a30061` (2026-03-29) was correct from creation with comment "Vybe domain is .xyz with X-API-Key auth (not .com, not Bearer)" — developer knew but never migrated SA path. SocialData: DB temporal pattern in `paper_trades_archive_20260421` shows `pct_sentinel(twitter_followers=-1)` jumped **18.7% (04-15) → 99.2% (04-16)**, 5 days BEFORE cliff; commit `35bdfe6` (2026-04-19 21:00 AEST, 2 days pre-cliff) explicitly stated: "**495/495 of most-recent 500 SD paper trades have twitter_followers=-1**". Pre-cliff sentinel-bucket (n=2101, sum +485.37 SOL, mean +0.231) **outperformed** populated-bucket (n=883, sum +112.49 SOL, mean +0.127) on per-trade PnL — Twitter feature was NOT load-bearing for the +598 SOL edge. **Reinforces parent's DO NOT REVERT recommendation:** the +598 SOL was a fee-model accounting artifact, not feature edge. Existing items VYBE-URL-CODE-DRIFT-001 + SOCIALDATA-AUTO-TOPUP-001 stand as **current-edge-restoration** items, NOT cliff-recovery items; estimated lift bounded above by post-cliff populated-vs-sentinel mean delta (~+0.0125 SOL/trade ≈ +0.4-0.5 SOL/day at current sizing). **Bonus finding:** SocialData drained AGAIN starting 2026-05-03 (10 days post-Jay's 04-22 top-up); strengthens SOCIALDATA-AUTO-TOPUP-001 auto-renewal case. Audit: `docs/audits/CLIFF_VYBE_SOCIALDATA_SUPPLEMENT_2026_05_05.md`. No code/env changes (read-only). |
| 2026-05-07 | STRATEGY-CLIFF-INVESTIGATION-001 | 🟡 STOP / NO REVERT (no code change) | Investigation per chat-side prompt comparing PRE-cliff archive (`paper_trades_archive_20260421`, mean +0.20 SOL/trade on 2,984 trades) vs POST-cliff current (mean +0.014 SOL/trade on 528 trades) revealed the cliff is **primarily a fee-model accounting artifact**, not a strategy regression. The two eras are not on the same accounting basis: PRE rows were written under the OLD paper fee model that under-counted fees by ~96× per FEE-MODEL-001 (commit `e078b4c`, deployed 2026-04-21 07:26 AEDT, ~0.39 SOL/trade per-trade correction). Apples-to-apples calculation: PRE-cliff at realistic fees ≈ -0.19 SOL/trade (-566 SOL on 2,984 trades) vs POST-cliff +0.014 SOL/trade. **Under fair accounting, the new strategy is +0.20 SOL/trade BETTER than the old.** Reverting GATES-V5 / FEE-MODEL-001 / DASH-RESET would re-introduce illusory paper PnL, larger position sizes (1.50 vs 0.25 SOL cap), looser stops (-35% vs -20%), and zero entry filters — strict regression. Counterfactual lift if reverted: 0 SOL/day (likely negative, bound -2 to -5 SOL/day on real edge). Signal-source shift hypothesis REFUTED (100% pumpportal both eras). MC-band shift CONFIRMED (89% in $1-5M PRE → 41.5% POST, gate-driven). Exit-reason shift CONFIRMED (BREAKEVEN_STOP 131→0 fires; staged_tp_+50/+100/+250/+400/+500% all removed via env override; staged_tp_+1000% retained at lower per-fire mean due to sizing). New roadmap items (5, all Tier 2 🟢, none V5a-blocking): **STRATEGY-CLIFF-FOLLOWUP-001** (re-validate at 14d POST sample ≥2026-05-12), **PRE-DEPLOY-PNL-VALIDATION-001** (process improvement: future strategy changes must include accounting-regime check), **BREAKEVEN-DECISION-001** (proper A/B test breakeven lock vs no-breakeven), **TP-SCHEDULE-EVAL-001** (re-evaluate full ladder TP), **SIGNAL-MIX-ANALYSIS-001** (per-gate ablation of HOLDER/BSR/PRE_FILTER/CFGI). NO services/* code change. Audit: `docs/audits/STRATEGY_CLIFF_INVESTIGATION_001_2026_05_05.md`. |
| 2026-05-06 | API-CREDITS-HEALTH-DIAGNOSTIC-001 | ✅ AUDIT COMPLETE `<hash>` | Read-only service-health snapshot across 12 dependencies. Findings: 4 🔴 / 9 🟡 / 7 🟢 / 1 ⚪. **Top blockers:** (1) **VYBE-URL-CODE-DRIFT-001 NEW Tier 1** — `services/signal_aggregator.py:753, 850, 2568` use `api.vybenetwork.com` → HTTP 404; `.xyz` → 401 (route exists). DOCS-004 fixed docs 2026-04-30 but never migrated code; HOLDER fallback + creator-history + KOL/MM signal modifier fail silently via `except Exception: pass`. Cost S, single-file 3-string fix. (2) **SocialData credits exhausted** — 113 `SocialData out of credits` ERROR/11min in SA log; `twitter_followers` permanently sentinel `-1`; promotes SOCIALDATA-AUTO-TOPUP-001 from QUEUED → ACTIVE (Jay action: top-up + auto-renewal alerting). (3) BUG-010 Anthropic credits still exhausted (governance log 13:55:58 UTC: `400 Your credit balance is too low`; falls back to CONSERVATIVE defaults, not V5a-blocking by current rules). (4) BUG-020 Discord 403 still firing every 5min on signal_listener. **New 🟡 (Tier 2 WATCH):** TREASURY-HELIUS-LOG-NOISE-001 (270+ misleading "Could not fetch trading wallet balance" warnings/22h are caused by `treasury.py:60` HELIUS_DAILY_BUDGET=0 gate, not RPC outage); DASHBOARD-CORRECTED-PNL-WARN-001 (127 warnings/28min on web — likely query against `trades` table without column); DASHBOARD-HEALTH-CHECK-PROBE-DEPTH-001 (`service:health.anthropic=ok` but only checks env-var presence — masks BUG-010); TABPFN-EXPIRY-DOC-DRIFT (handoff says 2033 — JWT exp = **2027-04-05 04:55:55 UTC**, ≈335 days runway); ANTHROPIC-KEY-SCOPE-001 + JUPITER-API-KEY-SCOPE-001 (both keys set on all 8 services unnecessarily); BOT-CORE-SOCIAL-FEATURE-DEGRADATION-001 (training rows during SocialData-out windows skew sample). **🟢:** Helius RPC + parseTx (key URLs identical across all 8 services); PumpPortal WS (~22 new_token/min + ~18 new_pool/min); Binance/Jupiter SOL price (agreement within $0.03); Sentry release-tagging active across all 8 services (release `ea0da2f89164` matches main HEAD); Railway services running (all 8 restarted same window — RAILWAY-REDEPLOY-DISCIPLINE-001 confirmed again); Nansen dry-run shield holding (NANSEN_DRY_RUN=TRUE on hot services makes split-key + budget-drift cosmetic); TabPFN auth valid until 2027-04. **V5a impact:** none of the 4 🔴 are V5a-blocking by current rules; original blockers (wallet 0.064 SOL, 48h obs, NORMAL window) unchanged from V5A_GO_NO_GO_2026_05_01.md. Audit: `docs/audits/SERVICE_HEALTH_SNAPSHOT_2026_05_05.md`. No code/env changes (read-only). |
| 2026-05-06 | MARKET-MODE-001-RE-CALIBRATE | 🟡 STOP / Path C (no code change) | Investigation per §3 revealed prompt's premise is incorrect for actual data: (a) binding constraint is **`dex_vol` not `grad_rate`** (matches §4 Path C explicit STOP example); (b) HIBERNATE is only 1.3% of snapshots — actual problem is DEFENSIVE-vs-NORMAL not HIBERNATE-vs-NORMAL; (c) **NORMAL has WORSE per-trade PnL than DEFENSIVE** (-1.09 SOL on 121 vs +0.25 SOL on 45) so expanding NORMAL share would have been actively harmful. Path A's grad_rate loosen 30→15 is a no-op for our data (live mig=215 already 7× current threshold). 3 new roadmap items filed: **DEFENSIVE-VS-NORMAL-PNL-INVERSION-001** (Tier 1 — investigate why NORMAL bleeds), **MARKET-MODE-001-RE-CALIBRATE-V2** (Tier 1 — re-scoped with corrected premise; gated on PnL inversion investigation), **PUMPFUN-VOL-PLACEHOLDER-001** (Tier 2 — pumpfun = 0.15 × dex_vol structurally degenerate; AND gate is effectively 2-metric), **MM-HYSTERESIS-ONLY-001** (Tier 2 — hysteresis still beneficial standalone). NO market_health.py edit, NO deploy, NO env change. Audit: `docs/audits/MARKET_MODE_001_RE_CALIBRATE_FINDINGS_2026_05_05.md`. |
| 2026-05-05 | TIMEZONE-AUDIT-001 | ✅ AUDIT COMPLETE | Repository-wide read-only sweep for hardcoded TZ offsets. Findings (production code): 2 🔴 BUG (`services/bot_core.py:754,776` — both covered by existing TIME-PRIME-AEDT-AEST-DRIFT-001 follow-up; single fix-point in `_compute_position_size_for_signal`), 2 🔵 MARGINAL (risk_manager UTC-by-design global-session bands; sigagg ML `hour_of_day` in UTC), 5 🟡 OK-WITH-CAVEAT (daily_reset UTC-midnight design choice; AEDT log/UI labels), 20+ 🟢 SAFE. **`services/market_health.py` is CLEAN** — fully DST-aware via `pytz.timezone("Australia/Sydney")`; **NOT BLOCKING for MARKET-MODE-001-RE-CALIBRATE**. Class-of-bug verdict: contained — single root cause already on the roadmap; no new 🔴 surfaced. 4 new LOW-priority hygiene items proposed (TZ-CONVENTION-DOC-001, RISK-MGR-TZ-COMMENT-001, SIGAGG-ML-HOUR-LABEL-001, DASH-AEDT-LABEL-001). Recommended convention added to AGENT_CONTEXT.md + CLAUDE.md: decision logic uses `ZoneInfo("Australia/Sydney")` or pytz; storage uses UTC; no hardcoded offsets anywhere except UTC-by-design global-session bands (which must be commented). Audit: `docs/audits/TIMEZONE_AUDIT_2026_05_05.md`. |
| 2026-05-05 | BOT-CORE-ML-GATE-001 | ✅ DEPLOYED `ea0da2f` | bot_core ML gate active at threshold 40 (env-active 2026-05-05T14:16:48Z UTC). Closes the AGGRESSIVE_PAPER override bypass that contaminated all prior threshold-tuning analysis (the ZERO-effect-on-paper-sample discrepancy that STOPped Session 4 / ML-THRESHOLD-DATA-DRIVEN-RETUNE-001). Code: 3 hunks in `services/bot_core.py` (+44 lines): module env reads (defaults 0 = disabled), `_ml_gate_reject_reason` helper, gate insertion in `process_signal` between governance max_concurrent and gov_size_mult. Helper semantics: ml_score=None → ACCEPT (fail open); threshold ≤ 0 → ACCEPT (disabled); ml_score < threshold → REJECT (logger.info "BOT_CORE_ML_GATE: skip ..."); ml_score == threshold → ACCEPT (boundary inclusive). Verify-script 8/8 PASS. §6 verification PASS (low-confidence single-sample: 0 below_40 admissions in 45min post-cutoff, 1 admission at score=47, market_mode=DEFENSIVE suppresses upstream throughput). 7d observation window for V5a + ML-THRESHOLD-DATA-DRIVEN-RETUNE-002 restarts from 2026-05-05T14:16:48Z UTC; re-queue ML-RETUNE-002 for ≥2026-05-12 14:16Z. Audit: `docs/audits/BOT_CORE_ML_GATE_001_2026_05_05.md`. |
| 2026-05-01 | V5A-GO-NO-GO-CHECKLIST-001 | ✅ AUDIT COMPLETE `<hash>` | **Verdict: NO_GO.** 3/10 PASS, 4/10 CONDITIONAL, 3/10 FAIL. Blocking: PC1 wallet 0.064 SOL << 0.5 floor (Jay action: top-up to ~3 SOL); PC5 48h observation window opens ~2026-05-03 12:55 UTC (Sessions 1-5 deployed today); PC7 market_mode=HIBERNATE at audit time (cycling per MARKET-MODE-001 recalibrated thresholds). Conditionals: PC3 TIME_PRIME small post-fix sample (forward PASS pending data); PC6 ML retune stopped (no deploy = trivially no rollback); PC8 ML threshold drift documented; PC9 7d -0.98 SOL (mildly negative, watch). Live flip NOT recommended. **When unblocked, recommended caps:** TEST_MODE=false on bot_core ONLY, MAX_POSITION_SOL=0.10, MAX_SD_POSITIONS=5 for first 24h. Re-run V5A-GO-NO-GO after wallet top-up + 48h elapsed + NORMAL window. Audit: `docs/audits/V5A_GO_NO_GO_2026_05_01.md`. |
| 2026-05-01 | LIVE-FEE-CAPTURE-002 (Path B) | ✅ DEPLOYED `<hash>` | Helius parseTransactions wired into bot_core live-close UPDATE path. New `services/helius_parser.py` helper (Path B); falls back to Path A `live_estimated_v1` on parse failure. Verify-fix PASS: id 6580 reconstruction = -0.094245 (matches on-chain truth within 0.000000 SOL — exact). Critical schema discovery: use `accountData[*].nativeBalanceChange` (NOT `nativeTransfers` — that field misses PDA-mediated swap proceeds; original parser bug had 0.281 SOL gap). id 6580 row backfilled in DB: corrected_pnl_sol -0.0064 → -0.094245, correction_method='live_actual_v1'. **Closes V5a parity-of-truth precondition**. New tracking items (Tier 2): `LIVE-CLOSE-PATH-B-LATENCY-001`, `LIVE-PATH-B-SLIPPAGE-DERIVATION-001`. Audit: `docs/audits/LIVE_FEE_CAPTURE_002_PATH_B_2026_05_01.md`. |
| 2026-05-01 | ML-THRESHOLD-DATA-DRIVEN-RETUNE-001 | ⏸ STOP per §8 `<hash>` | Env-var change to ML_THRESHOLD_SPEED_DEMON has ZERO effect on paper sample due to AGGRESSIVE_PAPER+TEST_MODE override (`signal_aggregator.py:158` sets effective SD threshold = 30 regardless of env value). bot_core has no second ML gate. Sweep analysis complete: 14d AND 7d both show **optimum threshold = 55** (sum_admitted +8.71 SOL on 448 / +1.48 on 293; WR 37.5%/30.4%). NO env-var change made. Recommended path: Option A (add bot_core ML threshold gate, single code change). New roadmap items: `BOT-CORE-ML-GATE-001` (Tier 1 🟡), `AGGRESSIVE-PAPER-DISABLE-001` (Tier 2 🟢, evaluation required). **Major correction to Session 3:** 280 graduation_* exits ALL from `analyst` (disabled 2026-04-28 13:02 UTC); SD has 0 post-grad entries; **POST-GRAD-ENTRY-GATE-001 actual current ROI ≈ 0** (bleed already stopped via ANALYST_DISABLED env); insurance value only. Audit: `docs/audits/ML_THRESHOLD_DATA_DRIVEN_RETUNE_2026_05_01.md`. |
| 2026-05-01 | POST-GRAD-LOSS-INVESTIGATION-001 | ✅ INVESTIGATION COMPLETE `<hash>` | Tested 5 hypotheses against post-graduation bleed. Verdicts: H1 PARTIAL (19.3% of TRAILING_STOP winners exited below -20%), H2 **REFUTED in original framing — REVEALS the real bleed: 280 post-grad-ENTRY trades (bc_progress=1.0 AT ENTRY) sum -14.60 SOL/14d (~-7.3 SOL/week)**, H3 SKIPPED (paper synthetic sigs), H4 REFUTED, H5 UNTESTABLE (`trailing_stop_active` is transient state, not "ever-engaged" flag). **Recommended highest-ROI patch: POST-GRAD-ENTRY-GATE-001** (gate out SD signals where `bonding_curve_progress >= 0.99` at signal_aggregator). Cost S, ROI ~+7 SOL/week. Other proposals: NO-MOMENTUM-90S-AUDIT-001 (Tier 1 🟡), OBS-TRAIL-ENGAGEMENT-001 (Tier 2), OBS-INTRA-HOLD-DRAWDOWN-001 (Tier 2), OBS-EXIT-TIME-BC-PROGRESS-001 (Tier 2). Audit: `docs/audits/POST_GRAD_LOSS_INVESTIGATION_2026_05_01.md`. No code change this session. |
| 2026-05-01 | STATE-RECONCILE-2026-05-01 | ✅ COMPLETED `<hash>` | Reconciled CLAUDE.md / AGENT_CONTEXT.md / ZMN_ROADMAP.md / MONITORING_LOG.md against verified production-DB findings A-E. Headline directions confirmed (AEST 18-20 worst, 40-50 ML band worst, post-grad + stop_loss_20% dominant losers, TRAILING_STOP dominant winner). Detail-level chat-side framings ADJUSTED to actual data: post-grad bleed −12.09 SOL/7d (NOT chat's −23 SOL); TRAILING_STOP captures ~69% of gains (NOT 98%); ML 50+ NOT "mostly profitable" — only 70-80 reliably positive (-0.18 / -0.70 / +1.63 / -0.35 by band). New roadmap items: POST-GRAD-LOSS-INVESTIGATION-001, ML-THRESHOLD-DATA-DRIVEN-RETUNE-001. New drift report: `docs/audits/USERMEMORIES_DRIFT_2026_05_01.md`. No code, no env-var changes. |
| 2026-05-01 | TIME-PRIME-CONTRADICTION-FIX-001 | ✅ DEPLOYED `<hash>` | TIME_PRIME 2× upsize at code aedt_hour 18-20 (= AEST 17-19 post-DST 2026-04-05 due to UTC+11 vs UTC+10 drift) was empirically the WORST window: AEST 18-20 −2.46 SOL on 114 trades / 7d (chat-side framing) and AEST 17-19 −2.76 SOL / 114 (actual code firing). §2 Step 3 confirmed multiplier ratio 2.05× via avg amount_sol comparison. Now env-controlled (TIME_PRIME_HOURS_AEST="" / TIME_PRIME_MULTIPLIER=1.0 by default = disabled). Verify-fix PASS: 2/5 test cases neutralized. New roadmap follow-ups: TIME-PRIME-AEDT-AEST-DRIFT-001 (LOW), TIME-PRIME-CALIBRATION-001 (MEDIUM). Audit: `docs/audits/TIME_PRIME_CONTRADICTION_FIX_2026_04_30.md`. |
| 2026-04-30 | MARKET-MODE-001 (HIBERNATE-forever fix) | ✅ DEPLOYED `932ae08` | signal_aggregator wrote market:grad_rate_estimate as RATIO (~0.0001); market_health thresholds (0.5/0.8/1.0/1.5) assumed RATE per hour. Definition mismatch → HIBERNATE-forever for ~weeks. Fix: market_health reads market:migration_count_1h directly + recalibrate to count-based thresholds (10/30/100/200). Sample 73 migs/hr → NORMAL post-deploy (verified live 13:36 UTC). |
| 2026-04-30 | SOCIAL-SCORING-001 (STATE C → fix) | ✅ DEPLOYED `627f4c9` | Audit verdict STATE C: social fetch + score modifier worked, but features_json captured only `has_social` (any) + `twitter_followers`. Missing `has_twitter` / `has_telegram` / `has_website` / `social_count`. Patch 2C adds 4 keys to features dict at signal_aggregator.py:2022. ML training gains 4 social-component features on fresh entries. |
| 2026-04-30 | DOCS-004 (Vybe URL) | ✅ FIXED | CLAUDE.md:478 + AGENT_CONTEXT.md:576 + 1883: `.com (NOT .xyz)` → `.xyz (NOT .com)`. Verified source `BITFOOT_2026_BASELINE_2026_04_23.md` §1. |
| 2026-04-30 | TUNE-004 (SA size env hygiene) | ✅ FIXED | SA env aligned with bot_core via Railway MCP: `SPEED_DEMON_BASE_SIZE_SOL=0.15` / `MAX_SIZE_SOL=0.25` / `MAX_SD_POSITIONS=20` / `MIN_POSITION_SOL=0.05`. Verified-fields-before-coding: SA code doesn't read these (zero references) — pure hygiene. |
| 2026-04-30 | DOCS-002 (ML threshold) | ✅ ALREADY DONE | Verified in `e9de6d7` — CLAUDE.md:203 + 242 already have strikethrough + SUPERSEDED note. Roadmap row updated. |
| 2026-04-30 | OBS-014 (stop_loss_35% SQL filter) | ✅ MOOT | No production code uses literal `stop_loss_35%`. Only historical artifacts. Roadmap row updated. |
| 2026-04-30 | STATUS-CONVENTION-001 | ✅ PROMOTED | 5+ sessions appending cleanly per Session E note + this session. Promoted from 🟡 IN_PROGRESS. |
| 2026-04-30 | SILENCE-RECOVERY-2026-04-28 | ✅ CLEARED | Per AGENT_CONTEXT §7 (recovered 2026-04-28 13:02 UTC → 2026-04-30 active). Roadmap row updated. |
| 2026-04-30 | SLIPPAGE-TIER-LIVE-PATH-A-001 (FEE-LATENCY-REALISM) | ✅ DEPLOYED `f3a1741` | Path A live close `_simulate_slippage("buy", ...)` was hitting default fallback `(0.5, 2.0, 0.3)` because `"buy"` is NOT a `SLIPPAGE_RANGES` key. Fix plumbs entry-time tier through new `Position.entry_slippage_tier` field. alpha_snipe avg 1.86% → 18.70% (10×); confirmation 1.86% → 12.47% (6.7×). Closes ~30% of Path A's +0.088 SOL gap on id 6580. Path B remains right answer for parity-of-truth. |
| 2026-04-30 | LATENCY-OBSERVABILITY-001 (NEW) | 📋 PLANNED | All 4 latency columns (`signal_detected_at`, `scored_at`, `traded_at`, `total_latency_ms`) NULL on every paper_trades row (0/1182). Population requires 4-file refactor across signal_listener + signal_aggregator + paper_trader + bot_core + Redis-payload schema changes. Not V5a-blocking; useful for SLIPPAGE-CALIBRATION-001 calibration analysis. |
| 2026-04-30 | SD_MC_CEILING_002 (BC-reserves MC compute, USD=3000) | ✅ DEPLOYED `<hash>` | Replaces _001's inert gate. Computes MC from `vSolInBondingCurve / vTokensInBondingCurve × 1B × market:sol_price` mirroring `bot_core.py:927` and `paper_trader.py:255-257`. Fail-open if any field missing. Step 6 + 24h verification queued. |
| 2026-04-30 | LIVE-FEE-CAPTURE-001 (Path A) | ✅ DEPLOYED Session D `401670a` | Wired `_simulate_*` into live close path; Path B (Helius parseTx) deferred — id 6580 backfill confirmed Path A undercorrects by 12× (gap +0.088 vs on-chain truth). |
| 2026-04-30 | SD_MC_CEILING_001 (USD=3000) | ⚠️ SUPERSEDED by _002 (was ⏪ ROLLED BACK Session C `282d9df`) | Gate read `raw_data.usdMarketCap` which is $0 for fresh pump.fun new_token signals; `market_cap_at_entry` is computed differently in bot_core. Gate inert. _002 replaces with BC-reserves compute. |
| 2026-04-30 | BUG-022 (Option A) | ✅ DEPLOYED Session B `392c928` (+ hotfix `17c2aac`) | 1111-row idempotent backfill + inline write at `paper_trader.py:395` and `bot_core.py:1063`. Hotfix used distinct param slots ($11/$12/$13) — asyncpg can't deduce single type when same param appears in columns of different declared types. |
| 2026-04-30 | SD_EARLY_CHECK relax (TUNE-009) | ⏸ DEFERRED Session A `fbd579d` | Empirical data rules out relax — 0/123 dead trades are slow-starters (mean exit pnl_pct -17.37%; max -1.85%); structural fix lives at entry filter, not exit. |
| 2026-04-30 | WALLET-DRIFT-2026-04-29 (1.5 SOL transfer 2026-04-21) | ✅ RESOLVED — Branch 1 (intentional) | Jay confirmed 2026-04-29 chat. CLAUDE.md "Live trading mode" block updated with the transfer note. Mechanical layer (top-up to ~3 SOL pre-V5a) tracked separately. |
| 2026-04-29 | LIVE-FEE-MODEL-AUDIT | ✅ AUDIT COMPLETE `a208aa5` | Major divergence found in 4 dimensions; spec for Session D Path A. |
| 2026-04-29 | ENV-AUDIT-2026-04-29 | ✅ AUDIT COMPLETE `4180e46` | Ground-truth Railway/Redis/DB. Surfaced WALLET-DRIFT, TREASURY-TEST-MODE-002, ML-THRESHOLD-DRIFT, RAILWAY-REDEPLOY-DISCIPLINE-001. |
| 2026-04-29 | TUNE-005 (HOLDER_COUNT_MIN 15→1) | ⏪ ROLLED BACK `5706d7e` | Speed Demon WR dropped 52.9% → 34.9% over 3 days post-deploy; hypothesis: gate=15 rejected winners with <15 early holders. 24h validation window. |
| 2026-04-28 | ANALYST-DISABLE-002 (code-level fix) | ✅ DEPLOYED `9d6e95c` | Closes graduation-sniper bypass that ANALYST-DISABLE-001 Redis override couldn't reach (predicted halflife realized in 6 min, 241 leaked trades). Env-var pattern extended to `_process_graduations`. |
| 2026-04-28 | BUG-022 INVESTIGATED | 🟡 INVESTIGATED `ca4812d`, then ✅ FIXED Session B | Verdict 2a: writer never existed as committed code. Recommended Option A (now Session B). |
| 2026-04-23 | ANALYST-DISABLE-001 (Redis override) | ✅ DEPLOYED `cc8e5c9` (superseded by 002) | Stops 11.9% WR / -2.48 SOL / 17h leak via `governance:latest_decision.analyst_enabled=false`. Halflife concern → led to 002. |
| 2026-04-23 | BITFOOT-2026-BASELINE | ✅ AUDIT VERDICT BASELINE-A directionally `2b4cb73` | 2-filter still concentrates winners in 2026 trending data; absolute rates survivorship-inflated; Raydium post-grad vanished; Meteora up 10%→44%. |
| 2026-04-23 | BITFOOT-DIP-SAMPLE-001 retry | ✅ AUDIT VERDICT DIP-D `e053448` | Bitfoot's "unflagged" label is NOT reliable ground-truth; 20.5% of unflagged retrievable pings hit 2x+. Survivorship-bias inflation. |
| 2026-04-22 | HOLDER-DATA-PIPELINE-001 | ✅ FIXED `fc87b03` + `4c5508b` | GeckoTerminal API schema change (holders int → dict); silent TypeError caught by broad `except Exception: pass` zeroed feature for ~24h. Plus age-bypass for fresh tokens. |
| 2026-04-21 | GATES-V5 (4 new entry gates + sizing caps + ML-012) | ✅ DEPLOYED `1dec17b` | First post-V4-rollback gate tightening. ML-012 fix (AGGRESSIVE_PAPER bypass requires TEST_MODE=true). Sizing caps (MAX_POSITION_SOL=0.25, SPEED_DEMON_BASE=0.15, MAX=0.25). HOLDER_COUNT_MIN=15 (later reverted via TUNE-005). |
| 2026-04-20 | FEE-MODEL-001 | ✅ DEPLOYED `e078b4c` | Single live trade proved paper fee model under-counts live by ~96× on pre-grad pump.fun. New `_simulate_*` helpers; round-trip cost included in `realised_pnl_sol`. |
| 2026-04-20 | Session 5 v4 (live trial) | ⏪ ROLLED BACK | T+12 on T5 trigger; 1 live trade, -0.094 SOL net. Triggers a chain of post-mortem learnings (T5-DESIGN-001, FIX-1, ML-012, FEE-MODEL-001). |
| 2026-04-19 | LIVE-FLIP-PRE-FLIGHT (CLEAN-003) | ✅ CODIFIED in CLAUDE.md | After 5-phantom-mint reconcile-residual incident; `scripts/live_flip_prep.sh` clears Redis state pre-flip. |

**Future-queued levers (not yet decided):**

| Lever | Status | Notes |
|---|---|---|
| LIVE-FEE-CAPTURE-002 (Path B) | ✅ DEPLOYED 2026-05-01 | Resolved via `services/helius_parser.py` + bot_core live-close wiring. id 6580 backfilled. See Decision Log entry above. |
| LIVE-CLOSE-PATH-B-LATENCY-001 | 📋 PROPOSED 🟢 Tier 2 | Path B helius_parse_signature is sync (await blocks close handler). Worst case 5s × 3 attempts × 2 sigs = 30s; typical <1s/call. If observed close-path latency causes missed exits in production, add async background-update pattern. No mitigation needed absent observed harm. |
| LIVE-PATH-B-SLIPPAGE-DERIVATION-001 | 📋 PROPOSED 🟢 Tier 2 | Path B writes `corrected_pnl_sol` from native delta but `slippage_pct` still uses Path A `_simulate_slippage` estimate. Could derive actual slippage from Path B fills vs quoted prices. Observability only; no PnL accuracy impact. |
| POST-GRAD-LOSS-INVESTIGATION-001 | ✅ COMPLETE 2026-05-01 | Investigation closed; see Decision Log entry above + audit `docs/audits/POST_GRAD_LOSS_INVESTIGATION_2026_05_01.md`. Real bleed is post-grad ENTRY (bc=1.0 AT ENTRY), not carry-through-grad. Highest-ROI patch: POST-GRAD-ENTRY-GATE-001. |
| POST-GRAD-ENTRY-GATE-001 | 📋 RE-SCOPED 🟢 Tier 2 (insurance only) | **Session 4 correction (2026-05-01):** all 280 graduation_* exits in 14d are from `analyst` (NOT speed_demon); SD has 0 post-grad entries. Analyst is disabled since 2026-04-28 (ANALYST-DISABLE-002), so the bleed has ALREADY STOPPED. Current ROI ≈ 0 SOL/week. Insurance value: would prevent recurrence if analyst is ever re-enabled OR if SD signal source ever produces post-grad signals. Re-scope to gate ALL personalities at `signal_aggregator` (or layer atop ANALYST_DISABLED). Defer until insurance need is concrete. |
| BOT-CORE-ML-GATE-001 | ✅ DEPLOYED 2026-05-05 | Resolved via `ea0da2f` + Railway env `ML_THRESHOLD_BOT_CORE_SD=40` set at 14:16:48Z. Helper `_ml_gate_reject_reason` at `services/bot_core.py:130-153` + gate at `services/bot_core.py:673-677`. Threshold 40 (matches existing SA env value, NOT Session 4's 55 — landed conservative; retune is the separate ML-THRESHOLD-DATA-DRIVEN-RETUNE-002 follow-up ≥7d post-deploy). See Decision Log entry above + audit `docs/audits/BOT_CORE_ML_GATE_001_2026_05_05.md`. |
| AGGRESSIVE-PAPER-DISABLE-001 | 📋 PROPOSED 🟢 Tier 2 (evaluation required) | Set `AGGRESSIVE_PAPER_TRADING=false` on `signal_aggregator`. Activates env ML thresholds for paper. Risk: AGGRESSIVE_PAPER exists to admit more samples for ML training data flow; disabling may reduce ML retrain sample volume. Needs evaluation: how much does ML training rely on the lower-score sample? Track separately. |
| NO-MOMENTUM-90S-AUDIT-001 | 📋 PROPOSED 🟡 Tier 1 | `no_momentum_90s` is largest single pre-grad loss source: -8.48 SOL on 423 trades / 14d (-0.020 mean). Avg hold 51s (90s-window with 2s eval). Separate audit needed: signal quality vs exit-timer tuning vs market regime. Investigation FIRST, patch SECOND. ROI uncertain pending audit. |
| OBS-TRAIL-ENGAGEMENT-001 | 📋 PROPOSED 🟢 Tier 2 | Add sticky `trail_was_engaged: BOOLEAN` column (set True on trail activation, never reset). Current `trailing_stop_active` is transient state (resets at exit) and inadequate for backward analysis. Cost S. Enables proper H1/H5 retest. |
| OBS-INTRA-HOLD-DRAWDOWN-001 | 📋 PROPOSED 🟢 Tier 2 | Add `min_price_during_hold` column to track intra-hold low-water. With existing `peak_price` (high-water) this enables direct measurement of "did stop_loss_20% amputate winners". Cost S. |
| OBS-EXIT-TIME-BC-PROGRESS-001 | 📋 PROPOSED 🟢 Tier 2 | features_json captured at entry only; no exit-time bc_progress. If future tokens DO carry through graduation, won't be detectable without this. Cost S. Current data shows zero such carry-through case, so low priority. |
| ML-THRESHOLD-DATA-DRIVEN-RETUNE-001 | 📋 PLANNED 🟡 Tier 1 (gated on POST-GRAD outcome) | Empirically retune `ML_THRESHOLD_SPEED_DEMON` based on band-by-band P&L analysis. Recent 7d shows 70-80 positive (+1.63), 40-50 worst (-1.98), 50-70 mixed/flat. Chat-side draft for Session 4 of 2026-05-01 chained sequence proposes raising threshold to 50 or 65 with 48h auto-rollback gate. Should run AFTER POST-GRAD investigation (if loss profile is dominated by graduation mechanics, ML retune impact is smaller). |
| TIME-PRIME-AEDT-AEST-DRIFT-001 | 📋 PLANNED 🟢 LOW (re-confirmed by TIMEZONE-AUDIT-001 2026-05-05) | `services/bot_core.py:754,776` use `timezone(_td(hours=11))` = UTC+11 (AEDT) but Sydney is UTC+10 (AEST) post-DST 2026-04-05. All TIME_GOOD/TIME_DEAD/TIME_SLEEP and WEEKEND_BOOST windows fire 1h earlier in Sydney clock than their labels suggest. Fix: replace with `zoneinfo.ZoneInfo('Australia/Sydney')`. Single 6-10 line diff in `_compute_position_size_for_signal` covers both 🔴 hits at lines 754 + 776. Originally surfaced by Session 1 (TIME-PRIME-CONTRADICTION-FIX-001); re-verified as the **only 🔴 in production code** by TIMEZONE-AUDIT-001 (2026-05-05). |
| TZ-CONVENTION-DOC-001 | 📋 PROPOSED 🟢 LOW (TIMEZONE-AUDIT-001) | Codify the timezone convention in `CLAUDE.md` Operating Principles. One-line rule: decision logic uses `ZoneInfo("Australia/Sydney")` or pytz; storage uses UTC; no hardcoded offsets except UTC-by-design global-session bands (must be commented). Already partially landed in AGENT_CONTEXT.md + CLAUDE.md edits this session. |
| RISK-MGR-TZ-COMMENT-001 | 📋 PROPOSED 🟢 LOW (TIMEZONE-AUDIT-001) | Add `# UTC by design — global market session bands` comment block to `services/risk_manager.py:160-174` (`_time_of_day_multiplier` Asia/EU/US session windows). Optional: rename labels from "Asia/EU/US" to explicit UTC band labels. Hygiene only — current code is internally consistent. |
| SIGAGG-ML-HOUR-LABEL-001 | 📋 PROPOSED 🟢 LOW (TIMEZONE-AUDIT-001) | Document in code comment + AGENT_CONTEXT that `hour_of_day` ML feature at `services/signal_aggregator.py:1968-1969` is **UTC hour** (not Sydney hour). Internally consistent train/predict; hazard is interpretation in feature-importance analyses. Hygiene only. |
| DASH-AEDT-LABEL-001 | 📋 PROPOSED 🟢 LOW (TIMEZONE-AUDIT-001) | Replace static `<th>Time (AEDT)</th>` headers in `dashboard/dashboard.html:529,544,559` with `<th>Time (Sydney)</th>` or dynamic-label JS that renders the live abbreviation. Misleading in winter (currently AEST) but underlying timestamps render correctly via Australia/Sydney JS. Cosmetic only. |
| **DEFENSIVE-VS-NORMAL-PNL-INVERSION-001** | 📋 PROPOSED 🟡 Tier 1 (MARKET-MODE-001-RE-CALIBRATE finding 2026-05-06) | Post-Session-2 sample (n=167 paper trades) shows **NORMAL bleeds, DEFENSIVE returns**: NORMAL 121 trades / -1.09 SOL / 24.8% WR vs DEFENSIVE 45 trades / +0.25 SOL / 28.9% WR. Investigate hypotheses: (a) SD personality NORMAL-mode sizing multipliers admit lower-quality entries; (b) DEFENSIVE entries already filtered by another path; (c) BOT-CORE-ML-GATE active sample (post-2026-05-05T14:16:48Z) will change the picture — need ≥7d post-gate data. **Required reading before any market-mode threshold change.** Gates MARKET-MODE-001-RE-CALIBRATE-V2. |
| **MARKET-MODE-001-RE-CALIBRATE-V2** | 📋 PROPOSED 🟡 Tier 1 (re-scoped) — **2026-05-20: absorbed by `GOVERNANCE-AGENT-MARKET-MODE-001`** (Tier 2). Original item not deleted — linked to successor. The recalibration concern (NORMAL bleeds, threshold structure may be wrong) becomes one input to the autonomous-classifier scope. | Re-prompt the recalibration session with corrected premise: binding constraint is `dex_vol` (not grad_rate), and the goal is NOT to expand NORMAL share (which bleeds). May involve TIGHTENING NORMAL thresholds, mode-conditional SD sizing, or regime-specific exits. Gated on DEFENSIVE-VS-NORMAL-PNL-INVERSION-001 diagnosis. Should run after ≥7d of post-BOT-CORE-ML-GATE-001 sample (≥2026-05-12). **Now part of `GOVERNANCE-AGENT-MARKET-MODE-001`** — see Tier 2 entry. |
| **PUMPFUN-VOL-PLACEHOLDER-001** | 📋 PROPOSED 🟢 Tier 2 (MARKET-MODE-001-RE-CALIBRATE finding 2026-05-06) | `services/market_health.py:390` has `pumpfun_vol_estimate = dex_vol * 0.15` — a placeholder, not an independent measurement. Algebraic verification: across all tier thresholds, pumpfun never independently binds the AND gate (auto-passes whenever dex_vol does). Wiring real PumpPortal stats API call would make pumpfun an independent metric and unlock Path B (majority-vote) refinement. Prerequisite for any future market-mode work that depends on pumpfun signal. |
| **MM-HYSTERESIS-ONLY-001** | 📋 PROPOSED 🟢 Tier 2 (extracted from MARKET-MODE-001-RE-CALIBRATE finding 2026-05-06) | The 3-cycle sustained-drop hysteresis from the original prompt §4 Path A is universally beneficial regardless of which threshold is right. Could land standalone as a small lever — replaces brittle "single 5-min dip drops mode" with "15-min sustained-drop drops mode". Cost S, single-file change in `services/market_health.py`. Independent of the threshold-recalibration work. |
| **DEFENSIVE-OVERRIDE-PROBE-EVAL-001** | 📋 PROPOSED 🟡 Tier 1 (DEFENSIVE-OVERRIDE-PROBE-001 follow-up 2026-05-07) | 48h evaluation session to decide KEEP / REVERT / EXTEND on the DEFENSIVE override probe. Run **after ≥2026-05-08T22:29Z UTC**. Pulls post-probe paper_trades sample (target ≥80 SD-paper trades under DEFENSIVE override), applies rollback-trigger checks (cumulative < -0.50 SOL on n≥30 / WR < 18% / throughput <5/day for 24h / HIBERNATE >2h), and decides outcome. Possible outcomes: (a) **inversion confirmed at scale** → KEEP override, file DEFENSIVE-AS-DEFAULT-001 to investigate making it permanent; (b) **inversion was sample noise** → DEL override, close DEFENSIVE-VS-NORMAL-PNL-INVERSION-001 as "noise"; (c) **partially confirmed but throughput collapsed** (e.g., +0.5 SOL on only 20 trades) → keep probe running another 48h; (d) **cliff investigation findings change framing** — coordinate with STRATEGY-CLIFF-FOLLOWUP-001. Do NOT auto-trigger — needs 48h wall-clock + paste-ready prompt. Concurrent with DEFENSIVE-OVERRIDE-DAILY-RENEWAL-001 (operational). |
| **DEFENSIVE-OVERRIDE-DAILY-RENEWAL-001** | 📋 OPERATIONAL 🟢 Tier 2 (DEFENSIVE-OVERRIDE-PROBE-001 follow-up 2026-05-07) | Until DEFENSIVE-OVERRIDE-PROBE-001 ends (KEEP, REVERT, or EXTEND), `market:mode:override=DEFENSIVE EX 86400` must be re-set every 24h via Redis MCP (`SET market:mode:override DEFENSIVE EX 86400`). Daily action. Auto-expires safely if forgotten — bot reverts to threshold-based mode determination, which is the safe failure mode (no destructive default). Renewal pattern matches the existing `nansen:disabled=true EX 86400` and (historically-deployed) `market:mode:override=NORMAL EX 86400` daily-renewal workarounds. If KEEP outcome from PROBE-EVAL-001, consider promoting to a proper env-var or auto-renewal mechanism. Filed alongside parent probe to make the operational dependency visible. |
| **STRATEGY-CLIFF-FOLLOWUP-001** | 📋 PROPOSED 🟢 Tier 2 (STRATEGY-CLIFF-INVESTIGATION-001 finding 2026-05-07) | Re-run §3/§5/§5b cliff comparison once POST-cliff sample reaches 14d (≥2026-05-12) to re-validate the no-revert diagnosis. The current POST-cliff sample is small; if the +0.014 SOL/trade mean trends meaningfully more negative over the next 7d, the diagnosis "POST is better than PRE under fair accounting" may need revisiting. Read-only audit. Cost S. |
| **PRE-DEPLOY-PNL-VALIDATION-001** | 📋 PROPOSED 🟢 Tier 2 (STRATEGY-CLIFF-INVESTIGATION-001 finding 2026-05-07) | Process improvement: future paper-PnL "regression" investigations must check whether the boundary date coincides with a fee-model or accounting deploy AND re-derive PnL under a single accounting model before attributing direction to strategy. Add to chat-side prompt template: "Look for accounting-regime deploys at the boundary; if found, the cliff is likely artifact-driven." Add memory note: "Comparing paper PnL across 2026-04-21 requires re-derivation under single fee model; raw realised_pnl_sol is incomparable across the boundary." Process-only; no code/env. Cost S (one-time docs). |
| **BREAKEVEN-DECISION-001** | 📋 PROPOSED 🟢 Tier 2 (STRATEGY-CLIFF-INVESTIGATION-001 finding 2026-05-07) | A/B test breakeven lock vs no-breakeven once paper sample is sufficient (≥14d, ≥1k trades each arm). PRE-cliff archive showed BREAKEVEN_STOP fired 131 times for -8.1 SOL realized (would have been larger losses without the lock OR could have continued rising and trailed up — counterfactual ambiguous). Current `TIERED_TRAIL_SCHEDULE_JSON` removed the `[0.30, 0.0]` breakeven entry; default code still has it. Toggle via env var, run shadow comparison. Cost S–M (env toggle + analysis). |
| **TP-SCHEDULE-EVAL-001** | 📋 PROPOSED 🟢 Tier 2 (STRATEGY-CLIFF-INVESTIGATION-001 finding 2026-05-07) | Re-evaluate full ladder TP schedules. Current env: `[[2.00,0.20],[5.00,0.375],[10.00,1.00]]` (3 tiers, 2x/5x/10x). Original code default: `[[0.50,0.25],[1.00,0.25],[2.00,0.25],[4.00,0.25]]` (4 tiers, 0.5x/1x/2x/4x). PRE-cliff archive showed staged_tp_+50/+100/+250/+400/+500% firing 22 times for +9.7 SOL combined (smaller wins guaranteed). POST-cliff has only +200/+1000% — under-harvests moderate winners that go to TRAILING_STOP instead. Run shadow with original ladder for 14d, compare net PnL/trade. Cost S (env toggle). |
| **SIGNAL-MIX-ANALYSIS-001** | 📋 PROPOSED 🟢 Tier 2 (STRATEGY-CLIFF-INVESTIGATION-001 finding 2026-05-07) | Per-gate ablation for HOLDER_COUNT_MIN, BUY_SELL_RATIO_MIN, PRE_FILTER_SCORE_MIN, CFGI_MIN. Disable each in turn for 24h paper, measure WR / mean PnL / volume delta. PRE-cliff bot was 89% concentrated in $1-5M MC band (+553 SOL old fees); POST is only 41.5% (-4.8 SOL realistic fees). The MC-band redistribution is gate-driven but per-gate contribution is unknown. HOLDER=15 was already rolled back to 1 via TUNE-005-ROLLBACK; remaining 3 gates are uncalibrated. Cost M (4× 24h windows + analysis). |
| **STOP-LOSS-20-RUG-FILTER-DEPLOY-001** | ✅ DEPLOYED 2026-05-11 `0f37e82` (env-active 12:24:12 UTC) | F1 fill-time MC ceiling deployed to `services/paper_trader.py:paper_buy` (env-controlled, default OFF in code; Railway env `BOT_CORE_FILL_MC_CEILING_USD=3000` on bot_core). Verified PASS at T+2 min: clean startup, 0/4 post-deploy SD-paper trades have mc>$3K. See Decision Log entry 2026-05-11 above. **Superseded by NO-MOMENTUM-90S-FILTER-RETUNE-DEPLOY-001 (2026-05-13)** — same env var retuned 3000 → 1000. Eval folded into combined STOP-LOSS-20-RUG-FILTER-EVAL-001 / NO-MOMENTUM-90S-EVAL-001 at ≥2026-05-27. |
| **NO-MOMENTUM-90S-FILTER-RETUNE-DEPLOY-001 (C1)** | ✅ DEPLOYED 2026-05-13 `a3ee421` (env-only retune, 03:29:21Z UTC; container restart 03:38:37Z UTC) | Single-env retune of `BOT_CORE_FILL_MC_CEILING_USD` 3000 → 1000 on bot_core. See Decision Log entry 2026-05-13 above. **Jay-authorized fast-track; T1 audit skipped** per audit §12. STOP gates all PASS pre-deploy on fresh 8.12d sample (C1 marginal ROI +1.49 SOL/day W3+W4, +3.02 W4-only; 0 FP winners; KEPT slice 523 / +32.62 / 91.4% WR). First post-deploy reject log `mc=$10753 > ceiling=$1000` confirms env plumbing. Rollback: env→3000, no code revert. |
| **STOP-LOSS-20-RUG-FILTER-EVAL-001** (+ NO-MOMENTUM-90S-EVAL-001) | 📋 QUEUED 🟢 Tier 2 (≥2026-05-27, +14d post-C1-deploy; original ≥2026-05-25 superseded) | Re-evaluate combined F1 + C1 ceiling at +14d post-C1-deploy. Verify cumulative `bot:filter:fill_mc_ceiling:rejects` counter matches projected rate; re-run `verify_intervention.py` with kept-slice tracking against actual paper_trades; confirm forward ROI tracks the +1.49-3.02 SOL/day projection. Decide: keep at $1k / tighten further (C3 with re-test for FP) / loosen (C2 $1500). Decide: enable in live-mode branch of `services/execution.py` (subject to V5a-go-no-go preconditions). T1 STOP-F regime-reversion test rolled into this combined eval (audit §12 explicitly skipped standalone T1). Read-only audit. Cost S. |
| **PAPER-ENTRY-PRICE-DENOMINATION-001** | 📋 PROPOSED 🟢 Tier 3 (STOP-LOSS-20-RUG-INVESTIGATION-001 follow-up 2026-05-09) | TRAILING_STOP winners cluster at `market_cap_at_entry` $321-832 (max $832 across 208 wins) — well below the BC fresh-mint baseline ~$2,400 and below any plausible USD/token pricing. Likely Jupiter v3 returning sub-baseline `price` (vs `usdPrice`) for fresh pump.fun pools, OR GeckoTerminal returning a quirky pool-derived price for thinly-liquidated post-launch pools. Doesn't affect F1 (the discriminator is empirically clean) but explaining it could surface a separate observability issue with paper-mode entry-price encoding. Observability investigation only; no PnL accuracy impact (since entry_price drives both MC and exit-PnL calculations consistently within a row). Cost S. |
| TIME-PRIME-CALIBRATION-001 | 📋 PLANNED 🟡 MEDIUM | After Session 1 disabled the 2× upsize at AEDT 18-20, the broader question remains: does ANY hour deserve a non-1.0× multiplier? Current data does not justify 2× upsize anywhere. Future deeper-history analysis with more sample. |
| SD_MC_CEILING_002 | ✅ DEPLOYED 2026-04-30 | Resolved via Option 2 (BC-reserves compute in SA gate). See Decision Log entry above. |
| TIME_PRIME-CONTRADICTION-001 | ✅ DEPLOYED 2026-05-01 | Resolved via env-driven multiplier (`TIME_PRIME_HOURS_AEST=""` / `TIME_PRIME_MULTIPLIER=1.0` defaults disable the 2× upsize). See Decision Log entry above. |
| SD_DEAD_ZONE_001 (AEDT 11-17 pause) | ⏸ DEFERRED | Sample size insufficient post-recovery; revisit after Sessions A-D 24-48h obs. |
| SD_ML_THRESHOLD_LIFT (40→50) | ⏸ DEFERRED | (40,50] band signal may shift after this chain's other changes; re-evaluate post-observation. |
| Top-up trading wallet to ~3 SOL | ⏸ JAY ACTION | V5a precondition. Confirmed Branch 1 means transfer can proceed. |
| V5a flip (TEST_MODE=false on bot_core) | 🚧 PRE-CONDITIONS | All preconditions in AGENT_CONTEXT §6. |

---

### Trading-tune env vars currently deployed (verified 2026-04-21 post-GATES-V5)

| Variable | Service | Value |
|---|---|---|
| `ML_THRESHOLD_SPEED_DEMON` | signal_aggregator (gating) | **65** (raised from 40 in GATES-V5) |
| `HOLDER_COUNT_MIN` | signal_aggregator | **1** (TUNE-005 ⏪ ROLLED BACK 2026-04-29 — Speed Demon WR dropped 52.9% → 34.9% over 3 days following the 2026-04-24 raise to 15; hypothesis is that gate=15 rejected legitimate winners with early holder counts <15. 24h validation window opens 2026-04-29 09:34 UTC; outcome decides codify-rollback vs reapply-TUNE-005-and-look-elsewhere) |
| `BUY_SELL_RATIO_MIN` | signal_aggregator | **3.0** (NEW GATES-V5) |
| `PRE_FILTER_SCORE_MIN` | signal_aggregator | **1.15** (NEW GATES-V5) |
| `CFGI_MIN` | signal_aggregator | **20** (NEW GATES-V5, circuit-breaker only — universal, no bypass) |
| `STOP_LOSS_PCT` | bot_core | **0.20** (NEW GATES-V5, was 0.35 hardcoded) |
| `MAX_POSITION_SOL` | bot_core | **0.25** (was 1.50 — GATES-V5 sizing cap) |
| `SPEED_DEMON_BASE_SIZE_SOL` | bot_core | **0.15** (was 0.45 — GATES-V5) |
| `SPEED_DEMON_MAX_SIZE_SOL` | bot_core | **0.25** (was 0.75 — GATES-V5) |
| `MAX_POSITION_SOL_FRACTION` | bot_core | 0.10 (unchanged from FIX-1) |
| `STAGED_TAKE_PROFITS_JSON` | bot_core | `[[2.00, 0.20], [5.00, 0.375], [10.00, 1.00]]` (unchanged) |
| `TIERED_TRAIL_SCHEDULE_JSON` | bot_core | `[[0.10, 0.30], [0.50, 0.25], [1.00, 0.20], [2.00, 0.15], [5.00, 0.12]]` (unchanged from LIVE-001 / Session 4) |
| `MIN_POSITION_SOL` | bot_core | 0.05 (unchanged) |
| `MAX_SD_POSITIONS` | bot_core | 20 (unchanged) |
| `DAILY_LOSS_LIMIT_SOL` | bot_core | 4.0 (unchanged) |
| `TEST_MODE` | all services | true (unchanged) |
| `AGGRESSIVE_PAPER_TRADING` | signal_aggregator | true (now gated on TEST_MODE=true via ML-012 fix) |
| `ANALYST_DISABLED` | signal_aggregator | **true** (set prior to 2026-04-23; env-level flag covers the normal signal path but NOT the graduation-sniper bypass at `services/signal_aggregator.py:_process_graduations`. On 2026-04-23 the leak was closed by Redis governance override `cc8e5c9` — see ANALYST-DISABLE-001 Tier 1 entry for halflife considerations) |
| Stale sig_agg copies (TUNE-004) | signal_aggregator | `SPEED_DEMON_BASE_SIZE_SOL=0.45`, `SPEED_DEMON_MAX_SIZE_SOL=0.75` (stale — sig_agg doesn't enforce sizing; bot_core is the binding service. Tracked as TUNE-004 for hygiene.) |

---

## How to read this roadmap

**Status legend:** ✅ COMPLETED · 🟡 IN_PROGRESS · 📋 QUEUED · ⏸️ DEFERRED · ⛔ SUPERSEDED · ❓ UNKNOWN (needs verification)

**Tier semantics:**
- **Tier 1** — env-var / docs / single-file changes; low risk; ≤30 min sessions
- **Tier 2** — single-session MCP integrations or focused features; 30–180 min; medium risk
- **Tier 3** — multi-session projects; high scope; coordinated across weeks
- **Tier 4** — prerequisites that gate other tiers (handle first)

**ID scheme:** `TUNE-*` trading tuning · `DOCS-*` documentation · `OBS-*` observability · `INFRA-*` infrastructure · `DASH-*` dashboard structure · `DASH-B-*` specific dashboard bugs · `DASH-ENTRY-*` bot_core entry-path writes feeding dashboard · `ML-*` model · `WHALE-*` smart-money · `MCP-*` MCP-build candidates · `LIVE-*` live-trading enablement · `SEC-*` security · `BUG-*` other known bugs · `CLEAN-*` cleanup · `TG-*` telegram · `REFACTOR-*` refactors (multi-session, pure hygiene) · `EXEC-*` execution-path code changes · `FEE-MODEL-*` paper fee-model fidelity · `DEVNET-*` devnet routing / validation infra

---

## Critical path — recommended sequencing (post BITFOOT-DIP-SAMPLE + BITFOOT-2026-BASELINE + ANALYST-DISABLE, 2026-04-23)

**Paper bot state:** Speed-Demon-only (ANALYST-DISABLE-001 landed `cc8e5c9`; Analyst leak stopped). HOLDER-DATA-PIPELINE-001 fix is deployed (24h verification window ends ~2026-04-23 23:00 UTC). BUG-022 (🔥 corrected_pnl_sol NULL on all post-reset rows) makes every paper-PnL number untrusted until fixed — that's the new #1 priority. Bitfoot strategy analysis has both needed verdicts (DIP-D + BASELINE-A directionally); ANALYST-POST-GRAD-001 unblocked for design-session kick-off.

1. ✅ **Session 1** (Sentry triage, `35bdfe6`), **Session 2 v2** (dashboard close-path, `5ac30cd`), **Session 2b** (entry INSERT, `dafe1fa`), **Session 3** (devnet/PumpPortal audit B-conditional, `fdd8aa8`), **Session 4** (LIVE-001 tune bundle), **EXEC-FIX** (EXEC-001 + EXEC-002 paired, `8073257`), **FEE-MODEL-001 FIX-2** (paper_trader corrected model, `e078b4c`), **FIX-1** (T5 rationale + CLEAN-003 + ML-012 investigation, `48c3097`), **GATES-V5** (ML-012 fix + 4 entry gates + stop-loss 0.20 + size caps, `1dec17b`), **MCP-RECON** (capabilities audit, `1e444bb` + `0c91492`), **DASH-RESET** (paper_trades archive + reset, `1176e12`), **Nansen v2 ship** (`c7f73ee`) + **Nansen Session A'** (payload shape fix, `c012475`).
2. ✅ **HOLDER-DATA-PIPELINE-001** (`fc87b03` + `4c5508b`, 2026-04-22) — GeckoTerminal dict-schema fix + age-bypass for GATES-V5 HOLDER/BSR gates on fresh tokens. 24h verification window ends ~2026-04-23 23:00 UTC.
3. ✅ **ANALYST-DISABLE-001** (`cc8e5c9`, 2026-04-23) — governance Redis override closes the graduation-sniper bypass that the `ANALYST_DISABLED=true` env flag did not cover. Stops the 11.9% WR / -2.48 SOL / 17h leak.
4. ✅ **BITFOOT-DIP-SAMPLE-001** (`e053448`, 2026-04-23) — verdict DIP-D: 20.5% of retrievable unflagged pings hit 2x+; Bitfoot labels not reliable ground-truth.
5. ✅ **BITFOOT-2026-BASELINE** (`2b4cb73`, 2026-04-23) — verdict BASELINE-A directionally: 2-filter still concentrates winners in 2026 trending post-grad data (88.9% 2x+ on n=18, survivorship-inflated); Raydium post-grad vanished; Meteora up from 10% → 44% of winners.
6. 🟡 **BUG-022 INVESTIGATED** `ca4812d` (2026-04-28) — verdict 2a: writer never existed in committed code; only ever populated by a one-shot 2026-04-13 backfill script that was never committed and never re-ran. All 853 current closed rows NULL. **Important downgrade vs prior #1 framing:** because all current rows are post-`e078b4c` (FEE-MODEL-001), `realised_pnl_sol` is already authoritative — `corrected_pnl_sol` would be a `pass_through` identity copy. The "every paper-PnL number is biased" concern was overstated; directional findings are already correct. Fix is still recommended for symbolic correctness + ML training query safety: Option A (5-min one-time UPDATE + ~10-line inline close-time write). Audit doc: `docs/audits/CORRECTED_PNL_INVESTIGATION_2026_04_28.md`. **Fix execution: queued, ~30m next session.**
7. 📋 **ANALYST-PAPER-AUDIT-001** — 45m read-only diagnostic on the legacy `analyst` module that leaked -2.48 SOL in 17h. Determine retire-vs-retune; identify what re-enabled it on 2026-04-22. Prevents silent re-enablement.
8. 📋 **DOCS-004** — CLAUDE.md Vybe URL fix (`.com` → `.xyz`, 5m). Bundle with any other docs session or standalone.
9. 📋 **STATUS-CONVENTION-001** — ensure convention ✅ LANDED (prompt pasted + STATUS.md + seeded entries + template). Verify next session.
10. ⏪ **TUNE-005** ROLLED BACK 2026-04-29 — `HOLDER_COUNT_MIN` reverted 15 → 1 on signal_aggregator (was ✅ COMPLETED 2026-04-24, raising 1 → 15). Trigger: Speed Demon WR dropped 52.9% → 34.9% over 3 days post-TUNE-005 deploy. 24h validation window opens. Verified live: container swap 2026-04-29 09:27:58 UTC; new banner; zero HOLDER reject; cadence alive; no errors. Outcome: if WR recovers to ~50% baseline → codify rollback; if WR stays depressed → reapply TUNE-005 and look elsewhere for the regression cause.
11. 📋 **V5a — 1h intensive-supervised live window.** Replaces prior single-window LIVE-002. Gates on: paper cadence stable post-Analyst-disable (≥24h Speed-Demon-only); BUG-022 resolved (paper-PnL trustworthy); new T5a/T5b trigger definitions (T5-DESIGN-001 delivered via v5a prompt); ML-012 elif branch exercised in live mode (startup log "ML-012 FIX: ... ignoring override" present); first-live-trade fee_delta within ±0.005 SOL at 0.15-0.25 SOL sizing; stop_loss_20% exit string writes correctly; reconcile prep script cleans Redis paper state before flip. Wallet target 5 SOL.
12. 📋 **ANALYST-POST-GRAD-001** (multi-session, parallelizable to V5a) — revised filter spec per Phase-1 design summary at top of Tier 2 entry (DEX ∈ {PumpSwap, Meteora DAMM v2} — Raydium dropped; top-10 ≥30 + MC $50-300k; dex_paid + pre-grad vol/MC dropped; ROI calibrated from paper window).
13. 📋 **NANSEN-CLIENT-V2-MIGRATE-001** → **NANSEN-FEATURE-NORMALIZER-001** — Session B' split into Path A (caller migration) + Path B (ML normalizer); unblocks Nansen-powered features.
14. 📋 **V5b → V5c → V5d escalation**. Each stage gates on the prior stage completing cleanly. V5d (unsupervised) requires SEC-001 complete.
15. 📋 **SEC-001** (Redis + Postgres + Nansen rotation). Required before v5d. Waiver framework allows v5a-c under supervision per prior rollback doc §SEC-001.
16. 📋 **WALLET-REV-001**, **WHALE-001-v2**, **BITFOOT-ML-FEATURE-INTEGRATION-001** — edge-exploration work, parallelizable with v5 staged path (do not depend on v5 outcomes).
17. 📋 **BITFOOT-MONITOR-001** — 2-week prospective monitoring of fresh graduations (not retrospective trending_pools). Runs in parallel with anything. Its un-biased forward data is the only path to a real ROI calibration for ANALYST-POST-GRAD-001 (both DIP-D and BASELINE-A are biased; neither is forward-usable).

Rationale: paper pipeline is healthy (HOLDER fix lands + Analyst leak stopped), but paper-PnL trust is broken (BUG-022) — fixing it is cheap and gates every downstream observability-based decision. Bitfoot research has both verdicts landed → ANALYST-POST-GRAD-001 can begin design in parallel with other v5 prep. The hygiene / docs items (TUNE-005, DOCS-004, STATUS-CONVENTION-001, ANALYST-PAPER-AUDIT-001) are small and bundleable. V5a is the next live-enable gate, blocked on BUG-022 + stable ≥24h Speed-Demon-only paper cadence.

---

## Tier 4 — Prerequisites (gate-keepers)

| ID | Title | Status | Source | Notes |
|---|---|---|---|---|
| GATE-001 | Durable rules refresh (`TEST_MODE` flip session-gated) | ✅ COMPLETED `e9de6d7` | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` Tier 4 | CLAUDE.md "Live trading mode — session-gated" replaces "Paper mode is non-negotiable". |
| GATE-002 | Live-trade forensics (resolve 2.07 SOL drain open thread) | ✅ COMPLETED `1b40df3` | `ZMN_LIVE_TRADE_FORENSICS_2026_04_19.md` | Verdict A — phantom drain. Real v4 cost ~3.4 SOL (not 1.32). All execution paths properly TEST_MODE-guarded. |
| GATE-003 | Sentry SDK integration (8 services) | ✅ COMPLETED `cb45d6b` | `session_outputs/ZMN_SENTRY_INTEGRATION_DONE.md` | 8 projects in rz-consulting; `zmn-signal-aggregator` capturing live events. |
| GATE-004 | Live-path gate + exit + sizing infrastructure ready for supervised window | ✅ COMPLETED `1dec17b` | `session_outputs/ZMN_GATES_V5_DONE.md` | GATES-V5 completes the pre-live minimum: ML-012 fix (AGGRESSIVE_PAPER gated on TEST_MODE), 4 new entry gates (HOLDER/BSR/PRE_FILTER/CFGI), stop-loss tightened 0.35→0.20 env-configurable, size caps MAX_POSITION_SOL=0.25 / SPEED_DEMON_BASE=0.15 / MAX=0.25. |

**No outstanding Tier 4 items.** Tiers 1-3 are unblocked.

---

## Tier 1 — Immediate wins (env vars, docs, single-file)

> **FULL-CODE-AUDIT-001 (2026-06-03) filed the fix-sessions below. The audit's §B is the canonical
> ordered pre-flip remediation sequence; each row maps to finding IDs in
> `docs/audits/FULL_CODE_AUDIT_001_2026_06_02.md` §A. All Phase-0..3 rows MUST be GREEN before any
> `TEST_MODE=false`. Each is a separate verified fix-session (TDD/verify/deploy, one lever each).**

| ID | Title | Status | Est | Impact | Depends on | Source |
|---|---|---|---:|---|---|---|
| FIX-PUBSUB-ISOLATION | **§B Phase-0 #1 (🔴, the bot is DOWN).** ✅ **CODE DEPLOYED 2026-06-03 commit `98c8007`.** Implemented as a single shared `supervise(coro_factory, name)` helper (NEW `services/async_utils.py`) wrapping every member of all 7 service top-level `asyncio.gather`s + the dashboard bg-tasks, and routing the `main.py` single-service entrypoint through the existing `run_service()` supervisor. A crashing task restarts with capped exp backoff while siblings keep running; restart re-subscribes (self-healing); CancelledError propagates (clean shutdown); clean return does not hot-loop. Chose supervise-at-the-gather over rewriting 7 listener bodies — minimal, uniform, reversible, and it satisfies the audit's "self-healing + supervised" requirement. Resolves PIPELINE-PUBSUB-ISOLATION-001 + BOT-CORE-EMERGENCY-LISTENER-PUBSUB-ISOLATION-001 in code. py_compile 9/9 PASS; verify 25/25 PASS. **Deploy observation pending** (expect bot_core + signal_listener RUNNING not CRASHED). | ✅ DEPLOYED + RUNTIME-CONFIRMED (`98c8007` + leak-hotfix `9fa45b0`) | 2-4h | restores the bot | none | AUDIT §A D01-F1/F2/F3/F4/F5/F6, D12-F5 |
| ↳ (runtime note) | **2026-06-03 prod-confirmed:** all 6 services ● Online, crash-loop RESOLVED, pipeline flowing, paper trades entering; `supervise` caught the exact redis TimeoutError in prod. **Round-2 hotfix `9fa45b0`:** supervise-restart exposed a connection leak (3 listeners lacked `finally: aclose()` → `MaxConnectionsError`); added cleanup-on-exit + concise supervise logging → MaxConnectionsError gone. | ✅ | — | — | — | — |
| FIX-MARKET-MODE-MISCLASSIFICATION | **§B Phase-0 #2 (🔴).** ✅ **CODE DEPLOYED 2026-06-03 commit `5a3e5aa`** (`services/market_health.py`). `_determine_market_mode` no longer conflates missing-data with dead-market: absent `market:migration_count_1h` → `None` sentinel that ABSTAINS (not veto; distinguished from genuine 0 via `is not None`); `_fetch_defillama` returns `None` (not `0.0`) + last-good ≤1h fallback; the fabricated `pumpfun_vol=0.15*dex_vol` leg DROPPED from the binding decision (kept as labelled estimate); total-data-loss → DEFENSIVE not HIBERNATE; `data_degraded` flag. py_compile PASS; `.tmp_market_mode/verify_market_mode.py` 10/10 PASS (incl. $1.75B dex + absent counter → NORMAL). Supersedes MARKET-MODE-001-RE-CALIBRATE-002. | ✅ CODE DEPLOYED `5a3e5aa` (runtime-observe pending) | 2-4h | stops false-HIBERNATE | FIX-PUBSUB-ISOLATION ✅ | AUDIT §A D07-F1/F3/F4/F5/F7, D06-F6 |
| REDIS-CLIENT-HARDENING-001 | **Phase-0 reliability (🟡, prod-observed 2026-06-03; do before live).** Persistent `Timeout reading from redis.railway.internal:6379` (~every 6s) = environmental Railway-Redis slowness. The pubsub-isolation fix TOLERATES it (services stay Online, self-heal) but the safety pubsub listeners (emergency/exit_check) back off to 60s restarts under it. Harden every `aioredis.from_url(...)` (all 8 services) with `socket_keepalive=True`, `health_check_interval=30`, `retry_on_timeout=True`, lenient `socket_timeout` — retry transient latency in-client instead of crashing the listener. | ✅ CODE DEPLOYED `b72fac2` (runtime-observe pending) — added the 3 kwargs to all 15 `from_url` sites across 11 files; verified accepted on redis-py 7.4.0; py_compile 11/11 PASS | 1-2h | safety listeners survive Redis latency | none | STATUS 2026-06-03; prod logs |
| MARKET-MODE-THRESHOLD-RECALIBRATE-003 | **Phase-0 reliability (🟡, prod-observed 2026-06-03; needs steady-state data).** Prod: `dex_volume_24h=$1.65B` (healthy) + `market:migration_count_1h=2` → HIBERNATE (FIX-MARKET-MODE-MISCLASSIFICATION correctly enforced a PRESENT-but-low counter; `data_degraded=false`). The 2/hr is a post-restart warm-up + Redis-timeout-dropped-increment artifact. Once REDIS-CLIENT-HARDENING-001 is live and the counter accumulates a full clean hour: (a) verify the bot's actual migration capture rate vs MARKET_MODES thresholds (DEFENSIVE≥10/NORMAL≥30 — bot may capture only ~3% of real graduations per the regime diagnostic → thresholds may be miscalibrated to the feed); (b) consider a post-restart warm-up bypass (abstain grad_rate for ~1h) or back the count with a Postgres rolling query so restarts don't reset it (D06-F6). | 📋 QUEUED 🟡 (NEW) | 1-2h | mode reads true at steady state | REDIS-CLIENT-HARDENING-001 + ≥1h clean data | STATUS 2026-06-03; market:health |
| DEPLOY-OBSERVABILITY | **§B Phase-0 #3 (🔴, why ~05-28 was silent).** ✅ **CODE DEPLOYED 2026-06-03 commit `51ed450`.** Implemented WITHOUT a new billable Railway worker — folded liveness checks into the existing `web` service `_service_health_checker` (runs every 60s): (a) `dashboard_api.py` adds internal-service rows to `service:health` (bot_core via heartbeat+bot:status, signal_aggregator via health key, signal_listener via signals:raw proxy, market_health via market:health proxy; TTL'd keys → absence==down) + a rate-limited (30min) Discord alert when a critical internal service has no liveness key; (b) `dashboard-analytics.html` new "ZMN Services" section renders the rows. A crashed internal service is now visible + alerted instead of mislabelled "PumpPortal DOWN". py_compile PASS; liveness keys verified present+fresh; HTML/JS aligned (5 grids↔5 sections). Dashboard visual not render-tested (Playwright gated on OBS-004; append-only markup, low risk). **Stronger-option follow-up:** `continuous_audit.py` as its OWN Railway service would survive a `web` outage too. | ✅ DEPLOYED + RUNTIME-CONFIRMED `51ed450` — `service:health` now carries internal rows all `ok` (bot_core heartbeat 8s, signal_aggregator 28s, signal_listener via signals:raw 0s, market_health 41s); no false-down → no spurious alerts; alerting armed | 1-2h | outage visible + alerted | none | AUDIT §A D12-F2/F3/F4/F6 |
| DASH-CORRECTED-PNL-COLUMN-001 | **Phase-3 dashboard fidelity (🟠, prod-observed 2026-06-03).** The `web` dashboard logs a repeating DB error every ~60s: `column "corrected_pnl_sol" does not exist`. A dashboard PnL/analytics query references `corrected_pnl_sol` against a table that lacks it — almost certainly the `trades` table in a mode path (migration `001_add_corrected_pnl_columns.sql` added the corrected columns to `paper_trades` ONLY, not `trades`). Effect: a PnL/analytics panel silently errors → no/wrong data; matters for the operator's go-live 4-6h watch (broken PnL panel). PRE-EXISTING (not introduced by Phase-0 work). **Fix:** either add the corrected_* columns to `trades` (migration), or guard the query to `COALESCE`-from-realised when corrected is absent / restrict the corrected query to `paper_trades`. Bundle with FIX-DASHBOARD-MODE-FIDELITY (§B Phase-3 #15, D05-F3/F4/F6). | 📋 QUEUED 🟠 (NEW, prod-observed) | 1h | dashboard PnL panels stop erroring | none | STATUS 2026-06-03; web logs |
| FIX-LIVE-SELL-RESULT-CHECK | **§B Phase-1 #4 (🔴, most dangerous live defect).** ✅ **CODE DEPLOYED 2026-06-03 commit `2a85508` — MERGED with #8.** New `if not result.success:` check on the live sell path (execute_trade returns success=False, never raises → old except was dead) + shared `_handle_failed_live_sell()` helper: increments sell-storm counter, parks past threshold, records, and returns WITHOUT booking/decrementing/popping → position stays OPEN for retry (no more stranded-SOL false closes). park-and-continue (never raises) so emergency_stop's loop survives. Also protects partial/staged-TP sells. py_compile PASS; verify 10/10. **NOT paper-observable (live `else:` branch); runtime-confirmed at flip.** | ✅ CODE DEPLOYED `2a85508` (flip-confirmed) | 2-3h | stops stranded-SOL + false closes | FIX-PUBSUB-ISOLATION ✅ | AUDIT §A D02-F1, D02-F6 |
| FIX-PARTIAL-SELL-SIZING | **§B Phase-1 #5 (🔴).** ✅ **CODE DEPLOYED 2026-06-03 commit `3b6d0b1`** (`execution.py` + `bot_core.py`). Threaded `sell_fraction: float = 1.0` through `execute_trade` → both sell routes; bot_core `_close_position` passes `sell_fraction=sell_pct`. **D02-F5:** pre-grad `_execute_pumpportal_local` SELL now sends `"X%"` (e.g. "25%") of the token balance, not hardcoded "100%". **D02-F8 (done here):** Jupiter post-grad sell now `int(balance * sell_fraction)`, not the full bag. Full closes (sell_pct=1.0) unchanged at 100%/full. py_compile PASS; arithmetic 10/10 + 4/4 structural. NOT paper-observable; flip-confirmed (validate vs a multi-staged-TP live close). | ✅ CODE DEPLOYED `3b6d0b1` (flip-confirmed) | 2-3h | staged TPs work in live | pairs w/ #4 ✅ | AUDIT §A D02-F5 + D02-F8 |
| FIX-BUY-IDEMPOTENCY | **§B Phase-1 #6 (🔴).** ✅ **CODE DEPLOYED 2026-06-03 commit `2f00b19`** (`execution.py`). New `_get_signature_status()` (getSignatureStatuses, 3-tier RPC → landed/failed/unknown). On a confirm-miss, execute_trade decides from on-chain status instead of blindly re-broadcasting (the D02-F3 double-spend): landed→success(no resubmit), failed→resubmit, BUY-unknown→record-pending-with-sig (never double-buy), SELL-unknown→failure(caller #4 parks+retries). Forced `use_jito=False` (D02-F2/F7 — bundle path returns UUID-not-sig + no tip → use real-sig `_send_transaction`; `JITO-REIMPLEMENT-001` follow-up). D02-F14: `_confirm_trade_helius` unset-URL → confirmed=False (verify via getSignatureStatuses) not blind-True. py_compile PASS; verify_idempotency.py 6/6 + 9/9 structural. NOT paper-observable; flip-confirmed. | ✅ CODE DEPLOYED `2f00b19` (flip-confirmed) | 2-3h | stops double-spend | none | AUDIT §A D02-F3/F2/F7/F14 |
| JITO-REIMPLEMENT-001 | **Follow-up (🟡, from #6).** Jito disabled for live (broken: returns bundle-UUID-as-sig D02-F2; no tip instruction D02-F7). A proper impl: extract the real tx signature from the signed VersionedTransaction before bundling (return it for confirmation/Path B), AND prepend a tip-transfer instruction to a Jito tip account before signing. Until then live uses `_send_transaction` (local RPC, no MEV protection). | 📋 QUEUED 🟡 (NEW) | 2-3h | MEV protection on live submits | #6 ✅ | AUDIT §A D02-F2/F7 |
| FIX-EMERGENCY-STOP-ROBUSTNESS | **§B Phase-1 #8 (🔴).** ✅ **CODE DEPLOYED 2026-06-03 commit `2a85508` — MERGED with #4.** emergency_stop now wraps each `_close_position` in try/except (one un-sellable mint can't abort the stop), detects "left open" via `key in self.positions` (since #4 leaves failed sells open without raising), sets the durable Redis `bot:emergency_stop` kill key (D04-F8 — survives restart, cross-service visible), and ALWAYS runs the Discord alert + `bot:status` publish (reports `positions_failed`). (D04-F10 sell-storm-park-persistence NOT done here — in-memory park is now less acute since Phase-0 stopped the crash-loop; deferred.) py_compile PASS; verify 10/10. NOT paper-observable; flip-confirmed. | ✅ CODE DEPLOYED `2a85508` (flip-confirmed) | 1-2h | emergency stop actually works | merged w/ #4 ✅ | AUDIT §A D03-F1, D04-F8/F10 |
| FIX-EXEC-001-002-ROUTING | **§B Phase-1 #7 (🟠) — FINAL Phase-1 item.** ✅ **CODE DEPLOYED 2026-06-03 commit `3e4b502`** (`bot_core.py`). Removed the `if pos.bonding_curve_progress > 0:` gate so `_check_pool_state_fresh` runs for EVERY live sell — fixes **D02-F4** (whale/Raydium bc=0 tokens were skipping refresh → 400 on pump.fun path) AND **D03-F3** (reconciler-restored positions lose bc_progress → stale pre-grad routing). The helper returns 1.0 (BC closed → non-local/Jupiter) / 0.0 (BC live → pre-grad), so refresh always routes correctly regardless of stored/lost value; avoids a schema migration. Fail-closed-to-stale on RPC error. EXEC-002 already resolved. D03-F8 force-abandon → deferred (EXEC-FORCE-ABANDON-001). py_compile PASS; 4/4 structural. NOT paper-observable; flip-confirmed. | ✅ CODE DEPLOYED `3e4b502` (flip-confirmed) | 1-2h | stops stale-route 400s | none | AUDIT §A D02-F4, D03-F3 |
| SELL-STORM-PARK-PERSISTENCE-001 | **Reliability follow-up (🟡, from #4/#8; D04-F10).** `_sell_failure_counts` + `_parked_mints` are in-memory → lost on restart. Less acute now Phase-0 stopped the crash-loop (process stays up), but a deploy/OOM still resets the breaker. Persist park state to Redis (TTL = park duration) so it survives restarts. | 📋 QUEUED 🟡 (NEW, deferred) | 1h | sell-storm breaker survives restart | none | AUDIT §A D04-F10 |
| EXEC-FORCE-ABANDON-001 | **Reliability follow-up (🟡, from #7; D03-F8).** A permanently-unsellable position (rug / zero liquidity) parks→retries forever (bounded by 5min park) and never reaches a terminal state, counting against MAX_SD_POSITIONS. After N park cycles, force-record it as a loss (synthetic close) + remove from `self.positions`. Entangles with Phase-3 accounting (synthetic-price handling, cf. D03-F5) — sequence with Phase 3. | 📋 QUEUED 🟡 (NEW, deferred) | 1-2h | stuck positions reach terminal state | Phase-3 accounting | AUDIT §A D03-F8 |
| FIX-HIBERNATE-LIVE-VETO | **§B Phase-2 #9 (🔴).** ✅ **CODE DEPLOYED 2026-06-03 commit `71272cd`** (`signal_aggregator.py` + `bot_core.py`). (A) SA HIBERNATE→DEFENSIVE bypass now gated on `AGGRESSIVE_PAPER and TEST_MODE` → live always hard-skips HIBERNATE. (B) bot_core independent live veto: `not TEST_MODE` → read `market:mode:current` fresh, `return` on HIBERNATE (doesn't trust SA-downgraded label or fail-open governance). **Paper impact NONE** (paper bypass preserved; veto is live-only). Runbook still sets `AGGRESSIVE_PAPER_TRADING=false` at flip (defense-in-depth). py_compile + 5/5 structural. NOT paper-observable (live veto); flip-confirmed. | ✅ CODE DEPLOYED `71272cd` (flip-confirmed) | 1-2h | no live trades in HIBERNATE | FIX-MARKET-MODE-MISCLASSIFICATION ✅ | AUDIT §A D04-F3/D07-F2/D08-F1/D11-F7 |
| FIX-GOVERNANCE-FAIL-OPEN | **§B Phase-2 #10 (🔴, BUG-010).** ✅ **CODE DEPLOYED 2026-06-03 commit `b273b54`** (`governance.py`). Fixed the `market:cfgi` read (D09-F2) — that key is never written, so governance's CFGI was permanently neutral-50; now reads `market:health` (prefers `cfgi_sol`). **Already in place (verified):** bot_core applies `gov.size_multiplier` (CONSERVATIVE→0.8 haircut works); the live regime VETO is now provided by **#9** (independent market:mode HIBERNATE check), covering the fail-open gap more reliably than the dead governance gate. **Deferred+flagged:** "stale-governance→PAUSE/cap" (`GOVERNANCE-STALENESS-POLICY-001`) — halt-risk while governance is dead. ML-unavailable-default-below-floor / CFGI-sentinel (D08-F5/D09-F4) → folded into a later sizing pass (not regime-critical given #9). py_compile + 4/4 structural. NOT paper-observable (cfgi only used when LLM revived); flip/revival-confirmed. | ✅ CODE DEPLOYED `b273b54` (regime veto via #9; cfgi fixed) | 2-3h | live regime veto exists (via #9) | #9 ✅ | AUDIT §A D04-F1/D09-F2 |
| GOVERNANCE-STALENESS-POLICY-001 | **Follow-up (🟡, from #10; needs decision).** Decide how bot_core treats a stale/dead governance decision in LIVE: a hard PAUSE would HALT (governance's LLM is currently DEAD on Anthropic credits → it'd block everything); a soft size-cap is strategy-adjacent + applies a permanent live haircut while dead. Needs a deliberate policy (ideally BUG-010 credit-restore first). NOT auto-resolved. **Current state (flag): dead governance writes CONSERVATIVE/0.8 → all paper trades currently sized at 0.8× base.** | 📋 QUEUED 🟡 (NEW, needs Jay decision) | 1h | defined dead-governance posture | BUG-010 credits | AUDIT §A D04-F1; STATUS 2026-06-03 |
| FIX-DAILY-LOSS-PERSISTENCE | **§B Phase-2 #11 (🔴→🟠).** ✅ **CODE DEPLOYED 2026-06-03 commit `e739e07`** (`bot_core._load_state`, bundled w/ #12). LIVE-only: reload TODAY's (UTC) realized PnL from `trades WHERE trade_mode='live' AND closed_at>=midnight` (uses `pnl_sol`; closed_at epoch float; safe fallback 0.0) instead of the unconditional hard-zero that let any restart launder the daily-loss accumulator. **Paper keeps the hard-zero** (no false-stop risk). py_compile + structural. NOT paper-observable (live-only); flip-confirmed. | ✅ CODE DEPLOYED `e739e07` (flip-confirmed) | 1-2h | daily-loss cap survives restart | FIX-PUBSUB-ISOLATION ✅ | AUDIT §A D08-F2/D04-F4/D06-F1 |
| FIX-LIVE-BALANCE-SEED | **§B Phase-2 #12 (🟠).** ✅ **CODE DEPLOYED 2026-06-03 commit `e739e07`** (`bot_core._load_state` + new `_fetch_onchain_balance_sol`, bundled w/ #11). LIVE-only: seed `total_balance_sol` from on-chain `getBalance` at startup (was the unfiltered latest snapshot → ~50 SOL paper figure → ~10×-inflated exposure/drawdown denominators vs the real ~5 SOL wallet). Falls back to snapshot w/ warning if RPC down. py_compile + structural. NOT paper-observable (live-only); flip-confirmed. | ✅ CODE DEPLOYED `e739e07` (flip-confirmed) | 1-2h | safety-rail denominators correct | none | AUDIT §A D04-F2 |
| FIX-SIZING-CAPS-WIRING | **§B Phase-2 #13 (🟠).** Real `MAX_SD_POSITIONS` env (phantom today; cap hardcoded 3 → V5A 5/7 ladder unenforceable); fill-MC-ceiling fail-CLOSED on price=0; `ZoneInfo("Australia/Sydney")` for ALL sizing branches + de-duplicate the double time-of-day multiplier; align MIN_POSITION_SOL; rug-cascade predicate. | 📋 QUEUED 🟠 | 2-3h | ladder operable; correct clock | none | AUDIT §A D04-F7/D08-F7/D08-F4/D11-F1/F2/D08-F6/D04-F6 |
| FIX-LIVE-STAGED-TP-PNL | **§B Phase-3 #14 (🟠, corrupts go/no-go data).** Port cumulative-PnL accumulation to the live close; collect all exit signatures and sum Path B native deltas; reconcile in-memory balance to on-chain. Without it the trial's own PnL lies on every multi-TP trade. | 📋 QUEUED 🟠 | 2-4h | trustworthy live PnL | none | AUDIT §A D05-F1/F2, D02-F12 |
| FIX-DASHBOARD-MODE-FIDELITY | **§B Phase-3 #15 (🟠).** Mode-filter + COALESCE(corrected,realised) on analytics/snapshot queries; fix the live entry-price 1e-6 sentinel; standardize column semantics. | 📋 QUEUED 🟠 | 2-3h | dashboard tells the truth in live | none | AUDIT §A D05-F3/F4/F6/F8/F10, D02-F13 |
| AUDIT-001-SECURITY-HYGIENE | **§B Phase-4 (🟡/🟢, mostly waived for supervised v5a-c).** Redact Redis-URL-with-password logging → SEC-001 rotation → harden Sentry → dashboard auth fail-closed (verify deployed `DASHBOARD_SECRET`). Plus the hygiene sweep (dead `market:loss_override`, Redis no-TTL leaks, Vybe URL drift, stale docs incl. AcceleratedMLEngine, fill-latency instrumentation, tests). | 📋 QUEUED 🟡 | 3-5h | security + hygiene | after FIX-SECRET-LOGGING for SEC-001 | AUDIT §A D10-*, D11-F3/F4/F9, D06-F2/F3/F4, D09-F5/F6, D12-F1/F7 |
| V5A-FLIP-RECONCILE-FILTER-001 | **Patch `bot_core._reconcile_positions` to filter by `trade_mode='live'` when `TEST_MODE=false`.** Filed by V5A-FLIP-001-V2 rollback (2026-05-20). ✅ **DEPLOYED 2026-05-21 commit `f3591eb` by V5A-FIXES-001.** Root cause was different from filed description — the fix at commit `4b647a7` (2026-04-16) added the partial filter for `paper_trades` table only; the live-mode reconciler reads the `trades` ML corpus (per LIVE-TRADES-LOGGING-AUDIT-001) where no filter was applied. Open paper rows accumulate in `trades` when `pos.trades_ml_id` is falsy (paper close skips the trades UPDATE). The deploy makes `mode_clause` unconditional in both `_reconcile_positions` and `_load_state` (both tables carry `trade_mode` post-LIVE-TRADES-LOGGING-AUDIT-001). +2 `[RECONCILE]` log markers added for post-restart verification. Verified live 2026-05-21 14:42:26Z: paper-mode reconciler loaded 0 positions (correct), no errors. Audit: `docs/audits/V5A_FIXES_001_2026_05_21.md` §4. | ✅ DEPLOYED `f3591eb` | 1-2h | unblocks V5A retry | none | `docs/audits/V5A_FLIP_001_2026_05_20.md` §7.1; `docs/audits/V5A_FIXES_001_2026_05_21.md` §4; `docs/audits/ORPHAN_PAPER_CLOSURE_INVESTIGATION_001_2026_05_21.md` |
| V5A-FLIP-CONTAMINATION-CLEANUP-001 | **Tag `paper_trades` ids 9940-9953 — the 14 phantom 'live' rows committed by V5A-FLIP-001-V2's failed flip.** ✅ **DONE 2026-05-21 by V5A-FIXES-001 Phase 2.** All 14 rows now have `correction_method='paper_orphan_at_flip_v5a_001'` and `correction_applied_at='2026-05-21T14:31:14+00:00'`. WHERE clause matched all 14 (eligibility filter: `trade_mode='live' AND entry_signature IS NULL AND exit_signature IS NULL AND correction_method IS NULL`). id 6580 (real on-chain live trade, `correction_method='live_actual_v1'`) verified unchanged. Live-mode analytics can now filter phantoms via `WHERE correction_method NOT LIKE 'paper_orphan%' OR correction_method IS NULL`. Audit: `docs/audits/V5A_FIXES_001_2026_05_21.md` §3. | ✅ DONE (DB UPDATE only — no commit) | 15m | clean live-mode analytics | DATABASE_PUBLIC_URL | `docs/audits/V5A_FLIP_001_2026_05_20.md` §7.2; `docs/audits/V5A_FIXES_001_2026_05_21.md` §3 |
| V5A-FLIP-CLOSE-TRADE-MODE-001 | **Defensive INSERT in live close path now writes trade_mode derived from `trades.trade_mode` lookup (was hardcoded 'live').** ✅ **DEPLOYED 2026-05-21 commit `3c50520` by V5A-FIXES-001 (Bug 2).** Plan's Option A (entry_signature discriminator) infeasible — Position dataclass has no entry_signature field. Adopted Option C (DB lookup): query `trades.trade_mode` for `pos.trade_id` at defensive INSERT time. Defense-in-depth against V5A-FLIP-RECONCILE-FILTER-001 regression. Emits `[ORIGIN_MISMATCH]` WARNING log if paper-origin position reaches the live close path. ~30 LOC in single file. Verify: all 14 V5A-FLIP-001-V2 incident trade_ids would now write 'paper' instead of 'live' (verified against live DB). Audit: `docs/audits/V5A_FIXES_001_2026_05_21.md` §5. | ✅ DEPLOYED `3c50520` | 1h | defense-in-depth post-Bug-1 | V5A-FLIP-RECONCILE-FILTER-001 ✅ | `docs/audits/V5A_FIXES_001_2026_05_21.md` §5 |
| V5A-SIZING-GRADUATION-LADDER-001 | **Staged position-sizing graduation for the V5A trial.** Not a session to run — a rule that governs manual sizing decisions during the live trial. Filed in the roadmap as a reference so the rule is discoverable from the roadmap, not buried in a findings doc. **Stages:** Hours 0-24 `MAX_POSITION_SOL=0.10` / `MAX_SD_POSITIONS=5`; Day 2-4 hold (gather Path B data); Day 5-7 advance to 0.15/5 only if ≥100 closed trades + cumulative PnL ≥ break-even + no rollback triggers fired (manual); Week 2 advance to 0.20/7 only if Week 1 net profitable AND Path B sample ≥ 10 rows (gates `PAPER-FEE-MODEL-CALIBRATION-001`). Replaces the originally proposed WR-trigger auto-scale rule (overruled — 80% WR in 24-80 trades is not statistically robust evidence; auto-scale on noise doubles risk surface). Scale on cumulative PnL trajectory + closed-trade count + Path B sample, not short-window WR. Full ladder + rationale: `docs/findings/V5A_GO_LIVE_DECISIONS.md` §2. | 📋 ACTIVE RULE 🟡 (governs V5A trial sizing — consult before any manual sizing change) | n/a (rule, not a session) | risk-bounded ramp; prevents over-leverage on noisy WR | V5A relaunch (PC1-PC4) | `docs/findings/V5A_GO_LIVE_DECISIONS.md` §2 + filed by V5A-GO-LIVE-DECISIONS-RECORD-001 |
| TUNE-001 | Lower trail activation tier from `[[0.30, 0.35], …]` to `[[0.10, 0.30], …]` | 📋 QUEUED | 15m + 24h obs | **+20-30 SOL/wk paper** | none | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` Tier 1 §1.1; `ZMN_RE_DIAGNOSIS_2026_04_19.md` pain 1 |
| TUNE-002 | `SD_EARLY_CHECK_SECONDS=60` + `SD_EARLY_MIN_MOVE_PCT=3.0` (60s momentum + 3% bar) | 📋 QUEUED | 15m + 24h obs | +5-15 SOL/wk paper | none | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §1.2 |
| TUNE-003 | Align bot_core `ML_THRESHOLD_SPEED_DEMON=40` (cosmetic — gate is signal_aggregator) | 📋 QUEUED | 5m | 0 (clarity) | none | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §1.3 |
| DOCS-001 | Correct v4 cost in CLAUDE.md and ZMN_POSTMORTEM_2026_04_16.md (~3.4 SOL not 1.32) | 🟡 CLAUDE.md PARTIAL (this commit) — ZMN_POSTMORTEM_2026_04_16.md pending | 15m | clarity for next live-enable | none | `ZMN_LIVE_TRADE_FORENSICS_2026_04_19.md` finding #2 |
| DOCS-002 | Remove stale "ML inverts above 40" Issue #1 in CLAUDE.md (already superseded by data block, but the original line still reads scary) | 📋 QUEUED (partially done in `e9de6d7`) | 15m | clarity | none | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §1.4 |
| DOCS-003 | Add Sentry MCP debugging recipe to CLAUDE.md — concrete patterns from Session 1: per-project unresolved enumeration (`search_issues` with projectSlugOrId), regression check pattern (search specific error string, confirm zero events post-fix-date), when to use `analyze_issue_with_seer` (Class B only — Seer is paid, not for known noise). No OR/AND in queries — MCP returns HTTP 400. | 📋 QUEUED | 20m | makes Sentry actually used for daily ops | GATE-003 ✅ | `ZMN_SENTRY_INTEGRATION_DONE.md`, `SENTRY_TRIAGE_2026_04_19.md` |
| OBS-008 | Silence 5 known-noise Sentry issues in UI with wontfix rationale notes: `ZMN-SIGNAL-AGGREGATOR-1` (SocialData credits — Pattern B, silenceable until ML-009), `ZMN-SIGNAL-LISTENER-1` (Discord 403 — see BUG-020), `ZMN-GOVERNANCE-1/2/3` (Anthropic credits exhausted — known state). Restores signal-to-noise for future sessions. | 📋 QUEUED (manual — Jay) | 15m | noise reduction | GATE-003 ✅, SENTRY_TRIAGE done | `SENTRY_TRIAGE_2026_04_19.md` |
| DASH-ENTRY-001 | **PRE-LIVE BLOCKER (Session 2b)** — live ENTRY path INSERT to paper_trades. `services/bot_core.py:876-890` currently inserts only into `trades`. Session 2 v2 (commit `5ac30cd`) closed the close-path gap; entry path still missing, so Open Positions widget can't surface live trades while they're open. Mirror paper_buy semantics: INSERT paper_trades row with `trade_mode='live'` + set `pos.trade_id=paper_trades.id` (NOT trades.id) to avoid the id-space overlap bug caught during Session 2 v2 recon. Gate on `if result.success:`. Also consider making this the moment to split `pos.trade_id` → `paper_trade_id` + `trades_log_id` (REFACTOR-001) if it fits in the session budget. | 📋 QUEUED | 30–45m CC session | unblocks Session 5 Open Positions | GATE-003 ✅, Session 2 v2 close-path ✅ | `session_outputs/ZMN_DASH_FIX_DONE.md` |
| SEC-001 | Rotate Redis + Postgres passwords on Railway (currently exposed in env vars; secrets scan was clean for tracked files but env-var leakage is a separate axis). **2026-04-20 waiver:** Jay accepted the risk of running ONE 60-min supervised live window (LIVE-002) without SEC-001 rotation, given bounded wallet (~1.6 SOL) + active supervision + Session 5 v2 Redis sentinel monitoring. **This waiver applies to LIVE-002 only. SEC-001 rotation remains required before any unsupervised live window OR any future live window without active supervision.** Session 5 v2 at `/mnt/user-data/outputs/SESSION_5_LIVE_ENABLE_v2.md` encodes this scoping in Step 0.2. **Waiver status 2026-04-20 (post-v4):** v4 attempt used the Jay-accepted single-window waiver but did NOT complete a full supervised window (rollback at T+12 before the 60-min window concluded). Per rollback doc §SEC-001 note: "the waiver as written applies to 'one supervised 60-min window'. Since that window did not execute, the waiver is effectively un-consumed." The same waiver framework can apply to Session 5 v5's first attempt OR Jay may rotate creds to remove the waiver entirely. Decision pending. **2026-04-21 update (MCP-RECON):** Nansen API key is HTTP 401 server-rejected on every call. A partial Nansen key (19 of 32 chars) was included in commit `1e444bb` and redacted in `0c91492` — full key already invalid (server-side), partial not reconstructable, but rotation of Nansen key should bundle with Redis/Postgres rotation. SEC-001 scope now includes: Redis password, Postgres password, **Nansen API key**. Waiver framework for v5a-c supervised windows unchanged; required before v5d unsupervised. | 📋 QUEUED | 30m (+ Nansen key generation) | security hygiene | none | `SECRETS_SCAN_2026_04_19.md`; `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`; `docs/audits/MCP_CAPABILITIES_2026_04_21.md` Known Auth section |
| SEC-002 | Pre-commit `detect-secrets` hook | 📋 QUEUED | 30m | prevents future leaks | none | `SECRETS_SCAN_2026_04_19.md` recommendation #1 |
| SEC-003 | Enable GitHub native secret scanning (settings toggle) | 📋 QUEUED | 5m | safety net | none (requires repo admin) | `SECRETS_SCAN_2026_04_19.md` recommendation #2 |
| BUG-010 | Governance CFGI hallucination fix (LLM outputs "CFGI at 50" regardless of actual) | 📋 QUEUED | 30m | governance correctness | Anthropic credits | prior `ZMN_ROADMAP.md` IN-FLIGHT, prior `DASHBOARD_AUDIT.md` B-010 |
| DASH-B-013 | `paper_trades.symbol` empty for all rows — populate via paper_trader fix + backfill | ⏸️ DEFERRED | 30m | dashboard "Recent Trades" symbol display | none | prior `ZMN_ROADMAP.md` |
| DASH-B-014 | Dashboard CFGI shows same value for BTC + SOL (cosmetic) — add `cfgi_btc` field | 📋 QUEUED | 10m | cosmetic | none | prior `ZMN_ROADMAP.md` |
| BUG-019 | Governance SQL type mismatch (`double precision > timestamp` in metrics query — cosmetic) | 📋 QUEUED | 15m | log noise | none | prior `ZMN_ROADMAP.md` #20 |
| INFRA-001 | Register postgres-mcp in `.mcp.json` (already pipx-installed; `--access-mode=restricted` mandatory) | 📋 QUEUED | 15m | replaces asyncpg shim in future sessions | none | `CC_TOOL_SURFACE_2026_04_19.md`; `docs/CLAUDE_TOOLING_INVENTORY.md` |
| LIVE-001 | Tier 1 trading-tune session bundle (TUNE-001 + TUNE-002 + TUNE-003 in one batch) | 📋 QUEUED | 30m + 24h obs | +25-45 SOL/wk paper combined | none (Tier 4 cleared) | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` "Ranked next 5 sessions" #2 |
| EXEC-001 | **PRE-LIVE BLOCKER** — pool routing state refresh in `_close_position` before sell. `Position.bonding_curve_progress` is set at signal-discovery / buy time and never refreshed during hold. `_close_position` passes the stale value to `execute_trade(..., bonding_curve_progress=pos.bonding_curve_progress)`, which routes to `_execute_pumpportal_local` even after the token has graduated and the pump.fun pool no longer exists → HTTP 400. Session 3 audit (commit `fdd8aa8`) named "mid-graduation pool drift" as a candidate transient cause but verdicted B without checking whether routing state is refreshed. Evidence: `platform` column in `paper_trades` is uniformly `pump.fun` across 6,352 rows; `bonding_curve_progress` in sampled `features_json` rows is `0.0` (populated at signal time, never updated). Fix shape: before calling `execute_trade` from `_close_position`, fetch current pool state via Helius `getAccountInfo` on the BC PDA (or an equivalent PumpPortal endpoint), update `pos.bonding_curve_progress` (and/or a new `pos.pool_route` field), then route on fresh state. **Must land paired with EXEC-002** — without EXEC-002, correctly-routed post-grad sells crash on the Jupiter NameError. | 📋 QUEUED | 45-75m CC | resolves likely root cause of 261 historical HTTP 400s | EXEC-002 (paired) | `docs/audits/SELL_PATH_DEVNET_VALIDATION_2026_04_19.md` correction block |
| EXEC-002 | **PRE-LIVE BLOCKER** — Jupiter NameError fix at `services/execution.py:491`. `_execute_jupiter` TX_SUBMIT log call references undefined `amount_sol` and `action` variables (function takes `input_mint`, `output_mint`, `amount_lamports`; `action` is only assigned inside the earlier TEST_MODE guard). Crashes on any successful Jupiter TX_SUBMIT with `NameError`. Pre-existing latent bug surfaced as Session 3 leftover concern #3 (commit `fdd8aa8`). Currently latent because (a) paper mode doesn't execute, (b) live mode uses stale routing per EXEC-001 so post-grad sells incorrectly stay on PumpPortal. Fix EXEC-001 without EXEC-002 and Jupiter starts firing → crashes. **Must land paired with EXEC-001** — neither deployed alone. | 📋 QUEUED | 15m CC | pairs with EXEC-001 | EXEC-001 (paired) | `docs/audits/SELL_PATH_DEVNET_VALIDATION_2026_04_19.md` Leftover concerns #1 |
| T5-DESIGN-001 | Session-5-prompt redesign of T5 trigger. v4 rollback fired on a benign position-open state (22.57% trigger read vs 5.68% real loss). Replace the single-threshold T5 `(T0_wallet - now_wallet) / T0_wallet > 20%` with T5a + T5b: **T5a** — `effective_wallet = onchain_sol + SUM(open_position_cost_sol)` and trigger when `drawdown_pct > 15%` of T0_effective_wallet (measures real loss, not position-open noise); **T5b** — `any closed live trade in last 5 cycles has realised_pnl_sol < -0.3 SOL` (catastrophic single-trade catch). Belongs in the next Session 5 prompt's monitoring-loop logic; no `services/` code change required for T5 itself. MAX_POSITION_SOL_FRACTION (landed in FIX-1) is the paired code-side change. **Post-GATES-V5 note (2026-04-21):** code-side hooks (MAX_POSITION_SOL_FRACTION, size caps) all landed; only remaining piece is the monitoring-loop definitions in the V5a prompt. Status elevated to 🟡 IN_PROGRESS — waits for V5a prompt authoring. | 🟡 IN_PROGRESS (code hooks ready; monitoring-loop defs pending V5a prompt) | 30m | prevents false-positive rollback | none | `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`, `session_outputs/ZMN_LIVE_ROLLBACK.md` |
| CLEAN-003 | Scripted pre-flip Redis cleanup before `TEST_MODE=false`. Session 5 v4 reconcile-residual: 5 paper positions leaked into live memory → 25 wasted Helius RPC calls. `scripts/live_flip_prep.sh` landed in FIX-1 clears `bot:status`, `paper:positions:*`, `bot:open_positions:*` before flip. CLAUDE.md "Live-flip pre-flight procedure (CLEAN-003)" section codifies the procedure. **Follow-up:** if the scripted pre-flip proves fragile in practice, revisit whether `_reconcile_positions` itself should enforce a `trade_mode` filter at every read site instead of relying on Redis cleanup. | ✅ COMPLETED FIX-1 | — | avoids phantom live-sell attempts at flip | none | `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`, `session_outputs/ZMN_LIVE_ROLLBACK.md` (reconcile-residual section) |
| ML-012 | **PRE-LIVE RELEVANT** — `AGGRESSIVE_PAPER_TRADING=true` on signal_aggregator overrides `ML_THRESHOLDS` at module load to `{"speed_demon": 30, "analyst": 30, "whale_tracker": 20}` regardless of `TEST_MODE`. Result: `ML_THRESHOLD_SPEED_DEMON=40` env var on Railway is cosmetic when `AGGRESSIVE_PAPER_TRADING=true`. Verified: 113 speed_demon trades in last 7d with `ml_score<40` despite floor policy = 40. During Session 5 v4 live window, mint `yh3n441…` entered at `ml_score=31.5` because effective live threshold was 30. Fix options: (a) gate the `AGGRESSIVE_PAPER` branch on `TEST_MODE=True` at module load, (b) move the override inside the per-signal loop with `trade_mode` check, or (c) remove `AGGRESSIVE_PAPER_TRADING` env var and tune via `ML_THRESHOLD_SPEED_DEMON` directly. Recommend (a) for minimum blast radius. **GATES-V5 landed option (a):** `if AGGRESSIVE_PAPER and TEST_MODE:` — live mode now honors env `ML_THRESHOLD_SPEED_DEMON=65`. Elif branch logs explicit "ML-012 FIX: ... ignoring override" warning when AGGRESSIVE_PAPER=true and TEST_MODE=false. | ✅ COMPLETED `1dec17b` | 15-30m | restores tuning-knob→behavior trust | none | `docs/audits/ML_BYPASS_INVESTIGATION_2026_04_20.md`; `session_outputs/ZMN_GATES_V5_DONE.md` |
| TUNE-004 | **Stale signal_aggregator size env vars.** signal_aggregator env still shows `SPEED_DEMON_BASE_SIZE_SOL=0.45` and `MAX_SIZE_SOL=0.75` while bot_core has 0.15 / 0.25 post-GATES-V5. Different scopes (sig_agg uses these for signal-strength tagging; bot_core is the binding sizing enforcement) so functionally not broken, but signal-side audit of "what size did sig_agg intend" shows stale values. Quick fix: set signal_aggregator env to match bot_core — env-only, no code change, 2 Railway MCP calls + readback. Bundle with V5a prep if that session runs, or standalone 5m. | 📋 QUEUED | 5m | hygiene + cross-service consistency | none | `session_outputs/ZMN_GATES_V5_DONE.md` leftover concern #5 |
| OBS-013 | **`sig_type=None` on paper opens.** 3 pre-deploy in-flight paper opens during GATES-V5 deploy window carried `signal_type=None` in features_json. Bypass logic for trending/migration/graduation in the new entry gates depends on sig_type being populated. If ALL Speed Demon signals arrive with sig_type=None, bypass is dead code — acceptable for Speed Demon (which doesn't get those signal types) but will break Analyst / Whale Tracker activation which depend on sig_type classification. Scope: trace where sig_type should be populated in signal_aggregator intake path; confirm whether it's an in-flight-during-deploy artifact or a real gap. | 📋 QUEUED | 30m | unblocks future personality re-enable (Analyst, Whale Tracker) | none | `session_outputs/ZMN_GATES_V5_DONE.md` leftover concern #3 |
| OBS-014 | **`stop_loss_35%` → `stop_loss_20%` SQL string transition.** GATES-V5 tightened `STOP_LOSS_PCT` from 0.35 to 0.20; exit_reason string is dynamically formatted (`stop_loss_{int(STOP_LOSS_PCT*100)}%`). Any historical SQL filtering on literal `exit_reason='stop_loss_35%'` will silently miss new rows. Audit + update: `scripts/rebaseline_paper_edge.py` (FEE-MODEL-001 rebaseline script — check if it slices by exit_reason), dashboard widgets, any monitoring queries. Fix pattern: change `= 'stop_loss_35%'` to `LIKE 'stop_loss_%'`. | 📋 QUEUED | 20m | prevents SQL silently missing stop_loss_20% exits | none | `session_outputs/ZMN_GATES_V5_DONE.md` leftover concern #1 |
| BUG-021 | **`daily_pnl_sol` + `api_paper_stats` ignore `trade_mode` filter.** Two aggregation sites in paper_trades reads don't filter by trade_mode: `services/bot_core.py:1963-1987` (`_portfolio_snapshot_task` writes `daily_pnl_sol` to `portfolio_snapshots` every 300s) and `services/dashboard_api.py:1148-1159` (`api_paper_stats` dashboard endpoint). Post-DASH-RESET the 6 preserved live rows' aggregate `realised_pnl_sol` = -3.205 SOL leaks into both "paper" aggregates until paper trades accumulate enough to mask it. Not a new bug — pre-existing, but surfaced by the reset shrinking the paper aggregate small enough for the live-leak to dominate. Fix both sites: add `AND trade_mode='paper'` (or mode-aware `CASE WHEN $TEST_MODE THEN 'paper' ELSE 'live' END`). Cosmetic during paper operation (small numbers); becomes misleading during v5 live windows where paper aggregates should stay separate. **✅ PARTIAL 2026-04-22 `bc622eb`:** dashboard_api sites (`api_paper_stats` + `api_portfolio_history`) now call `_get_mode(request)` and apply trade_mode filter. Paper-view excludes live rows; live-view selects `market_mode='LIVE_ONCHAIN'` snapshots. Response includes `"mode": "paper"\|"live"` for callers to verify the slice. Still pending: `services/bot_core.py:1963-1987` `_portfolio_snapshot_task` — this is the daily_pnl_sol WRITE path (paper_trader writes `daily_pnl_sol` with no mode filter). Follow-up to complete the ticket; bot_core scope deferred. | 🟡 PARTIAL `bc622eb` (dashboard done, bot_core write-path deferred) | 20m (dashboard) + 15m (bot_core follow-up) | paper/live aggregate separation post-v5-flip | none | `session_outputs/ZMN_DASH_RESET_DONE.md` concerns #1 + #5; `docs/audits/HOLDER_PIPELINE_FIX_2026_04_22.md` |
| CLEAN-004 | **Reset `consecutive_losses` counter to 0 before v5 live window.** DASH-RESET surfaced that bot_core restores `consecutive_losses=4` from Postgres `bot_state` + Redis `bot:consecutive_losses=4` on every restart. This counter feeds the sell-storm circuit breaker (triggers 5-min cooloff at `consecutive_losses=5+`). Currently at 4 — one live loss in v5 fires the circuit breaker unnecessarily (state artifact from pre-reset paper trades, not a real live signal). Pre-v5-flip cleanup: `UPDATE bot_state SET value_text='0' WHERE key='consecutive_losses'` + `DEL bot:consecutive_losses`. Add to `scripts/live_flip_prep.sh` so future flips always reset the counter. Bundles with V5a prep. **✅ COMPLETED 2026-04-22:** (a) runtime reset: Redis `bot:consecutive_losses=0` + Postgres `bot_state.consecutive_losses='0'` both set; (b) script update `322e105`: `scripts/live_flip_prep.sh` now idempotently resets both stores on every future flip (psql branch gated on `DATABASE_PUBLIC_URL` env being set — Redis-only reset is always performed). | ✅ COMPLETED (reset + script) `322e105` | 10m + 15m script update | prevents spurious sell-storm trip on first v5 live loss | none | `session_outputs/ZMN_DASH_RESET_DONE.md` concern #4; `docs/audits/HOLDER_PIPELINE_FIX_2026_04_22.md` |
| HOLDER-DATA-PIPELINE-001 | **PRE-LIVE BLOCKER (NEW 2026-04-22) — ✅ FIXED 2026-04-22 fc87b03 + 4c5508b.** Two sequential fixes: (1) `fc87b03` — GeckoTerminal `/tokens/{mint}/info` changed `holders` field from bare int to dict `{"count": N, "distribution_percentage": {...}}`. Both call sites in `services/signal_aggregator.py` (main feature build path L782 + graduation processor L2441) did `int(attrs.get("holders", 0))` which raises TypeError on dict form, caught by outer `except Exception: pass`. Fixed with isinstance(dict) branch reading .count. (2) `4c5508b` — age-bypass for GATES-V5 HOLDER + BSR gates when `token_age_seconds < GATES_V5_MIN_AGE_SEC` (default 30s). Rationale: fresh pump.fun new_token signals at age=0s have no GeckoTerminal index, no token:stats populated yet, and no raw.holders — gate rejecting `0 < 1` is meaningless because data isn't available yet, not "organic interest absent". Let ML + rugcheck handle fresh signals; reapply gates when data should be present. Full evidence chain + verification plan in `docs/audits/HOLDER_PIPELINE_FIX_2026_04_22.md`. | ✅ COMPLETED `fc87b03` + `4c5508b` | 30-45m diagnostic + fix | **unblocked paper/live trading** | none | `docs/audits/HOLDER_PIPELINE_FIX_2026_04_22.md`; SIGNAL-FUNNEL + HOLDER-LOWER sessions 2026-04-22; `session_outputs/ZMN_HOLDER_DISABLE_DONE.md` (BSR sentinel corroboration) |
| TUNE-005 | Roll back `HOLDER_COUNT_MIN` from 1 → 15 AFTER `HOLDER-DATA-PIPELINE-001` lands. Currently at 1 only because HOLDER-LOWER session proved threshold is irrelevant while data pipeline returns 0. Once pipeline produces real counts, threshold restoration should be a 5m env-only change. **⏪ ROLLED BACK 2026-04-29 (HOLDER_COUNT_MIN 15 → 1 reverted; 24h validation window opens). Trigger: Speed Demon WR dropped 52.9% → 34.9% over 3 days post-TUNE-005 deploy. Hypothesis: gate=15 rejected legitimate winners with early holder counts <15. Outcome decides codify-rollback or reapply-and-look-elsewhere. Verified live: container swap 2026-04-29 09:27:58 UTC; new banner; zero HOLDER reject; cadence alive (~5-7 ML SCORE/min); no errors.** Original status (kept for history): **✅ COMPLETED 2026-04-24 (env-only change, no git commit).** Variable set via Railway MCP `set-variables` (signal_aggregator, `HOLDER_COUNT_MIN=15`). Railway auto-redeploy triggered: old container stopped 14:32:21 UTC, new container started 14:32:10 UTC with expected env (TEST_MODE=True, ANALYST_DISABLED=True). Post-change verification window ~5 min: zero `HOLDER reject: 0 < 15` pattern observed (STOP condition not triggered); ML SCORE cadence 5-8/min (baseline was ~12/min over a burstier window — not a 5-min collapse); `PRE_FILTER reject: 1.00 < 1.15` seen firing with real values confirming downstream gate path healthy; no errors or tracebacks. HOLDER gate itself was mostly bypassed by age<30s gate (GATES_V5_MIN_AGE_SEC) — most tokens in observation window were fresh pumpportal new_token signals — so no gate rejections were exercised in the 5-min window. This is expected and does not indicate broken pipeline; the gate will fire when it reaches tokens with age≥30s AND holder_count <15, which is the intended gating behavior. | ⏪ ROLLED BACK 2026-04-29 pending validation (was ✅ COMPLETED 2026-04-24) | 5m (actual: env set + ~7 min deploy + 5 min verify) | restore intended gating behavior | HOLDER-DATA-PIPELINE-001 ✅ | HOLDER-LOWER session 2026-04-22; `TUNE-005` session 2026-04-24 |
| SOCIALDATA-AUTO-TOPUP-001 | Monitor SocialData credit balance; alert + auto-pause if <$2 remaining. Today's outage (balance hit $0 → `FILTER:no_social_links` killed 100% of signals for multiple days before detection) is repeatable. Fix options: (a) cron that hits SocialData balance endpoint + Sentry alert at threshold, (b) fail-open config where `no_social_links` rejects only if credits unavailable AND `ENFORCE_SOCIAL_STRICT=true`. Recommend (a) — 30m single-file cron worker. | 📋 QUEUED | 30m | prevent silent outage repeats | none | 2026-04-22 SocialData $0 outage (Jay top-up to $10 restored flow) |
| ANALYST-DISABLE-001 | **Closes the pre-existing `analyst` personality paper-PnL leak.** Environment flag `ANALYST_DISABLED=true` was already set on signal_aggregator but only covered the normal signal path; the graduation-sniper bypass at `services/signal_aggregator.py:_process_graduations` continued to emit `analyst`-tagged entries to `signals:scored`. On 2026-04-23 a Redis governance override was written to `governance:latest_decision` with `analyst_enabled: false`, halting the leak at the bot_core gate (`services/bot_core.py:612` reads `gov.get("analyst_enabled", True)`). Result: 59 closed paper trades over 17h had -2.48 SOL realized PnL / 11.9% WR on the `analyst` personality before disable — leak stopped post-override. **Halflife caveat:** governance service writes `governance:latest_decision` with `ex=28800` (8h TTL) and re-runs every `next_review_hours=4`; next governance write (even in LLM-failed fallback at `services/governance.py:346`) will SET the key to `GOVERNANCE_DEFAULTS` which has `analyst_enabled: True`, clobbering the override. Re-apply the override every ≤4 hours until proper fix lands (tracked as "ANALYST-DISABLE-HALFLIFE" open sub-thread). Proper fix options deferred: (a) add `if ANALYST_DISABLED: continue` at `services/signal_aggregator.py:2428` (after `brpop`); (b) flip `GOVERNANCE_DEFAULTS["analyst_enabled"] = False` in `services/governance.py`; (c) add a new Redis key `bot:personality:analyst:force_disable` and a bot_core check. **Halflife realized 2026-04-25:** override clobbered ~6 min after first apply; 241 analyst trades leaked through 2026-04-23 → 2026-04-25 at 17.0% WR / -12.5 SOL, tripping `DAILY_LOSS_LIMIT_SOL=4.0` EMERGENCY_STOP at 2026-04-25 21:52:50 UTC. Superseded by **ANALYST-DISABLE-002** (commit `9d6e95c`, 2026-04-28). | ✅ COMPLETED `cc8e5c9` (band-aid; superseded by ANALYST-DISABLE-002 `9d6e95c`) | 5m + Redis write | stops -2.48 SOL/day leak | none | `session_outputs/ZMN_ANALYST_DISABLE_DONE.md`; STATUS.md 2026-04-23 08:19 UTC entry |
| ANALYST-DISABLE-002 | **Code-level fix that closes the graduation-sniper bypass that ANALYST-DISABLE-001 could not reach.** Extends the existing `ANALYST_DISABLED` env-var pattern at `services/signal_aggregator.py:1810` (main signal path) to `_process_graduations` at line 2443. Drain queue silently when disabled; function stays alive (no orphan task) so re-enable via env + container restart resumes processing immediately. Startup log now reports `Graduation sniper processor started (ANALYST_DISABLED=true|false)` at boot for visibility. Verified live 2026-04-28 13:08:06 UTC — new banner observed, zero GRAD_ACCEPT/GRAD_REJECT events in 2+ min post-swap (function silently draining as designed). Companion runtime action: governance:latest_decision Redis override re-applied as belt-and-suspenders (no longer load-bearing). **Out-of-scope this session (escalated for next):** 67h bot silence is a SEPARATE consequence of the 2026-04-25 EMERGENCY_STOP trigger — multi-step state recovery recipe documented in audit doc §6.3 (consecutive_losses=0 + market:mode:override renewal + signals:scored drain + bot_core restart). Audit: `docs/audits/ANALYST_DISABLE_FIX_2026_04_28.md`. | ✅ COMPLETED `9d6e95c` | 45m (incl. diagnosis + verify) | durable analyst-leak closure | none | `docs/audits/ANALYST_DISABLE_FIX_2026_04_28.md`; STATUS.md 2026-04-28 entry |
| SILENCE-RECOVERY-2026-04-28 | **🔥 Bot is offline (EMERGENCY_STOPPED) since 2026-04-25 21:52:50 UTC.** Trigger: risk_manager `Daily loss limit: -4.05 SOL` (correctly-firing safety rail; the upstream cause was the analyst leak now closed by ANALYST-DISABLE-002). Recovery is a 4-step Redis + restart recipe documented in `docs/audits/ANALYST_DISABLE_FIX_2026_04_28.md` §6.3: (1) `SET bot:consecutive_losses 0`; (2) `SET market:mode:override NORMAL EX 86400` (auto-renewal lapsed → market_health auto-tripped to HIBERNATE); (3) `DEL signals:scored` (337 stale entries piled up over 67h — replaying risks bot opening positions on stale prices); (4) restart bot_core to clear in-memory `self.emergency_stopped`. **NOT executed inline this session** because each step is documented but the combination has non-trivial blast radius (337-entry queue drain is opinionated; consecutive_losses=7 means 1 bad post-recovery trade re-trips the breaker). Per session prompt's STOP rule on multi-cause silences: escalated to Jay for explicit go-ahead. Verification post-recovery: `bot:consecutive_losses=0`, fresh bot_core INFO log entries, paper_trades shows new Speed-Demon-only entries within 30 min. | 📋 QUEUED 🔥 (escalated) | 5-10m if executed in order | brings bot back online | ANALYST-DISABLE-002 ✅ (this session) | `docs/audits/ANALYST_DISABLE_FIX_2026_04_28.md` §6 |
| GOVERNANCE-RESILIENCE | **Reduce dependency on healthy Anthropic-credit governance for personality toggles.** Root cause behind ANALYST-DISABLE-001 halflife: `services/bot_core.py:612` defaults `gov.get("analyst_enabled", True)` to True when governance is unavailable, and governance writes `GOVERNANCE_DEFAULTS` (analyst_enabled=True) every 4h when LLM call fails on creds-out. ANALYST-DISABLE-002 closes the analyst-specific path via env var; Speed Demon and Whale Tracker remain on the same fragile pattern. Fix shape options: (a) add per-personality env-var equivalents (`SPEED_DEMON_DISABLED`, `WHALE_TRACKER_DISABLED`) and check at the same site as `ANALYST_DISABLED`; (b) flip `GOVERNANCE_DEFAULTS` defaults from True → False (fail-closed); (c) move personality enable map entirely out of governance into env vars. Not urgent — analyst-specific path is now durable. Soft. | 📋 QUEUED (deferred) | 30-45m | hardens against future analyst-leak-style halflife issues for other personalities | none | `docs/audits/ANALYST_DISABLE_FIX_2026_04_28.md` §4 |
| ANALYST-PAPER-AUDIT-001 | **Read-only diagnostic on the legacy `analyst` personality.** This is NOT the same as `ANALYST-POST-GRAD-001` (different MC band, different hold duration, different exit reasons). Scope: (1) analyze the 59 closed trades the module produced 2026-04-22 14:11 UTC → 2026-04-23 disable; exit-reason breakdown 25 × `graduation_stop_loss` / 22 × `graduation_time_exit` / 9 × `graduation_tp_30pct` / 2 × `stale_no_price` / 1 × `TRAILING_STOP`; hold duration median 3.7 min; MC at entry $9-76k (pre-grad, below Bitfoot's $50-300k); `holder_count` populated 35-800 range (different pipeline from Speed Demon). (2) Determine retire vs retune: retuning adds regression surface; retiring (keep disabled + rename module to `analyst_legacy` or gate behind explicit `ENABLE_ANALYST_LEGACY` env var) is cleaner; ANALYST-POST-GRAD-001 is a from-scratch rewrite anyway so legacy isn't needed. (3) Find what re-enabled it on 2026-04-22: commit or env change that flipped it from previously-disabled to active; add guardrails so it can't re-enable without explicit intent. Output: `docs/audits/ANALYST_PAPER_AUDIT_2026_04_DD.md`. No code changes; any deprecation is a follow-up. | 📋 QUEUED | 45m | prevents silent regression of the same leak | ANALYST-DISABLE-001 ✅ | chat 2026-04-23 paper_trades_export analysis; `session_outputs/ZMN_ANALYST_DISABLE_DONE.md` |
| DOCS-004 | **CLAUDE.md Vybe URL correction.** `CLAUDE.md` line (under "Vybe Network MCP" section) currently reads `API base: https://api.vybenetwork.com (NOT .xyz)`. Exactly backwards — every `.com` endpoint returns 404; the correct base is `.xyz`. Verified during BITFOOT-2026-BASELINE session (2026-04-23) on `/v4/tokens/{mint}/top-holders`; documented in §1 of `docs/audits/BITFOOT_2026_BASELINE_2026_04_23.md`. 5-min docs fix. Also check `AGENT_CONTEXT.md` for the same wrong URL and update if present. | 📋 QUEUED | 5m | prevents future sessions burning time on 404s | none | `docs/audits/BITFOOT_2026_BASELINE_2026_04_23.md` §1 |
| BUG-022 | **`corrected_pnl_sol` NULL on all 1111 closed paper_trades rows — ✅ COMPLETED 2026-04-30 via Option A.** Per CLAUDE.md "Trade P/L Analysis Rule" (2026-04-13), `corrected_pnl_sol` was set as the authoritative PnL column because `realised_pnl_sol` was historically buggy on staged-TP trades (44 pre-fix rows). **2026-04-28 investigation `ca4812d`:** verdict 2a — the writer never existed as committed code. **Fix execution 2026-04-30 (Session B):** (1) one-time idempotent SQL UPDATE backfilled all 1111 closed rows with `corrected_pnl_sol = realised_pnl_sol, corrected_pnl_pct = realised_pnl_pct, corrected_outcome = outcome, correction_method = 'pass_through', correction_applied_at = NOW()`. POST-state: `null_closed=0, pass_through=1111, max(|corrected - realised|) = 0.0`. Spot-check on 3 most recent rows (id 7740/7741/7742) confirms identity. (2) Inline write added at `services/paper_trader.py:392-407` close-time UPDATE so future paper_sell rows get populated automatically. (3) Inline write added at `services/bot_core.py:1059-1078` staged-TP correction UPDATE (TEST_MODE branch) — split the prior `paper_trades if TEST_MODE else trades` conditional so the corrected_* columns only land on paper_trades (which carries them); trades-table branch unchanged. (4) CLAUDE.md "Trade P/L Analysis Rule" rewritten to reflect post-FEE-MODEL-001 + post-BUG-022 reality — both columns interchangeable; ML training has no `correction_method` filter requirement. Live-mode rows will write `correction_method='live_estimated_v1'` per Session D LIVE-FEE-CAPTURE Path A. **Out of scope this session:** live close UPDATE (`bot_core.py:1268`) + live close fallback INSERT (`bot_core.py:1301`) — both are Session D scope (LIVE-FEE-CAPTURE Path A); they get `correction_method='live_estimated_v1'` + fees_sol/slippage_pct via `_simulate_*` import. Original investigation: `docs/audits/CORRECTED_PNL_INVESTIGATION_2026_04_28.md`. Fix audit: `docs/audits/BUG_022_FIX_2026_04_30.md`. | ✅ COMPLETED 2026-04-30 (Session B) | 5m SQL + 10m code + 15m verify | restores symbolic PnL-column policy + future-proofs ML query safety; directional PnL findings are already correct on current data | none | `docs/audits/BUG_022_FIX_2026_04_30.md`; investigation `ca4812d`; fix `<hash>` |
| STATUS-CONVENTION-001 | **Single-file state tracker `STATUS.md` at repo root.** Updated at end of every CC session, uploaded to every Claude web chat. Convention: append-only dated entries, newest at top, ≤15 lines per entry, mandatory read of bot state (TEST_MODE / paper opens / live opens / consecutive_losses) before writing. CLAUDE.md Operating Principle block added under "STATUS.md — single-file state tracker" (commit `2243d1f`). Template file `STATUS.md.template` at repo root. Seed entry landed 2026-04-23 08:01 UTC (STATUS-CONVENTION-001 itself); subsequent entries in ANALYST-DISABLE, BITFOOT-DIP-SAMPLE-RETRY, BITFOOT-2026-BASELINE, ROADMAP-CONSOL-2026-04-23-LATE all follow the convention. | 🟡 IN_PROGRESS (seed commit landed, 4 subsequent entries confirming convention works — promote to ✅ once one more session appends cleanly) | 20-30m (already in-flight) | eliminates stale-context handoffs between Claude chats | none | chat 2026-04-23; `/mnt/user-data/outputs/SESSION_STATUS_CONVENTION_001.md`; commit `2243d1f` |
| WALLET-DRIFT-2026-04-29 | **✅ RESOLVED 2026-04-30 (Branch 1 confirmed by Jay) — INVESTIGATED 2026-04-30 as Outcome C.** Reconciliation gap = **0 lamports** (sub-1e-9 SOL). 8 on-chain transactions in window 2026-04-19 → present; **7 of 8 documented**: 1 ELONX dust sale (+0.048 SOL, external — `1b40df3` §2.4); 4 yh3n441 pump.fun trades (id 6580 entry/exit + 1 failed 3rd-party + 1 creator-fee distribution, net −0.094 SOL); **1 undocumented outgoing transfer**: 2026-04-21 10:04:48 UTC, sig `42dnuS1xv…`, **−1.50008 SOL** to `7DSQ3ktYiirRfs4YQojyDTqUM9Cwj9YgzwwegyiCAgUy` (System Program TRANSFER, fee payer = trading wallet itself, signed with its private key). Destination has zero references in repo (CLAUDE.md, AGENT_CONTEXT.md, STATUS.md, audits, services, .env). NOT the holding wallet (`2gfHQvyQ…`). Vanity pattern (`7DSQ`+`AgUy` prefix/suffix). **Critical correction:** session prompt assumed 6 paper_trades "live" rows summing to −3.21 SOL realised PnL would explain the drain — **5 of those 6 have NULL signatures and are reconcile-residual paper-position force-closures from Session 5 v4** (per `ZMN_LIVE_ROLLBACK.md` lines 96-122; no on-chain SOL effect). Only id 6580 was a real on-chain round-trip. 3 dust-phishing txs at 10:05 UTC (one mimics destination's vanity pattern) — informational, no security impact. **V5a disposition: 🔥 → 🟡.** Two-layer blocker: (1) mechanical — needs top-up ≥ 1.5-2.5 SOL; (2) confirmation pending — Jay confirms Branch on the 2026-04-21 transfer. Branch 1/2 (benign / consolidation / exchange deposit / Jay-controlled) → blocker collapses to layer 1 (top-up only); STATUS backfill. Branch 3 (unintended/unauthorized) → Outcome D treatment, halt V5a, security investigation (rotate `TRADING_WALLET_PRIVATE_KEY`, audit signing-machine 2026-04-20 21:00 → 2026-04-21 10:05 UTC, enumerate other wallets from seed). 9-day post-transfer idle is mildly reassuring not conclusive. **Companion (out of scope this audit):** holding wallet drain (~0.089 SOL) deferred unless Branch 3. | ✅ RESOLVED 2026-04-30 — Branch 1 confirmed by Jay (intentional transfer); CLAUDE.md "Live trading mode" block appended with the 2026-04-21 1.5 SOL transfer note in Session B (BUG-022 fix). Mechanical layer (top-up to ~3 SOL pre-V5a) tracked separately as part of V5a preconditions. | 0m | unblocks V5a wallet decision; CLAUDE.md updated | ENV-AUDIT-2026-04-29 ✅ | `docs/audits/WALLET_DRIFT_INVESTIGATION_2026_04_29.md`; `docs/audits/ENV_AUDIT_2026_04_29.md` §1 + §7; `docs/audits/ZMN_LIVE_TRADE_FORENSICS_2026_04_19.md` (`1b40df3`); `session_outputs/ZMN_LIVE_ROLLBACK.md` |
| TREASURY-TEST-MODE-002 | **`treasury` service has `TEST_MODE=false` while every other service is `true`.** Verified 2026-04-29 12:39 UTC via Railway MCP `list-variables`. Treasury auto-transfers SOL when `trading_wallet > TREASURY_TRIGGER_SOL=30.0` to bring wallet back to `TREASURY_TARGET_SOL=25.0`. Trigger is currently dormant because trading wallet is 0.064 SOL (≪30 trigger), but treasury would move real SOL on-chain regardless of bot_core's TEST_MODE if the wallet ever crossed the trigger. **Undocumented in any STATUS entry — discovered by ENV-AUDIT-2026-04-29.** Resolution options: (a) flip `TEST_MODE=true` on treasury for paper-symmetric safety while supervised; (b) document the intent (e.g., "treasury is allowed to move SOL even in paper mode for legitimate housekeeping") in CLAUDE.md "Live trading mode — session-gated"; (c) leave as-is since it's dormant — but then any future top-up that crosses 30 SOL triggers a real on-chain transfer. **Recommend (a) before any wallet top-up; revisit at V5a-supervised window.** | 📋 QUEUED 🟡 | 5m env flip OR 5m docs note | eliminates latent on-chain risk if wallet crosses trigger | none | `docs/audits/ENV_AUDIT_2026_04_29.md` §2.6 + §8.2 |
| ML-THRESHOLD-DRIFT-2026-04-29 | **`ML_THRESHOLD_SPEED_DEMON` differs across services: signal_aggregator=65, bot_core=40, web=45.** Same drift on `ML_THRESHOLD_ANALYST` (55/35/50) and `ML_THRESHOLD_WHALE_TRACKER` (55/35/50). Verified 2026-04-29 12:39 UTC via Railway MCP. Last 100 closed paper trades contain **44 entries with `ml_score ∈ (0, 40]`** — confirming Jay's chat-side observation. The `(40,50]` band turned net-negative (-0.42 SOL on n=27). `AGGRESSIVE_PAPER_TRADING=true` on signal_aggregator + bot_core appears to bypass the SA-side threshold for paper rows. **Mechanism investigation deferred** to a separate read-only session that walks the entry-decision code path (`services/signal_aggregator.py` ML gating + bot_core entry filter + AGGRESSIVE_PAPER bypass site) to confirm which env var actually gates entries. Until then, treat the (0,40] paper performance as informative-but-experimental — not "the model is broken" but "the threshold isn't acting as documented". CLAUDE.md "ML threshold floor=40" guidance referred to bot_core; signal_aggregator's =65 is the live-mode gate. **Out of scope:** changing thresholds, changing AGGRESSIVE_PAPER_TRADING semantics. In scope (when this lands): document the actual gate mechanism + reconcile the three values into one source of truth. | 📋 QUEUED 🟡 (deferred research) | 30-45m code-path audit + docs | resolves ambiguity over which threshold is enforced; reconciles userMem claim vs reality | none | `docs/audits/ENV_AUDIT_2026_04_29.md` §1 + §4.4 + §5; Jay chat-side observation |
| TUNE-010 | **`dex_paid` feature evaluation for Analyst / Whale Tracker — verdict ❌ DISCARD 2026-04-30.** Tested whether DexScreener's paid-promotion flag (Bitfoot's meta-signal) has signal for Analyst's higher-MC entries. Sample: 304 closed Analyst trades (2026-04-22 → 2026-04-28). DexScreener `/orders/v1/{chainId}/{tokenAddress}` exposes `paymentTimestamp` per order — enabled timestamp-aware "paid AT entry time" methodology (sidesteps the prompt's Scenario A/B problem). 318/318 mints looked up cleanly (0 API errors). Result: **`dex_paid_at_entry` inverts vs hypothesis** — PAID (n=164) WR=14.0% vs NOT_PAID (n=140) WR=17.9%; diff -3.83pp; PnL diff +0.0001 SOL/trade (zero discriminatory power); fisher_p=0.43, MWU_p=0.40. **Base-rate problem:** 76.6% of Analyst's tokens are paid lifetime, 53.9% paid at entry — feature too prevalent to discriminate. Best secondary feature `social_count > 0` shows +5.6pp WR but p=0.25 (not significant) and PnL diff +0.002 SOL/trade (below 0.02 threshold). **SD scope exclusion correct:** 0/14 SD big winners (≥0.10 SOL post-recovery) have any DexScreener footprint — all `no_pair=True` (rugged or never-graduated). **Whale Tracker has zero historical trades — cannot evaluate.** No deploy criterion met by any feature. Re-evaluate if: Analyst reactivated + 200 new trades in different regime; OR ANALYST-POST-GRAD-001 (post-grad MC $50-300k) accumulates sample with non-trivial dex_paid variance; OR Whale Tracker activated with meaningful sample. | ❌ DISCARD 2026-04-30 | 0m (decision made) | confirms SD scope exclusion; closes Bitfoot dex_paid hypothesis for Analyst | none | `docs/audits/TUNE_010_DEX_PAID_EVALUATION_2026_04_30.md` (NEW); session prompt SESSION_TUNE_010 |
| TUNE-009 | **SD_EARLY_CHECK relaxation evaluated 2026-04-30 — verdict ⏸ DEFERRED.** Session prompt SESSION_A hypothesized that the `no_momentum_90s` exit (123 post-recovery trades, 0% WR, -2.915 SOL bleed) was killing winners that broke out *after* 60s. Empirical investigation **disproves** the hypothesis: (a) check fires precisely at 50.03-51.97s window opening (`bot_core.py:1599` `early_check_sec - 10 < hold_sec` with env=60); (b) **dead-trade pnl_pct distribution is decisive — mean -17.37%, median -16.12%, max -1.85%, ZERO trades in the 1-3% slow-starter band that Alpha-A relaxation would save**; (c) 13 big winners post-recovery all bypassed the check (2 hit staged_tp at <2s, 11 hit TRAILING_STOP at ~600s after surviving 50-90s window with ≥3% pnl); (d) 97.6% (120/123) of dead trades enter at MC $800-$3000 — Session C's `SD_MC_CEILING_USD=3000` only filters 3/123 of current bleed. **Conclusion:** the check is doing its designed job efficiently (-0.024/trade vs alternative -0.074/trade stop_loss); the structural fix is upstream entry filtering, not exit-logic relaxation. Re-evaluate if any of: dead-trade pnl_pct distribution shifts to include slow-starter band; shadow-measure shows trades would recover with 90 more seconds; post-Session-C bleed remains > -1.5 SOL on equivalent volume AND distribution moved up. | ⏸ DEFERRED 2026-04-30 | 0m (decision made — re-eval after observation) | prevents counterproductive relaxation; confirms structural fix lives at SD_MC_CEILING (Session C scope) | none | `docs/audits/SD_EARLY_CHECK_RELAX_2026_04_30.md` (NEW); session prompt SESSION_A |

### LIVE-001 / Session 4 — T+24h gate outcome (appended 2026-04-20)

> **T+24h gate result (2026-04-20):** Strict gate FAIL on `stop_loss_35%` count-reduction criterion (23% reduction over 21.7h window vs 50% target); PASS on total-PnL criterion (+28% vs pro-rated baseline). **Jay-accepted** on PnL-up read. Env vars demonstrably took effect: `no_momentum` median hold_seconds dropped 81.1s → 51.2s (60s env var applied); TRAILING_STOP min peak_mult dropped 1.310 → 1.114 (+10% activation applied). Yellow flags: TRAILING_STOP WR dropped 20.9pp (91.8% → 70.9%) with per-trade avg PnL essentially flat; `no_momentum` per-trade loss worsened 33% (-0.058 → -0.080 SOL). Neither is a rollback trigger individually but both warrant continued observation in the second 24h window. Full assessment in `docs/audits/TUNE_T24H_GATE_2026_04_20.md`.

---

## Tier 2 — One-session MCP integrations / focused features

| ID | Title | Status | Est | Impact | Depends on | Source |
|---|---|---|---:|---|---|---|
| BOT-CORE-EMERGENCY-STOP-LIVENESS-001 | **Resolved-as-non-bug 2026-05-21 by V5A-FIXES-001 Phase 5.** Investigation found the per-decision `bot:emergency_stop` Redis check ALREADY EXISTS at `services/bot_core.py:604-609` inside `process_signal`. V5A-FLIP-001-V2 audit's "Phase 5 finding" was a misinterpretation: (1) `heartbeat.emergency=false` was conflated with the per-decision check status — heartbeat reflects in-memory `self.emergency_stopped`, not the Redis flag; the Redis-flag check IS honored at signal-entry time (proven by post-rollback logs at 10:26:57Z showing "Emergency stop active — skipping all new signals"). (2) The 14 phantom positions had `entry_time` from 2026-05-12 to 2026-05-19 — DAYS before V5A-FLIP-001-V2 incident. They were NOT opened during the 9-minute drain window. The per-decision check worked correctly; the phantoms came from pre-existing `trades`-table orphans. Filed observability follow-up `HEARTBEAT-EMERGENCY-STOP-REFLECTION-001` Tier 3 — surface the Redis flag state in heartbeat alongside in-memory flag, so operators see "halted" when only Redis is set. Audit: `docs/audits/V5A_FIXES_001_2026_05_21.md` §6 + `.tmp_v5a_fixes/05_scope.md`. | ✅ CLOSED-AS-NON-BUG 2026-05-21 | n/a (no code change) | n/a — feature already exists | n/a | `docs/audits/V5A_FIXES_001_2026_05_21.md` §6; `docs/audits/V5A_FLIP_001_2026_05_20.md` §7.3 (superseded interpretation) |
|---|---|---|---:|---|---|---|
| GOVERNANCE-AGENT-MARKET-MODE-001 | **Governance agent for autonomous market-mode classification and operational gating.** Autonomous classification of `market:mode` from CFGI, SOL price velocity, BTC correlation, volume, on-chain metrics. **Halt authority** under defined conditions (can flip `EMERGENCY_STOP=true` or revert `TEST_MODE`); **probably NOT start authority** — keeping go-live decisions human-gated is the right safety asymmetry. **Absorbs `MARKET-MODE-001-RE-CALIBRATE-V2`** (linked, not deleted) — the recalibration concern (NORMAL bleeds, threshold structure may be wrong) becomes one input to the classifier scope. Broader purpose: autonomous handling of the class of operational gates currently held manually (TTL renewal, regime monitoring, position-sizing graduation, kill-switch threshold tuning). **Design happens in a future investigation session — not designed here. This entry is the strategic-direction pointer.** | 📋 QUEUED Tier 2 🟡 | TBD (design session first, then build) | autonomous operational gating; reduces V5A manual surface over time | V5A live data accumulated across ≥2 distinct market regimes (NORMAL + CAUTIOUS or DEFENSIVE) so the classifier has training signal in both states; `LATENCY-OBSERVABILITY-001` (regime-shift latency may matter as a feature) | `docs/findings/V5A_GO_LIVE_DECISIONS.md` §3 + filed by V5A-GO-LIVE-DECISIONS-RECORD-001 |
| PAPER-FEE-MODEL-CALIBRATION-001 | Recalibrate `PAPER_FEE_PUMPFUN_PCT` / `PAPER_PRIORITY_FEE_*` / `PAPER_JITO_TIP_*` env knobs to Path-B-derived per-component fee reality. **NO code change** — env-only deploy after ≥10 Path B (`live_actual_v1`) rows accumulate (currently 1; needs V5A relaunch). Addresses ML-TRAINING-COST-FIDELITY-AUDIT-001's ~17.6× under-count factor. Single-knob recalibration per Path B sample; preserves rollback-via-env. | 📋 QUEUED (Tier 2 🟡) | 30m env-only deploy + 7d post-recalibration obs window | closes the dominant sim-to-real cost gap; prerequisite for trustworthy Analyst Phase 0 | ≥10 Path B rows (V5A relaunch sustained) + LIVE-PATH-B-SLIPPAGE-DERIVATION-001 | `ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md` §7.2 Option A |
| OBS-001 | DexPaprika fallback in exit-price cascade (5th-rank after Redis/BC/Jupiter/Gecko) | 📋 QUEUED | 45m + 24h obs | resilience for `stale_no_price` | none | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §2.2 |
| OBS-002 | Helius `getPriorityFeeEstimate` in `services/execution.py` (replace hardcoded tiers) | 📋 QUEUED | 90m (devnet test included) | live trial reliability | DEVNET-OK | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §2.3 |
| OBS-003 | Vybe pre-entry concentration measurement (metrics-only N days). **2026-04-21 update:** UNBLOCKED by MCP-CAPS-001 — concrete endpoints identified (`/v4/tokens/{mint}/top-holders` + `/v4/tokens/{mint}/holders-count-ts` for historical time-series). Previously blocked by unknown Vybe MCP capabilities. Can execute standalone or bundle into WALLET-REV-001 analysis session. | 🟡 UNBLOCKED | 60m + 7d obs + 45m analysis | TBD — measurement-first | MCP-CAPS-001 ✅ | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §2.4; `docs/audits/MCP_CAPABILITIES_2026_04_21.md` |
| WHALE-001 | ~~Nansen-MCP-based `watched_wallets` refresh job~~ **⛔ SUPERSEDED by WHALE-001-v2 (2026-04-21).** Original scope was Nansen-only. MCP-RECON found Nansen MCP is currently auth-broken (HTTP 401) and Vybe covers the core Solana SM-discovery use case (`/v4/wallets/labeled-accounts`, `/v4/wallets/top-traders`, `/v4/tokens/{mint}/top-pnl-traders`). Revised plan (WHALE-001-v2) does NOT drop Nansen — it layers Nansen as Phase 2 enrichment after SEC-001 Nansen rotation. Retained here for changelog. | ⛔ SUPERSEDED | 120m | — | — | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §2.5; `docs/audits/MCP_CAPABILITIES_2026_04_21.md` |
| WHALE-001-v2 | **Whale Tracker activation (Vybe-first, Nansen-later).** Phase 1 uses Vybe's `/v4/wallets/labeled-accounts`, `/v4/wallets/top-traders`, `/v4/tokens/{mint}/top-pnl-traders`, `/v4/tokens/{mint}/trader-activity` to identify SM cohort on Solana memecoin activity. Phase 2 (post-SEC-001 Nansen rotation) layers in Nansen SM labels + `smart_traders_and_funds_token_balances` as enrichment. Phase 3 wires the cohort into signal_aggregator as a new gate criterion (N-of-M SM wallets buying within time window). Depends on: WALLET-REV-001 (informs SM-discovery calibration) + MCP-CAPS-001 ✅. Supersedes WHALE-001 Nansen-only plan. | 📋 QUEUED | 120-180m CC (Phase 1 alone) | real-time SM-signal gate | WALLET-REV-001 + MCP-CAPS-001 ✅ | `session_outputs/ZMN_MCP_RECON_DONE.md`; `docs/audits/MCP_CAPABILITIES_2026_04_21.md` |
| MCP-CAPS-001 | MCP capability enumeration session — produced `docs/audits/MCP_CAPABILITIES_2026_04_21.md` (408 lines) + use-case-to-tool-name mapping (11 ops). Reference doc for all future ZMN sessions using MCPs. Key finding: Vybe is a full 50-endpoint Solana-analytics surface covering most Nansen use cases. Nansen auth-broken. Birdeye session-expiry on first call is documented known-workaround. | ✅ COMPLETED `1e444bb` + `0c91492` | 25m | future sessions skip MCP-tool guessing | — | `session_outputs/ZMN_MCP_RECON_DONE.md` |
| WALLET-REV-001 | **Reverse-engineer a profitable Solana wallet's strategy (Vybe-powered).** Jay follows a specific wallet believed to use a Speed-Demon-like strategy (pre-grad pump.fun sniping) but with consistent profitability. Pull 30-90d of trades via Vybe's `/v4/wallets/{owner}/pnl-ts` + `/v4/trades`; reconstruct token state at entry time via Vybe's `/v4/tokens/{mint}/holders-count-ts` (historical holder counts), `/v4/tokens/{mint}/candles`, `/v4/tokens/{mint}/trader-activity`; compare entry criteria against current Speed Demon gates (ML≥65, holders≥15, bsr≥3). Output: analysis report identifying features Speed Demon could add or relax + candidate new-gate proposals. **Does NOT depend on Nansen auth** — Vybe covers the core use case per MCP-CAPS-001 discovery. Wallet address provided at session start. Complements (not replaces) WHALE-001-v2. | 📋 QUEUED | 60-90m CC | informs future gate tuning + WHALE-001-v2 cohort discovery | MCP-CAPS-001 ✅ | `session_outputs/ZMN_MCP_RECON_DONE.md`; `docs/audits/MCP_CAPABILITIES_2026_04_21.md` |
| OBS-004 | Playwright headless triage on Win11 (unblocks dashboard regression suite) | 📋 QUEUED | 30m | unblocks DASH-T-001 | none | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §2.6; `DASHBOARD_TESTING_PLAN_2026_04_19.md` |
| OBS-005 | Live-trade table forensics + reconciliation (find why `trades` table writes in TEST_MODE) | ✅ COMPLETED `1b40df3` | — | answered: bot_core.py:793 unconditional INSERT for ML training audit | — | merged into GATE-002 |
| OBS-006 | Add real on-chain `getBalance` polling to `portfolio_snapshots` (mark `market_mode='LIVE_ONCHAIN'`) | 📋 QUEUED | 60m | prevents future "stale snapshot inherited as on-chain balance" confusion | none | `ZMN_LIVE_TRADE_FORENSICS_2026_04_19.md` finding #1 |
| ML-001 | ML training code update — read `corrected_pnl_sol` not `realised_pnl_sol` | 📋 QUEUED | 30-45m | model labels correct after backfill | none | prior `ZMN_ROADMAP.md` READY #2 |
| INFRA-002 | Helius budget enforcement — global `helius_call()` wrapper with Redis daily counter | 📋 QUEUED (HARD DEADLINE 2026-04-23) | 60-90m | prevents next budget exhaustion | none | prior `ZMN_ROADMAP.md` #4 |
| INFRA-003 | Helius enrichment caching (300s TTL on 4 uncached enrichment functions) | 📋 QUEUED | paired with INFRA-002 | reduces Helius spend | INFRA-002 | prior `ZMN_ROADMAP.md` #5 |
| ML-002 | Broader feature default cleanup — audit remaining features in `_build_features` for default-to-zero bug | 📋 QUEUED | 60-90m | prerequisites for ML retrain | none | prior `ZMN_ROADMAP.md` #6 |
| TG-001 | Telegram Yeezus listener audit — determine current Telethon integration state | 📋 QUEUED | 30m | unblocks TG-002 | Telegram credentials may need regen | prior `ZMN_ROADMAP.md` #7 |
| TG-002 | Telegram Yeezus per-source exit schedule override (+300/500/750/1000/2000%) | 📋 QUEUED | 15-30m | enables Yeezus calls | TG-001 | prior `ZMN_ROADMAP.md` #8 |
| ML-003 | Analyst CFGI threshold review (keep / lower / remove the CFGI<20 auto-pause) | 📋 QUEUED | 30m | inform Analyst revival | corrected data available ✅ | prior `ZMN_ROADMAP.md` #12 |
| ML-004 | Analyst re-enable investigation (3 trades id=3670/3879/3893 — all 0-2s holds, all stop_loss_20%) | 📋 QUEUED | 30-45m | go/no-go for Analyst revival | none | prior `ZMN_ROADMAP.md` IN-FLIGHT |
| OBS-007 | Shadow Trading Phase 2 analysis (`shadow:measurements` Redis list — latency dist, TP overshoot, peak gap) | 📋 QUEUED | 45-60m read-only | informs TP / trail tuning | 24h+ shadow data accumulated ✅ | prior `ZMN_ROADMAP.md` IN-FLIGHT |
| INFRA-004 | CFGI Stage 2 cutover — top up cfgi.io credits + verify SOL CFGI repopulates as primary | 📋 QUEUED | 30-45m | trading-mode accuracy | Jay tops up cfgi.io credits | prior `ZMN_ROADMAP.md` IN-FLIGHT |
| LIVE-002 | **Supervised live-enable V5-staged (replaces prior single-window LIVE-002).** Ratcheting sequence: **v5a** (1h supervised intensive) → **v5b** (2-4h supervised) → **v5c** (overnight supervised) → **v5d** (unsupervised overnight). Each stage uses the same env var + code baseline (GATES-V5 landed `1dec17b`); differs only in window duration, trigger tightness, and supervision level. **v5a gates on:** T5a/T5b trigger definitions active (T5-DESIGN-001 landed via V5a prompt); ML-012 elif branch exercised in live mode (startup log "ML-012 FIX: ... ignoring override" present); first-live-trade fee_delta within ±0.005 SOL at 0.15-0.25 SOL sizing; stop_loss_20% exit string writes correctly; reconcile prep script cleans Redis paper state before flip. Each subsequent stage requires the prior stage to complete cleanly (all exit conditions NOT triggers) before escalation. **Wallet target:** 5 SOL (transfer planned 2026-04-21 — current on-chain 0.064 SOL). **v5a success criteria:** (1) supervision yields zero unknown-class errors in Sentry during window; (2) all closed live trades reach `exit_time IS NOT NULL`; (3) net wallet impact within ±0.5 SOL on the 1h window; (4) rollback triggers never fire OR fire correctly if warranted. **v5d requires:** SEC-001 complete (Redis + Postgres + Nansen rotation). **All prior v4-era success criteria preserved** (zero trapped positions, dashboard populated correctly, fee_delta tracking per OBS-011, EXEC-001/EXEC-002 paired routing fix). **v4 attempt outcome (2026-04-20):** ROLLBACK at T+12:00 on T5 trigger (single live trade, -0.094 SOL / 5.68%). Trigger fired on benign position-state — see T5-DESIGN-001. **Pre-flight requirements (all LANDED):** FEE-MODEL-001 ✅ `e078b4c`, CLEAN-003 ✅ FIX-1, ML-012 ✅ `1dec17b`, EXEC-001 + EXEC-002 ✅ `8073257`, GATES-V5 ✅ `1dec17b`, MAX_POSITION_SOL_FRACTION ✅ FIX-1, DASH-RESET ✅ `1176e12`. **Remaining pre-flight:** V5a prompt (delivers T5-DESIGN-001 monitoring loop). | 📋 QUEUED (staged) | v5a 60-90m, v5b 2-4h, v5c 12h obs, v5d open-ended | begins live operation — staged escalation | FEE-MODEL-001 ✅ + CLEAN-003 ✅ + ML-012 ✅ + GATES-V5 ✅ + DASH-RESET ✅ + V5a prompt authoring | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` Tier 3 §3.7; `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`; `session_outputs/ZMN_GATES_V5_DONE.md` |
| OBS-009 | Cache SocialData `-1` result in Redis with 60s TTL at `services/signal_aggregator.py:465-470` to reduce Sentry event rate from ~12/min → ~0.5/min. Pattern B confirmed (silent no-op) so no trading behavior change. Low priority; bundle with ML-009 social-filter rebuild. | 📋 QUEUED | 15m | reduces Sentry noise | none (ML-009 rebuild will supersede) | `SENTRY_TRIAGE_2026_04_19.md` |
| OBS-010 | `portfolio_snapshots` rows tagged `market_mode='LIVE_ONCHAIN'` (added in commit `5ac30cd`) currently carry `daily_pnl_sol` from paper portfolio. Semantic wart — mixes paper P/L with on-chain balance truth. Either (a) add a live P/L tracker and populate, or (b) NULL the column for LIVE_ONCHAIN rows. Future forensics hazard only; low priority. | 📋 QUEUED | 30m | observability hygiene | none | `session_outputs/ZMN_DASH_FIX_DONE.md` leftover concern #3 |
| BUG-020 | Housekeeping: Discord Nansen alert poller at `services/signal_listener.py:844-987` — either disable (Nansen is disabled per `nansen:disabled` Redis key, so poller reads a channel it can't access AND alerts won't arrive) OR re-auth the Discord bot channel read permission. Currently fires ~52 events/24h captured as `ZMN-SIGNAL-LISTENER-1` (403 permission denied). | 📋 QUEUED | 30m | eliminates 1 Sentry noise class | none | `SENTRY_TRIAGE_2026_04_19.md` |
| ML-010 | `fear_greed_at_entry` defaults to `50.0` and `market_mode_at_entry` defaults to current portfolio value on live `paper_trades` INSERTs (close-path Session 2 v2, commit `5ac30cd`) because `Position` dataclass doesn't capture these at entry time. Taints ML retraining input columns for live-era rows. Fix: extend `Position` dataclass to carry `fear_greed_at_entry` + `market_mode_at_entry` captured at `_execute_buy` time. Alternative: accept flat defaults until ML-005 retrain gate (500+ clean samples). | 📋 QUEUED | 30m | ML feature quality (live-era rows) | bundles with ML-005 gate | `session_outputs/ZMN_DASH_FIX_DONE.md` leftover concern #2 |
| OBS-011 | Paper-vs-live PnL delta tracker for live windows. During live windows (LIVE-002 and successors), compute per-trade `on_chain_observed_pnl - paper_expected_pnl` and surface in the monitoring status line. Systematic negative delta > 0.003 SOL/trade indicates fee model under-counts real costs beyond the priority-fee + Jito baseline. Observability only, not a fix. Source of finding: chat-layer fee-model analysis 2026-04-20 (see FEE-MODEL-001). Intended as a Session 5 monitoring-prompt diff; no `services/` change. | 📋 QUEUED | 15m (prompt diff only) | live-window observability | LIVE-002 scheduled (consumer) | chat-layer 2026-04-20 fee-model analysis |
| FEE-MODEL-001 | Expand `paper_trader.fees_sol` calculation. Gap: paper `fees_sol` median is 0.503% of position size across all size buckets — models a single percentage fee only. Missing: (a) real platform fee (pump.fun is 1% each side = 2% round-trip, not 0.5%), (b) priority fee (0.001 SOL fixed per round-trip), (c) Jito tip (0.001-0.005 SOL fixed per round-trip), (d) realized slippage on 95.8% of trades (verified: `realised_pnl_sol = (exit/entry - 1) * amount - fees_sol` holds exactly on 6,087 of 6,352 closed trades). Impact: paper PnL overstates live by ~0.004 SOL/trade at current 0.05 SOL sizing (~8% of position). Break-even position size under realistic costs: 0.009 SOL @ 100% edge retention, 0.019 SOL @ 50%, 0.048 SOL @ 25%. Current `MIN_POSITION_SOL=0.05` sits at the pessimistic break-even — defensible for supervised windows, tight for unsupervised. Fix shape: parameterize as `(pct_platform + pct_lp) * 2 * amount + priority_fee * 2 + jito_tip * 2 + slippage_realized * amount`; env vars per component so real-world calibration feeds back without code changes. **Priority escalation 2026-04-20 (post-Session-5-v4):** first live data point proved the paper fee model understates live by ~96× on pre-grad pump.fun round-trips. Single trade on mint `yh3n441...` at 0.365 SOL showed -0.094 SOL on-chain (25.8% of position) vs +0.002 SOL paper prediction. Dominant gap is slippage (~22.5% of position), not fixed fees. Escalated from Tier 2 (paper fidelity improvement) to **Tier 1 PRE-LIVE BLOCKER**: all Session 5 v5+ live windows blocked until paper model is recalibrated and historical paper edge re-baselined under corrected model. FIX-2 session (commit `e078b4c`) implemented the fix in `services/paper_trader.py` + rebaseline in `docs/audits/PAPER_EDGE_REBASELINE_2026_04_20.md`. SLIPPAGE-CALIBRATION-001 is the ongoing operational loop to refine coefficients from live data. | ✅ COMPLETED `e078b4c` (Tier 1 PRE-LIVE BLOCKER) | 45-60m CC + 7d paper obs recalibration | paper-live edge parity + v5 sizing inputs | EXEC-003 (for default values) | chat-layer 2026-04-20 fee-model analysis; `docs/audits/PAPER_EDGE_REBASELINE_2026_04_20.md` |
| EXEC-003 | Live-execution tip / priority-fee / Jito audit — read-only. `services/execution.py` visible payload in Session 3 audit shows `"priorityFee": "0.0005"` hardcoded on PumpPortal Local sell path. Unknown whether (a) this value is env-parameterized elsewhere, (b) any Jito tip parameter is sent to PumpPortal or Jupiter, (c) `_execute_jupiter` sets `prioritizationFeeLamports` or uses Jupiter's Jito-bundle params. Helius `getPriorityFeeEstimate` is available but not known to be wired in (tracked as OBS-002 separately; that item is the dynamic-fee fix, this item is the audit that informs what to fix). Scope: read all three execution paths (`_execute_pumpportal_local`, `_execute_pumpportal`, `_execute_jupiter`) end-to-end. For each, produce a table: (parameter name, value source, is_parameterized, is_dynamic, matches current API spec). Cross-reference current PumpPortal Local trade-api docs and Jupiter v6 swap API for available-but-unused fields (Jito tip accounts, dynamic priority fee, compute unit limits). Output: `docs/audits/LIVE_EXECUTION_TIPS_FEES_AUDIT.md` + proposed parameterization plan. No code changes; any fixes become separate follow-up sessions (likely EXEC-004 dynamic priority fee via Helius, EXEC-005 Jito tips if supported). Consequential: under-paid priority fee causes failed txs in congestion (eats SOL for zero outcome); absent Jito tip means sells lose to MEV on post-grad tokens. Neither is a safety blocker for first supervised window (wallet bounded), both matter for steady-state profitability. | 📋 QUEUED | 30m CC (read-only audit) | informs FEE-MODEL-001 defaults + execution hardening backlog | none | chat-layer 2026-04-20 fee-model analysis |
| EXEC-005 | Jito tip support on the PumpPortal Local (pre-grad) execution path. `_execute_pumpportal_local` in `services/execution.py` does not call `_send_jito_bundle`; Jito is only wired into `_execute_pumpportal` (post-grad JSON path). ~95% of Speed Demon live entries ship without MEV protection. **v4 evidence (2026-04-20):** live window ran for ~15 min with 1 live trade on pre-grad pump.fun path. No Jito protection on this path was NOT a dominant factor for the single-trade data point — the 22.5% slippage observed is BC price impact, not MEV. EXEC-005 remains Tier 2; its absence did not cause meaningful damage in the v4 window. Marginal cost impact (~0.3% of a 0.365 SOL trade per rebaseline analysis). Re-prioritize from live data once multi-trade samples exist. MEV protection value is real at steady state; schedule organically rather than forcing before v5. | 📋 QUEUED | 60-90m CC + devnet validation | MEV protection on majority of live entries | DEVNET-001 (for validation) | `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`, `docs/audits/PAPER_EDGE_REBASELINE_2026_04_20.md` |
| SLIPPAGE-CALIBRATION-001 | Ongoing slippage calibration from live data. FEE-MODEL-001 (FIX-2, commit `e078b4c`) ships a first-cut size-aware slippage model based on a SINGLE live data point (v4 rollback: -0.094 SOL on 0.365 SOL pre-grad). Better than the old flat model but unlikely to be correct in the tail. As more live trades accumulate, recalibrate: (1) pull all closed live trades; (2) compute `observed_slippage = actual_exit_price - expected_exit_price` per trade; (3) regress against `(amount_sol, bonding_curve_progress, pool)` to produce updated coefficients for `SLIPPAGE_RANGES`; (4) update env vars `PAPER_FEE_*` and in-code `SLIPPAGE_RANGES` tuples. No code change required to the simulator structure — FIX-2 makes everything env-overridable. This is an operational tuning loop, not a bug fix. Recommended cadence: monthly or per 10+ live trades accumulated. | 📋 QUEUED (recurring) | 30m per calibration pass | paper-live edge parity improves over time | FEE-MODEL-001 ✅ | `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`, `docs/audits/PAPER_EDGE_REBASELINE_2026_04_20.md` |
| NANSEN-CLIENT-V2-MIGRATE-001 | **Session B' Path A — migrate 9 remaining `nansen_client` v1 callers to v2 shape.** Session A' (commit `c012475`) fixed the base client + 16 tests; downstream code (`wallet_fetcher.py` + others) still calls v1 signatures. Path A scope: migrate callers only, do not change ML feature semantics yet. Path B (ML feature normalizer adaptation) split out as separate item NANSEN-FEATURE-NORMALIZER-001 to avoid stacking behavioral changes. | 📋 QUEUED | 60m CC | completes Nansen v2 migration (caller side) | Nansen A' ✅ `c012475` | `session_outputs/ZMN_NANSEN_A_PRIME_DONE.md`; `session_outputs/ZMN_NANSEN_B_ABORT.md` (split rationale) |
| NANSEN-FEATURE-NORMALIZER-001 | **Session B' Path B — write `ml_feature_normalizer.py` adapting Nansen v2 response shape to existing ML feature schema.** Separate session from NANSEN-CLIENT-V2-MIGRATE-001 to avoid stacking behavioral changes. Output is a single file mapping v2 response fields → existing feature column names; no downstream code changes beyond the normalizer wire-in. | 📋 QUEUED | 60m CC | enables Nansen-powered ML features post-migration | NANSEN-CLIENT-V2-MIGRATE-001 | `session_outputs/ZMN_NANSEN_A_PRIME_DONE.md` |
| ANALYST-POST-GRAD-001 | **⚠ COST-FIDELITY GATE ADDED 2026-05-14 (by COST-FIDELITY-FINDINGS-DOCUMENTATION-001):** Analyst Phase 0 (paper-mode activation, sub-session (c) below) **must not ship until `PAPER-FEE-MODEL-CALIBRATION-001` is deployed AND ≥7 days of post-calibration paper data has accumulated** — so the Analyst sample is on the recalibrated fee model, not the ~17.6×-optimistic one currently in the training corpus. Rationale: Analyst is ML-driven on mature features at higher position sizing (0.2-0.5 SOL fixed vs SD's 0.05-0.25 SOL); the cost-fidelity gap (per `docs/audits/ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md` §7.4.3 + `docs/findings/COST_FIDELITY_GAP.md`) transfers into a new personality where it bites harder at the larger position scale. Sub-sessions (a) graduation detection + filter scaffold, (b) exit-logic extension, and (d) dashboard widget are NOT gated by this — only (c) paper-mode activation is. **Phase-1 design summary (distilled 2026-04-23 — validated via 2025 dataset + BITFOOT-2026-BASELINE 2026 re-check, for kick-off of the first implementation sub-session):** Entry gate = (DEX ∈ {PumpSwap, Meteora DAMM v2} — Raydium dropped, 0/42 in 2026 sample) AND (top10_concentration_pct ≥ 30) AND (market_cap_usd ∈ [$50k, $300k]) AND (age_post_graduation_s ∈ [30, 300]). **Dropped from design:** Dex Paid rule (untestable — no signal source on GT free tier); pre-grad vol/MC <1.5 (not replicable on fresh graduations; the h24 proxy rejects known winners like NORMIE 29x / Nintondo 33x / RND 17x). **Soft signals (log, don't gate):** post-grad h24 vol/MC; time-to-first-2x; top-10 sub-distribution (th1 dominance vs flat). **ROI calibration:** paper-window observation only — neither 88.9% (BASELINE-A) nor 20.5% (DIP-D) is forward-usable; hit rate must come from paper measurement, not retrospective research. **Execution prerequisite:** Meteora DAMM v2 routing verified in `services/execution.py` before paper activation (2026 data: 44% Meteora among winners vs 10% in 2025 dataset). **⚠ DIP-D reframe (2026-04-23) + BASELINE-A 2026 calibration (2026-04-23):** (1) BITFOOT-DIP-SAMPLE-001 verdict (`session_outputs/bitfoot_analysis/BITFOOT_DIP_SAMPLE_DONE.md`) — Bitfoot labels are NOT reliable ground-truth; 20.5% of unflagged retrievable pings hit 2x+. Strategy design must NOT condition on Bitfoot flagged/unflagged as an entry signal. (2) BITFOOT-2026-BASELINE (`docs/audits/BITFOOT_2026_BASELINE_2026_04_23.md`) — 2-filter Bitfoot (top10 ≥30 + MC $50-300k) still concentrates winners in 2026 trending post-grad data (88.9% 2x+ on n=18, bias-inflated) → edge persists directionally. **Required revisions for design:** (a) DROP `dex_paid 🔴` rule — untestable on current stack, no signal source on GT free tier; (b) DROP pre-grad `vol/MC <1.5` gate — un-replicable without Bitfoot-style pre-grad visibility, and the h24 proxy rejects actual winners (NORMIE 29x / Nintondo 33x / RND 17x all had h24 vol/MC >1.5); (c) EXPECT Meteora to be a meaningful share (44% of 2026 winner set vs 10% of 2025 dataset) — ensure Meteora DAMM v2 routing is validated in execution layer; (d) Raydium absent from 2026 post-grad trending (0/42, vs 39% in 2025) — de-prioritize Raydium entry allocation; (e) ROI must be calibrated from live paper window, not historical Bitfoot math — do NOT use 88.9% hit rate as a forward expectation. See both deliverables' §6 for full guidance. **Bitfoot-inspired Analyst personality for post-graduation Solana tokens** (multi-session build). Strategy shape (from 32,851-ping 10.3-month Bitfoot Era-1 dataset, 2025-01→2025-12, loaded at `session_outputs/bitfoot_analysis/`): **Entry filter — three counterintuitive rules, robust across H1+H2 2025:** (1) Top-10 holder concentration ≥ 30% (higher concentration predicts BETTER outcomes; 32.2% 2x-hit-rate vs 28.7% baseline, stable across both halves); (2) Volume/Market-Cap ratio < 1.5 (low pre-ping turnover is better; vol/MC <0.5 → 35% 2x rate vs vol/MC >8 → 18%); (3) Dex Paid = 🔴 (unpaid) or ignore Dex Paid (paid is an anti-signal — mean peak 1.96x paid vs 2.79x unpaid). **Additional hard filters:** Age 30-300s post-graduation, MC $50-300k, DEX ∈ {PumpSwap, Raydium, Meteora}. **Entry window:** wait 30-60s after detection to verify (fast <1min pumps UNDERPERFORM slow 1-30min pumps; no arms race needed — 83% of total profit capturable with 2-min reaction time, 93% with 1-min). **Exit logic:** staged TP (50% at 2x, 25% at 5x, 25% loose trail); hold duration HOURS not seconds (median time-to-peak 30min; 10-20x median 2.1h; 50-100x median 10h; 100x+ median 1.2d); 24h time-stop; 50% stop-loss. **Sizing:** fixed 0.2-0.5 SOL per trade; Kelly-inspired f\* ≈ 0.05-0.10 wallet fraction; max 10-20 concurrent positions (higher than Speed Demon because hold is longer). **Expected performance (rough, pending BITFOOT-DIP-SAMPLE-001 verification):** ~30-50 trades/mo, +$600-2000/mo under mid-range assumptions; ROI ±30% depending on what the 71% unflagged pings actually did. **Dependencies:** Helius graduation detection (already in signal pipeline), Vybe for top-10 / volume enrichment (free tier sufficient), independent graduation signal source (does NOT depend on Bitfoot Telegram subscription — replicates filter heuristics on own pipeline, bypasses adverse-selection problem of being Nth-in-line). Can be built as a new personality module in signal_aggregator (parallel to Speed Demon, doesn't interfere). Initial deploy as paper-only, observe 2 weeks, decide on live activation. **Sub-sessions:** (a) graduation detection confirmation + filter logic scaffold in signal_aggregator, (b) exit logic extension in bot_core to handle hours-long holds + staged TP variant, (c) paper-mode activation + observation instrumentation, (d) dashboard widget for Analyst-Post-Grad perf separate from Speed Demon. | 📋 QUEUED (multi-session) | 4-6 CC sessions | **highest-return new strategy** based on 10mo labeled dataset | BITFOOT-DIP-SAMPLE-001 (informs ROI confidence), HOLDER-DATA-PIPELINE-001 (if Analyst uses same holder-count feed) | `session_outputs/bitfoot_analysis/` (dataset + scripts); chat 2026-04-22 FINDINGS_ML.md + FINDINGS_TIME_TO_PEAK.md |
| BITFOOT-DIP-SAMPLE-001 | **Resolve Bitfoot 71% unflagged uncertainty.** Pending CC prompt embedded in chat 2026-04-22 (`SESSION_BITFOOT_DIP_SAMPLE.md`). 100 random unflagged pings + 30 control (flagged) → query GeckoTerminal free API for actual post-ping peak multipliers → verdict DIP-A/B/C/D determines whether Bitfoot strategy ROI is −30% (rugs) or +60% (chop) under realistic assumptions. Zero credit cost (free API). Unblocks ANALYST-POST-GRAD-001 performance-confidence estimation. **✅ COMPLETED 2026-04-23 — verdict DIP-D:** 20.5% of 39 retrievable-data rows hit 2x+ (8 winners: BEANER 2.64x / gng 2.27x / MCRN 2.10x / BBC 2.32x / JARS 2.17x / SPT 2.26x / MSPC 2.35x / MCV 5.61x). Bitfoot labels are NOT ground-truth for winners. **Important caveat:** zero losses observed in 39 rows is almost certainly survivorship bias (CoinGecko Public API 180-day retention cap made 55 of 100 sample rows unreachable; GT pool-index curation filters dead pools from results; 5 `no_post_ping_data` rows may represent implicit rugs). Retry used `x-cg-demo-api-key` auth (free Demo tier, zero credits). Pool lookups: 44/44 ok. 0 × 401 after auth. Verdict triggers ANALYST-POST-GRAD-001 reframe (see adjacent entry). | ✅ COMPLETED 2026-04-23 | 35m actual (vs 30-45m est) | **verdict DIP-D — reframes ANALYST-POST-GRAD-001** | none | `session_outputs/bitfoot_analysis/BITFOOT_DIP_SAMPLE_DONE.md`; fetch script at `session_outputs/bitfoot_analysis/fetch_dip_peaks.py` |
| BITFOOT-MONITOR-001 | Prospective monitoring of the current Bitfoot Telegram channel (April 2026) to verify whether the current channel still generates comparable signal quality to the Era-1 dataset. Channel changed format ~2025-12; multi-chain in 2026. Manual 2-week subscription + log capture, then run same analysis against live data. Informs whether Bitfoot-as-live-signal is viable, independent of ANALYST-POST-GRAD-001 (which replicates filters without subscribing). | 📋 QUEUED | 2w observation + 1h analysis | verifies current Bitfoot signal quality | none | `session_outputs/bitfoot_analysis/FINDINGS.md` §context |

---

## Tier 3 — Multi-session projects

| ID | Title | Status | Est | Impact | Depends on | Source |
|---|---|---|---:|---|---|---|
| DASH-001 | Dashboard v2 mobile monitor (RE-SCOPED 2026-05-14 from Concept C "Unified Cockpit" → lightweight glanceable on-the-go monitor at `/m`, **7 cards** (was 6 — biggest-wins added per amendment), mobile-first, PWA-installable; legacy `dashboard.html` retained). Build sessions (re-stated 2026-05-14 with amendment folded in): BUILD-0 (`/api/active-alerts` + `/api/top-wins` + `/api/lifetime-pnl` endpoints, **1.0h**), BUILD-1 (UI scaffold + Cards 1/2/4/**7** + `/m` route, **2.8h**), BUILD-2 (Cards 3/5/6 + **celebration FX ≥3x trigger** + PWA manifest/SW + push handler stub, **2.8h**). | 📋 SCHEDULED (June, parallel with Analyst Phase 0 — NOT May trading-logic critical path) | 3 sessions × ~2.8h = **~8.5h** (was 7.5h) | answers Jay's "alive / making money / on fire" glance on mobile + adds motivational surface (biggest wins + celebration FX); G-01 + G-08 observability surface; G-05 mode-mix safety; does NOT auto-resolve B-001-B-014 in legacy `dashboard.html` | BUILD-1+2 depend on BUILD-0 | `DASHBOARD_DESIGN_REALIGNMENT_001_2026_05_14.md` (active spec — amended 2026-05-14); `DASHBOARD_REDESIGN_2026_04_19.md` superseded-for-scope; `DASHBOARD_ANALYSIS_2026_04_19.md` |
| DASH-BIGGEST-WINS-SCOPING-001 | Revisit Card 7 hardcoded post-C1 paper-only data floor. **Prereq `LIVE-TRADES-LOGGING-AUDIT-001` CLOSED 2026-05-14 (commit `b867daa`)** — `trades` now carries `trade_mode` discriminator, live-side contamination reconciled. Floor remains active because it also guards pre-F1 / pre-C1 paper-side variance + pre-cliff archive accounting. Revisit conditions: after Jay reviews ≥30d of post-C1 sample and decides whether to relax the floor. | 📋 OPEN (Tier 3 🟢) | 15m revisitation after ≥30d post-C1 sample review | data-integrity guardrail | LIVE-TRADES-LOGGING-AUDIT-001 ✅ + Jay-review of post-C1 sample | `DASHBOARD_DESIGN_REALIGNMENT_001_2026_05_14.md` §3 Card 7 + §11 |
| DASH-PUSH-NOTIFICATIONS-001 | Push-notification version of celebration FX when app is closed. Needs backend event emitter on `_close_position` (must NOT be touched during SD validation), Web Push API + VAPID keys + service-worker push handler. v1 ships in-app-only; SW push stub is logged-to-console only. | 📋 OPEN (Tier 3 🟢) | TBD; gated on SD-validation completion + V5A flip stability | nice-to-have engagement; not V5A-blocking | post-2026-05-27 combined eval + V5A flip | `DASHBOARD_DESIGN_REALIGNMENT_001_2026_05_14.md` §3.2 + §11 |
| DASH-CELEBRATION-FX-THRESHOLD-TUNE-001 | After Jay sees ≥3x fire-rate in practice, optionally retune `BIG_WIN_PCT_THRESHOLD` JS constant. Trivial. | 📋 OPEN (Tier 3 🟢) | 5m | tuning to taste | DASH-001 BUILD-2 shipped + observation window | `DASHBOARD_DESIGN_REALIGNMENT_001_2026_05_14.md` §3.1 + §11 |
| DASHBOARD-PUMPPORTAL-HEALTH-PROBE-001 | `dashboard_api.py:2041` reports `service:health.pumpportal=warn ("no signals")` when the `last_signal` Redis key hasn't been written, even though the pipeline is alive — observed during API-MCP-PREFLIGHT-001 (2026-05-20) with `market:new_token_count_1h=10257` and `market:migration_count_1h=67` showing 10K+ signals/hour flowing. The probe should fall back to checking a live counter (e.g. `market:new_token_count_1h > 0` over the last minute, or `signals:evaluated` list length growth) when `last_signal` is absent or stale, instead of warning. Cosmetic operator-facing observability defect — does NOT affect actual signal pipeline or trading. Surfaced because the WARN nearly triggered unnecessary investigation during the V5A flip preflight. | 📋 OPEN (Tier 3 🟢) | 30m | observability fidelity; eliminates a false-WARN that could distract operators during live trading | none | `API_MCP_PREFLIGHT_001_2026_05_20.md` §4 Phase 2 #3 |
| MCP-REFERENCE-CORRECTION-001 | Sweep historical audit docs (`docs/audits/ZMN_RE_DIAGNOSIS_2026_04_19.md`, `docs/audits/ZMN_OPTIMIZATION_PLAN_2026_04_19.md`, `docs/audits/ZMN_CC_HANDOVER_2026_04_19.md`, `docs/audits/CC_TOOL_SURFACE_2026_04_19.md`, `docs/CLAUDE_TOOLING_INVENTORY.md` L5) to add a "(via asyncpg shim — not an actual MCP)" qualifier where "Postgres MCP" is used as shorthand. These are accurate-in-historical-context (the shim *is* the Postgres-MCP-equivalent) but new readers without the context could misread them. CLAUDE.md line 32 already qualified "(once installed)" — no edit needed there. Not blocking anything; documentation hygiene. | 📋 OPEN (Tier 3 🟢) | 20m | docs hygiene; reduces risk of future prompts incorrectly referencing "Postgres MCP" as a callable resource | CLAUDE-MD-MCP-INDEX-001 ✅ (this session) | `CLAUDE.md` "MCP servers available" §Notably absent + Phase 3 grep output from `CLAUDE-MD-MCP-INDEX-001` |
| DASH-CLEAN-DATA-FLOOR-FIX-001 | Fix `BIGGEST_WINS_CLEAN_DATA_FLOOR_TS` constant in DASHBOARD-DESIGN-REALIGNMENT-001 amendment: current value `1747104561.0` is **2025-05-13 02:49:21 UTC** (wrong year by 1). Correct value for documented intent ("C1 deploy 2026-05-13 03:29:21Z UTC") is **`1778642961`**. Without correction, Card 7 "biggest wins" floor would include 2026-04-22 → 2026-05-13 pre-C1 paper data (28 days, 2,966 trades) — defeating the purpose of the hardcoded floor. Found by `STOP-LOSS-20-NO-MOMENTUM-90S-COMBINED-EVAL-001` (2026-05-20) when the eval prompt's §3.1 SQL template used the same wrong value and the first query pass returned 2,966 trades / 28d instead of the expected ~7d post-C1 sample. Same wrong value is also in `AGENT_CONTEXT.md` historical entries referencing the constant. Fix in two places: (a) DASHBOARD-DESIGN-REALIGNMENT-001 amendment doc + (b) any reference to `BIGGEST_WINS_CLEAN_DATA_FLOOR_TS` literal in DASH-001 BUILD-0/BUILD-1 endpoints + Card 7 frontend. Verify by direct unix→datetime computation: 2026-01-01 UTC = 1767225600 unix; + 132 days × 86400 = 1778630400; + 3h29m21s = 1778642961. | 📋 OPEN (Tier 3 🟢) | 15m doc fix + ensure BUILD-1 uses correct constant | docs accuracy + correctness when DASH-001 BUILD-1 ships Card 7 | DASH-001 BUILD-1 (must land before Card 7 ships) | `STOP_LOSS_20_NO_MOMENTUM_90S_COMBINED_EVAL_001_2026_05_20.md` §10 + `DASHBOARD_DESIGN_REALIGNMENT_001_2026_05_14.md` §3 Card 7 |
| PAPER-LATENCY-MODEL-001 | Add synthetic signal→fill delay + price-refetch to `paper_buy` to model real on-chain submit-land latency. Either backfill the 4 latency columns first OR add a synthetic delay-and-refetch step. Closes the zero-latency optimism in paper fills. | 📋 OPEN (Tier 3 🟢) | 60-90m + 7d obs | secondary sim-to-real fix (cost gap is dominant per audit §5); recommended before Analyst at scale | LATENCY-OBSERVABILITY-001 (column backfill, currently 100% NULL on 2874 rows) | `ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md` §7.3 |
| PAPER-MEV-SLIPPAGE-MODEL-001 | Add tiered MEV/sandwich/in-flight-pump slippage component to `_simulate_slippage` (currently captures spread but not MEV). Structural addition (not just env knob). Defer until ≥10 Path B rows support per-component calibration. | 📋 OPEN (Tier 3 🟢) | 60-90m + calibration window | refines the cost model beyond what env-knob recalibration achieves; lower priority than PAPER-FEE-MODEL-CALIBRATION-001 | ≥10 Path B rows + PAPER-FEE-MODEL-CALIBRATION-001 baseline | `ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md` §7.2 Option B |
| ML-TRAINING-MODE-FILTER-001 | Decide whether `ml_model_accelerator.train` includes/excludes/weights `trade_mode='live'` rows now that the discriminator exists. **RE-SCOPED 2026-05-14 (ML-TRAINING-COST-FIDELITY-AUDIT-001 §7.1)** from open question → Tier 3 🟢 hygiene only: with 41/8680 ≈ 0.47% live share, mode-filtering is a rounding error on the training signal. Fidelity problem lives in the paper sim, not the paper/live blend. Land for clarity, NOT as a V5A or retune prerequisite. | 📋 OPEN (Tier 3 🟢) | 20m | hygiene; not material to retune outcome | LIVE-TRADES-LOGGING-AUDIT-001 ✅ | `LIVE_TRADES_LOGGING_AUDIT_001_2026_05_14.md` §9.1 + `ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md` §7.1 |
| ML-CONTAMINATION-FILTER-BIAS-001 | Targeted investigation: does the existing contamination filter (`exit_price BETWEEN entry_price * 0.97 AND 1.03`) disproportionately remove rows where sim-cost flipped marginal outcomes from win to loss near zero? Surfaced as unresolved §9.3 of cost-fidelity audit, not investigated. | 📋 OPEN (Tier 3 🟢) | 45m read-only DB analysis | confirms or refutes a possible sim-flattering bias in the training corpus | none | `ML_TRAINING_COST_FIDELITY_AUDIT_001_2026_05_14.md` §9.3 |
| DASH-T-001 | Dashboard regression suite (Playwright + webapp-testing skill) — covers B-001 to B-014 | 📋 QUEUED | 3-4 hours | prevents dashboard regression | OBS-004 (Playwright triage) | `DASHBOARD_TESTING_PLAN_2026_04_19.md` |
| WHALE-002 | Whale Tracker entry-signal wiring (after `watched_wallets.last_active_at` fresh). **2026-04-21 dependency update:** upstream WHALE-001 superseded by WHALE-001-v2. Re-point: WHALE-002 now depends on WHALE-001-v2 Phase 3 (signal wiring). Scope unchanged. | 📋 QUEUED | 90m | activates Whale Tracker as entry-decision driver | WHALE-001-v2 Phase 3 | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §3.2 |
| MCP-001 | PumpPortal MCP build (read-only tools: `getTokenStats`, `getRecentTrades`, `getNewLaunches`, `getBondingCurveState`) | 📋 QUEUED | 90-120m | unlocks per-mint forensics; Whale Tracker signal | none | `MCP_BUILDER_CANDIDATES_2026_04_19.md` #1 |
| SKILL-001 | Build `zmn-trade-analysis` skill (corrected_pnl_sol gotcha + 7d exit-reason / ML-band queries + wallet-drain forensics pattern) | 📋 QUEUED | 75m | future sessions skip rediscovery | none | `ZMN_OPTIMIZATION_PLAN_2026_04_19.md` §3.4; `MCP_BUILDER_CANDIDATES_2026_04_19.md` |
| ML-005 | ML model retrain on corrected labels | 📋 QUEUED (BLOCKED) | 120m when unblocked | 70+ clean post-fix samples accumulating | 500+ clean samples (ETA 2026-04-25 to 05-05) | prior `ZMN_ROADMAP.md` #10 |
| ML-006 | FEATURE_COLUMNS pruning (55 → ~20 populated features) | 📋 QUEUED | paired with ML-005 | reduces noise pre-retrain | ML-005 | prior `ZMN_ROADMAP.md` #11 |
| WHALE-003 | Smart Money Wallet Mining sanity check (winner count >= 50 AND backfill done). **2026-04-21 note:** Nansen-side dependency contingent on SEC-001 Nansen rotation; Vybe-side work unblocked via WHALE-001-v2. | ⏸️ DEFERRED | TBD | trigger-gated | winner count >= 50 AND backfill done ✅; Nansen-layer needs SEC-001 | prior `ZMN_ROADMAP.md` #13 |
| WHALE-004 | Smart Money Wallet Mining curation pipeline. **2026-04-21 note:** Nansen-side dependency contingent on SEC-001 Nansen rotation. | ⏸️ DEFERRED | 90-120m | refines Whale Tracker source | WHALE-003 + SEC-001 for Nansen | prior `ZMN_ROADMAP.md` #14 |
| WHALE-005 | Smart Money Webhook Monitoring (Helius webhooks for watched wallets). **2026-04-21 note:** Nansen enrichment depends on SEC-001 rotation. | ⏸️ DEFERRED | TBD | real-time whale signals | WHALE-004 + Helius credit reset (April 26) + SEC-001 | prior `ZMN_ROADMAP.md` #15 |
| WHALE-006 | Smart Money Entry Trigger Rule (ENTRY decision based on whale activity). **2026-04-21 note:** WHALE-001-v2 Phase 3 is the first-cut signal-wiring (Vybe-powered); WHALE-006 is the stricter downstream refinement. | ⏸️ DEFERRED | TBD | trade off whale signal directly | WHALE-005 stable for 7 days | prior `ZMN_ROADMAP.md` #16 |
| WHALE-007 | Nansen Day-1 Enablement (re-enable Nansen for Analyst signal source). **2026-04-21 note:** Explicitly blocked on SEC-001 Nansen rotation. | ⏸️ DEFERRED | TBD | depends on Analyst revival | ML-004 unpauses Analyst OR WHALE-004 needs Nansen + SEC-001 for Nansen | prior `ZMN_ROADMAP.md` #17 |
| ML-007 | Analyst personality rework (50-100k pullback strategy) | ⏸️ DEFERRED | 2-5 sessions | rebuilds Analyst from scratch | ML-003 + Helius credits + WHALE-005 stable | prior `ZMN_ROADMAP.md` #18 |
| ML-008 | ~~Vybe investigation (only if Helius + Nansen aren't enough)~~ **⛔ SUPERSEDED by MCP-CAPS-001 + WHALE-001-v2 (2026-04-21).** MCP-CAPS-001 enumerated Vybe's 50 endpoints; WHALE-001-v2 incorporates Vybe as primary Solana-analytics surface. No separate "Vybe investigation" needed. | ⛔ SUPERSEDED | TBD | — | — | `docs/audits/MCP_CAPABILITIES_2026_04_21.md` |
| ML-009 | Social filter (Speed Demon Option C strict — Twitter required Stage 1, 90d age + 3k followers Stage 2) | 📋 QUEUED | 75-90m | additive entry filter | ML-001 + first stable post-TP day | prior `ZMN_ROADMAP.md` READY #3 |
| MCP-002 | Jito MCP build (getBundleStatus, getRecentTipFloor, getValidatorList) | ⏸️ DEFERRED | 60-90m | partial overlap with Helius MCP — only if needed during live debugging | none | `MCP_BUILDER_CANDIDATES_2026_04_19.md` #2 |
| MCP-003 | SocialData MCP build | ⏸️ DEFERRED | 60-75m | low ROI until ML-009 ships | ML-009 | `MCP_BUILDER_CANDIDATES_2026_04_19.md` #3 |
| CLEAN-001 | Archive 30 root-level session-report `.md` files to `docs/archive/` | 📋 QUEUED | 30m | root directory readability | none | `DEPLOYMENT_BLOAT_2026_04_19.md`; `ORPHAN_FILES_2026_04_19.md` |
| REFACTOR-001 | Split `pos.trade_id` into explicit `paper_trade_id` + `trades_log_id` fields on `Position` dataclass. In paper mode `trade_id` currently points to `paper_trades.id`; in live mode to `trades.id`. Id-spaces overlap (paper 1..5974, trades 1..3175) — caused a near-corruption bug caught during Session 2 v2 Phase 1 recon (`UPDATE paper_trades WHERE id=pos.trade_id` would have touched an unrelated paper row in live mode). Update all call sites. Fix is touched-by (DASH-ENTRY-001) so consider doing both in one go. | 📋 QUEUED | 2-3 hours | eliminates id-space ambiguity | touches DASH-ENTRY-001 scope if bundled | `session_outputs/ZMN_DASH_FIX_DONE.md` leftover concern #1 |
| CLEAN-002 | requirements.txt audit (find unused deps) — needs runtime introspection | ⏸️ DEFERRED | 60-90m | smaller container images | none | `DEPLOYMENT_BLOAT_2026_04_19.md` |
| DEVNET-001 | Add devnet routing override to `services/execution.py`. Gap: Session 3 Phase 2 (devnet Jupiter buy→sell validation) was blocked because `services/execution.py` hardcodes mainnet URLs for Jupiter (`api.jup.ag/swap/v2/order|execute`) and PumpPortal (`pumpportal.fun/api/trade-local`) endpoints; only Helius RPC is env-configurable. Public devnet faucet was also rate-limited at the time — orthogonal blocker. Fix shape: add env-var overrides for `JUPITER_ORDER_URL` / `JUPITER_EXECUTE_URL` / `PUMPPORTAL_LOCAL_URL` with mainnet defaults. Session 3-style devnet validation sessions can then actually execute the full cycle. Low priority — unblocks future sell-path validation sessions, not a live-path concern. | 📋 QUEUED | 60m CC | unblocks devnet validation | none | `docs/audits/SELL_PATH_DEVNET_VALIDATION_2026_04_19.md` Phase 2 blockers + A2.3 proposal |
| OBS-012 | Dashboard "ml_score" column source verification + alignment with gate-comparison value. ML-012 investigation (FIX-1) found the dashboard's "ML" column on the Speed Demon signal feed may display something other than the score actually gated on (e.g., model A's output while gate uses model B, or a composite rendering a sub-score). Session 5 v4 live trade showed displayed `ml=31.5` while configured `ML_THRESHOLD_SPEED_DEMON=40` — ML-012 resolved the bypass mechanism (AGGRESSIVE_PAPER_TRADING override) but the dashboard display alignment is a separate concern. Even after ML-012 lands, operators watching the dashboard can't tell when a trade legitimately passes the gate unless the display matches the gated value. Scope: (1) trace `ml_score` column source in dashboard SQL → confirm it reads the gate-comparison value, not a cosmetic sub-score; (2) if mismatch, align or annotate with the bypass reason. | 📋 QUEUED | 15-20m | operator trust in dashboard display | ML-012 | `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`, `docs/audits/ML_BYPASS_INVESTIGATION_2026_04_20.md` |
| DEPLOY-DISCIPLINE-001 | Railway auto-deploy latency investigation. Session 5 v4 attempt: Railway `set-variables` at 20:35:57 UTC did not trigger `bot_core` redeploy until ~20:45 (~10 min latency vs typical 60-90s). Known Railway behavior — env-var changes do not always trigger immediate redeploy. For live-window sessions where timing matters, choose one of: (a) use `railway up -s bot_core` explicitly after env change to force redeploy (conflicts with CLAUDE.md "no railway up AND git push in same session" — would need rule carve-out); (b) monitor Railway MCP deploy status polling immediately after `set-variables` to detect whether a redeploy fires, fall back to `railway up` if not; (c) document the observed behavior in CLAUDE.md so future Session 5 prompts account for variable-set-to-redeploy latency. No code change; procedure + documentation only. | 📋 QUEUED | 20-30m (diagnostic only) | predictable live-window timing | none | `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`, `session_outputs/ZMN_LIVE_ROLLBACK.md` |
| BITFOOT-ML-FEATURE-INTEGRATION-001 | **Cross-pollinate Bitfoot-derived features into ZMN's existing ML feature set.** Specifically: `top10_concentration_pct` as a POSITIVE predictor (flip sign if currently treated as rug-warning); `vol_mc_ratio` as a NEGATIVE predictor at high values; `dex_paid_status` as negative or neutral (not positive) feature; `age_at_signal_seconds` with sweet-spot non-linear encoding (30-60s best, >10min worse); time-to-first-2x as a SIZE feature — slower pumps ultimately yield bigger peaks, so sizing can scale with "patience expected". Implementation: add as features in signal_aggregator ML scoring, retrain ZMN ML model on combined dataset (paper_trades_archive + Bitfoot labeled set), evaluate lift. Multi-session. | 📋 QUEUED | 3-4 CC sessions | improves Speed Demon + Analyst ranking quality | ANALYST-POST-GRAD-001 first-observation data | `session_outputs/bitfoot_analysis/FINDINGS_ML.md` |

---

## Open threads (surfaced but not yet tiered — review each session)

| Thread | Status | Notes |
|---|---|---|
| `ML_THRESHOLD_SPEED_DEMON` env-var split (bot_core=30 unused vs signal_aggregator=40 gating) | merged into TUNE-003 | confirmed via Railway MCP `list-variables` |
| Birdeye MCP session expiry on first call | open | requires session-refresh story for any auto-loop using Birdeye. **2026-04-21 update:** documented as known workaround in `docs/audits/MCP_CAPABILITIES_2026_04_21.md` Known Auth section — pattern is "warmup call with retry-loop before actual query". No current dependency. |
| `bot_core.py:793` unconditional `INSERT INTO trades` (dual-write, not a security bug — recording duplication) | open (merged into LOW finding) | per `ZMN_LIVE_TRADE_FORENSICS_2026_04_19.md` finding #3 — either document the dual-purpose or add a `mode` column to `trades` |
| `live_trade_log` retention (9,044 historical errors from v4 era make "errors in last 7d" queries misleading) | open | per forensics LOW finding — TTL or docs note |
| Dashboard on-chain balance widget (forensics LOW finding #5) | open | bundle into DASH-001 |
| `2gfHQ…` holding wallet 0.098 SOL — does treasury sweep ever clear it? | open | low-priority unknown |
| Reconcile-on-mode-flip residual (CLAUDE.md flags as "discipline to codify") | **resolved as tracked** — CLEAN-003 ✅ landed FIX-1 (`scripts/live_flip_prep.sh` + CLAUDE.md "Live-flip pre-flight procedure" section). Sell-storm circuit breaker remains the downstream safety net. |
| Historical pre-grad BC state at timestamp T (no MCP covers this) | open | surfaced as gap in MCP-CAPS-001 reference doc. No MCP returns "what was BC progress on mint X at time T". Workarounds: our own `token:reserves:{mint}` Redis cache (30-min TTL) or on-chain `getAccountInfo` at historical slot (expensive, not historically complete). Affects WALLET-REV-001 analysis for trades older than the Redis cache retention. Acknowledge in WALLET-REV-001 prompt; solve later if it blocks. |
| ZMN-SIGNAL-AGGREGATOR-1 SocialData credits exhausted (Sentry-captured) | **resolved as tracked** — Session 1 confirmed Pattern B (silent no-op, no trades rejected). Silenceable in Sentry UI via OBS-008; code-side caching optional via OBS-009. Deferred to ML-009 social-filter rebuild. |
| Session 1 regression checks (Solders `.sign()`, missing Helius URL, PumpPortal HTTP 400) | **resolved as tracked** — all 3 show 0 events in 14-day Sentry window. cd266de Helius URL fix + Solders 0.21 migration both holding. `SENTRY_TRIAGE_2026_04_19.md`. |
| Session 2 v2 `pos.trade_id` semantic overload (paper → paper_trades.id, live → trades.id; id-spaces overlap) | **tracked as REFACTOR-001** — fix Tier 3; interim mitigation landed in commit `5ac30cd` (use INSERT not UPDATE at live close so id overlap doesn't corrupt paper rows). |
| Session 2 v2 `fear_greed_at_entry` / `market_mode_at_entry` defaulting on live rows | **tracked as ML-010** — bundle with ML-005 retrain gate. |
| Session 2 v2 `LIVE_ONCHAIN` portfolio_snapshots carry paper `daily_pnl_sol` | **tracked as OBS-010** — future forensics hazard only; low priority. |
| Session 2 v2 Open Positions widget for live mode empty by design until DASH-ENTRY-001 lands | **tracked as DASH-ENTRY-001 (Session 2b, PRE-LIVE BLOCKER)** — 30-45 min CC session to add live entry path INSERT to paper_trades. |

---

## Completed items (recent — for changelog reference)

| Date | ID | Title | Commit |
|---|---|---|---|
| 2026-04-21 | DASH-RESET | Paper_trades archive + reset — preserve 2,923-trade history in `paper_trades_archive_20260421`, wipe `trade_mode='paper'` rows, reset Redis `bot:portfolio:balance` to `STARTING_CAPITAL_SOL=20.0`. Clean-slate accumulation starts 2026-04-21 post-GATES-V5 deploy. Live rows preserved untouched. | `1176e12` |
| 2026-04-21 | MCP-RECON | MCP capability enumeration + 408-line reference doc `docs/audits/MCP_CAPABILITIES_2026_04_21.md`. Key finding: Vybe is a full 50-endpoint Solana-analytics surface (not previously surveyed); covers most Nansen use cases. Nansen MCP HTTP 401 (auth broken). Birdeye session-expiry on first call documented. Use-case-to-tool mapping (11 ops). | `1e444bb` + `0c91492` (redaction) |
| 2026-04-21 | GATES-V5 | ML-012 fix (AGGRESSIVE_PAPER_TRADING gated on TEST_MODE=true) + 4 new entry gates (HOLDER_COUNT_MIN=15, BUY_SELL_RATIO_MIN=3.0, PRE_FILTER_SCORE_MIN=1.15, CFGI_MIN=20 universal) + stop-loss tightened 0.35→0.20 via STOP_LOSS_PCT env + size caps (MAX_POSITION_SOL=0.25, SPEED_DEMON_BASE=0.15, MAX=0.25). ML_THRESHOLD_SPEED_DEMON raised to 65. Runtime HOLDER-reject lines confirmed new-code activation. | `1dec17b` |
| 2026-04-20 | Session 3 | Sell-path devnet validation + PumpPortal Local spec audit. **Verdict: B-conditional** (amended 2026-04-20 post-session) — spec matches code, BUT audit did not investigate whether routing state is refreshed between buy and sell. Subsequent evidence (`paper_trades.platform` uniformly `pump.fun` across 6,352 rows; `bonding_curve_progress` frozen at signal-discovery time in sampled `features_json`) indicates the 261 HTTP 400s are predominantly stale-routing sells of graduated tokens, not the "transient" causes originally named. Spec-vs-code match finding itself remains valid. Root-cause + fix tracked as **EXEC-001** + **EXEC-002** (paired). Phase 2 blocked (faucet + structural URL hardcoding — DEVNET-001). No code change this session. | `fdd8aa8` |
| 2026-04-19 | Session 2b | DASH-ENTRY-001 live-entry paper_trades INSERT + close-path UPDATE refactor | `dafe1fa` |
| 2026-04-19 | Session 2 v2 | Dashboard live close-path write-back (paper_trades INSERT + `LIVE_ONCHAIN` portfolio_snapshots + Redis `bot:onchain:balance`) | `5ac30cd` |
| 2026-04-19 | Session 1 | Sentry triage — 5 unresolved issues classified (all Class A noise), 3 regression checks PASS (Solders, Helius URL, PumpPortal) | `35bdfe6` |
| 2026-04-19 | GATE-003 | Sentry SDK integration across 8 services (8 projects in rz-consulting org) | `cb45d6b` |
| 2026-04-19 | GATE-002 | Live trade forensics — Verdict A: phantom drain, real v4 cost ~3.4 SOL | `1b40df3` |
| 2026-04-19 | GATE-001 | Durable rules refresh — TEST_MODE flip is now session-gated | `e9de6d7` |
| 2026-04-19 | (audit) | Deep recon — 7 audit docs covering tool surface, re-diagnosis, optimization plan, handover, dashboard, mcp-builder candidates | `4a37598` |
| 2026-04-19 | (audit) | MCP fixes — Vybe MCP URL corrected to docs.vybenetwork.com/mcp | `d7ae512` |
| 2026-04-19 | (audit) | GitHub MCP PAT-based replaces Copilot MCP | `b6083ab` |
| 2026-04-19 | (audit) | 4 broken MCP registrations corrected | `41192b0` |
| 2026-04-17 | LIVE-pre-001 | Helius URL resolver (3-tier fallback) + sell-storm circuit breaker (cd266de) | `cd266de` |
| 2026-04-17 | (live trial) | Live trial v3 — signing verified on mainnet, 0 SignatureFailure / 83 attempts | `cd266de` |
| 2026-04-17 | (live trial) | Live trial v4 — 4+ TX_SUBMITs on-chain, wallet 5.0 → 1.6 SOL (~3.4 SOL traded) | `cd266de` |
| 2026-04-17 | DASH (Tier 1) | Dashboard mode filter + MCAP columns + LIVE view honest | (commits in roadmap COMPLETED RECENTLY section) |
| 2026-04-15 | BUG-011 | `paper_trades.outcome` column NULL — RESOLVED (backfill 2,966 rows) | `77d6a8a`, `429dd87` |
| 2026-04-14 | (recovery) | signal_aggregator restored after 21-hour outage; hardened with retry + heartbeat | `85768c5` |
| 2026-04-14 | INFRA (CFGI) | cfgi.io Stage 1 dual-read deployed | `146ca38`, `859c0fa`, `1ac9cb8` |
| 2026-04-13 | (audit) | Dashboard Tier 1 audit + P/L source fixes | `dbbffd3`, `40dadb6`, `cac5202` |
| 2026-04-13 | (data) | Historical paper_trades backfill | `cf16627`, `2f76a91` |
| 2026-04-12 | TUNE | Feature default fix (BSR/wallet_velocity) | `a8a390b` |
| 2026-04-12 | TUNE | Staged TP reporting fix | `5b92226` |
| 2026-04-11 | TUNE | Entry filter v4 | `56421ab` |
| 2026-04-09 | TUNE | Exit strategy fix | `bf57117` |
| 2026-04-07 | TUNE | Paper trader price bug fix | `9b880e1` |
| 2026-04-19 | CLEAN-pre | js/ orphan files deleted (~4.2 MB off every Railway deploy) | (in deep recon commit set) |

### Completed staged-progression items (#9.5 - #9.7)

| Original ID | Status | Notes |
|---|---|---|
| #9.5 Execution Path Audit | ✅ COMPLETED 2026-04-16 | `EXECUTION_AUDIT_2026_04_16.md` — found solders signing API drift, drove v1/v2 fix |
| #9.6 Shadow Mode Implementation | ⛔ SUPERSEDED | Shadow measurements collected via Redis instrumentation; formal `SHADOW_MODE` flag never built — v3 verified path directly on mainnet |
| #9.7 Micro-Live Validation | ✅ COMPLETED 2026-04-16/17 | Done on main wallet at 0.05 SOL position (skipped originally-planned secondary wallet) |
| #9.8 Real-Size Live on Main Wallet | 🟡 IN_PROGRESS | **~3.4 SOL net cost in v4 window** (corrected 2026-04-19 per forensics `1b40df3`; prior "1.32 SOL" figure under-reported by ~2.5×). Wallet 5.0 → ~1.6 SOL end-of-v4. Paused pending LIVE-001 + DASH-ENTRY-001 + SEC-001 + Session 3 devnet, then LIVE-002. |

---

## Deferred / Superseded items

| ID | Title | Reason | Source |
|---|---|---|---|
| DROPPED-001 | Kronos Foundation Model Integration | Wrong latency, wrong pretraining domain. ZMN's edge is on-chain features, not OHLCV. | prior `ZMN_ROADMAP.md` DROPPED |
| DROPPED-002 | Original "Subscribe to Nansen-labeled SM" architecture | SM labels don't exist at pump.fun scale (Finding 3) | prior `ZMN_ROADMAP.md` |
| DROPPED-003 | ML Feature Expansion (pre-retrain) | Returns as option after ML-005 retrain | prior `ZMN_ROADMAP.md` |
| DROPPED-004 | ML Score Cap at 65 | Was symptom-fix for "score inversion" claim now SUPERSEDED by data showing higher scores win more | prior `ZMN_ROADMAP.md`; `ZMN_RE_DIAGNOSIS_2026_04_19.md` ML band table |
| DROPPED-005 | Kronos ASX Equities Bot | Parked as separate project | prior `ZMN_ROADMAP.md` |
| ⛔ MCP-LETSBONK | LetsBonk MCP build | Low ROI; LetsBonk volume tiny fraction of pump.fun's ~$50M/day | `MCP_BUILDER_CANDIDATES_2026_04_19.md` #4 |
| ⛔ DASH-PATCH | Patch dashboard B-001 to B-014 individually | Per `frontend-design` skill: rebuild-not-patch (DASH-001) — every B-fix risks compounding the design debt that caused the bugs | `DASHBOARD_ANALYSIS_2026_04_19.md` |
| ⛔ MCP-RUGCHECK | Rugcheck MCP registration | Not on npm; current `services/signal_aggregator.py` calls rugcheck.xyz API directly with `RUGCHECK_REJECT_THRESHOLD=2000` env gate — no MCP needed | inferred from `.mcp.json` audit + env vars |
| ⛔ DOCKERIGNORE | Add `.dockerignore` | Nixpacks (current builder) ignores `.dockerignore` — no effect | `DEPLOYMENT_BLOAT_2026_04_19.md` |
| ⛔ ML_INVERSION | "ML inverts above 40" guidance | Pre-2026-04-12 claim, superseded by 7d data (every band 30→80+ profitable, WR climbs with score) | `ZMN_RE_DIAGNOSIS_2026_04_19.md` pain 2; CLAUDE.md ML-state block |
| ⛔ STAGED-CHAIN-RULE | "Do not skip stages" rule for live trading | Overtaken by events on 2026-04-16/17 (live trials v3+v4); replaced by CLAUDE.md "Live trading mode — session-gated" | `e9de6d7` rules refresh |

---

## Source audit cross-references

When investigating an item's evidence, open the linked audit doc:

| Audit doc | What it covers | Items it informs |
|---|---|---|
| `docs/audits/CC_TOOL_SURFACE_2026_04_19.md` | Per-MCP smoke tests, JSON-typing failure modes, skill descriptions | INFRA-001, MCP-001, OBS-004 |
| `docs/audits/ZMN_RE_DIAGNOSIS_2026_04_19.md` | 6 pain points re-examined with fresh data | TUNE-001/002/003, OBS-001/003, WHALE-001 |
| `docs/audits/ZMN_OPTIMIZATION_PLAN_2026_04_19.md` | Tier 1/2/3 ranked plan with expected SOL impact | TUNE-*, OBS-*, WHALE-001, LIVE-001/002, MCP-001 |
| `docs/audits/ZMN_CC_HANDOVER_2026_04_19.md` | Single-file context pack (state snapshot + open threads) | All — meta |
| `docs/audits/ZMN_LIVE_TRADE_FORENSICS_2026_04_19.md` | Wallet drain forensics — Verdict A | DOCS-001, OBS-006, GATE-002 |
| `docs/audits/ZMN_LIVE_ENABLE_HANDOFF_2026_04_19.md` | Abort report with paste-able Steps A-I checklist | LIVE-002 |
| `docs/audits/DASHBOARD_DESIGN_REALIGNMENT_001_2026_05_14.md` | **Active spec for DASH-001** (re-scoped to mobile-first 6-card monitor at `/m`; 3 build sessions × 2.5h; June parallel-track) | DASH-001 |
| `docs/audits/DASHBOARD_REDESIGN_2026_04_19.md` | Current state inventory + Concept A/B/C — **SUPERSEDED for scope** by DASHBOARD_DESIGN_REALIGNMENT_001 2026-05-14 | DASH-001 (historical) |
| `docs/audits/DASHBOARD_ANALYSIS_2026_04_19.md` | frontend-design skill applied to dashboard bugs (rebuild-not-patch — rationale still valid) | DASH-001 |
| `docs/audits/DASHBOARD_TESTING_PLAN_2026_04_19.md` | Playwright regression suite plan (blocked on stability; test list shrinks under 2026-05-14 re-scope) | DASH-T-001, OBS-004 |
| `docs/audits/MCP_BUILDER_CANDIDATES_2026_04_19.md` | 4 candidate MCPs scoped (PumpPortal/Jito/SocialData/LetsBonk) | MCP-001/002/003, MCP-LETSBONK |
| `docs/audits/DEPLOYMENT_BLOAT_2026_04_19.md` | Railway upload audit | CLEAN-001/002 |
| `docs/audits/ORPHAN_FILES_2026_04_19.md` | Orphan file scan | CLEAN-001 |
| `docs/audits/SECRETS_SCAN_2026_04_19.md` | Tracked-files secrets scan (clean) | SEC-002, SEC-003 |
| `session_outputs/ZMN_SENTRY_INTEGRATION_DONE.md` | Sentry SDK integration outcome | GATE-003 |
| `session_outputs/ZMN_SENTRY_DSNS.md` (gitignored) | DSN reference for the 8 Sentry projects | GATE-003 reference only |

---

## Operating principles (preserved from prior roadmap)

- **One substantive lever per session** — multiple parallel changes make failure attribution impossible
- **Verification windows BEFORE tuning** (24h+ minimum) — don't tune without observation data
- **All deploys must have auto-revert conditions** — `git revert <hash>` is the rollback
- **All thresholds env-var-configurable** for kill switch — never hardcode trading parameters
- **Read-only diagnostic prompts BEFORE write prompts** on ambiguous items
- **Never deploy multiple Railway services simultaneously** (per CLAUDE.md)
- **No `railway up` AND `git push` in the same session** (duplicate deploys waste build minutes)
- **Update this roadmap at the end of every session** — append a changelog entry

---

## Review cadence

- **Daily during active deploy weeks** — check top-3 priority Tier 1 items
- **Weekly when stable** — review all 📋 QUEUED + ⏸️ DEFERRED items, re-date or promote
- **Triggered** — when a `Depends on` clears, re-evaluate that item immediately

Items sitting in ⏸️ DEFERRED for 30+ days without a status change get either DROPPED or re-scheduled with a new trigger. No drift.

---

## Changelog

- **2026-06-03** (§B Phase-1 #7 FIX-EXEC-001-002-ROUTING `3e4b502` — **PHASE 1 CODE-COMPLETE**) — `bot_core.py`. Removed the `bonding_curve_progress > 0` gate so `_check_pool_state_fresh` runs for every live sell → fixes D02-F4 (whale/Raydium bc=0 → 400 on pump.fun path) + D03-F3 (reconciled positions lose bc_progress → stale routing); helper returns 1.0/0.0 so refresh always routes correctly; avoids a schema migration; fail-closed-to-stale. py_compile PASS; 4/4 structural. **§B Phase 1 (live-execution correctness) DONE in code: #4+#8, #5+D02-F8, #6, #7 — all flip-confirmed-only (live branch).** Deferred follow-ups filed: SELL-STORM-PARK-PERSISTENCE-001 (D04-F10), EXEC-FORCE-ABANDON-001 (D03-F8), JITO-REIMPLEMENT-001. Next: §B Phase 2 (safety rails). Rollback: `git revert 3e4b502`.
- **2026-06-03** (§B Phase-1 #5 FIX-PARTIAL-SELL-SIZING +D02-F8 `3b6d0b1` — staged-TP live sells) — `execution.py` + `bot_core.py`. D02-F5: pre-grad sell hardcoded "100%" → dumped the whole bag on every partial/staged-TP; D02-F8: Jupiter sell sold full balance. Threaded `sell_fraction` through `execute_trade` → both routes; `_close_position` passes `sell_fraction=sell_pct`; pre-grad sends "X%", Jupiter `int(balance*frac)`; full closes unchanged at 100%. py_compile PASS; arithmetic 10/10 + 4/4 structural. NOT paper-observable; flip-confirmed. One lever. Rollback: `git revert 3b6d0b1`. Next: #7 EXEC-001/002 routing (final Phase-1).
- **2026-06-03** (§B Phase-1 #6 FIX-BUY-IDEMPOTENCY `2f00b19` — stops live double-spend) — `services/execution.py`. **D02-F3:** confirm-miss used to re-broadcast the same tx (double-spend on a landed buy). New `_get_signature_status()` (getSignatureStatuses, 3-tier) gates the decision: landed→success(no resubmit), failed→resubmit, BUY-unknown→record-pending-with-sig (never double-buy), SELL-unknown→failure (#4 parks+retries). **D02-F2/F7:** `use_jito=False` (bundle path broken: UUID-not-sig + no tip) → real-sig `_send_transaction`; JITO-REIMPLEMENT-001 filed. **D02-F14:** unset parse-URL → confirmed=False (getSignatureStatuses verifies) not blind-True. py_compile PASS; verify_idempotency.py 6/6 (mocked-RPC unit test) + 9/9 structural. NOT paper-observable; flip-confirmed. One lever. Rollback: `git revert 2f00b19`. Next: #7 EXEC-001/002 (+D02-F8 Jupiter sizing).
- **2026-06-03** (§B Phase-1 #4+#8 MERGED `2a85508` — live-execution correctness begins) — `services/bot_core.py`. **#4 (D02-F1):** failed live sells were booked as successful closes (`execute_trade` returns success=False/never raises → old `except` dead → SOL stranded, fabricated PnL, position popped). New `if not result.success:` + shared `_handle_failed_live_sell()` (park-and-continue, never raises) leaves the position OPEN for retry, no booking/pop; protects partial/staged-TP sells too. **#8 (D03-F1/D04-F8):** emergency_stop guards each close (one failure can't abort it), detects left-open via `key in self.positions`, sets durable Redis `bot:emergency_stop`, always alerts+publishes. Merged because #4's failure semantics determine #8's safety. py_compile PASS; `.tmp_phase1/verify_phase1_4_8.py` 10/10 PASS. **NOT paper-observable (live `else:` branch) — runtime-confirmed at the flip.** One lever. Rollback: `git revert 2a85508`. Next: #6 buy-idempotency, #7 EXEC-001/002 (+D02-F8).
- **2026-06-03** (Phase-0 #3 RUNTIME-CONFIRMED + new finding `DASH-CORRECTED-PNL-COLUMN-001`) — `service:health` (Redis) now carries internal-service rows all `ok` (bot_core heartbeat 8s, signal_aggregator 28s, signal_listener via signals:raw 0s, market_health 41s) → a 05-28-style internal crash is now visible on the dashboard + Discord-alerted. **§B Phase 0 fully verified** (#1 pubsub+leak, #2 market-mode, #2.5 redis-hardening, #3 observability). NEW `DASH-CORRECTED-PNL-COLUMN-001` (🟠 Phase-3, prod-observed): `web` logs `column "corrected_pnl_sol" does not exist` every ~60s — a dashboard PnL query references corrected_pnl_sol on a table lacking it (likely `trades`; migration `001` added it to `paper_trades` only); PRE-EXISTING; filed for FIX-DASHBOARD-MODE-FIDELITY (§B Phase-3). DEPLOY-OBSERVABILITY row → RUNTIME-CONFIRMED. Begins revised §B Phase 1 (merged #4+#8 next).
- **2026-06-03** (REDIS-CLIENT-HARDENING-001 `b72fac2` — Phase-0 reliability) — Prod observation of #2: `market:health` dex=$1.65B healthy + `market:migration_count_1h=2` → HIBERNATE (market-mode fix correctly enforced a present-but-low counter; `data_degraded=false`); the 2/hr is a warm-up + Redis-timeout-dropped-increment artifact. Hardened ALL 15 `aioredis.from_url` sites across 11 files with `socket_keepalive=True, health_check_interval=30, retry_on_timeout=True` to stop dropped increments/reads + safety-listener churn. Verified construct-safe on redis-py 7.4.0 before editing (all-services change); py_compile 11/11 PASS. NEW `MARKET-MODE-THRESHOLD-RECALIBRATE-003` filed (verify capture-rate vs thresholds + warm-up bypass once a clean hour of counter data exists). Rollback: `git revert b72fac2`.
- **2026-06-03** (DEPLOY-OBSERVABILITY `51ed450` — §B Phase-0 #3; **PHASE 0 COMPLETE**) — Makes a crashed internal service visible + alerted (D12-F2/F3/F4 — the gap that made 05-28 silent). Folded liveness checks into the existing `web` `_service_health_checker` (no new Railway worker): internal-service rows in `service:health` (bot_core/signal_aggregator via heartbeat keys; signal_listener/market_health via freshness proxies) + rate-limited Discord down-alert; dashboard "ZMN Services" section added. py_compile PASS; HTML/JS aligned. REDIS-CLIENT-HARDENING-001 runtime-confirmed alongside (`2337565`): bot_core supervise-restarts=0, migration counter climbing 2→6. §B Phase-0 complete (#1+leak, #2, #2.5, #3 all ✅); bot recovered+hardened+observable. Next: §B Phase 1 (live-execution correctness) before any live flip. Open: MARKET-MODE-THRESHOLD-RECALIBRATE-003 (steady-state data). Rollback: `git revert 51ed450`.
- **2026-06-03** (FIX-PUBSUB-ISOLATION RUNTIME-CONFIRMED + leak hotfix `9fa45b0` + FIX-MARKET-MODE-MISCLASSIFICATION `5a3e5aa`) — **Bot RECOVERED.** Prod observation of `98c8007`: all 6 services ● Online, crash-loop RESOLVED, pipeline flowing, paper trades entering; `supervise` confirmed catching the exact redis `TimeoutError` in prod. Leak-hotfix `9fa45b0` fixed a connection leak the supervise-restart exposed (3 listeners lacked `finally: aclose()` → `MaxConnectionsError`); added cleanup-on-exit + concise supervise logging; MaxConnectionsError gone. **§B Phase-0 #2 FIX-MARKET-MODE-MISCLASSIFICATION** (`market_health.py`): missing-data != dead-market — absent migration counter → `None` abstains (vs genuine 0), DefiLlama None+last-good (not 0.0), fabricated pumpfun leg dropped from binding decision, total-data-loss → DEFENSIVE not HIBERNATE, `data_degraded` flag; 10/10 classifier tests PASS (incl. outage case → NORMAL); supersedes MARKET-MODE-001-RE-CALIBRATE-002. **NEW `REDIS-CLIENT-HARDENING-001` (🟡 Phase-0)** filed from prod obs: persistent `Timeout reading from redis.railway.internal` is environmental Railway-Redis slowness the resilience fix tolerates but doesn't cure (safety listeners back off to 60s) — harden `aioredis.from_url` before live. Decision Log row + Tier-1 rows updated. Rollbacks per-commit.
- **2026-06-03** (FIX-PUBSUB-ISOLATION — §B Phase-0 #1; CODE DEPLOYED `98c8007`) — The #1 flip-unblocker. NEW `services/async_utils.py` `supervise(coro_factory, name)` (restart-on-crash + capped backoff; stop on clean return; propagate CancelledError) now wraps every member of all 7 service top-level gathers + dashboard bg-tasks; `main.py` single-service entrypoint routes through `run_service()`. Isolates a crashing pubsub listener (transient redis TimeoutError) from its siblings and self-heals it via restart-resubscribe, instead of crash-looping the whole container. Resolves PIPELINE-PUBSUB-ISOLATION-001 + BOT-CORE-EMERGENCY-LISTENER-PUBSUB-ISOLATION-001; fixes D01-F1/F2/F3/F4/F5/F6 + D12-F5. py_compile 9/9 + verify 25/25 PASS; runtime observation on Railway pending. One lever (no trading-logic change). FIX-PUBSUB-ISOLATION Tier-1 row → ✅ CODE DEPLOYED. Rollback: `git revert 98c8007`.
- **2026-06-03** (FULL-CODE-AUDIT-001 — read-only comprehensive pre-flip codebase audit) — Systematic 12-dimension audit of the whole codebase (Opus 4.8 + multi-agent workflow: recon→12 dims→adversarial verify; 2 workflows, 77 agents, ~2.1M tokens). ~90 findings; post adversarial verification 🔴×14 / 🟠×~33 / 🟡×~26 / 🟢×~17. **Two verified NON-findings:** TEST_MODE money-path gating is correct & defense-in-depth (no real send in paper); wallet private key not leaked in code. **NEW 🔴 execution blockers** (masked because validated trade id 6580 was a single full round-trip): D02-F1 failed live sells booked as closed; D02-F5 partial/staged-TP live sell dumps the whole position; D02-F3 buy double-submit on confirm timeout; D03-F1 emergency_stop unreliable. **Confirms+extends** PIPELINE-PUBSUB-ISOLATION-001 (crash class in 5 services not 2; all 6 gathers miss return_exceptions; main.py no supervised restart) and adds the invisibility cluster (heartbeats unread, no internal-service dashboard rows, continuous_audit.py undeployed). Confirms RESOLVED: EXEC-002 (Jupiter NameError) + solders signing API. Filed 16 fix-session follow-ups in Tier 1 mapped to the audit's §B remediation sequence (Phase 0 restore → 1 execution → 2 safety → 3 accounting, all GREEN before `TEST_MODE=false`; Phase 4 security/hygiene). Decision Log row added. ZERO state writes. Audit: `docs/audits/FULL_CODE_AUDIT_001_2026_06_02.md`. Zero existing items dropped/renamed.
- **2026-04-19** — initial consolidated roadmap. Merged from prior `ZMN_ROADMAP.md` (20+ items) + 13 audit docs under `docs/audits/` + 1 session-output. All actionable items now use the unified ID schema and tier system. Audit docs retained as evidence (each gets a header pointing here for current status). Dashboard, forensics, rules-refresh, and Sentry sessions reflected in COMPLETED. Prior roadmap items #1–#20 + B-001/B-014 mapped to new IDs. No actionable items lost; superseded items moved to the Deferred / Superseded section with explicit reason.
- **2026-04-19** (later, post Session 2 v2) — consolidated findings from Session 1 (Sentry triage, `35bdfe6`) and Session 2 v2 (dashboard live close-path write-back, `5ac30cd`). New items: **OBS-008** (silence 5 Sentry noise issues, manual), **OBS-009** (cache SocialData -1 60s), **OBS-010** (LIVE_ONCHAIN daily_pnl_sol semantic), **BUG-020** (Discord poller housekeeping), **DASH-ENTRY-001** (Tier 1 PRE-LIVE BLOCKER, Session 2b, 30–45m), **REFACTOR-001** (split `pos.trade_id` → paper_trade_id + trades_log_id), **ML-010** (live-INSERT `fear_greed_at_entry`/`market_mode_at_entry` flat defaults). Updates: **DOCS-001** CLAUDE.md partial (this commit), **DOCS-003** enriched with Session 1's Sentry MCP query patterns, **LIVE-002** Open Positions criterion amendment. ID scheme extended: `DASH-ENTRY-*`, `REFACTOR-*`. Added **Critical path** section reflecting the Session 1 → 2 v2 → 4 → (2b ∥ 3 ∥ SEC-001) → 5 parallelizable sequence. **#9.8 cost corrected** 1.32 → ~3.4 SOL per forensics `1b40df3`. Open threads for Session 2 v2 findings (id overload, features defaulting, LIVE_ONCHAIN pnl, Open Positions gap) all converted to tracked items. Zero items dropped.
- **2026-04-20** (Session 3 — sell-path devnet validation + PumpPortal spec audit) — **Verdict B: spec matches code.** Comprehensive field-by-field audit of `_execute_pumpportal_local` sell payload (L335-344) against current PumpPortal Local API docs: all 7 required fields present; percentage-string `"100%"` sells explicitly documented as supported; pool enum current; content-type mismatch (form-urlencoded vs JSON example) is functionally irrelevant given 97.1% historical success. The 261 historical HTTP 400s (v3/v4) are condition-specific (dust balance / mid-graduation pool drift / low liquidity / transients), not payload-format drift. **No code change.** Phase 2 (devnet Jupiter buy→sell cycle) blocked on two independent issues: (a) public devnet faucet completely rate-limited (solana.com faucet + direct JSON-RPC both returned 429), (b) structural — `services/execution.py` hardcodes mainnet URLs for Jupiter/PumpPortal with no devnet-override hooks; only Helius RPC is env-configurable. Followed plan's fallback branch (proceed to Phase 3 on Phase 2 failure). **Session 5 clearance: YES-WITH-CONDITIONS** — tightened sell-side error budget + Sentry body capture on first HTTP 400 during live window. Three A2-style follow-up proposals documented in audit (400-body Sentry search / 400-specific Jupiter fallback / devnet routing capability); all require separate sessions. Three leftover concerns flagged: (1) faucet IP flag; (2) HELIUS_STAKED_URL Bearer-auth convention needs separate audit; (3) `_execute_jupiter` at `services/execution.py:491` references undefined `amount_sol` + `action` variables — will NameError on any successful Jupiter TX_SUBMIT (pre-existing latent bug; flagged for follow-up, not fixed this session).
- **2026-04-20** (post-Session-3 consolidation — routing-refresh findings + fee-model gap + T+24h gate outcome) — **Session 3 verdict amended from B to B-conditional.** Subsequent investigation (see correction block at top of `docs/audits/SELL_PATH_DEVNET_VALIDATION_2026_04_19.md`) found that `paper_trades.platform` is uniformly `pump.fun` across all 6,352 rows and sampled `features_json.bonding_curve_progress` values are frozen at `0.0` — indicating routing state is captured at signal-discovery / buy time and never refreshed during hold. The 261 historical HTTP 400s are predominantly mid-graduation pool drift (stale routing on sells of graduated tokens), NOT the "transient" causes originally named. **Mechanism + fix now tracked as paired items EXEC-001** (routing state refresh in `_close_position`, Tier 1 PRE-LIVE BLOCKER, 45-75m CC) + **EXEC-002** (Jupiter NameError fix at `services/execution.py:491`, Tier 1 PRE-LIVE BLOCKER, 15m CC). Session 3 audit body preserved unchanged as historical record; correction block added at top. **Session 4 T+24h gate outcome** captured as new subsection under Tier 1 (after LIVE-001 row) + new audit `docs/audits/TUNE_T24H_GATE_2026_04_20.md` — strict FAIL on `stop_loss_35%` count-reduction (23% vs 50% target), PASS on total-PnL (+28% vs pro-rated baseline); Jay-accepted on PnL-up read. Yellow flags (TRAILING_STOP WR -20.9pp; `no_momentum` per-trade -33%) warrant continued 2nd-24h observation. **Chat-layer fee-model analysis** landed three items: **FEE-MODEL-001** (Tier 2, expand `paper_trader.fees_sol` to include platform delta + priority fee + Jito tip + realized slippage — paper currently models only ~0.5% flat vs ~2pp+ live reality), **OBS-011** (Tier 2, per-trade paper-vs-live PnL delta tracker for live windows — prompt diff only, 15m), **EXEC-003** (Tier 2, read-only audit of what execution paths actually submit for priority fee + Jito tip — informs FEE-MODEL-001 defaults). **DEVNET-001** (Tier 3) tracks adding env-var overrides for Jupiter + PumpPortal URLs to unblock future devnet validation (Session 3 Phase 2's structural blocker). **SEC-001 waiver** for LIVE-002's one supervised window added as sub-note (required for all unsupervised future windows). **LIVE-002 success criteria** extended with "zero trapped positions" criterion (detects EXEC-001 / EXEC-002 incomplete deployment). ID scheme extended: `EXEC-*`, `FEE-MODEL-*`, `DEVNET-*`. Zero items dropped, reordered, renamed, merged, or restructured. All pre-existing items preserved verbatim.
- **2026-04-21** (FIX-1 — Session 5 v4 rollback follow-up: T5 trigger redesign + reconcile cleanup + ML bypass investigation) — Three threads. FIX-1's `services/bot_core.py` code change converged on the identical implementation produced independently by parallel FIX-2 (commit `e078b4c`), which committed first — git saw FIX-1's bot_core.py edits as already present in HEAD. No conflict. FIX-1's own commit covers docs, script, and roadmap updates only. (1) **`MAX_POSITION_SOL_FRACTION` landed in `e078b4c`** (via FIX-2 inclusion): position capped at `min(MAX_POSITION_SOL, wallet_sol * fraction)`, default 0.10. At 1.66-SOL wallet, binds at 0.166 SOL — roughly half v4's 0.365-SOL actual position. **T5-DESIGN-001** new roadmap item captures the T5a/T5b monitoring-loop redesign (belongs in next Session 5 prompt, not services code). Rationale doc: `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md`. (2) **CLEAN-003 landed:** `scripts/live_flip_prep.sh` clears `bot:status`, `paper:positions:*`, `bot:open_positions:*` before TEST_MODE flip. CLAUDE.md "Live-flip pre-flight procedure (CLEAN-003)" section codifies the procedure. Addresses the 5-phantom-mint reconcile-residual that caused 25 wasted Helius RPC calls in the v4 window. (3) **ML-012 new roadmap item** — investigation found `AGGRESSIVE_PAPER_TRADING=true` on signal_aggregator unconditionally overwrites `ML_THRESHOLDS` to `{"speed_demon": 30, ...}` at module load regardless of TEST_MODE. Verified: 113 speed_demon trades in last 7d with `ml_score<40` despite floor policy = 40; yh3n441 trade entered at 31.5 because effective live threshold was 30, not 40. Outcome (C) — actual bug. Fix deferred to a future session per prompt. Diagnosis doc: `docs/audits/ML_BYPASS_INVESTIGATION_2026_04_20.md`. Zero items dropped, reordered, renamed, merged, or restructured. All pre-existing items preserved verbatim. `FEE-MODEL-001` landed in parallel FIX-2 commit `e078b4c` alongside the paper-trader rebaseline.
- **2026-04-21** (Session 5 v4 rollback consolidation — docs only, after FIX-1 + FIX-2 landed) — Session 5 v4 first live attempt (2026-04-20): rollback at T+12:00 on T5 trigger, 1 live trade executed, -0.094 SOL net wallet impact, zero trapped positions. Rollback machinery validated even though trigger fired on benign state. **New roadmap items (3, not 6 — three were already landed by FIX-1):** **EXEC-005** (Tier 2, Jito on pre-grad path — v4 evidence shows MEV not the dominant factor on 0.365 SOL trades), **SLIPPAGE-CALIBRATION-001** (Tier 2, ongoing live-data calibration loop seeded by FEE-MODEL-001), **OBS-012** (Tier 3, dashboard ml_score display alignment with gate-comparison value), **DEPLOY-DISCIPLINE-001** (Tier 3, Railway variable-set-to-redeploy latency observed at ~10 min vs typical 60-90s). **Escalated:** FEE-MODEL-001 explicitly marked **Tier 1 PRE-LIVE BLOCKER ✅ COMPLETED `e078b4c`** — single live trade proved paper fee model under-counts live by ~96× on pre-grad pump.fun. **Updated:** LIVE-002 with v4 attempt outcome + revised v5 success criteria referencing the landed fixes (FEE-MODEL-001 ✅, CLEAN-003 ✅) and remaining gates (ML-012, T5-DESIGN-001, MAX_POSITION_SOL_FRACTION). EXEC-005 v4 evidence integrated into new entry. SEC-001 waiver marked un-consumed (v4 rolled back before full window completed — same waiver framework can apply to v5 or Jay may rotate creds). **CLAUDE.md:** new "paper-as-hypothesis" Operating Principle added (Phase 3.2 Live-flip pre-flight procedure skipped — already landed by FIX-1). **New audit doc:** `docs/audits/SESSION_5_V4_ROLLBACK_FINDINGS.md` — repo-committed version of v4 observations (rollback deliverable is gitignored). **Handover correction block** appended with 7-point dated entry. ID scheme extended: `SLIPPAGE-CALIBRATION-*`, `DEPLOY-DISCIPLINE-*`. Zero items dropped, reordered, renamed, merged, or restructured. All pre-existing items preserved verbatim. Docs-only diff (no services/, no env vars, no Railway deploys).
- **2026-04-21** (GATES-V5 + MCP-RECON + DASH-RESET consolidation — post first-post-v4-rollback gate tightening and MCP reference doc) — Three sessions landed. **GATES-V5 (commit `1dec17b`):** ML-012 fix (AGGRESSIVE_PAPER_TRADING override now requires TEST_MODE=true), 4 new entry gates in signal_aggregator (HOLDER_COUNT_MIN=15, BUY_SELL_RATIO_MIN=3.0, PRE_FILTER_SCORE_MIN=1.15, CFGI_MIN=20), stop-loss tightened 0.35 → 0.20 via STOP_LOSS_PCT env var, sizing caps applied (MAX_POSITION_SOL=0.25, SPEED_DEMON_BASE=0.15, MAX=0.25). ML_THRESHOLD_SPEED_DEMON raised to 65. Paper steady-state cadence target 15-25 trades/hr under new gates (down from ~70/hr pre-deploy). Runtime logs confirmed new code (HOLDER reject `4a7ne7Z7SZVQ: 0 < 15` at 10:28:06 UTC + 10:30:18 UTC). **MCP-RECON (commits `1e444bb` + `0c91492` redaction):** 408-line reference doc `docs/audits/MCP_CAPABILITIES_2026_04_21.md` enumerating tool catalogs for nansen, birdeye, helius, vybe, dexpaprika, coingecko, socket, defillama + infra MCPs + use-case-to-tool mapping (11 operations). **Biggest discovery: Vybe is a full Solana-analytics surface** (50 endpoints including `/v4/tokens/{mint}/holders-count-ts` — historical holder count time-series that was the missing primitive for wallet reverse-engineering). **Nansen MCP currently HTTP 401 server-rejected** — bundles into SEC-001 rotation. Birdeye session-expiry on first call is documented known-workaround (warmup call before actual query). **DASH-RESET (commit `1176e12`):** `paper_trades` archived to `paper_trades_archive_20260421` + `trade_mode='paper'` rows deleted + dashboard aggregates reset + Redis `bot:portfolio:balance` reset to `STARTING_CAPITAL_SOL=20.0`. Live rows preserved untouched. Fresh post-GATES-V5 accumulation starts 2026-04-21. **New Tier 1 items:** TUNE-004 (stale signal_aggregator size env vars — 5m env-only fix), OBS-013 (`sig_type=None` on paper opens — 30m trace), OBS-014 (`stop_loss_35%`→`stop_loss_20%` SQL migration — 20m). **New Tier 2 items:** MCP-CAPS-001 (✅ COMPLETED — the recon session itself), WALLET-REV-001 (Vybe-powered wallet reverse-engineering — 60-90m, does NOT depend on Nansen), WHALE-001-v2 (Whale Tracker activation revised to Vybe-first-Nansen-later — supersedes original WHALE-001). **New Tier 4: GATE-004** (✅ COMPLETED — live-path gate + exit + sizing infrastructure ready via GATES-V5). **Status changes:** ML-012 📋 → ✅ COMPLETED `1dec17b` (via GATES-V5); T5-DESIGN-001 📋 → 🟡 IN_PROGRESS (code hooks ready; monitoring-loop defs go in V5a prompt); OBS-003 📋 → 🟡 UNBLOCKED (Vybe endpoints now known); WHALE-001 📋 → ⛔ SUPERSEDED by WHALE-001-v2; ML-008 ⏸️ → ⛔ SUPERSEDED by MCP-CAPS-001 + WHALE-001-v2; LIVE-002 structure replaced with V5-staged sequence (v5a/b/c/d). **SEC-001 scope** extended to include Nansen API key rotation (beyond original Redis+Postgres). **5 SOL transfer planned** to trading wallet (current on-chain 0.064 SOL) in preparation for v5 staged attempts. State snapshot updated with current env values. Critical-path rewritten to reflect post-GATES-V5 sequencing. ID scheme extended: `WALLET-REV-*`, `MCP-CAPS-*`, `WHALE-*-v2` for version-successor pattern. **Open threads updated:** Birdeye entry now references MCP_CAPABILITIES workaround doc; reconcile-on-mode-flip moved to resolved-as-tracked (CLEAN-003 ✅); new open thread added for "historical pre-grad BC state at timestamp T" gap (no MCP covers; affects WALLET-REV-001 for pre-cache-retention trades). **CLAUDE.md:** one line added under MCP Usage Rules pointing to `docs/audits/MCP_CAPABILITIES_2026_04_21.md` for tool-name reference. Zero items dropped, reordered, renamed, or merged without explicit rationale. All pre-existing items preserved. Docs-only diff (no services/, no env vars, no Railway deploys).
- **2026-04-21** (DASH-RESET detail amendment to prior GATES-V5+MCP-RECON+DASH-RESET consolidation) — Follow-up to commit `057f938` (earlier this day) after DASH-RESET deliverable details became available. **DASH-RESET specifics (commit `1176e12`):** `paper_trades` archived to `paper_trades_archive_20260421` with **6,635 rows** preserved; **6,629** `trade_mode='paper'` rows DELETEd; **6 live rows (IDs 6575-6580, all closed)** intentionally preserved untouched; new baseline `portfolio_snapshots` row (`id=39038`) written at 20.0 SOL; Redis `bot:portfolio:balance` + `bot:status` cleared and rewritten at baseline. **Procedural finding:** required **3 bot_core redeploy iterations** to land the baseline cleanly due to a race between the DELETE+baseline-INSERT SQL and bot_core's 5-min portfolio-snapshot cycle — the running bot_core would snapshot the transient post-DELETE state before the baseline INSERT committed, overwriting the baseline. Clean sequence (to codify in CLAUDE.md for future resets): (1) DELETE paper rows, (2) baseline INSERT in same transaction, (3) nudge bot_core redeploy within 60s of the INSERT to force a fresh `_load_state` read, (4) verify `bot:portfolio:balance=20.0` via Redis GET before the next 5-min tick. **Two new Tier 1 items surfaced by reset outcomes:** **BUG-021** (20m — `daily_pnl_sol` writer in `bot_core.py:1963-1987` and `api_paper_stats` in `dashboard_api.py:1148-1159` aggregate paper_trades without filtering `trade_mode`; post-reset the 6 preserved live rows' -3.205 SOL leaks into "paper" aggregates until paper trades accumulate. Pre-existing bug, surfaced by the shrunk paper aggregate); **CLEAN-004** (10m — bot_core restores `consecutive_losses=4` from both Postgres `bot_state` and Redis `bot:consecutive_losses=4` on every restart; one live loss in v5 trips the sell-storm circuit breaker at 5+ unnecessarily. Pre-v5-flip reset: `UPDATE bot_state SET value_text='0' WHERE key='consecutive_losses'` + `DEL bot:consecutive_losses`. Add to `scripts/live_flip_prep.sh` so every future flip resets the counter). Zero items dropped, reordered, renamed, or merged. All pre-existing items preserved. Docs-only diff.
- **2026-04-22 (later — HOLDER-DATA-PIPELINE fix lands + BUG-021 partial + CLEAN-004 reset)** — After Jay's roadmap consolidation (prior entry), dedicated fix session landed. **HOLDER-DATA-PIPELINE-001 ✅ FIXED** via two commits: (1) `fc87b03` — GeckoTerminal `/tokens/{mint}/info` endpoint schema change from bare int to dict `{"count": N, "distribution_percentage": {...}, "last_updated": "..."}` broke `int(attrs.get("holders", 0))` at two call sites (`_fetch_gecko_pool_data` L782 + `_process_graduations` L2441). `int(dict)` raises TypeError, caught silently by outer `except Exception`, `holders` stays 0. Fixed with isinstance(dict) branch reading `.count`. Post-deploy: `GRAD_ACCEPT: 61VJc9L1Xekp score=70 holders=198 kol=0 whale=1.00` at 13:39:15 UTC confirmed graduation path working. (2) `4c5508b` — age-bypass for HOLDER + BSR gates when `token_age_seconds < GATES_V5_MIN_AGE_SEC` (default 30). Rationale: fresh pump.fun new_token signals at age=0s legitimately have no GeckoTerminal data yet, no token:stats populated yet. Gate rejecting `0 < 1` is meaningless — the gate exists to filter "no organic interest", not "data hasn't arrived yet". PRE_FILTER_SCORE_MIN=1.15 is NOT age-bypassed because `pre_filter_score = position_size_multiplier` is calculable at age=0s. New env var `GATES_V5_MIN_AGE_SEC`. Audit doc: `docs/audits/HOLDER_PIPELINE_FIX_2026_04_22.md`. **BUG-021 🟡 PARTIAL** via `bc622eb` — dashboard_api sites (`api_paper_stats` + `api_portfolio_history`) now apply `trade_mode` filter via `_get_mode(request)`. Paper-view excludes live rows; live-view selects `market_mode='LIVE_ONCHAIN'` snapshots. Response body now includes `"mode": "paper"\|"live"` for caller verification. `services/bot_core.py:1963-1987` `_portfolio_snapshot_task` write-path deferred (still writes `daily_pnl_sol` unfiltered). **CLEAN-004 ✅ RESET** — Redis `bot:consecutive_losses` set to 0; Postgres `bot_state.consecutive_losses` updated to `'0'`. Follow-up script update pending (add to `scripts/live_flip_prep.sh`). Status changes: HOLDER-DATA-PIPELINE-001 📋 → ✅ COMPLETED; BUG-021 📋 → 🟡 PARTIAL; CLEAN-004 📋 → ✅ COMPLETED (with script follow-up). ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim.
- **2026-04-22** (roadmap consolidation — HOLDER-DATA-PIPELINE discovery + Nansen A' ship + Bitfoot dataset analysis) — Absorbs 2026-04-22 findings, session outcomes, and new work items. **Sessions landed:** Nansen v2 client ship (`c7f73ee`) + Session A' payload shape fix for TGM + SM + profiler families (`c012475`, 16 tests passing, end-to-end verified; research brief was wrong on all 3 families — flat root body, not nested `{parameters:...}`). **Diagnostic sessions (no code):** POST-GATES-DIAG (pipeline alive but upstream `FILTER:no_social_links` + `RUGCHECK_BLOCK` killed 100% of signals pre-gate); SIGNAL-FUNNEL 60-min capture (12.5-min Railway log stream: 242 mints → 125 ML-scored → 41 reached HOLDER → 0 passed, all `holders=0`; BSR/PRE_FILTER/CFGI never fired because HOLDER short-circuits; ML<65 rejects: 86; verdict FUNNEL-A); HOLDER-LOWER session (`HOLDER_COUNT_MIN` 15→1, still 0/23 regardless of threshold; verdict HOLDER-C: **data pipeline broken, not calibration**); HOLDER-DISABLE session (`HOLDER_COUNT_MIN` 1→0 to observe 4 downstream gates — 27 BSR rejects all with `bsr=-1.00` sentinel, 0 PRE_FILTER, 0 CFGI, 0 entries; verdict B with corroborating finding that the `buy_sell_ratio_5min` feature shares the same sentinel-for-new-tokens pattern as `holders=0`; reverted HOLDER to 1 after window; `session_outputs/ZMN_HOLDER_DISABLE_DONE.md`). **Runtime fixes (non-code):** Redis workaround keys renewed (`market:mode:override=NORMAL` + `nansen:disabled=true`, both 24h TTL — both had expired overnight); SocialData credit top-up to $10 after $0 outage caused multi-day silent `no_social_links` rejection (dashboard previously showed $0/day → drove `FILTER:no_social_links`; Jay restarted signal_aggregator manually to clear cooldown). **Research artifact:** Bitfoot 32,851-ping + 22,288-multiplier 10.3-month Era-1 dataset (2025-01→2025-12) analyzed — three counterintuitive filters hold across H1+H2 2025 (top10≥30%, vol/MC<1.5, Dex Paid=🔴); entry latency tolerance 1-2 min (93%/83% profit capturable); Analyst-horizon exits (median time-to-peak 30min, 10-20x median 2.1h, 50-100x median 10h, 100x+ median 1.2d); LightGBM AUC 0.55-0.57; heuristic top10≥30% filter delivers +8.5% ROI vs all-pings −10.9% under realistic assumptions. Dataset + scripts loaded at `session_outputs/bitfoot_analysis/`; FINDINGS_ML.md + FINDINGS_TIME_TO_PEAK.md + `SESSION_BITFOOT_DIP_SAMPLE.md` embedded in chat (not yet saved as repo files). **New items added (9 net):** Tier 1 — **HOLDER-DATA-PIPELINE-001** 🔥 top priority (blocks all paper/live trading; 30-45m read-only diagnostic), **TUNE-005** (roll back HOLDER=1→15 post-pipeline-fix, 5m blocked-on-pipeline), **SOCIALDATA-AUTO-TOPUP-001** (30m alert worker to prevent outage repeat). Tier 2 — **NANSEN-CLIENT-V2-MIGRATE-001** (60m, migrate 9 v1 callers to v2), **NANSEN-FEATURE-NORMALIZER-001** (60m, ML feature normalizer), **ANALYST-POST-GRAD-001** (multi-session, Bitfoot-inspired personality for post-grad tokens), **BITFOOT-DIP-SAMPLE-001** (30-45m, resolve 71% unflagged uncertainty via GeckoTerminal free API), **BITFOOT-MONITOR-001** (2w obs, verify current Bitfoot channel still generates comparable signals). Tier 3 — **BITFOOT-ML-FEATURE-INTEGRATION-001** (3-4 sessions, cross-pollinate top10/vol-mc/dex-paid/age features into ZMN ML). **Note:** BUG-021 and CLEAN-004 were already added in the 2026-04-21 `2519fea` DASH-RESET detail amendment — not duplicated here despite prompt listing them as "carryover not yet in roadmap". **State snapshot updated:** mode annotated with HOLDER-gate silence; 7d perf blocked on pipeline-fix; HOLDER_COUNT_MIN env row updated to reflect temporary 1 (was 15 in GATES-V5); latest commits prepended with `c012475` + `c7f73ee`; SocialData noise attenuation note added; new rows for Redis daily-renewal TTL status + Bitfoot research artifact location. **Blocking issues:** "No critical blockers" → **🔥 Critical blocker: HOLDER-DATA-PIPELINE-001** (bot silent; blocks v5a). Consecutive_losses=4 + SOCIALDATA-AUTO-TOPUP-001 added to MEDIUM list. **Critical path rewritten** (was 6 items, now 10): HOLDER-DATA-PIPELINE-001 #2 (post-prior-sessions), hygiene bundle (TUNE-005 + CLEAN-004 + BUG-021) #3, V5a #4, BITFOOT-DIP-SAMPLE-001 + ANALYST-POST-GRAD-001 parallelizable at #5-6, Nansen v2 migration chain at #7, V5b/c/d at #8, SEC-001 #9, WALLET-REV-001 + WHALE-001-v2 + BITFOOT-ML-FEATURE-INTEGRATION-001 parallelizable at #10. Rationale block rewritten to reflect HOLDER-DATA-PIPELINE-001 as hard prerequisite. **Status changes:** none for pre-existing items. ID scheme extended: `HOLDER-DATA-PIPELINE-*`, `SOCIALDATA-*`, `NANSEN-CLIENT-*`, `NANSEN-FEATURE-*`, `ANALYST-POST-GRAD-*`, `BITFOOT-*`. Zero items dropped, reordered, renamed, or merged without explicit rationale. All pre-existing items preserved verbatim. Docs-only diff (no services/, no env vars, no Railway deploys).
- **2026-04-24** (TUNE-005 — HOLDER_COUNT_MIN 1→15 rollback post HOLDER-DATA-PIPELINE-001 verification) — Env-only change via Railway MCP `set-variables`, no git commit for the var itself (docs-only commit for this consolidation). Variable `HOLDER_COUNT_MIN` set on signal_aggregator: 1 → 15, restoring the intended GATES-V5 gating threshold. Railway auto-redeploy: old container stopped 14:32:21 UTC, new container started 14:32:10 UTC (staggered) with expected env (TEST_MODE=True, ANALYST_DISABLED=True). **Pre-change check:** HOLDER-DATA-PIPELINE-001 fix deploy (`fc87b03` + `4c5508b`, 2026-04-22) verified to be the live commit; zero `HOLDER reject: 0 < N` patterns in pre-change log sample (pipeline fix already working at MIN=1 — gate bypassed for age<30s tokens by the age-bypass rule from `4c5508b`, real graduation-path holder counts confirmed in `HOLDER_PIPELINE_FIX_2026_04_22.md` audit). **Post-change verification (5-min window):** new container healthy — zero `HOLDER reject: 0 < 15` pattern (STOP condition not triggered; pipeline fix remains live); ML SCORE cadence 5-8/min (baseline over a burstier 15-min window was ~12/min — not a 5-min silence / no collapse); `PRE_FILTER reject ...: 1.00 < 1.15` observed firing with real values confirming downstream gate path functional; no errors or tracebacks. HOLDER gate itself was mostly bypassed by age<30s gate in the observation window (bulk of signals are fresh pumpportal new_tokens at age=0s); this is expected — the tightened gate will exercise on age≥30s + holder_count<15 tokens as they arrive. **Status changes:** TUNE-005 📋 QUEUED (blocked) → ✅ COMPLETED 2026-04-24. Env var table `HOLDER_COUNT_MIN` annotated with new value + rationale. Critical path #10 updated. **Open follow-up (soft):** 24-48h observation to confirm steady-state SCORED cadence post-gate-tightening; the 5-min sample is small, real throughput reduction from gate effect vs natural burstiness variance cannot be disentangled yet. ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim. Docs-only diff (STATUS.md + ZMN_ROADMAP.md). No services/, no Redis writes, no code changes; only the `HOLDER_COUNT_MIN` Railway env var changed on signal_aggregator and triggered auto-redeploy.
- **2026-04-23** (late-day consolidation — ANALYST-DISABLE landed, BUG-022 flagged, DOCS-004 flagged, ANALYST-POST-GRAD-001 design summary distilled) — After this morning's BITFOOT-2026-BASELINE (`2b4cb73`) + DIP-SAMPLE retry (`e053448`) + ANALYST-DISABLE (`cc8e5c9`) commits. Absorbs: **ANALYST-DISABLE-001** (✅ COMPLETED `cc8e5c9` — governance Redis override closes the graduation-sniper bypass that the `ANALYST_DISABLED=true` env flag alone did not cover; stops 11.9% WR / -2.48 SOL / 17h leak on the pre-existing legacy Analyst personality; session output `session_outputs/ZMN_ANALYST_DISABLE_DONE.md`; halflife caveat captured in Tier 1 entry — governance writes clobber the override every ≤4h until proper code fix lands); **ANALYST-PAPER-AUDIT-001** (📋 NEW — 45m read-only diagnostic to determine retire vs retune on the legacy Analyst module, analyze 59-trade / -2.48 SOL leak, and identify what re-enabled it on 2026-04-22); **DOCS-004** (📋 NEW — CLAUDE.md Vybe URL correction `.com` → `.xyz` per BITFOOT-2026-BASELINE §1 side finding; 5m docs fix); **BUG-022** (📋 NEW 🔥 — `corrected_pnl_sol` NULL on all 215 post-DASH-RESET paper_trades rows; correction job not running; invalidates all current paper-PnL trust; prompt at `/mnt/user-data/outputs/SESSION_CORRECTED_PNL_INVESTIGATION.md`; blocks v5 sizing decisions); **STATUS-CONVENTION-001** (🟡 IN_PROGRESS — single-file state tracker convention seed commit `2243d1f` landed + 4 subsequent session entries confirming it works; promote to ✅ after one more clean append); distilled **Phase-1 design summary** at the top of ANALYST-POST-GRAD-001's Tier 2 entry preamble (DEX ∈ {PumpSwap, Meteora DAMM v2}; top10 ≥ 30; MC ∈ [$50k, $300k]; age ∈ [30s, 300s]; dex_paid dropped; pre-grad vol/MC dropped; Meteora routing verified prerequisite; ROI calibration from paper window only). **State snapshot updated 2026-04-22 → 2026-04-23:** Analyst disabled; Speed-Demon-only paper mode; ANALYST_DISABLED env-table row annotated with date + Redis-override context; BUG-022 added to MEDIUM blockers at top; research artifacts row extended with 2026-04-23 deliverables. **Critical path re-sequenced (10 → 17 steps):** (1) prior ✅ sessions; (2) HOLDER-DATA-PIPELINE-001 ✅; (3) ANALYST-DISABLE-001 ✅ NEW; (4) BITFOOT-DIP-SAMPLE-001 ✅; (5) BITFOOT-2026-BASELINE ✅; (6) 🔥 BUG-022 #1 priority — blocks paper PnL trust; (7) ANALYST-PAPER-AUDIT-001; (8) DOCS-004; (9) STATUS-CONVENTION-001 confirm ✅; (10) TUNE-005 (unblocked — HOLDER fix landed; 5m env-only); (11) V5a supervised; (12) ANALYST-POST-GRAD-001 multi-session (parallelizable to V5a); (13) Nansen v2 chain; (14) V5b-d escalation; (15) SEC-001; (16) WALLET/WHALE/FEATURE parallelizable; (17) BITFOOT-MONITOR-001 2-week prospective. **Status changes:** pre-existing items unchanged except TUNE-005 (📋 blocked → 📋 UNBLOCKED, HOLDER-DATA-PIPELINE-001 dependency satisfied). ID scheme extended: `DOCS-004`, `BUG-022`, `ANALYST-DISABLE-001`, `ANALYST-PAPER-AUDIT-001`, `STATUS-CONVENTION-001`. Zero pre-existing items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim. Docs-only diff (no services/, no env vars beyond what was already logged, no Redis writes, no Railway deploys).
- **2026-04-23** (BITFOOT-2026-BASELINE — does the 2025 edge still exist in April 2026, verdict BASELINE-A directionally) — Read + external-API diagnostic session. Goal: verify whether Bitfoot's 2025-derived filter profile (top10 ≥30 / vol-MC <1.5 / Dex Paid=🔴 / MC $50-300k / age 30-300s post-grad / DEX ∈ {PumpSwap, Raydium, Meteora}) still concentrates winners on April 2026 Solana post-grad tokens. **Sample:** 200 pools fetched from GT `/networks/solana/trending_pools` pages 1-10; 42 unique base_mints passed phase-1 (post-grad DEX + ≤14d + MC $20k-$500k); 18 passed 2-filter Bitfoot (top10 + MC band); 4 passed 3-filter (+ h24 vol/MC <1.5 proxy). **Headline numbers (2-filter n=18):** 88.9% hit 2x+, 50% hit 5x+, 44% hit 10x+, 0% rugged. Top winners: NORMIE 29.4x, Nintondo 33.4x, ?? 26.2x, roi 25.6x, FCS 19.7x, PVE 18.1x, RND 17.4x, Yuji 13.9x, Rudi 8.5x. **Verdict: BASELINE-A directionally.** Bitfoot 2-filter still concentrates winners. **But absolute hit rates are bias-inflated** by GT trending_pools survivorship (same pattern as yesterday's dip-sample session — rugs and dead pools are curated out before we see them). Do NOT treat 88.9% as a forward-realizable rate. **New structural findings:** (1) **Raydium post-grad vanished** — 0/42 phase-1 candidates on Raydium; 2025 dataset had 39% Raydium share. Post-grad migration now routes pumpswap-first, meteora-second. (2) **Meteora DAMM v2 over-represented in winners** — 21% of phase-1, 44% of 2-filter matches, 4 of 8 10x+ hitters. (3) **MC band $50-300k remains live** — 2026 median matched-set MC $95.9k vs 2025 $86k, no drift. (4) **Dex Paid rule un-testable** — GT free tier does not expose `dex_paid` on trending endpoint. (5) **Pre-grad vol/MC rule un-replicable cross-era** — 2025 measured pre-graduation turnover via Bitfoot channel; 2026 GT post-grad only gives h24 activity, and the proxy rejects actual winners (NORMIE/Nintondo/RND all had h24 vol/MC >1.5 and hit 10x+). **Side finding: CLAUDE.md Vybe URL is wrong** — line says `API base: https://api.vybenetwork.com (NOT .xyz)` but every `.com` endpoint returns 404; correct base is `.xyz`. Verified on `/v4/tokens/{mint}/top-holders`. Flagged for follow-up CLAUDE.md fix session. **Implications for ANALYST-POST-GRAD-001 design (landed in Tier 2 entry preamble alongside DIP-D flag):** proceed with revisions — (a) drop `dex_paid` rule; (b) drop pre-grad `vol/MC <1.5` gate; (c) expect Meteora share, de-prioritize Raydium; (d) top10 ≥30 + MC $50-300k remains the candidate 2-filter; (e) ROI un-pinned, must come from paper window. **Status changes:** none for queued items — this is a diagnostic informing ANALYST-POST-GRAD-001 design, not a queue item itself. ANALYST-POST-GRAD-001 Tier 2 entry preamble updated to cite both this session and the DIP-D finding. ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim. Docs-only diff (no services/, no env vars, no Redis, no Railway deploys). API tally: 10 GT trending_pools + 42 Vybe top-holders + 18 GT OHLCV = 70 requests; 0 × 401; 0 × 429; 0 credits. Sample + script local-only under `.tmp_bitfoot2026/` (gitignored); committed artifact: `docs/audits/BITFOOT_2026_BASELINE_2026_04_23.md`.
- **2026-04-23** (BITFOOT-DIP-SAMPLE-001 retry — authenticated GeckoTerminal, verdict DIP-D) — 2026-04-22 session aborted on unauthenticated GT's anti-abuse 401 cascade; today's retry used CoinGecko Demo tier auth (`x-cg-demo-api-key`, free) with an added 180-day pre-filter for rows GT's Public API data-retention cap would reject. **Mid-run discovery:** GT's `/pools/{pool}/ohlcv/hour` returns 401 with `"You can only access data from the past 180 days with Public API"` when `before_timestamp` is older than 180 days. Rule is on `before_timestamp` parameter, not on ping_ts or per-candle data. Script patched to bucket such rows as `out_of_window_180d` and skip API calls. **Sample quality:** 100 stratified pings, 55 unreachable on Demo tier (older ts_sydney), 44 pool lookups attempted (100% success), 39 had OHLCV data, 5 `no_post_ping_data` (pool exists, no trades after ping — possible implicit rugs). **Verdict DIP-D:** 8 of 39 retrievable-data rows (20.5%) hit 2x+ post-ping — 4× the 5% DIP-D threshold. 8 winners: BEANER 2.64x / gng 2.27x / MCRN 2.10x / BBC 2.32x / JARS 2.17x / SPT 2.26x / MSPC 2.35x / MCV 5.61x. **Secondary finding (survivorship bias):** zero rows hit <1.0x in the retrievable subset. Real-world rug rates would predict many; their absence is explained by (a) 180-day cap censoring older pings, and (b) GT pool-index curation filtering out delisted pools from results before we query them. Loss data is un-obtainable with current zero-credit scope. **Implication:** ANALYST-POST-GRAD-001 design must NOT condition on Bitfoot label output (flagged/unflagged) as entry signal — 20%+ of unflagged pings were actual winners. Three feature filters (top-10 / vol-MC / dex-paid) remain candidate gates but need prospective paper validation, not retrospective Bitfoot-label correlation. ROI estimate is un-pinned; derive from paper observation window, not historical Bitfoot math. **Status changes:** BITFOOT-DIP-SAMPLE-001 📋 → ✅ COMPLETED 2026-04-23 (Tier 1-prime #5 + Tier 2 entry); ANALYST-POST-GRAD-001 flagged with DIP-D reframe preamble (status unchanged 📋 — still queued as multi-session build, updated design constraints). **New artifact:** `session_outputs/bitfoot_analysis/BITFOOT_DIP_SAMPLE_DONE.md` (8-section deliverable). **Patched:** `session_outputs/bitfoot_analysis/fetch_dip_peaks.py` (auth header + 180d pre-filter + 401 escalation logic + `out_of_window_180d` bucket). **New:** `session_outputs/bitfoot_analysis/analyze_dip_results.py` (post-run bucket summary + DIP verdict). ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim. Docs-only diff (no services/, no env vars, no Redis writes, no Railway deploys). Auth header pinned: `x-cg-demo-api-key`. API tally: 44 pool lookups + 44 OHLCV calls (88 requests + ~7-8 × 429 retries); 0 × 401; 0 credits; pacing stayed at 2.5s baseline (escalation not triggered). Sample CSV `bitfoot_dip_results.csv` has 99 rows (3 prior-session kept via resume + 96 new; 1 mint `54553` truncated when Jay requested early reporting with 7 rows remaining — verdict was stable and insensitive to the missing row).
- **2026-04-28** (ANALYST-DISABLE-002 — code-level fix for graduation-sniper bypass + bot-silence diagnosis escalated) — Triggered by paper_trades export 2026-04-28 showing the ANALYST-DISABLE-001 (`cc8e5c9`, 2026-04-23) Redis governance override had been clobbered ~6 min after first apply (predicted halflife realized), allowing 241 analyst trades to leak between 2026-04-23 08:24 UTC and 2026-04-25 21:47 UTC at 17.0% WR / -12.5 SOL. The accumulated daily losses tripped `DAILY_LOSS_LIMIT_SOL=4.0` at 2026-04-25 21:52:50 UTC: risk_manager raised CRITICAL EMERGENCY_STOP, bot_core has been offline 67h since. **Sessions in this commit:** (1) Re-applied governance:latest_decision Redis override as band-aid (TTL 86400s, override_source=ANALYST-DISABLE-REAPPLY-2026-04-28 — no longer load-bearing post code fix, kept as belt-and-suspenders). (2) Diagnosed silence: signal_aggregator is healthy (~12-15 ML SCORE/min), bot_core is EMERGENCY_STOPPED with consecutive_losses=7 (TTL=-1), market:mode:current=HIBERNATE because market:mode:override expired (24h TTL lapsed during the 67h silence), 337-entry signals:scored backlog. Confirmed via Redis snapshot + bot_core log showing risk_manager EMERGENCY_STOP at 2026-04-25 21:52:50. (3) Traced ANALYST_DISABLED env-var enforcement: line 145 reads it at module load; line 1810 enforces on main signal path; line 2421 `_process_graduations` graduation sniper bypassed it entirely (hardcodes `personality: "analyst"` at line 2521 + pushes to signals:scored at line 2534). (4) Implemented code fix `9d6e95c`: extended the line-1810 env-var pattern to graduation sniper (line 2443 — silent drain when disabled, function stays alive); added startup banner `Graduation sniper processor started (ANALYST_DISABLED=true|false)` (line 2436) for visibility. (5) Verified live: container swap landed 2026-04-28 13:08:06 UTC (Railway build was unusually slow due to fresh dependency download — ~17m vs typical 2-3m); new banner observed; zero GRAD_ACCEPT/GRAD_REJECT events in 2+ min post-swap (function silently draining as designed). **Bot silence NOT recovered this session — escalated to Jay** per the session prompt's STOP rule on multi-cause silences (3 parallel state issues with non-trivial blast radius: 337-stale-signal queue replay risk + consecutive_losses=7 means 1 bad post-recovery trade re-trips breaker + restart is hard-to-reverse). Recovery recipe documented in `docs/audits/ANALYST_DISABLE_FIX_2026_04_28.md` §6.3 (4 steps, 5-10m if executed in order). **Status changes:** ANALYST-DISABLE-001 ✅ COMPLETED `cc8e5c9` annotated as superseded by ANALYST-DISABLE-002; ANALYST-DISABLE-002 ✅ COMPLETED `9d6e95c` (NEW Tier 1 row); SILENCE-RECOVERY-2026-04-28 📋 QUEUED 🔥 (NEW Tier 1 row, escalated); GOVERNANCE-RESILIENCE 📋 QUEUED (NEW Tier 1 row, deferred — soft hardening for non-analyst toggles). ID scheme extended: `ANALYST-DISABLE-002`, `SILENCE-RECOVERY-*`, `GOVERNANCE-*`. **BUG-022** (`corrected_pnl_sol` NULL on all post-DASH-RESET rows) further confirmed by paper_trades export — 100% NULL on all 835 rows; remains the recommended next session after silence recovery. Zero pre-existing items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim. Diff: `services/signal_aggregator.py` +17/-2 (commit `9d6e95c`); `docs/audits/ANALYST_DISABLE_FIX_2026_04_28.md` (NEW); STATUS.md prepended; ZMN_ROADMAP.md (this entry + 3 new Tier 1 rows + ANALYST-DISABLE-001 supersedence annotation). No env vars touched, no Railway deploys other than the auto-deploy from `git push` on signal_aggregator. Redis writes: 1 (governance:latest_decision band-aid).
- **2026-04-29** (LIVE-FEE-MODEL-AUDIT — read-only paper/live divergence check, verdict MAJOR DIVERGENCE) — Read-only audit comparing `services/execution.py` (live) vs `services/paper_trader.py` (paper) for slippage, fees, PnL formula, TP/SL trigger, sizing, and `features_json`. Plus empirical closed-form check on 6 historical live rows from `paper_trades`. **Verdict: MAJOR DIVERGENCE.** Four load-bearing differences: (1) live PnL formula at `services/bot_core.py:1232` is `(exit/entry-1)*amt` with **no `- fees` term**; paper subtracts fees explicitly. (2) Live writes `slippage_pct=0.0` and `fees_sol=0.0` on every paper_trades row (entry INSERT and close UPDATE both omit those columns). Verified empirically — all 6 live rows have both columns at exactly 0.0. (3) Live entry/exit prices are queried RPC prices, not actual fill prices — `execute_trade` returns no fee/price data; bot_core calls `_get_token_price` after success. (4) Live `features_json` is NULL on all 6 historical live rows. **Closed-form check on 4 live rows with valid prices:** all 4 match `(exit/entry-1)*amt` (no fee subtraction) to ≤ 1e-5 SOL. STOP condition (>1 row mismatched >1e-3 SOL) NOT tripped — divergence is consistent across all rows. id=6580 v4 yh3n441 trade explained: paper recorded +0.0019 SOL; on-chain actual −0.094 SOL; FEE-MODEL-001 corrected paper model predicted −0.094 within ±0.02. **TP/SL trigger logic = parity** (single code path). **Sizing logic = parity** (`risk_manager.calculate_position_size`). **V5a wallet math (carry):** `_max_pos = min(MAX_POSITION_SOL=1.50, wallet * MAX_POSITION_SOL_FRACTION=0.10)`. At 0.064 SOL wallet: `_max_pos_frac = 0.0064`. `size_sol = max(MIN_POSITION_SOL=0.15, min(any, 0.0064)) = 0.15` — exceeds wallet → swap router rejects. **V5a BLOCKED until trading wallet ≥ 1.5 SOL.** **New Tier 1 items (V5a-blocking):** **LIVE-FEE-CAPTURE-001** 🔥 (capture actual fee + slippage on live trades, write to `paper_trades.fees_sol`/`slippage_pct`; Path A use `_simulate_*` from live close path, fast/low-fidelity; Path B Helius `parseTransactions` for actual fill data, slow/high-fidelity); **LIVE-PNL-FEE-FORMULA-001** 🔥 (change `bot_core.py:1232` to subtract fees; pairs with LIVE-FEE-CAPTURE-001). **New Tier 1 items (high, non-blocking):** **LIVE-ROW-BACKFILL-001** (backfill 6 historical live rows with FEE-MODEL-001 estimated fees; mark `correction_method='live_estimated_v1'`; pairs with BUG-022 backfill); **LIVE-FEATURES-JSON-001** (add features_json UPDATE on live entry path; mirror paper). **New Tier 2 items:** **SIZING-WALLET-FLOOR-001** (rationalize `bot_core.py:685-690` so tiny wallet rejects rather than floors up to MIN_POSITION_SOL); **TIME_PRIME-CONTRADICTION-001** (`bot_core.py:695-696` upsizes 2.0× at AEDT 18-20 — directly contradicts `SD_DEAD_ZONE_001` proposal's identification of that as the worst window; resolve in TUNE-006 implementation). **New Tier 3 item:** **ENV_AUDIT_2026_04_29** (referenced as prerequisite but not produced; generate before next state-changing live session — process gap, not content gap; current audit's findings are robust to env values within documented defaults). **Status changes:** none for pre-existing items. ID scheme extended: `LIVE-FEE-*`, `LIVE-PNL-*`, `LIVE-ROW-*`, `LIVE-FEATURES-*`, `SIZING-*`, `TIME_PRIME-*`. Zero items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim. **Audit doc:** `docs/audits/LIVE_FEE_MODEL_AUDIT_2026_04_29.md` (NEW, 11 sections incl. reproducibility queries). Docs-only diff (STATUS.md + ZMN_ROADMAP.md + audit doc). No services/ changes; no env changes; no Redis writes; no Railway deploys.
- **2026-04-29** (TUNE-005 ⏪ ROLLED BACK — HOLDER_COUNT_MIN 15 → 1, testing Speed Demon WR-regression hypothesis) — Trigger: Jay's analysis showed Speed Demon WR dropped 52.9% → 34.9% over 3 days following the 2026-04-24 TUNE-005 deploy (which raised the gate from 1 → 15). Hypothesis: HOLDER_COUNT_MIN=15 rejected legitimate winners with early holder counts <15, biasing the Speed-Demon-only paper sample toward losers. **Action this session:** revert `HOLDER_COUNT_MIN` to 1 on signal_aggregator (set via Railway web UI by Jay; auto-redeploy triggered) and open a 24h validation window. Outcome decides whether the rollback is codified (gate=1 stays, TUNE-005 permanently reversed) or reverted (TUNE-005 reapplied and look elsewhere for the WR-regression cause). **Verification (post-deploy):** Railway MCP listed `HOLDER_COUNT_MIN=1` confirming variable change accepted; container swap landed 2026-04-29 09:27:58 UTC (~12 min after Jay's UI change — Railway build slow, consistent with prior TUNE-005's 17-min build); new startup banner observed (`Signal Aggregator starting (TEST_MODE=True, ANALYST_DISABLED=True)`); zero `HOLDER reject` lines in 5+ min post-swap (gate=1 is permissive — only zero-holder tokens would block; live tokens always have ≥1 holder); ML SCORE cadence ~5-7/min; no errors / exceptions / tracebacks. **Operational side note:** Railway CLI auth was non-interactively broken at session start, blocking both `railway` CLI and `mcp__railway__*` tools (MCP wraps the CLI). User restored access mid-session. **Status changes:** TUNE-005 ✅ COMPLETED 2026-04-24 → ⏪ ROLLED BACK 2026-04-29 pending validation. Env var table `HOLDER_COUNT_MIN` annotated with new value (1) + rollback rationale. Critical-path #10 updated. Tier 1 row updated with verification details + rollback context (original ✅ COMPLETED status preserved in the description for history). All pre-existing items preserved verbatim. Zero items dropped, reordered, renamed, or merged. **Out of scope this session:** anything other than the rollback (per session prompt). BUG-022 fix execution still queued; ANALYST-POST-GRAD-001 design still queued; SILENCE-RECOVERY cleared per the recovery work between sessions. Docs-only diff (STATUS.md + ZMN_ROADMAP.md). No code changes; no Redis writes; no other env vars touched.
- **2026-04-29** (ENV-AUDIT-2026-04-29 — read-only Railway/Redis/DB ground-truth inventory) — Read-only audit producing per-service env-var inventory across 8 deployable Railway services + Redis state snapshot via Python (REDIS_PUBLIC_URL) + DB state snapshot via asyncpg (DATABASE_PUBLIC_URL) + on-chain wallet balances via Helius getBalance MCP. Cross-references against userMemories / handoff / earlier-audit claims; produces drift table + V5a wallet math + Railway redeploy-discipline observation. **3 new Tier 1 blockers:** **WALLET-DRIFT-2026-04-29** 🔥 (V5a-blocking — trading wallet `4h4pstXd…` on-chain = 0.064 SOL vs ~1.6 SOL in v4-forensics CLAUDE.md baseline; 1.5 SOL drain unaccounted-for in any STATUS / ZMN_ROADMAP / audit; bot has been TEST_MODE=true since 2026-04-25 EMERGENCY_STOP so this is NOT bot-driven; recommended next: Helius `getWalletTransfers` since 2026-04-19 to attribute outflows; ~15-30m read-only); **TREASURY-TEST-MODE-002** 🟡 (treasury alone has TEST_MODE=false; latent on-chain risk if wallet ever crosses 30 SOL trigger; recommend flip to true OR document intent before any wallet top-up); **ML-THRESHOLD-DRIFT-2026-04-29** 🟡 (signal_aggregator=65 / bot_core=40 / web=45 for SPEED_DEMON; 44 of last 100 closes have ml_score ∈ (0,40] confirming Jay's chat-side observation that the (40,50] band turned negative; AGGRESSIVE_PAPER_TRADING=true appears to bypass the SA gate for paper; mechanism investigation deferred to a separate code-path-walkthrough session). **Audit confirmations (no-action):** BUG-022 unchanged (1080/1080 NULL — was 853 at `ca4812d`); userMem TPs `[[2.0,0.2][5.0,0.375][10.0,1.0]]` confirmed on bot_core; userMem trail (no breakeven lock) confirmed; TUNE-005-ROLLBACK live; Speed Demon-only on last 100 closes (ANALYST-DISABLE-002 effective). **Audit side findings (deferred):** `signals:scored` LLEN=89 (regrowing post-recovery, below 100 STOP); `nansen:disabled` Redis TTL EXPIRED + NANSEN_DAILY_BUDGET=2000 on SA/ml_engine/signal_listener (drift vs CLAUDE.md 50/day); `signals:raw` LLEN=2.9M (slow memory leak, no TTL); `bot_core:health` Redis key absent (only SA writes a heartbeat); `governance:latest_decision.analyst_enabled = true` (Redis override clobbered, env-var fix still load-bearing per ANALYST-DISABLE-002); two different Nansen API keys in production simultaneously (SEC-001 split-key); railway.toml has no `paths` filter → RAILWAY-REDEPLOY-DISCIPLINE-001 likely real (bot_core restart at 09:48 followed docs commit `5706d7e` at 09:34 with no other apparent trigger). **Tool gap:** Railway CLI v4.6.0 on this machine; `mcp__railway__list-deployments` requires v4.10.0+, partial-block on Step 5; not blocking the rest of the audit. **Status changes:** none for pre-existing items. ID scheme extended: `WALLET-DRIFT-*`, `TREASURY-TEST-MODE-002`, `ML-THRESHOLD-DRIFT-*`. Zero pre-existing items dropped, reordered, renamed, or merged. All preserved verbatim. **Audit doc:** `docs/audits/ENV_AUDIT_2026_04_29.md` (NEW, 10 sections incl. drift table + V5a wallet math + reproducibility). **Recommended next prompt:** WALLET-DRIFT investigation (read-only Helius forensics) BEFORE BUG-022 fix and BEFORE LIVE-FEE-CAPTURE-001. Docs-only diff (STATUS.md + ZMN_ROADMAP.md + audit doc). No services/ changes; no env changes; no Redis writes; no Railway deploys.
- **2026-04-30** (BUG-022-FIX-2026-04-30 — Option A landed; Session B in chain A→B→C→D→E) — Per CORRECTED_PNL_INVESTIGATION_2026_04_28 (`ca4812d`), Option A is the lowest-risk fix: idempotent SQL backfill + inline write at paper close-time. **Step 2 backfill executed:** `UPDATE paper_trades SET corrected_pnl_sol = realised_pnl_sol, corrected_pnl_pct = realised_pnl_pct, corrected_outcome = outcome, correction_method = 'pass_through', correction_applied_at = NOW() WHERE corrected_pnl_sol IS NULL AND exit_time IS NOT NULL` — touched 1111 rows. POST-state: null_closed=0, pass_through=1111, max(|corrected - realised|) = 0.0 (identity preserved). Spot-check 3 most recent rows confirms identity. **Step 3 inline writes:** (a) `services/paper_trader.py:392-407` close-time UPDATE extended with `corrected_pnl_sol=$4, corrected_pnl_pct=$5, corrected_outcome=$10, correction_method='pass_through', correction_applied_at=NOW()` (param reuse $4/$5/$10 — valid PG); (b) `services/bot_core.py:1059-1078` staged-TP correction UPDATE split into TEST_MODE / non-TEST_MODE branches because trades table doesn't carry corrected_* columns; TEST_MODE branch writes corrected_* aligned with cumulative realised_pnl_sol value. Both files compile. Live-mode close UPDATE (`bot_core.py:1268`) and fallback INSERT (`bot_core.py:1301`) remain unchanged — Session D scope (LIVE-FEE-CAPTURE Path A; correction_method='live_estimated_v1'). **Step 4-5 CLAUDE.md edits:** (a) "Trade P/L Analysis Rule" rewritten — both columns interchangeable post-FEE-MODEL-001 + post-BUG-022; ML training has no correction_method filter requirement; live rows will write 'live_estimated_v1' per Session D. (b) "Live trading mode — session-gated" Operating Principle block appended with the 2026-04-21 1.5 SOL outgoing transfer note (Branch 1 confirmed by Jay 2026-04-29 chat) per WALLET_DRIFT_INVESTIGATION §7.4 recommendation. **Pre-conditions verified:** signals:scored LLEN=0 (well below 200 STOP); bot:status RUNNING + test_mode=true (verified via Redis MCP). **Status changes:** BUG-022 🟡 INVESTIGATED → ✅ COMPLETED 2026-04-30. WALLET-DRIFT-2026-04-29 🟡 PENDING_USER_CONFIRMATION → ✅ RESOLVED 2026-04-30 (Branch 1 confirmed; CLAUDE.md updated; mechanical top-up layer tracked separately as V5a precondition). All other pre-existing items unchanged. ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. All preserved verbatim. **Audit doc:** `docs/audits/BUG_022_FIX_2026_04_30.md` (NEW, 8 sections incl. pre-state, backfill execution, code change diffs, CLAUDE.md edits, deploy verification placeholder, reproducibility, forward consequences, what this fix does NOT do). Diff: services/paper_trader.py +5 lines (1 comment block + 3 new SQL columns); services/bot_core.py +13 lines (split branch); CLAUDE.md +25/-15 lines. Single git push triggers bot_core auto-redeploy (paper_trader.py is imported by bot_core).
- **2026-04-30** (SD-EARLY-CHECK-RELAX-2026-04-30 — verdict ⏸ DEFERRED, no deploy) — Read-only investigation of the `no_momentum_90s` exit-reason bleed (Session A in chain A→B→C→D→E). 123 post-recovery SD trades exit at 50.03-51.97s with 0% WR for -2.915 SOL bleed. Mechanism identified at `services/bot_core.py:1590-1604` — single env-driven check, window `(early_check_sec - 10, early_check_sec + 30)` with `SD_EARLY_CHECK_SECONDS=60` opens at 50s, threshold `SD_EARLY_MIN_MOVE_PCT=3.0`, label `no_momentum_90s` is legacy artefact from when code default was 90 (purely cosmetic mismatch). **Decisive empirical:** dead-trade gross pnl_pct distribution is mean -17.37%, median -16.12%, max -1.85%, min -39.15% — **ZERO trades in the 1-3% slow-starter band** that the prompt's recommended Alpha-A relaxation would save. Big winners (13 post-recovery, ≥0.10 SOL) all bypass the check: 2 hit `staged_tp_+1000%` at <2s (rocket-immune), 11 hit `TRAILING_STOP` at ~600s (survived 50-90s window with ≥3% pnl). 97.6% (120/123) of dead trades enter at MC $800-$3000 — Session C `SD_MC_CEILING_USD=3000` only filters 3/123 of current bleed. **Verdict Option Gamma** (defer, no env-var change, no code change, no deploy) — relaxation would either save zero trades (Alpha-A) or convert -0.024/trade no_momentum into larger -0.074/trade stop_loss exits (Beta). Structural fix lives at entry filter (Session C scope). **New Tier 1 row:** **TUNE-009** ⏸ DEFERRED 2026-04-30 with re-evaluation conditions in audit doc §10 (slow-starter band emerging, shadow-measure recovery evidence, post-Session-C bleed > -1.5 SOL on equivalent volume with distribution shift). **Status changes:** none for pre-existing items. ID scheme extended: `TUNE-009`. Zero pre-existing items dropped, reordered, renamed, or merged. All preserved verbatim. **Audit doc:** `docs/audits/SD_EARLY_CHECK_RELAX_2026_04_30.md` (NEW, 12 sections incl. mechanism finding, hold-time distribution, big-winners table, decisive pnl_pct distribution, MC band analysis, exit-reason mix, reproducibility, footnote on what would change the verdict). Docs-only diff (STATUS.md + ZMN_ROADMAP.md + audit doc). No services/ changes; no env changes; no Redis writes; no Railway deploys (per RAILWAY-REDEPLOY-DISCIPLINE-001 from env audit §6, this docs-only commit may auto-trigger redeploys; if so, evidence for the discipline gap accrues).
- **2026-04-30** (WALLET-DRIFT-INVESTIGATION-2026-04-29 — read-only Helius forensics, verdict Outcome C) — Read-only reconciliation of the 1.5 SOL trading-wallet drain flagged by ENV-AUDIT-2026-04-29. Methodology: Helius MCP `getBalance` + `getTransactionHistory(mode=signatures)` + `parseTransactions` for 8-tx window 2026-04-19 → 2026-04-30; cross-reference against `paper_trades.trade_mode='live'` rows (asyncpg via DATABASE_PUBLIC_URL); reconcile against `1b40df3` v4 forensics (1.610 SOL deep-recon baseline) and `session_outputs/ZMN_LIVE_ROLLBACK.md` (1.658 SOL T0 + 1.564 SOL final for Session 5 v4 window). **Verdict: Outcome C** (drain explained by trades + UNDOCUMENTED transfer). Reconciliation gap = **0 lamports** (sub-1e-9 SOL). 8 on-chain txs in window: 1 ELONX dust sale (+0.048, external — already documented in `1b40df3`), 1 yh3n441 entry (-0.374), 1 failed 3rd-party tx (zero impact), 1 yh3n441 exit (+0.280), 1 creator-fee distribution (zero impact), **1 outgoing 1.5 SOL transfer** (sig `42dnuS1xv…`, 2026-04-21 10:04:48 UTC = 20:04:48 AEDT, destination `7DSQ3ktYiirRfs4YQojyDTqUM9Cwj9YgzwwegyiCAgUy`, fee payer = trading wallet itself — signed with its private key), 2 dust-phishing inbound txs (combined 0.000020 SOL, vanity-mimic of destination — informational only). Walk-forward chain matches 3 independent reference points to sub-lamport precision (`1b40df3` deep recon 1.610, `ZMN_LIVE_ROLLBACK.md` T0 1.658400592 + final 1.564155614, `ENV_AUDIT_2026_04_29.md` §7 0.064095633). **Critical correction to session-prompt working hypothesis:** the 6 paper_trades "live" rows summing to −3.21 SOL realised PnL do NOT explain the drain. **5 of 6 have NULL signatures** and are reconcile-residual paper-position force-closures from Session 5 v4 (per `ZMN_LIVE_ROLLBACK.md` lines 96-122 — bot inherited 5 stale paper positions in self.positions when bot_core restarted with TEST_MODE=false at 20:44:30 UTC, attempted live sells against mints with no on-chain position, all 25 sell attempts failed at swap router — no on-chain SOL effect). Only id 6580 (yh3n441) was a real on-chain round-trip (-0.094 SOL, exact match to ZMN_LIVE_ROLLBACK.md `fee_delta` of -0.094245 SOL). **WALLET-DRIFT-2026-04-29 status: 🔥 → 🟡 PENDING_USER_CONFIRMATION.** V5a blocker is now two-layer: (1) mechanical — wallet 0.064 SOL << MIN_POSITION_SOL × wallet-fraction floor, needs top-up to ≥ 1.5-2.5 SOL; (2) intent confirmation — Jay confirms Branch on the 2026-04-21 1.5 SOL transfer. Branch 1/2 (benign / consolidation / Jay-controlled) → blocker collapses to layer 1, V5a chain resumes with STATUS backfill recording transfer purpose. Branch 3 (unintended) → escalates to Outcome D, halt V5a, security investigation (rotate `TRADING_WALLET_PRIVATE_KEY`, audit signing-machine 2026-04-20 21:00 → 2026-04-21 10:05 UTC, enumerate other wallets from same seed). 9-day post-transfer idle is mildly reassuring not conclusive. **Side findings:** (a) `LIVE_FEE_MODEL_AUDIT_2026_04_29.md` §4's "6 historical live rows" framing should be re-read with this audit's correction — divergence findings still hold for id 6580, but for the other 5 rows the comparison is moot (no on-chain fill); `LIVE-ROW-BACKFILL-001` should scope only to id 6580 for FEE-MODEL-001 estimates (the other 5 are accurate accounting fictions — SOL was spent at v4 entry into the `trades` table, paper_trades just records the close). (b) CLAUDE.md "Live trading mode — session-gated" block currently covers only the v4 wallet drain (5.0 → 1.6); does not cover the 2026-04-21 1.5 SOL outflow — recommend a 5-min Tier 1 docs session to append after Jay's branch confirmation. (c) Holding wallet `2gfHQvyQ…` drain (~0.089 SOL) deferred unless Branch 3 escalates. (d) Dust-phishing pattern observed (vanity prefix `7DSQ` + suffix `AgUy` mimicry, 50s after the 1.5 SOL transfer) — informational hygiene note; never copy-paste a destination from this wallet's tx history without verifying full address. **Status changes:** WALLET-DRIFT-2026-04-29 📋 QUEUED 🔥 → 🟡 PENDING_USER_CONFIRMATION (Tier 1 row updated with full §3 + §5 + §7 details + audit doc reference). All other pre-existing items unchanged. ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. All pre-existing items preserved verbatim. **Audit doc:** `docs/audits/WALLET_DRIFT_INVESTIGATION_2026_04_29.md` (NEW, 9 sections incl. reference points / on-chain inventory / trade-pair reconciliation / untracked txs / reconciliation math / verdict / V5a disposition / reproducibility). Helius credit cost: ~210 (1 getBalance + 10 getTransactionHistory + 100 parseTransactions × 1 batch of 8). Docs-only diff (STATUS.md + ZMN_ROADMAP.md + audit doc). No services/ changes; no env changes; no Redis writes; no Railway deploys (per RAILWAY-REDEPLOY-DISCIPLINE-001 finding from env audit §6, this docs-only commit may auto-trigger redeploys; if so, that itself remains evidence for the discipline gap).
- **2026-04-30** (TUNE-010 DEX-PAID-FEATURE-EVALUATION — verdict ❌ DISCARD, no deploy) — Read-only data study evaluating whether DexScreener's `dex_paid` flag (Bitfoot Alpha Pings' meta-signal) has signal for Analyst / Whale Tracker entries. Sample: 304 closed Analyst trades (2026-04-22 → 2026-04-28). Whale Tracker has zero closed trades — un-evaluable. Methodology: DexScreener `/orders/v1/{chainId}/{tokenAddress}` exposes `paymentTimestamp` per order/boost — used for **timestamp-aware "paid AT entry time"** analysis (sidesteps the prompt's anticipated Step-4 Scenario A/B problem). 318/318 mints looked up cleanly; 0 API errors. Result: **`dex_paid_at_entry` inverts vs hypothesis** — PAID (n=164) WR=14.0% vs NOT_PAID (n=140) WR=17.9%, diff -3.83pp; PnL/trade diff +0.0001 SOL (zero discriminatory power); fisher_p=0.43, MWU_p=0.40; Cohen's h=-0.105, Cliff's delta=-0.056 (both below "small effect"). **Base-rate problem:** 76.6% of Analyst's tokens are paid lifetime; feature too prevalent to discriminate. **Best secondary feature `social_count > 0`: WR diff +5.62pp** but p=0.25 (not significant) and PnL diff +0.0021 SOL/trade (below 0.02 deploy threshold). **SD scope exclusion correct:** 0/14 SD big winners (≥0.10 SOL post-recovery) have any DexScreener footprint — all 14 have `no_pair=True` (rugged or never-graduated; SD enters/exits before any DEX promotion could exist). **No deploy criterion met by any feature tested.** Verdict-changing conditions: (a) Analyst reactivated + 200 new trades in different market regime; (b) ANALYST-POST-GRAD-001 (post-graduation MC $50-300k personality) accumulates a sample where DEX-pair existence is the norm and dex_paid variance is non-trivial; (c) Whale Tracker activated with meaningful sample. **Status changes:** TUNE-010 ❌ DISCARD 2026-04-30 (NEW row added to Tier 1 above TUNE-009). All other pre-existing items unchanged. ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. **Audit doc:** `docs/audits/TUNE_010_DEX_PAID_EVALUATION_2026_04_30.md` (NEW, 10 sections incl. methodology / sample distribution / primary statistical results / secondary features / deploy threshold check / SD big winners sanity / verdict / what's NOT in this audit / operational notes / metadata). DexScreener calls: 636 (2 endpoints × 318 mints), 0 rate-limits, free tier. Helius credit cost: 0. Anthropic credit cost: 0. Docs-only diff (STATUS.md + ZMN_ROADMAP.md + audit doc). No services/ changes; no env changes; no Redis writes; no Railway deploys (per RAILWAY-REDEPLOY-DISCIPLINE-001, may auto-trigger redeploys; if so, that itself remains evidence).
- **2026-04-30** (BUG-022-HOTFIX — distinct param slots for corrected_* columns; Session B follow-up) — Bot_core post Session-B redeploy threw `ERROR: Exit check error for speed_demon:<mint>: inconsistent types deduced for parameter $10` on every exit attempt. Three trade IDs (7743/7744/7745) loop-failed for ~2 min before fix landed. Root cause: BUG-022 inline-write reused $4/$5/$10 for both realised_* and corrected_* columns; asyncpg prepared-statement protocol cant deduce a single PostgreSQL type when the same param is referenced for columns of different declared types (schema has `outcome` as character varying; corrected_outcome as text or similar divergence). Fix `17c2aac`: pass each value twice with distinct param slots — paper_trader.py uses $11/$12/$13 for corrected_*; bot_core.py staged-TP correction uses $5/$6/$7. Compile-checked. Single push triggered bot_core auto-redeploy. Backfill (1111 rows from `392c928`) is unaffected — that was a separate ad-hoc UPDATE without parameter reuse. **Lesson preserved:** chain prompt asserted "param reuse is valid PostgreSQL" — true in raw psql, false for asyncpg with prepared statements when param appears in columns of different declared types. Future inline-write changes that touch dual-write should use distinct slots even if it costs 3 extra Python args. ID scheme unchanged. Zero items dropped. 
- **2026-04-30** (SD-MC-CEILING-DEPLOY-2026-04-30 — env var + code gate; Session C in chain A→B→C→D→E) — Original SD_MC_CEILING_001 proposal was $5k from 4-day audit; revised tighter to $3k based on 35h post-recovery data: 39 trades >$3k = -1.77 SOL / 0% WR vs 20 trades >$5k = -1.04 SOL / 0% WR. $3k cuts ~83% more loss without losing any winners (all 14 big winners ≥0.10 SOL entered at MC < $800). Cross-confirmed by Session A audit: 97.6% (120/123) of `no_momentum_90s` dead trades enter at $800-$3000. **Implementation:** (a) Module-level env var `SD_MC_CEILING_USD = float(os.environ.get("SD_MC_CEILING_USD", "3000.0"))` at `services/signal_aggregator.py:53`. (b) Gate inserted at L1826-1838 BEFORE the existing prefilter block (which does Twitter API lookup) — saves the API call on rejected tokens. Drops `speed_demon` from `targets`; `if not targets: continue` short-circuits the rest of the pipeline including ML scoring. MC value read from same `raw_data.get("usdMarketCap", raw_data.get("market_cap_usd", 0))` chain that the existing FILTER log uses — no semantic drift between gate and diagnostics. (c) `SD_MC_CEILING_USD=3000` set on signal_aggregator via Railway MCP `set-variables` (auto-redeploys SA). Compile-checked. **Verification queued** (audit doc §5): post-redeploy expect ≥1 `SD reject ... ceiling 3000` log line within 30 min of normal signal flow; query of last 60min SD trades expects ZERO with `market_cap_at_entry > 3000`. **24h queued** (audit doc §6): compare to 35h baseline (272 trades, 23.4% WR, +0.140 SOL); expected trade count down ~14%, WR up to ~27%+. **Status changes:** TUNE-006 partial — SD_MC_CEILING component ✅ DEPLOYED; SD_DEAD_ZONE_001 + SD_ML_THRESHOLD_LIFT remain queued. **Companion B HOTFIX** (separate changelog entry above) corrected the asyncpg type-inference regression in flight at Session-C commit time. ID scheme unchanged. Zero pre-existing items dropped, reordered, renamed, or merged. All preserved verbatim. **Audit doc:** `docs/audits/SD_MC_CEILING_DEPLOY_2026_04_30.md` (NEW, 10 sections).
- **2026-04-30** (SD-MC-CEILING attempt #1 ⏪ ROLLED BACK — design flaw discovered in verification; SD_MC_CEILING_002 follow-up queued) — Step 5 verification (~30 min post-deploy) found 2 of 14 fresh SD trades since 08:01 UTC entered with `market_cap_at_entry > 3000` (id 7749 at $4,482 entered 08:10:50 UTC; id 7757 at $9,807 entered 08:28:00 UTC — both definitively after SA container swap at ~08:08 UTC per build timestamp 08:03:40 UTC). Root cause: gate reads `raw_data.get("usdMarketCap", raw_data.get("market_cap_usd", 0))` which is `0` at signal-time for fresh pump.fun new_token events (visible in FILTER logs as `mc=$0`). The actual MC stored in `paper_trades.market_cap_at_entry` is computed in bot_core at entry-time as `entry_price * 1_000_000_000` (total supply) where entry_price derives from BC reserves (`vSolInBondingCurve / vTokensInBondingCurve` * SOL_USD). Gate reads the wrong source — structurally inert for the dominant SD signal source (fresh new_token signals from pumpportal). Not a code bug; design flaw in the chain prompts gate placement. **Rollback executed:** `SD_MC_CEILING_USD=999999999` set on signal_aggregator via Railway MCP. Code retained as harmless no-op at 999M threshold; re-enable when proper fix lands. **Follow-up SD_MC_CEILING_002:** Option (a) move gate to bot_core at entry decision (uses actual MC; gate placement closer to truth but later in pipeline) OR Option (b) compute MC from BC reserves in SA gate (mirrors bot_core; matches chain prompts intent of filtering at signal-gate stage; introduces dependency on `market:sol_price` Redis key in SA gating path). Recommend (b). ETA ~30m next session. **Status changes:** SD_MC_CEILING_001 ✅ DEPLOYED → ⏪ ROLLED BACK 2026-04-30. SD_MC_CEILING_002 📋 NEW (deferred follow-up). All other pre-existing items unchanged. ID scheme extended: SD_MC_CEILING_002. Zero items dropped, reordered, renamed, or merged. **Audit doc:** `docs/audits/SD_MC_CEILING_DEPLOY_2026_04_30.md` (updated §5 with verification fail + rollback record + proper-fix options).
- **2026-04-30** (LIVE-FEE-CAPTURE-PATH-A-2026-04-30 — three code changes + id 6580 backfill; Session D in chain A→B→C→D→E) — Lands Path A from `LIVE_FEE_MODEL_AUDIT_2026_04_29.md`. Wires paper helpers (`_simulate_slippage`, `_simulate_fees`) into bot_core live close path. **Three code changes:** (3a) `bot_core.py:1247-1271` — PnL formula now subtracts paper-estimated round-trip fees from gross. Closes LIVE-PNL-FEE-FORMULA-001. (3b) `bot_core.py:1284-1305` — live close UPDATE writes `slippage_pct`/`fees_sol`/`corrected_*`/`correction_method=live_estimated_v1` (distinct param slots $11-$15 per BUG-022 hotfix lesson). Closes LIVE-FEE-CAPTURE-001 Path A + LIVE-ROW-BACKFILL-001 (partial, id 6580 only — other 5 live rows are reconcile-residual paper closures per WALLET-DRIFT audit §4). (3c) `bot_core.py:922-948` — live entry INSERT writes `slippage_pct`/`fees_sol`/`features_json` (16-column INSERT, was 13). Closes LIVE-FEATURES-JSON-001. **Plus import** at `bot_core.py:73-77` — `_simulate_*` imported unconditionally (outside `if TEST_MODE:`) so live path can call them; helpers are pure functions with no side effects at import. **id 6580 backfill (Step 5):** computed via `_simulate_slippage("buy"|"sell", amt)` + `_simulate_fees("buy"|"sell", amt, "auto", bc_progress=0.0)`. Result: `total_fees_sol=0.008306`, `avg_slippage_pct=10.41`, `corrected_pnl_sol=-0.006430`. **Path A NOT empirically validated:** gap +0.087815 SOL between Path A estimate (-0.006) and on-chain actual (-0.094 per `ZMN_LIVE_ROLLBACK.md`). Above ±0.02 tolerance. Path A delivers parity-of-record (live rows now write fees/slippage/features_json) but NOT parity-of-truth (paper fee model under-counts real Solana priority fees, MEV, and slippage by ~12×). Per chain prompt: "If it doesnt [validate], thats a finding — Path As estimates are off and Path B becomes more urgent." **Status changes:** LIVE-FEE-CAPTURE-001 (Path A) ✅ DEPLOYED; LIVE-PNL-FEE-FORMULA-001 ✅ DEPLOYED; LIVE-FEATURES-JSON-001 ✅ DEPLOYED; LIVE-ROW-BACKFILL-001 🟡 PARTIAL (id 6580 only). LIVE-FEE-CAPTURE-002 📋 NEW (Path B Helius parseTransactions for actual fill data — V5a-blocking-but-degradable, ETA ~3-5h). LIVE-CLOSE-FALLBACK-INSERT-001 📋 NEW (cleanup — `bot_core.py:1318` legacy fallback INSERT not extended with new columns; low-traffic path). All other pre-existing items unchanged. ID scheme extended: `LIVE-FEE-CAPTURE-002`, `LIVE-CLOSE-FALLBACK-INSERT-001`. Zero pre-existing items dropped, reordered, renamed, or merged. **Audit doc:** `docs/audits/LIVE_FEE_CAPTURE_PATH_A_2026_04_30.md` (NEW, 9 sections incl. verdict, code change diffs, backfill result, Path A vs Path B trade-off table, deploy verification placeholder, forward consequences, open issues, reproducibility, dimension comparison). Diff: services/bot_core.py +27 lines (3 changes + 1 import). Single git push triggers bot_core auto-redeploy.
- **2026-04-30** (SESSION_E_PERSISTENCE_HARDENING — docs-only; chain A→B→C→D→E complete) — Promotes AGENT_CONTEXT.md to authoritative current-state file, adds Decision Log to ZMN_ROADMAP, writes empirical drift report, codifies Persistence Convention in CLAUDE.md. **Four deliverables:** (1) `AGENT_CONTEXT.md` rewritten — 11 sections incl. bot mode, deployed config (post-Sessions-A-D), wallets, personalities, 35h post-recovery performance baseline, V5a preconditions, known unresolved Tier-1 carry, Redis state snapshot (~08:52 UTC), DB state snapshot (1138 closed paper trades, 0 NULL corrected_pnl_sol, 1137 pass_through + 1 live_v1, 6 live rows), doc index, reproducibility. Prior 2026-04-05 prepend preserved as historical archive at bottom. (2) `ZMN_ROADMAP.md` "Decision Log" — 18 dated entries newest-first with status + 1-line reasoning per lever (LIVE-FEE-CAPTURE-001 Path A ✅, SD_MC_CEILING_001 ⏪, BUG-022 Option A ✅, TUNE-009 ⏸, WALLET-DRIFT ✅ Branch 1, LIVE-FEE-MODEL-AUDIT ✅, ENV-AUDIT-2026-04-29 ✅, TUNE-005 ⏪, ANALYST-DISABLE-002 ✅, BUG-022 INVESTIGATED 🟡, ANALYST-DISABLE-001 ✅, BITFOOT-2026-BASELINE BASELINE-A ✅, BITFOOT-DIP-SAMPLE DIP-D ✅, HOLDER-DATA-PIPELINE-001 ✅, GATES-V5 ✅, FEE-MODEL-001 ✅, Session-5-v4 ⏪, LIVE-FLIP-PRE-FLIGHT ✅) plus Future-queued levers table (LIVE-FEE-CAPTURE-002 Path B, SD_MC_CEILING_002, TIME_PRIME-CONTRADICTION-001, SD_DEAD_ZONE_001, SD_ML_THRESHOLD_LIFT, wallet top-up, V5a flip). (3) `docs/audits/USERMEMORIES_DRIFT_2026_04_30.md` (NEW) — 13-claim drift table from this audit cycle with 4 severity classes (🔴 ACTION-CHANGING n=2 / 🟡 SCOPE-CONFUSION n=2 / 🟢 SAMPLE-STALE n=2 / 🔵 NUANCE-MISSING n=1; 4 ✅ accurate; rest with caveats). Includes 6 mitigation recommendations + "what we DID right this audit cycle" + "when CAN you trust userMemories" guidance. (4) `CLAUDE.md` "Persistence Convention" section added before "Trade P/L Analysis Rule" — codifies userMemories is NOT a source of truth; what to trust memory for (patterns, conventions, pointers) vs NOT (specific env values, trade counts, wallet balances); anchor AGENT_CONTEXT.md as authoritative current-state. **Status changes:** STATUS-CONVENTION-001 🟡 → ✅ COMPLETED (5th+ session appending cleanly). All other pre-existing items unchanged. ID scheme unchanged. Zero items dropped, reordered, renamed, or merged. **Audit doc:** `docs/audits/USERMEMORIES_DRIFT_2026_04_30.md` (NEW, 7 sections). Diff: AGENT_CONTEXT.md +180 lines (rewrite + archive header); ZMN_ROADMAP.md +35 lines (Decision Log + future-queued); CLAUDE.md +35 lines (Persistence Convention); STATUS.md +50 lines (this entry); USERMEMORIES_DRIFT_2026_04_30.md +200 lines NEW. **Chain A-E summary:** A ⏸ DEFERRED, B ✅ + hotfix, C ⏪ ROLLED BACK + design-flaw lesson, D ✅ Path A, E ✅ this session. 8 commits across the chain.
